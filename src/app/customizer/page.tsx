import { CustomizerApp } from '../../features/mech-customizer/components/CustomizerApp';

export default function CustomizerPage(): JSX.Element {
  return (
    <main className="min-h-screen">
      <CustomizerApp />
    </main>
  );
}

