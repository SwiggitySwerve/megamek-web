# Tasks: Harden GM + Two-Player Live Campaign Sessions

The PR order, dependency graph, ownership boundaries, exact-main regression cadence, and review evidence are maintained in `implementation-plan.md`. The event-history implementation sections are decomposed and frozen according to `event-history-wave-map.md`; execute their leaf-change tasks rather than implementing a duplicate umbrella seam. Each implementation PR MUST reference the requirement names, leaf task IDs, and umbrella acceptance IDs it closes.

## 1. Durable-Baseline Preflight

- [x] 1.1 Verify the repository-supported Node version and rebuild/reinstall the native `better-sqlite3` dependency until `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts` passes; record Node, ABI, and command output in `test-results/gm-two-player/preflight/` before any durability implementation.
- [x] 1.2 Add RED unit/contract tests for `Atomic Command Event Batches`, `Commit Precedes Recipient Publication`, and crash-before/after-commit behavior at `IMatchStore.ts`, `DurableMatchStore.ts`, `InMemoryMatchStore.ts`, and `ServerMatchHostIntent.ts`; prove partial-batch and false-success behavior fail before implementation.
  - Receipt (2026-08-26 — CLOSES this box): all three named behaviours are proven, and each proof is falsified rather than merely green. **ATOMIC COMMAND EVENT BATCHES**: `matchStoreBatchContract.test.ts` runs nine rows against the store's `appendCommandBatch` - commits a contiguous batch with a describing receipt, refuses an empty batch, refuses a gapped batch AND writes none of it, recognises an identical retry instead of committing twice, refuses the same command id carrying different work, checks IDENTITY BEFORE REVISION (so a retry arriving after someone else moved the stream is reported as a retry rather than sent off to rebuild state it already has), refuses a stale expected revision, throws for an unknown match, and carries the expected post-state digest onto the receipt. **COMMIT PRECEDES RECIPIENT PUBLICATION**: `commitThenPublish.test.ts` (4 rows) pins the two-pass ordering at `ServerMatchHostIntent.ts` - nothing reaches a recipient from inside the commit pass, a mid-command failure publishes NOTHING, the failure is a truthful typed frame that closes the match, and appending stops at the first failure. **CRASH-BEFORE-COMMIT**: added here, and deliberately against a REAL FILE rather than `:memory:`, because the property under test is what SURVIVES and an in-memory handle cannot answer that. A batch whose SECOND event cannot be serialised (a `BigInt`, valid in memory and fatal to `JSON.stringify`, so the failure lands mid-transaction rather than being caught up front) throws; the store is closed; the same file is reopened as a restarted process would see it; and the log holds ONLY the good command - not even the doomed batch's FIRST event, which had already been inserted when the second threw. The doomed command id is then proven free, so a retry is a fresh command rather than a duplicate of a half-written one. FALSIFIED 1:1: removing `this.db.transaction` from `appendCommandBatch` makes the orphaned event survive the reopen and reds exactly that row - which is what shows the test reaches the insert loop at all. That check was not optional: an earlier atomicity test in this program passed WITH the transaction removed, because the contiguity guard rejected the batch before any insert ran. NO FALSE SUCCESS is asserted directly - the failing call must not hand back a receipt. **ON `InMemoryMatchStore.ts`**, stated plainly rather than quietly skipped: it has no `appendCommandBatch` at all, so the contract suite runs against `DurableMatchStore` only (`matchStoreBatchContract.test.ts` line 81). That is the deliberate current design - task 2.2 says the in-memory store is updated 'only as a contract-compatible' follow-on - so there is no batch behaviour there to test yet, and adding a second implementation purely to have something to assert would be inventing the thing under test. The 'RED before implementation' framing is historical: the implementation landed first (atomic batch #1343, commit-ordering #1360), so these rows are the same proofs written after rather than before. What the task actually asks for - that partial-batch and false-success behaviour are proven to fail - is satisfied, and falsifiably. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; durable-store suite 16/16; FULL unit suite green.
- [x] 1.3 Add RED protocol/client tests for `Stable Intent Identity Survives Retries`, `Authority and Viewer Sequences Are Separate`, replay/live overlap, sequence collision, and `Heartbeat Is Bidirectional` in `src/types/multiplayer/Protocol.ts`, `src/lib/multiplayer/client.ts`, and their existing test suites.
  - Receipt (2026-08-27 — CLOSES this box): all five named behaviours now have proofs in the client/protocol suites, and every one was falsified rather than merely green — the convention 1.2 set. `Stable Intent Identity Survives Retries` — the reconnect-retry rows added with 5.2, which were the blocker this box's previous note named. `Authority and Viewer Sequences Are Separate` — `clientDeliveryGap.test.ts`, over the per-viewer delivery sequence #1377 introduced. `Replay/live overlap` — the replay-buffering rows, where live traffic arriving mid-replay is held and released in order at `ReplayEnd`. `Sequence collision` — `client.test.ts`, including the neighbouring row proving an identical repeat is a DUPLICATE rather than a collision, so the guard cannot pass by calling everything a fork. `Heartbeat Is Bidirectional` — both halves: the client's outbound keepalive (#1358) and the inbound liveness deadline (#1391), the latter falsified twice so it cannot degenerate into a timer that closes healthy connections. WORTH RECORDING ABOUT THIS BOX: it reads like pure test-writing and was not. Two of the five behaviours had NO IMPLEMENTATION when the box was written — the client never noticed a silent server, and it never stamped an intent identity at all — so the tests could not have been written first in any useful sense; writing them is what found the gaps. The box was blocked on 5.2 rather than on effort, which is why its previous note said so explicitly instead of leaving the next reader to rediscover it.
  - Progress (2026-08-26 — `Heartbeat Is Bidirectional` is now proven on BOTH sides; box STAYS OPEN, blocked on 5.2): auditing this box's five named behaviours found four covered and one missing, and the missing one turned out to be a live defect rather than an absent test. THE CLIENT NEVER NOTICED A SILENT SERVER. `startHeartbeat` (#1358) fixed the outbound half — the server was reaping quiet clients — but the INBOUND `Heartbeat` handler said "clients do not need to echo" and then discarded it, and nothing else tracked when the server had last spoken. A half-open socket, or a server process killed without a FIN, is indistinguishable from a healthy idle one from the client's side, so the client waited in a dead connection instead of reconnecting into a live one. The spec's `Missing response triggers reconnect` scenario had no implementation at all. THE FIX arms a deadline re-armed by VALID inbound traffic only — the spec's wording, and the reason the re-arm sits after `safeParse` rather than on raw receipt: a peer emitting garbage is not a peer still speaking the protocol. Our own keepalives deliberately do not re-arm it, since a frame we wrote proves nothing about the peer. Expiry just closes the socket, because `onclose` already reports the drop and schedules the reconnect. Held to the SERVER's own `HEARTBEAT_TIMEOUT_MS` rather than a number of its own, so the two halves cannot drift apart and start disagreeing about when a connection is dead. FALSIFIED TWICE, INDEPENDENTLY, because this could easily have been a dumb timer: neutralising the deadline reds only the silent-server row, and neutralising the re-arm reds only the still-talking control. A guard that closed every connection on a schedule would pass the first and fail the second. WHY THE BOX STAYS OPEN, and this is the useful part for whoever picks it up: of the five behaviours, `sequence collision` is covered (client.test.ts), `Authority and Viewer Sequences Are Separate` is covered (clientDeliveryGap.test.ts), replay/live overlap is covered by the replay-buffering rows, and `Heartbeat Is Bidirectional` is covered as of this change. The fifth, `Stable Intent Identity Survives Retries`, CANNOT be proven yet: it requires the client to persist a pending command identity and reuse it across a reconnect, which is task 5.2 and is unbuilt. This box is therefore blocked on 5.2, not on test-writing effort — a distinction worth recording, because the box reads like pure test work and is not.
  - Progress (2026-08-26, STAYS OPEN — one of the five named requirements is now characterized): `Authority and Viewer Sequences Are Separate` is VIOLATED TODAY, in both halves, and `viewerSequenceConcealmentLeak.test.ts` measures by how much. The requirement says the server "SHALL assign a gapless delivery sequence independently for each viewer projection stream" and that payloads "SHALL NOT expose hidden authority identifiers or gaps that reveal concealed events". Every `Event` frame carries the RAW AUTHORITY SEQUENCE - proven by the second row, which shows every number a player receives is the same number the durable authority log uses, so there is no viewer-scoped numbering at all - and under fog the server skips the send for events a viewer may not see while every event it DOES send keeps that sequence. The holes are therefore a side channel: arithmetic on numbers the player was handed tells them HOW MANY events were concealed and WHERE in the ordering they fell, using nothing but their own frames - no second client, no timing, no server access. The two players' concealed sets differ, so each is learning about the OTHER's activity specifically rather than sharing a blind spot. WRITTEN AS A CHARACTERIZATION, NOT A RED TEST, deliberately: a permanently-red row would break the build, and the honest form of "RED" for a violation that cannot be fixed in this task is a passing test that documents the violation and names its inversion trigger. When per-viewer delivery sequences land (umbrella section 11, pre-serialization viewer projection), `concealedCount` becomes 0 and these assertions FLIP - the failure is the signal that the leak closed. Falsified 1:1: turning fog OFF reds the leak row, which is what shows it measures concealment rather than passing on any sparse stream. NOT ADDRESSED HERE and still open on this box: `Stable Intent Identity Survives Retries`, replay/live overlap, sequence collision, and `Heartbeat Is Bidirectional` (that last one is BUILT and proven under 7.2, but this box asks for its protocol-level test, which is a different row). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 2/2; FULL unit suite green.
  - Progress (2026-08-25, STAYS OPEN — three of the five named properties are now covered): `STABLE INTENT IDENTITY SURVIVES RETRIES` is the one closed here, and the gap it filled was a real one. `AcceptedIntentTracker` had its own unit tests, but the DEFENCE those tests exist for had none - `DUPLICATE_INTENT` appeared nowhere outside production code, and `ServerMatchHost.recover`, the factory that rebuilds the replay window from the durable log on restart, was reached by NO test at all. That combination is the dangerous one: a window that silently failed to rebuild would reopen on every restart, and nothing in the suite would have noticed, because the tracker's own unit tests keep passing regardless of whether anything wires them up. Three rows now cover it: a replayed intent id is refused with `DUPLICATE_INTENT` and appends no event; it is STILL refused after a restart rebuilt through `hydrateGameSessionFromEvents` -> `fromSessionAsync` -> `ServerMatchHost.recover`; and a FRESH intent id is still accepted after that same restart. The third is the control, and it earns its place - without it the second would pass equally against a recovered host that had stopped accepting anything, which is a different bug wearing the same green tick. Falsified 1:1: replacing the `fromEventLog` rebuild with a fresh empty tracker reds ONLY the after-restart row (the live-session row stays green, which is exactly the asymmetry that made this worth testing); neutralising the duplicate guard reds two. `HEARTBEAT IS BIDIRECTIONAL` was delivered under 7.2 (#1358) - the client half did not exist and a quiet player was being reaped for silence. `SEQUENCE COLLISION` was already covered (`DurableMatchStore.test.ts` pins `MatchStoreSequenceCollisionError`). STILL OPEN ON THIS BOX: `AUTHORITY AND VIEWER SEQUENCES ARE SEPARATE` - grepped, and no `viewerSeq`/`authoritySeq` distinction exists anywhere in the codebase, so there is nothing to write a RED test against; the property has to be built before it can be tested. REPLAY/LIVE OVERLAP is likewise untested here. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 3/3; FULL unit suite 33163 passed / 0 failed.
- [x] 1.4 Keep SUPERHEAVY gyro serialization, zero-BV campaign materialization, Quick Game bootstrap recovery, and fail-open determinism CI in separate changes; document only their external prerequisite status in `implementation-plan.md`.
  - Receipt (2026-08-30 - CLOSES this box): documentation-only, exactly as the box mandates. implementation-plan.md gained an 'External prerequisite status' section recording that all four named fixes - determinism CI fail-closed, SUPERHEAVY gyro serialization, CAMP-03 zero-BV materialization, GAME-02 Quick Game bootstrap recovery - stayed in separate changes and are COMPLETE, each cross-referenced to its own checked plan todo (1-4) with commit evidence already recorded there; the CAMP-03 entry names the external repair (PR #1072) and the residual launch-race close. Nothing of the four is implemented or gated inside this change. Section 1 remainder: 1.1-1.3 were closed earlier; this seals section 1 at 4/4.


## 2. Additive Authority Schema and Store Contracts

- [x] 2.1 Add additive SQLite migrations and TypeScript records for command receipts, command batches, recipient-neutral outbox, campaign sessions, participants, viewer cursors, branches, supersession, checkpoints, outcome inbox/receipts, private GM audit references, and append-once private rejection-audit records keyed by session/command identity, implementing `Atomic Command Event Batches`, `Campaign Journal Migration Is Additive and Idempotent`, and `Branches Preserve Immutable Supersession Lineage`.
  - CLOSED BY RECEIPT (2026-09-02): the additive migrations and TypeScript records the letter names are all on main and pinned - the 2.1 idempotency rows (re-apply at head is a no-op; migration ledger pin) landed with #1566, and the four schema residuals this box deferred to 2.3 (the publication outbox table, the action_audit session-identity pair with its no-update twin (v28), the private-record rejection-detail once-only column (v29), and the durable branch/participant/cursor tables composed on the campaign database) closed with #1575 and the 2.2 compose seam. Nothing in the letter is left un-migrated; the schema rows live under the migration test catalogue, and the durable-schema constraints are 2.3's receipts. No code changed for this closure.

  - EVIDENCE MAP + PROGRESS (2026-09-02, Wave E map + 2.1-tests seam): the catalog this box enumerates is on main in two stores, bound item by item. COVERED: command batches (event_journal_batches + event_journal_events, v8, write-once trigger, idempotent-reopen test); branches (event_history_branches + effective heads, v23, repeated-migration test); supersession (event_history_supersessions with no_update/no_delete triggers, v23); checkpoints (replay_checkpoints v10 + write-once trigger, idempotent re-init test; v27 is a rebuild, not a second CREATE); private GM audit references (private_record with opaque_ref PK + private_access_audit, v12, idempotent test). PARTIAL, now CLOSED on the TEST half by this seam - additive + idempotent re-apply rows (copying the deliveryEpochs / replayCheckpoints precedents: temp file, sqlite_master name+type+sql snapshot toStrictEqual, PRAGMA table_info column list from the migration source, a seeded row preserved across the second apply): campaign_participant_cursor (v16), campaign_session_participant (v17), campaign_session (v20), campaign_combat_outcome_inbox (v21, extending its head+UNIQUE test), and the match store's own SCHEMA_SQL (mp_command_receipts + mp_match_outbox re-applied by constructing DurableMatchStore twice on one file). STILL OPEN - schema residuals that belong to 2.3, each a linked blocker: (5) no campaign-session recipient-neutral outbox table (only mp_match_outbox exists); (7) action_audit's PK is command_id alone, not (session, command) as the box names; (8) action_audit has no _no_update trigger - action_audit_no_rewrite still admits one UPDATE class (accepted -> published); (9) private_record rejection-detail rows are neither unique on (campaign_session_id, command_id) nor write-once. Types bound: ICommandReceipt / CommandReceiptSchema, IMatchCommandReceipt, ICampaignCommandReceipt, IAppendEventBatch, IMatchPublication, ICampaignSessionState, ICampaignSessionMembership, IParticipantDeliveryCursor / IDeliveryCursor, IEventHistoryBranch / IEventHistoryEffectiveHead, IEventHistorySupersession, IReplayCheckpointMetadata, ICampaignCombatOutcomeReceipt / IMatchCombatOutcomeOutbox, IPrivateRecordIdentity, IActionAuditRecord. Orchestrator mutants in the PR body.

- [x] 2.2 Extend `IMatchStore.ts` and the campaign event-store boundary with atomic batch, receipt lookup, outbox claim/acknowledgement, branch, participant, and cursor operations; update `InMemoryMatchStore.ts` only as a contract-compatible test/dev adapter.
  - CLOSED (2026-09-02, Wave E 2.2-compose): the two SQLite-backed stores now implement the same three ports the prefix named, and the contract runs the nine capability rows over ALL FOUR builders (InMemoryMatchStore, InMemoryCampaignEventStore, DurableMatchStore, JournalCampaignEventStore). JournalCampaignEventStore forwards the branch port 1:1 to its optional SQLiteEventHistoryBranchStore (the methods are ABSENT, not faked, when no branch store is given, so hasHistoryBranchStore stays honest), the participant port to the shipped CampaignSessionParticipantStore functions plus the isRevoked query moved out of campaignSessionMembershipPort (that port no longer pokes SQLite), and the cursor port to the shipped participantDeliveryCursor helpers with the grant/viewer authorization re-derived from the call's double (never hidden). DurableMatchStore composes the same three on a capabilityDb (default getSQLiteService().getDatabase()) and NEVER on its own multiplayer-matches.db - branch/participant/cursor tables live in mekstation.db (migrations 23/17/16); a new row proves the match file never grows an event_history_branches table while the capability db does. The contract's SQLite builders bring up a temp-file SQLiteService through the full migration catalog (never a hand-picked subset). Orchestrator mutants in the PR body. NOT CLAIMED: callers (rewind routes, campaignSessionMembershipPort's other reads, handleCampaignGrant*) still construct the shipped stores directly - adopting the ports at those call sites is later cleanup, not a contract gap; no outbox lease (single-host per match); no listBranches/readSupersessions/backfillGenesisBranches on the port (test-only today); durable schema stays 2.3's.

  - PROGRESS (2026-09-02, Wave E 2.2-prefix): the 2026-08-25 note above is STALE on two of its five leftovers - receipt lookup (getCommandReceipt on IMatchStore, DurableMatchStore, InMemoryMatchStore, ICampaignEventStore, JournalCampaignEventStore) and outbox claim/ack (IPublicationOutboxStore.listPendingPublications + markPublicationsPublished, drained by ServerMatchHostPublication; no claim lease exists and none is invented - this process is single-host per match) are already on both boundaries with production callers. The three that remained land here as OPTIONAL capabilities on BOTH IMatchStore and ICampaignEventStore (a facade: participant and cursor keys are campaign/session/grant, not matchId; branch methods take IEventHistoryStreamRef so match and campaign streams share one port): IEventHistoryBranchPort (readBranch/requireBranch/readEffectiveHead/requireEffectiveHead/createBranch/transitionBranchStatus), ICampaignSessionParticipantPort (the seven shipped exports plus isRevokedCampaignSessionParticipant so campaignSessionMembershipPort can stop poking SQLite), IParticipantDeliveryCursorPort (readParticipantDeliveryCursor/recordParticipantAcknowledgement with the grant+viewer authorization supplied on the call), guards hasHistoryBranchStore/hasParticipantStore/hasDeliveryCursorStore in the hasPublicationOutbox shape. IN-MEMORY adapters for both stores: same EventHistoryBranchError codes, createBranch refused by the default seam exactly as production, null effective head stays null (no invented genesis), one GM + two tactical seats inside one check-then-write, revoke is a timestamp, active* treat revoked as absent, ack kinds applied/stale/gap/foreign-epoch/not-authorized, never persisted past highestAssigned. CONTRACT: a second describe.each in matchStoreBatchContract over the two in-memory builders (rows: exposes the three optional ports; readEffectiveHead is null until an effective branch exists; requireBranch unknown-branch; createBranch honors the disabled production seam; bind then active membership, revoke hides it, isRevoked true; a second active GM is gm-seat-taken; a third tactical player is tactical-seats-full; cursor read is null, an identical ack applies then is stale; an ack past highestAssigned is gap). Orchestrator mutants in the PR body. NOT CLAIMED: DurableMatchStore and JournalCampaignEventStore do not implement the ports yet (next seam: journal forwards to the shipped SQLite stores on the campaign db; durable composes on SQLiteService, NEVER on its own multiplayer-matches.db - branch/participant/cursor tables live in mekstation.db); callers keep constructing the shipped stores; no listBranches/readSupersessions/backfillGenesisBranches (test-only), no outbox lease, no per-recipient outbox; 2.3 owns durable schema.

  - Progress (2026-08-25, STAYS OPEN — one of six operations is adapted): the ATOMIC BATCH half of this task now holds on BOTH stores, and 'contract-compatible' is a fact rather than a claim. `InMemoryMatchStore` had no `appendCommandBatch` at all - the operation was a structural flag that only `DurableMatchStore` carried - so every test written against the dev adapter was describing a store production does not have, and the difference would only have surfaced once something real depended on it. THE CONTRACT IS NOW WRITTEN ONCE AND BOTH STORES PASS IT: a `describe.each` over both implementations covers commit-and-receipt, empty-batch refusal, gap refusal with nothing written, identical-retry recognition, same-id-different-work refusal, identity-checked-BEFORE-revision, stale-revision refusal, unknown-match throw, and post-state-digest carriage. The durable side runs at `:memory:`, which still exercises SQLite's real transactional path, so neither side is a lookalike. Two of those rows were WRONG when first written and failed identically on both stores, which is what a shared contract is for: a batch that is also non-contiguous is refused for THAT, so the revision check is never reached - the fixtures had to be made contiguous-from-the-stale-expectation before they tested what they claimed. ATOMICITY IS REACHED DIFFERENTLY ON THE TWO SIDES and that is deliberate: the durable store wraps a transaction, the in-memory one runs every check before anything is written and then performs writes that cannot fail. Falsified 1:1 five times against the adapter only - neutralising the identity check reds three rows (duplicate, integrity-conflict, and identity-before-revision), and the contiguity, empty-batch, revision, and fingerprint-comparison checks red one each - with the durable rows staying green throughout, which is what shows the neutralisation was isolated. FLAGGED, NOT CLAIMED: the fingerprint keys on `(commandId, actorId, expectedRevision, [sequence:id:type])` - event IDENTITY, never payload. Two batches differing only in payload under the same event ids therefore read as a DUPLICATE and the store keeps the first. Whether that is reachable depends on how event ids are minted on a retry, which was NOT established here; the question only becomes live when `appendCommandBatch` gets a production caller (leaf-4 task 2.3 - the combat intent path still appends one event at a time today), and it should be settled there. WHAT REMAINS FOR THIS BOX: receipt lookup, outbox claim/acknowledgement, branch, participant, and cursor operations - five of the six named operations - plus the campaign event-store boundary. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; shared contract suite 18/18 (9 rows x 2 stores); FULL unit suite 2418 suites / 33154 passed / 0 failed.
- [ ] 2.3 Implement the additive schema and atomic operations in `DurableMatchStore.ts` and the selected campaign persistence adapter with uniqueness and foreign-key constraints for session-scoped idempotency, outcome versions, authority sequence, and branch activation.
  - PROGRESS (2026-09-02, Wave E 2.3 schema residuals): the four schema residuals the 2.1 evidence map named, additive only, no shipped migration edited, versions contiguous by test (head 27 -> 28 -> 29). Migration 28 action_audit_session_command_identity: CREATE UNIQUE INDEX IF NOT EXISTS idx_action_audit_session_command ON action_audit (campaign_session_id, command_id) - the box names session-plus-command identity; the PK stays command_id alone because readByCommandId, linkPublishedReceipt, PUBLISH_SQL and the rejection audit all rely on global command identity, so the pair index NAMES the identity and lets a dropped-index mutant go red - plus the missing _no_update twin action_audit_no_update, a BEFORE UPDATE trigger with exactly the WHEN the shipped action_audit_no_rewrite admits (the accepted -> published stamp with every identity column unchanged), so the catalog carries the same append-once vocabulary its peer tables do; no_rewrite is untouched. Migration 29 private_record_rejection_detail_once: a PARTIAL unique index on (campaign_session_id, command_id) WHERE record_kind = 'rejection-detail' and a BEFORE INSERT trigger refusing a rejection-detail row with a NULL or blank command_id (SQLite UNIQUE treats NULLs as distinct, so the trigger is what makes the pair identity real); other kinds may still omit or share a command_id; no UPDATE trigger is added - erase and redact keep working through the shipped private_record_no_rewrite. Residual 5 (a campaign-session recipient-neutral outbox) CLOSES BY RECEIPT: campaign delivery is journal + delivery-epoch + live subscribe (projectCampaignStreamForGrant re-reads the journal, campaignGrantChannelSession re-projects on wakeup, CampaignSyncSession attaches a live sink), the match store already has mp_match_outbox, and outcome handoff is campaign_combat_outcome_inbox - nothing would consume a campaign outbox, so a table would be speculative schema. Rows: pair duplicates refused UNIQUE; the same command_id in a second session still refused by the PK; the accepted -> published stamp still admitted through the exact PUBLISH_SQL shape; a digest rewrite and a DELETE refused naming the new trigger; two rejection-detail rows for one (session, command) refused; NULL and blank command_id refused on insert; gm-draft rows may still omit or share; erase/redact still admitted on rejection-detail while an identity rewrite is refused; both migrations re-apply idempotently (sqlite_master snapshot toStrictEqual) and the exact-list trigger catalogs gain the new names. Orchestrator mutants in the PR body. NOT CLAIMED: no PK rebuild; no backfill (there is no live rejection-detail writer yet, so dirty duplicates fail closed); no FK into the journal; the adapter operations of 2.3 (atomic batch, receipt lookup, outbox claim/ack, branch/participant/cursor ports on IMatchStore and ICampaignEventStore) remain.

- [x] 2.4 Add migration/idempotency/rollback contract tests, including repeated backfill, ambiguous ownership fail-closed behavior, and a schema reader compatible with preserved materialized snapshots; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.
  - CLOSED (2026-09-02, Wave E12): the contract rows the letter names that no suite carried. (1) a REPEATED genesis backfill is idempotent - SQLiteEventHistoryBranchStore.backfillGenesisBranches run twice over a real temp-file SQLiteService (full catalog, never a hand-picked subset) leaves the same rows, count and digests (census incl. base_digest and created_at; the older row only compared return counts, which a rewrite could hide). (2) ambiguous ownership FAILS CLOSED - when two seats claimed the same force the campaign snapshot backfill answers the typed force-claimed-by-several refusal and writes nothing (journal and marker census; the 8.2 rows only covered an UNCLAIMED force). (3) a snapshot written under the previous schema version is read by the head reader with identical projected fields after migrating to head - built through the real runner stopped one catalog step short via a test-only maxMigrationVersion cap on SQLiteService (unused in production), then migrated to head; this row was red against the unmodified runner (Expected v28, Received v29). (4) the runner at head is a no-op is NOT a new row: the v17 / v10 / v28 / v29 migration suites already pin idempotent re-initialisation. Orchestrator mutants in the PR body. Honest note: rows (1) and (2) were already green against the product - they pin behavior the letter demands that no row had pinned; only (3) died first.


## 3. Atomic Batch Append API

- [x] 3.1 Implement command-receipt digesting and atomic multi-event append in the store adapters, satisfying `Atomic Command Event Batches` and `Command Receipts Persist for the Authority Lifetime`.
  - Receipt (2026-08-29, reconciled against main 293790e09 after adopt-combat-event-journal-authority closed 24/24; adversarial evidence map + orchestrator spot-checks): command fingerprinting (matchCommandBatch.ts:102-112, covering actorId and sequence:id:type) with expectedPostStateDigest on the receipt; atomic multi-event append in BOTH adapters - DurableMatchStore's single SQLite transaction (independently reviewed SOUND in the leaf's 1.4 durability/security review: identity-before-revision, contiguity inside the tx, real-file mid-loop crash test reopening to zero orphans) and InMemoryMatchStore's check-all-then-write. Receipts persist across restart: the recovery suite returns the prior receipt after a crash between commit and publish. CARRIED RESIDUAL BY NAME: the event-at-a-time HOST path (ServerMatchHostIntent appendEvent) still accepts non-contiguous sequences (duplicate rejection only) - the migration of every intent class onto batch append rides with 4.1's serialized-executor work, not this store-adapter box.

- [x] 3.2 Add controlled no-network/no-sleep store seams that fail before commit, during a middle event, during head update, and during outbox insert; assert full rollback and no successful receipt.
  - Receipt (2026-08-29 - CLOSES this box; the fourth named seam was the evidence mapper's gap and lands with this PR): all four seams now exist, all in-process (no network, no sleeps), all asserting full rollback and no successful receipt. BEFORE COMMIT: typed refusals (duplicate-command, integrity-conflict, non-contiguous, revision-conflict) write nothing - matchStoreBatchContract rows in both stores. DURING A MIDDLE EVENT: the BigInt poison fails JSON.stringify mid-loop against a REAL FILE; reopen shows only the good batch, doomed identity free. DURING HEAD UPDATE (new): a test-only seam (_setFailAtHeadUpdateForTests, the codebase's established _set*ForTests pattern) throws between the receipt INSERT and the mp_matches head bump; the test proves the receipt row rolls back with the events across a real-file reopen and the identity retries cleanly - the dangerous half-state here is a receipt without a head bump, and the assertion targets exactly that. Mutant: swallowing the seam inside the transaction reds the row. DURING OUTBOX INSERT: the committed events go down with a publication row that cannot be written (matchPublicationOutbox transactionality rows). Gates: tsc 0; oxlint 0 errors / 77 warnings; multiplayer tree 108 suites / 829 passed with the new row.

- [x] 3.3 Add restart tests proving an identical retry returns the prior receipt and a reused identity with different actor, branch, kind, or payload returns a typed integrity conflict.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 3 at 4/4): restart-shaped proofs over a real SQLite file - append, discard the store instance, reopen a NEW instance on the same file - now pin command-identity integrity across restart: an identical retry returns the prior receipt field-for-field (duplicate-command); a reused command id with a different actor, a different event type, or a DIFFERENT PAYLOAD returns typed integrity-conflict appending nothing. The payload row was the charter's predicted red-first find, confirmed for real: matchCommandFingerprint covered commandId/actor/revision/sequence:id:type but NOT payload content, so payload-divergent reuse acked as duplicate. Fix: fingerprint v2 adds a per-event sha256 payload digest, with matchesCommandFingerprint accepting EITHER the v2 or the legacy stored format so receipts persisted before the upgrade keep deduping identical retries instead of throwing false conflicts - proven by a row that writes an old-format fingerprint receipt in-store then retries the identical batch through the new code. Both stores fixed (Durable + InMemory), and the payload-divergence row rides the shared store contract suite so the two adapters cannot drift. Branch divergence recorded N/A-here honestly: no branch concept exists in this store until add-authoritative-history-branches lands; that leaf owns the branch-mismatch row. Mutations, worker-authored and reviewer-re-run: dropping the payload digest dies at three rows; rejecting the legacy format dies at exactly the upgrade-compat row; the reviewer's novel mutant - actor removed from the fingerprint - dies at exactly the actor-reuse row. Gates: tsc 0; oxlint 0 errors / 77 warnings; focused 3 suites / 51; full multiplayer tree 110 suites / 864. Codex worker capped mid-run after completing the full surface; orchestrator ran all verification. 5 files, +268/-7.

- [x] 3.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/InMemoryMatchStore.test.ts src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.
  - Receipt (2026-08-29, run twice on main 293790e09 - evidence mapper and orchestrator independently): 3 suites / 27 passed / 0 failed. HONESTY NOTE: the named suites do not cover 3.2's missing head-update crash seam or 3.3's branch/payload integrity-conflict dimensions - those siblings stay open and this box closes only what its literal command verifies. MatchStoreStress is a latency/rate budget suite, not a concurrency stress; 4.3 owns the concurrency proof.


## 4. Per-Session Serialized Command Cutover

- [x] 4.1 Add one bounded serialized executor per match/session and route `ServerMatchHostIntent.ts` validation, reducer execution, and batch creation through it, implementing `Per-Session Command Execution Is Serialized`.
  - Receipt (2026-08-29 - CLOSES this box, and the leaf-2.5-carried ForfeitMatch residual with it): the executor is a per-host promise chain on ServerMatchHost.handleIntent - the same law as the campaign save chain: each link runs the ENTIRE intent path (validation, reducer execution, batch creation, publication - lobby intents included, since they branch inside the same routed function) to completion before the next begins; the tail swallows rejections so one failed intent cannot wedge later commands; the chain is per-host so unrelated matches never share it. Failing-test-first with a live interleave capture: a gated store parked intent A mid-append and intent B's append OPENED INSIDE A's window (two starts in the parked snapshot) - the defect observed before the fix. Mutant, killed twice: bypassing the chain reds both serialization rows. Full multiplayer tree 109 suites / 834 passed - the executor breaks nothing.

- [x] 4.2 Split atomic store capability from host cutover behind a disabled-by-default shadow/cutover flag; compare existing and journal-derived results before the new path becomes authoritative.
  - Receipt (2026-08-29, delivered whole by the leaf - PRs #1447/#1449/#1455, verified on main 293790e09): store capability landed months before cutover (leaf PR 1); the flag is the reviewed three-way COMBAT_JOURNAL_AUTHORITY_MODE off|shadow|enabled, production const 'off' (matchJournalAuthority.ts:18). Shadow re-derives each legacy command on the pre-dispatch state by replaying the tick's stamped rolls, compares canonical events + post-state digest, and records mismatches host-side - no dual-authoring, no wire change, no live mutation, each law a mutation-proven test; the scratch can no longer author the outcome bus (#1455, the 4.5 reviewer's blocker honored). The new path becomes authoritative only per-match through the admission ladder whose tripwire refuses on any recorded mismatch. Audience-level (per-viewer projection) digest comparison remains 24.1's open clause - this box's state/event comparison is delivered.

- [x] 4.3 Prove concurrent Player 1/Player 2 commands form deterministic non-interleaved batches while commands in unrelated sessions continue independently.
  - Receipt (2026-08-29 - CLOSES this box, same PR as 4.1): serializedIntentExecutor.test.ts proves all three clauses live. P1 and P2 submit concurrently: exactly one append opens in the gated window, and the full trace's start sequences are strictly ascending - cross-command interleave would surface as a descending pair, while within-command overlap (one command's own events) stays legitimate, a distinction the first draft got wrong by demanding strict start/end pairing. Deterministic order: FIFO by submission through the chain. Unrelated sessions: a match parked mid-append does not delay another match's command completing (Event frame observed while the slow match is gated). Falsifications: the chain-bypass mutant reds the interleave row AND the P1/P2 row.

- [x] 4.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHostEngineDispatch.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.
  - Receipt (2026-08-29 - CLOSES this box; the evidence mapper's earlier vacuity note is resolved): the named command runs green (3 suites / 22 tests) AND is no longer vacuous with respect to its siblings - 4.1's executor and 4.3's concurrency proof now exist, with the serialization suite (3/3) running beside the named set. Section 4 of the umbrella closes 4/4.


## 5. Stable Intent and Viewer Delivery Protocol

- [x] 5.1 Extend production `Protocol.ts` and `client.ts` envelopes with stable command/idempotency identity, branch, gapless per-viewer `deliverySequence`, projection digest, acknowledgement, and typed gap/collision receipts, implementing the `multiplayer-sync` requirements without exposing private authority sequences.
  - Receipt (2026-08-30 - CLOSES this box): the envelope contract is complete on the delivery axis. Stable command/idempotency identity, gapless per-viewer deliverySequence, and typed gap/collision behavior were shipped by prior waves; this wave adds the ACKNOWLEDGEMENT: a new DeliveryAck client frame (delivery-space only - it carries NO player field, the server keys the receipt on the connection's verified principal, and NO authority sequence, satisfying the box's without-exposing clause) persisted durably in a new mp_viewer_delivery_ack table behind a structural-guard store port (Durable implements; absence is the flag, the shipped viewer-delivery pattern). Typed gap/collision receipts mapped rather than invented: a reported delivery gap is answered by the typed ReplayChunk recovery the client already requests (the tail IS the receipt), and a sequence collision blocks CLIENT-side with a typed integrity error and no server round-trip by design - recorded as the design answer. DEFERRALS recorded: envelope branch fields belong to add-authoritative-history-branches (no branch concept exists in this store yet - same deferral pattern as 11.1's authority-sequence clause); wire-carried projection digests ride the audience-digest machinery when a consumer needs them - the client's dedup key already pairs identity with digest where frames carry one. Codex worker built the client half whole then capped; orchestrator built the server half (adapter, host handler keyed on verified principal, dispatch case) and ran all verification.

- [x] 5.2 Persist pending command identity client-side until a terminal receipt and reuse it across reconnect attempts; clear only the matching committed, rejected, vetoed, timed-out, or integrity-conflict state.
  - Receipt (2026-08-27 — CLOSES this box): the client now keeps a command until it is answered, retries it with the identity it originally carried, and clears only the one that was answered. PERSIST: `sendClientIntent` records the validated envelope in `pendingIntents` keyed by its `intentId`, BEFORE the write rather than after — a send that throws is precisely the case this exists for, and the command is pending either way. REUSE ACROSS RECONNECT: the retry fires when the replay window closes, not on socket open. That timing is the load-bearing part — until `ReplayEnd` the client is still being caught up, so a command sent into that window races the very replay that would have told it the command already landed. Retries re-send the stored envelope AS IT IS; minting a fresh id would let authority apply the same command twice when the first attempt landed before the socket died, which is the exact double-apply the identity exists to prevent. Repeated failures keep it pending, so a second and third reconnect retry it again. CLEAR ONLY THE MATCHING: both terminal shapes carry the id back, so both settle by id and nothing else does. Authority stamps the id onto the first event a command produces (`stampIntentIdOnNewEvents`), which is how a COMMITTED command reports; a refusal — rejected, vetoed, duplicate, scope — correlates its `Error` frame with the same id. Another player's events and unrelated errors leave the command pending, which is the whole reason this keys on the id instead of clearing on any traffic. FALSIFIED IN BOTH DIRECTIONS, independently, and the second one matters more than it looks: neutralising the retry reds only the retry row; neutralising the SETTLING reds only the control. That second falsification is what proves the control is green for the right reason — before the fix it passed trivially, because nothing retried at all, and a change that retried everything forever would have passed the retry row while silently re-applying committed commands. The `mintIntentId` seam that #1392 left for this work is now held rather than called again, exactly as that receipt anticipated.
  - Progress (2026-08-26, first step — THE IDENTITY NOW EXISTS AT ALL; box STAYS OPEN for persistence and reuse): this box is about holding onto a pending command's identity across a reconnect. Reading the client to find where that identity is created turned up that THERE WAS NO IDENTITY. `sendClientIntent` built the `Intent` envelope with no `intentId` field whatsoever. AND THAT IS NOT A LABELLING PROBLEM. `intentId` is optional on `IntentSchema` for pre-M2 clients, and the server's replay guard reads `if (envelope.intentId != null && acceptedIntents.isDuplicate(...))` — so a missing id does not fail the duplicate check, it SKIPS it. Every intent the production client has ever sent was exempt from replay-attack protection: a captured envelope could be re-sent to re-trigger a movement or attack, which is precisely what design D7 was written to stop. THE COMMENT SAID OTHERWISE, and that is the third time this session a comment has asserted a guarantee no code provided. `Protocol.ts` states "the M2 client always stamps one" directly above the optional field. It did not. Like `ownsCoopUnit`'s claim that the host already validated unit ownership, and `foreign-force`'s name promising an ownership check it never made, the false premise is what kept anyone from looking: the field reads as belt-and-braces for old clients rather than as the only thing standing between a replayed envelope and a re-applied attack. Optional fields make these claims invisible, because the degraded path is silent by construction. MINTED PER SEND, deliberately: two identical commands a player genuinely issues are two commands, and giving them one id would have the server refuse the second as a replay. Reusing an id across a RETRY of the same attempt is the opposite case and is the rest of this box. FALSIFIED with two rows that catch different failures. Removing the field reds both. Replacing it with a CONSTANT id passes the presence row and reds the uniqueness row — which is the failure worth guarding, because a constant id is worse than none: it would have the server reject the player's second command of the match as a replay. STILL OPEN: persistence and reuse. The client does not track a pending command, so a reconnect after submission and before receipt still cannot retry with the same identity — the scenario `Reconnect retries same identity`. Nor does it clear pending state on a terminal receipt (`committed`, `rejected`, `vetoed`, `timed-out`, `integrity-conflict`). `mintIntentId` is the seam that work will hold onto rather than call again. Task 1.3 is blocked on exactly that remainder.
- [x] 5.3 Replace max-high-water application with contiguous exactly-once reducer application, replay/live buffering, gap recovery, and sequence-collision blocking.
  - Receipt (2026-08-30 - CLOSES this box): slice B landed - the transitional authority-sequence dual-key is GONE from client admission; delivery sequence is the only numbered admission key, proven by the lying-authority-sequence row (a frame whose event.sequence lies applies fine on a correct deliverySequence - and the reviewer's novel resurrect-the-old-key mutant dies exactly there). Contiguous exactly-once application, replay/live overlap dedup by identity+digest, gap recovery entering a typed syncing state, and sequence-collision blocking were slice A and stay green untouched. Acks are contiguous-only: apply 0,1,3 and the ack stops at 1 (the worker's own mutant comment names the killer); a reducer failure acks nothing and requests the existing controlled recovery (SessionJoin tail). The durable ack row survives cold store reopen and can never walk backwards (monotonic in the SQL - the reviewer's drop-the-WHERE mutant dies at that row). HONEST SCOPE NOTE: a cold-reloaded client with no local state still gets a FULL replay regardless of its durable ack - it needs the events to rebuild state; resume-after-ack as a wire optimization materializes when a client durable-state mirror exists, and the durable receipt is already the floor that work will consume. Mutants: ack-past-gap dies at the contiguous row; monotonic-drop dies at the never-walks-backwards row; authority-key-resurrection dies at the lying-sequence row. TWO REVIEWER FINDS beyond the claim set: (1) the worker missed the SECOND client suite (clientDeliveryGap) whose rows still pinned the retired lastSeq wire hint - re-anchored to the delivery axis preserving each property; (2) a REAL defect in the worker slice B, caught by those pre-existing rows: a BACKFILLED recovered frame overwrote the stream head in noteDeliveryGap, so the next live frame looked discontiguous and the just-released cursor re-pinned - the head is now monotone (backfill neither moves it nor flags a gap, and steps the cursor only across the exact delivery it fills, trace-verified). Two supporting semantics fixes: identityOf no longer falls back to a type-based key (two id-less events of one type are DIFFERENT events - the fallback silently collapsed un-migrated servers frames), and an unstamped id-less replay item after numbered hydration is refused outside a gap recovery, bounded by the identity window during one - the fog-withheld resurrect control and the eviction-window law both transfer to the delivery axis without the authority key.

  - Wave provenance (2026-08-28, applies to the four notes below): four parallel worker PRs were BLOCKED by an adversarial review wave (6 blocking findings, every one proven by mutation - deleting or inverting the guard left the whole suite green), remediated with mutation-proven fixes, re-verified by a second adversarial pass re-running the ORIGINAL mutations, then merged in dependency order (owner-directed admin path after per-chain verification). Post-merge, the full suite on merged main: 2437/2437 suites, 33323 passed, 0 failures; typecheck clean; oxlint 0 errors / 77 warnings (78 -> 77 across the merge wave - the new pin). The methodological lesson recorded for every future wave: falsification proves your test notices YOUR guard being neutralized; only MUTATION of the underlying property proves the suite notices the property breaking. The outbox transaction claim survived falsification and fell to mutation - both are now required.
  - Progress (2026-08-28, GAP RECOVERY NOW COMPLETES END TO END; box STAYS OPEN for contiguous application across reconnect): four PRs closed the recovery round trip. #1396 made the client quote its last CONTIGUOUS delivery sequence instead of the high-water mark (the reviewer's blocking finding - `deliveryResumeCursor = null` in openSocket made every reconnect fall back to the authority high-water this task condemns - was fixed and mutation-proven in the remediation); #1399 fixed the server half (the replay parameter is now fromSeq, the FIRST sequence to send, so a first-missed value is not decremented past); #1401 admitted the recovered frame on arrival (measured red before: DROP authority 8, JOIN with cursor 5, CHUNK [8,10,11] arrived and frame 8 was discarded); #1406 stopped a second socket from splitting the player's delivery record (per-socket assign appended two entries per frame; one memoized number per player per frame now, falsified independently by the landing reviewer). STILL OPEN, stated plainly: (1) contiguous exactly-once application across a RECONNECT still resumes from the authority high-water when no delivery record survives - closing it needs the authority sequence gone from player frames (11.1's next slice) and the client re-keyed on its per-viewer number; (2) sequence-collision blocking exists on the authority sequence only - a repeated or backward DELIVERY sequence is currently diagnosed as a delivery gap and triggers a full resync, a mislabelled diagnosis rather than a silent loss.
  - Progress (2026-08-26, fourth step — GAP RECOVERY now exists; box still OPEN): the third step established that contiguity is only well-defined over a per-viewer sequence, and #1377 built one. This adds the recovery half. On a hole in its DELIVERY sequence the client re-sends `SessionJoin`, which already carries `lastSeq`, so the server replays exactly the tail that client is missing rather than the whole match - the reconnect path reused rather than reinvented. ONE RECOVERY AT A TIME: every resync is a full replay, so a burst of losses firing one each turns a small loss into a stampede; the guard clears on `ReplayEnd` so the second real loss of a session still recovers. WHY THIS IS SAFE NOW AND WAS NOT BEFORE, restated because the distinction is the whole point of the reverted regression: a hole in the AUTHORITY sequence means nothing under fog - usually that the viewer was never allowed to see that event - so acting on one stalls the client forever; a hole in the per-viewer delivery sequence means a frame was genuinely LOST. Same-shaped signal, opposite meaning, and only the second is actionable. The events that DID arrive are still applied - recovery pulls what is missing, it does not hold what is present. Falsified 1:1 on both properties: disabling recovery reds three rows, removing the one-at-a-time guard reds exactly the stampede row. REMAINING ON THIS BOX: sequence-collision blocking, and the contiguous-application half itself, which still waits on the client being able to use the delivery sequence as its RESUME CURSOR - that needs the authority sequence gone from player frames and replay resuming from a delivery cursor, which is 11.1's next slice.
  - Progress (2026-08-26, third step — WHY THIS BOX IS BLOCKED, established rather than asserted): the previous two notes record an attempt at contiguous live application that shipped as a regression and was reverted. The root cause is now named precisely, and it is not a client bug: THERE IS NO SEQUENCE ON THE WIRE THAT CONTIGUITY IS WELL-DEFINED OVER. `Event` frames carry the global AUTHORITY sequence, and a fog viewer's slice of it is legitimately sparse, so "contiguous exactly-once application" cannot be enforced client-side without stalling on events the viewer may never see - which is exactly what the reverted attempt did. The requirement this box needs is `Authority and Viewer Sequences Are Separate` (specs/multiplayer-sync/spec.md), which mandates a per-viewer GAPLESS delivery sequence. That does not exist: grepped this session, `viewerSeq` / `viewerSequence` / `deliverySequence` appear NOWHERE in `src/types/multiplayer/Protocol.ts`. So 5.3 is gated on section 11's pre-serialization viewer projection, and attempting it before that is attempting it against the wrong sequence - which is the mistake already made once here. The violation is now measured rather than argued: `viewerSequenceConcealmentLeak.test.ts` shows a player can count and locate the events hidden from them, and that same sparsity is what makes client-side contiguity unimplementable today. When the per-viewer sequence lands, both that test and `fogViewerSequenceSparsity.test.ts` flip, and THAT is the moment this box becomes workable.
  - CORRECTION (2026-08-25, second step — THE PREVIOUS STEP WAS WRONG AND IS REVERTED): the note below claimed contiguous live application as a fix. It was a REGRESSION, and it shipped. A fog-of-war viewer's stream is LEGITIMATELY SPARSE: `broadcastEvent` filters each event per recipient and, when it is not visible to them, SKIPS THE SEND ENTIRELY (`if (!filtered) continue;`) while every event that IS sent keeps its authority sequence. Measured on a two-player fog match rather than reasoned about - one player received sequences [2,3,4,5,6,7,8,10,11,12] and the other [2,3,4,5,6,7,9,10,11,12], each missing precisely the event the other could see. Holding at the first 'gap' would have stalled BOTH clients permanently, because the sequence each was waiting for is one it is never allowed to see. The full suite passed throughout, because no test drove a CLIENT against a fog server - which is exactly the shape of hole that lets a regression merge. WHAT IS REVERTED: gap-holding and the out-of-order buffer are gone, and a sequence ahead of the cursor is applied. WHAT IS KEPT, because both are sound under sparsity: duplicate suppression (an already-applied sequence is ignored) and SEQUENCE-COLLISION BLOCKING - a repeat of a sequence carrying a DIFFERENT event identity means the stream forked, so application stops and an error is emitted rather than the second silently vanishing. Replay goes through the same admission, and its comment no longer claims contiguity: replay is fog-filtered per viewer too. THE REAL LESSON, and it is the task's own point: CONTIGUITY CANNOT BE ENFORCED ON THE AUTHORITY SEQUENCE AT ALL. That is precisely why the requirement asks for a per-viewer `deliverySequence` that is gapless BY VIEWER (5.1, `Authority and Viewer Sequences Are Separate`) - a fact this correction learned the expensive way. The sparsity is now pinned by `fogViewerSequenceSparsity.test.ts`, so the client's decision is anchored to the server's measured behaviour instead of a comment; if a future change makes per-viewer streams contiguous, that test fails, and the failure is the signal that client-side contiguity becomes enforceable. Falsified 1:1 on what remains: neutralising collision detection reds the collision row, neutralising the block reds it too, and reverting `identityOf` to type-only reds it - while the sparse-stream rows stay green. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 16/16; FULL unit suite 2422 suites / 33170 passed / 0 failed. THIS BOX STAYS OPEN, and further from closed than the note below implied: contiguous exactly-once application is BLOCKED on 5.1's per-viewer delivery sequence, which does not exist on the wire.
  - Progress (2026-08-25, STAYS OPEN — the LIVE path is contiguous now; replay and collision-blocking are not): MAX-HIGH-WATER WAS EXACTLY WHAT THE CLIENT DID, and it lost events rather than merely delaying them. `updateLastSeq` moved the cursor to the highest sequence seen, and the `Event` handler applied whatever arrived - so a dropped frame advanced the cursor PAST the missing sequence, the reconnect replay resumed after it, and nothing ever noticed the hole. Written failing-test-first: three rows were RED before the change. Live application is now contiguous and exactly-once, with three cases that are each a different kind of normal - the NEXT sequence applies and then drains anything buffered behind it (filling a gap can release a run); an ALREADY-APPLIED sequence is ignored, because at-least-once delivery makes a duplicate ordinary traffic rather than an error; and a sequence AHEAD of the cursor is HELD, not dropped and not applied. Holding is the deliberate choice: reordering is usually momentary and the missing frame arrives right after, so re-fetching the whole tail would be a heavy answer to a light problem - and because the cursor does not move, a reconnect resumes from the hole rather than past it, which IS the gap recovery for the live path. Events without a numeric sequence are not part of the sequenced stream and pass straight through. Falsified 1:1: forcing every event through unconditionally reds all three rows; disabling only the buffer drain reds only the reordering row. A TESTING TRAP WORTH RECORDING: the first version of these rows injected `ReplayStart`/`ReplayEnd` frames with the wrong fields (`lastSeq` instead of `toSeq`, and no `totalEvents`). Both frames failed schema validation and were silently dropped, so `ready` never became true and the events sat in `pendingLiveEvents` unemitted - the rows failed for a reason that had nothing to do with the defect. The failure output said `[]` rather than `[0, 1]`, which is what gave it away; a row asserting only 'not applied' would have passed for the wrong reason. STILL OPEN ON THIS BOX: REPLAY-path buffering (the `ReplayChunk` handler still calls `updateLastSeq` per event with no contiguity check - the server emits replay contiguously by construction, so this is a latent hole rather than an observed one), and SEQUENCE-COLLISION BLOCKING on the client (the store rejects a colliding sequence, but the client has no notion of one arriving). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 13/13; FULL unit suite 2421 suites / 33166 passed / 0 failed.
- [x] 5.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/__tests__/client.test.ts src/lib/multiplayer/server/__tests__/replayStream.test.ts src/lib/multiplayer/server/__tests__/reconnectionFlow.test.ts`.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 5 at 4/4): the literal command runs green in THIS session - npx jest --runInBand --runTestsByPath client.test.ts replayStream.test.ts reconnectionFlow.test.ts - alongside tsc 0, oxlint 0 errors / 77 warnings, and the full multiplayer tree; exact numbers recorded in the PR body.


## 6. Durable Admission, Active Routing, and Reauthentication

- [x] 6.1 Persist participant-to-campaign-session and participant-to-match bindings, and require authenticated durable membership before `bindMultiplayerSocketConnection.ts` or `bindCampaignSyncConnection.ts` registers a replay/fan-out recipient, implementing `Authenticated Membership Precedes Socket Attachment`.
  - Receipt (2026-08-24, fourth step — CLOSES this box: BOTH named binders are gated): the requirement names two binders, so the earlier notes deliberately left it open while only the campaign one was done. The combat binder turned out to be already gated, by work the ARCHIVED `add-authority-audit-and-privacy-proof` leaf delivered - verified rather than assumed by tracing the chain: `bindMultiplayerSocketConnection` calls `host.admitSocket`, which resolves through `AuthorizedViewerResolver` backed by `MatchSeatMembershipSource(IMatchStore)`, i.e. the DURABLE match store, and `attachSocket` runs only after `resolve()` succeeds. Its 29 admission tests already pin the substance, including 'a verified non-member is refused with typed frames only and never attaches', 'membership derives from durable seats - a store change is the revocation', and 'the seat membership source is durable-state-only'. WHAT WAS GENUINELY MISSING was the BINDER's own half of that contract - nothing proved a refusal actually STOPS the bind rather than the socket being wired up anyway and merely lacking a viewer. Two rows now cover it: a refused admission returns null, never calls `attachSocket` (so never becomes a fan-out recipient), and releases the connection slot rather than leaking it; and an inbound message after a refusal is never dispatched, because the message handler is registered BEFORE admission resolves and waits on a promise that only settles on success - so a refusal leaves inbound traffic permanently undelivered rather than merely unauthorized. The mock's `admitSocket` had to be widened to production's `IAuthorizedViewer | null`, because a mock that can only succeed cannot express the case the binder has to handle. Falsified 1:1: neutralising the binder's null check reds both new rows. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; binder suite 7/7, admission suites 3 suites / 29 tests; multiplayer battery 69 suites / 496 tests; FULL unit suite 2411 suites / 33078 passed / 0 failed. Both halves of the requirement now hold: participant bindings are PERSISTED (campaign_session_participant for campaigns, durable match seats for combat) and authenticated durable membership PRECEDES registration as a replay/fan-out recipient on both sockets.
  - Receipt (2026-08-24, third step — the record is now CONSULTED on the CAMPAIGN socket; box STAYS OPEN for the combat binder): `bindCampaignSyncConnection` takes membership as an optional PORT and production supplies the store-backed adapter from `server.js`. Two properties exist now that did not before. A REVOKED member is refused even holding a valid room code - previously revocation lasted exactly as long as they stayed disconnected, because the code was the whole check, so reconnecting undid it. A RETURNING member is routed by their membership rather than by re-presenting the invite, so a rotated or expired code no longer locks out someone already admitted. The room code keeps its real job as the NEWCOMER invite, and a newcomer it admits is recorded durably (host as `gm`, room-code guest as `player`) so it is not needed again. ABSENCE OF THE PORT IS THE STRUCTURAL FLAG: a socket bound without it keeps the pre-6.2 behaviour exactly, and all ten pre-existing tests in that suite pass unchanged with the port absent - which is the evidence that the fallback really is the old path rather than a subtly different one. The adapter is supplied by production rather than defaulted inside the bind function, because a default would reach for SQLite from every test that binds a socket and those tests have no database. TWO DESIGN NOTES: `isRevoked` is a SEPARATE question from `isActive` rather than inferred from it, because the store deliberately reports a revoked row as absent - through that lens a newcomer and a revoked member are indistinguishable, and only one of them may be admitted; and the port does NOT escalate the store's seat refusals, because this call records an admission the socket layer already granted by its own rules, and turning a bookkeeping refusal into a disconnect would drop a player the session had just accepted (enforcing seat limits AT admission is task 9, where the refusal has somewhere to go). A durable member whose live session cannot take them falls through to the ordinary paths rather than failing - not an authorization problem, and those paths report the real reason. Falsified 1:1: neutralising the revocation check reds the revoked-member row, neutralising the durable-routing check reds the wrong-room-code row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 14/14; multiplayer battery 69 suites / 494 tests; FULL unit suite 2411 suites / 33076 passed / 0 failed. **SCOPE OF THIS CLOSURE, STATED EXACTLY:** the requirement names TWO binders and only `bindCampaignSyncConnection.ts` is done. `bindMultiplayerSocketConnection.ts` (the combat socket) was not audited and is not claimed - it is tracked under task 6.2's remaining routing work rather than silently folded in here.
  - Progress (2026-08-24, second step — the durable RECORD now exists; see both notes): `campaign_session_participant` (SQLite migration 17) plus `CampaignSessionParticipantStore` persist one row per (campaign, session, participant) with the seat they hold. REVOCATION IS A TIMESTAMP, NEVER A DELETE - deleting would destroy the fact that they were ever a member (what the audit timeline needs) and make revoke-then-rejoin indistinguishable from never having left. ONE ACTIVE GM per session is enforced by a PARTIAL unique index rather than by application care, because a second GM is an authority split; partial so a revoked GM does not block their replacement, which is the whole reason the revoked row is kept. TWO TACTICAL SEATS is a count SQLite cannot express as a constraint, so it sits beside the insert and INSIDE the same transaction - counting free seats then inserting as separate statements would let two simultaneous joins each see one seat left and both take it. A reconnecting active member is `already-bound` (not an error, not a second seat); a revoked one is REFUSED rather than reinstated, or revocation would last exactly until they tried again; a second revoke returns false so a caller can tell "I revoked them" from "they were already out" without racing another revoker. Migration-ladder pins moved together: MAX(version) 16->17 and COUNT 15->16 (they differ because the ladder skips version 8). Falsified 1:1 - dropping the partial index reds the GM row, dropping the seat count reds the third-player row. Store suite 10/10; persistence migrations 8 suites / 87 tests; FULL unit suite 2411 suites / 33072 passed / 0 failed. STILL OPEN: nothing CONSULTS this record yet - the socket admission path still decides from the room code, so wiring it in is 6.2 and cold reauthentication from it is 6.3.
  - Progress (2026-08-24, first step — the ordering fix): found and fixed a live fan-out leak that this requirement's own title forbids. `addSocketToMatch(matchId, socket)` was the FIRST statement of `handleCampaignJoin`, executing before the room code was ever checked. A guest presenting the WRONG room code was answered `UNKNOWN_MATCH` and the handler returned - but the socket was already in `socketsByMatch`, nothing removes it, and `broadcast()` fans out to every socket in that set. So a client holding a valid auth token but no right to the campaign was refused entry and then received the campaign's events anyway: a refusal that refused nothing. MEASURED, not theorised - a RED test drove a rejected join and then a real host proposal, and the refused socket received **27 further messages** including `CampaignEvent` frames after its `UNKNOWN_MATCH`. FIX: registration now happens on each ACCEPTED path and never before - the host branch (admitted by identity, since `role` is host precisely because the verified player id matches the registry's host), the grant-channel `served` branch, and the room-code branch after `joinGuest` succeeds. The rejection paths simply never register. All nine pre-existing tests in that suite still pass unchanged, which is what shows the legitimate host and guest fan-out was not narrowed along with the leak. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; `bindCampaignSyncConnection` suite 10/10; multiplayer battery 69 suites / 490 tests; FULL unit suite 2410 suites / 33062 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the requirement has two halves and only the ORDERING half is done. There is still no PERSISTED participant-to-campaign-session or participant-to-match binding - admission is decided per connection from the room code and the registry's in-memory host identity, so membership does not survive a restart and cannot be revoked out of band. Task 6.2 ("replace active room-code routing with durable campaign-session/match routing") and 6.3 (cold reauthentication from durable membership) are the other half, and this box closes with them. What is true today is narrower and worth stating exactly: a socket is no longer a fan-out recipient before its join is accepted. `bindMultiplayerSocketConnection.ts` was NOT audited here and is not claimed.

- [x] 6.2 Replace active room-code routing with durable campaign-session/match routing after join while preserving invite expiry for newcomers.
  - Receipt (2026-08-24 — CLOSES this box): the two halves were pulling against each other, and each had a defect that only showed up once the other was proven. THE MEMBER PATH WAS EXPRESSED IN TERMS OF THE INVITE: `bindCampaignSyncConnection` admitted a durable member by re-presenting `entry.roomCode` on their behalf through `joinGuest`, so it worked only while the invite was still live - expiring the invite locked out exactly the people expiry is never aimed at. Members now route through a new `CampaignSyncSession.joinMember`, which hydrates identically but asks nothing about the code; the invite and the membership answer different questions and no longer share a code path. THE NEWCOMER PATH FELL BACK TO A CODE THAT NEVER EXPIRES: `roomCodeAdmitsGuest` resolved the expected invite as `entry.syncSession.getRoomCode() ?? entry.roomCode`, and `entry.roomCode` is a readonly field fixed at construction - so the moment the session stopped resolving the code, this predicate handed back the original and admitted the newcomer anyway. The same shape as the `envelope.roomCode ?? entry.roomCode` defect fixed in #1354, one layer up: a fallback that turns 'expired' back into 'valid'. Fallback removed - `null` means expired, and expired means refused. AND REHYDRATION COULD NOT SEE AN EXPIRED CAMPAIGN AT ALL: `getOrCreate` bailed on `!meta.roomCode`, but launching a match sets `clearRoomCode` (`ServerMatchHostLobbyIntents.LaunchMatch`), so a co-op campaign became UNREACHABLE the moment its invite was cleared - the members inside it could not cold-recover after a restart. It now rehydrates on `meta.coopCampaign` alone and opens the session through a new `openWithoutInvite()` rather than `open()`, because `open()` always ends with a LIVE invite and would have minted a replacement - rehydration must not re-open the door that launching closed. `opened` is tracked separately from `roomCode` for exactly that reason: an open session may legitimately hold no invite, and `roomCode` alone cannot tell 'opened without one' from 'not opened yet'. WHAT IS AND IS NOT CLAIMED, PRECISELY: these are correctness fixes for a state the code models and can enter, NOT proof of a presently-exploitable hole. Checked in this session rather than assumed - `hostDisconnected()`, the one path that clears a live invite, has NO production caller today (tests only), and the `LaunchMatch` -> co-op-campaign-match sequence is NOT traced end-to-end here, so before this change the expired-invite state was largely unreachable and the fallback was latent rather than live. The `getOrCreate` fix is the one with unambiguous present-day value. RESIDUAL, NAMED: `resyncGuest` still gates on `this.roomCode === null`, the same conflation - left alone deliberately because it has no production caller either, and rewriting unwired code to match a proof is churn, not coverage. THE MATCH HALF was verified by trace in 6.1's fourth receipt and re-confirmed here: `DurableMatchStore` indexes a room code only while `status === 'lobby'` (line 226), so launching expires the match invite, and combat routing resolves through `MatchSeatMembershipSource` over the durable store rather than through the code. Falsified 1:1, three separate neutralisations, each reddening exactly one row and only that row: restoring the `?? entry.roomCode` fallback reds 'refuses the original code once the invite expired' (1 failed / 8 passed); restoring `joinGuest(entry.roomCode)` on the member path reds 'cold-recovers a durable member after the invite expired' (1 failed / 17 passed); restoring the `|| !meta.roomCode` bail reds 'boots a LAUNCHED campaign whose invite the store already cleared' (1 failed / 6 passed). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; multiplayer battery 69 suites / 507 tests; FULL unit suite 2411 suites / 33089 passed / 0 failed. Both scenarios of `Durable Active Bindings Survive Invite Expiry` now hold: an active participant cold-recovers after the invite expired, and an identity without membership is rejected on that same expired invite without disturbing any existing binding.
- [x] 6.3 Implement cold reauthentication from account/vault identity plus durable membership and mint scoped session tokens outside URLs; reject revoked or mismatched identities before payload disclosure.
  - ERRATA (2026-09-02, findings #26/#28/#30/#33, Wave A3d - box stays closed for the SOCKET admission it claimed; the campaign REST surface was never inside it): the campaign REST routes under src/pages/api/campaigns/** read no Authorization header at all. Fixed in this PR: /commands took authorPlayerId from the BODY (finding #28 - forgeable journal attribution; flag-gated-future behind CAMPAIGN_JOURNAL_AUTHORITY_ENABLED, gated BEFORE the cutover so no journal is ever written with forgeable attribution) - the actor now comes from auth.playerId, a body still carrying the field is refused 400 author-not-accepted (typed, never stripped), the caller must hold any active seat (isActiveCampaignSeat, caller-scoped: commanding is every participant's, administering is the GM's); /replica-sync was SSRF + store poisoning (finding #30 - an unauthenticated caller chose the dial target, the credential and the destination store; not-a-replica asks about the campaign) - every verb needs a verified caller and a start is bound to the record's grantId, this campaign, an unexpired grant and the participant claimed (grant-mismatch / grant-expired / participant-mismatch); the session scope has ONE definition derived from the campaign record (campaignSessionScope.ts) shared by grants, commands and replica-sync. FINDING #33: #1521's grants gate REGRESSED the two-device share story - a session-less source campaign has no seats so nobody could issue a grant (401/403), and campaign-two-device-drive.spec.ts ran in no gated group so nothing went red (second #21-class miss; also corrects the A3c claim that grants are only issued via co-op). Decision, the #29 boundary: a campaign with ANY active seat keeps the GM gate exactly as shipped; a campaign with NO seats has no principal to authorize against and a player token there proves only that the caller minted one (issuePlayerToken signs client-side with a fresh keypair), so the route asks for nothing it cannot check - the exposure that remains is #29's (the body is already readable unauthenticated) and closes when the record gains an owner principal. The spec now presents credentials on the dial half and is registered as two-device-pack (green twice). Worker mutants killed: commands gate removed (3 red), scope from the request, actor from the body, replica-sync gate removed, grant binding removed (2 red), any participant accepted, seat-count branch inverted (8 red). NOT claimed: the grant token's signature is not verifiable on a consuming device (the pinned issuer key lives in the source's grant row) and the dial target stays caller-chosen - both need three fields redeem already verifies and discards (participantId, issuerPublicKey, a source address / sourceInstanceId resolution) on IReplicaCampaignAuthority; adopt, [id] GET/PUT/DELETE and index remain unauthenticated on the single-player path (finding #29, its own Epic - no half-measure). redeem.ts untouched: the signed Ed25519 grant IS its credential and it must work before any token exists.

  - Receipt (2026-08-28, ALL THREE CLAUSES EXIST; box CHECKED with the cutover residual named): #1420 closes the word the second step left open - SCOPED. The signed payload now carries an optional scope ({ kind: 'match' | 'campaign-session', id }) INSIDE the canonical Ed25519 bytes, and the test proves the placement: flipping one character of the scope id on the wire fails as `bad-signature`, not as a comparison miss - a scope outside the signature would be decoration an attacker rewrites. Verification is fail-closed in BOTH directions: a scoped token on a surface that does not pass an expectedScope is refused `scope-unchecked` (a forgotten check must fail closed, never silently widen), and a mismatch is refused `scope-mismatch` before any frame is sent. Both socket binders pass their expected scope from the upgrade path; the client mints per-connection scoped tokens and its cache refuses to reuse one match's token for another. THE CLAUSE LEDGER: cold reauthentication = the client re-derives identity from the stored keypair and admission is gated on durable membership (6.1's port, consulted on both binders) with revocation a timestamp that is never a delete; tokens travel in `Sec-WebSocket-Protocol`, never URLs (second step, 2026-08-24); mismatched identity is refused before payload disclosure at unit level on both sockets (second step); and the session token now NAMES ITS SESSION (#1420). EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-first - 11 rows red before the verifier existed. Worker mutations: ignoring expectedScope kills the mismatch and unchecked rows; signing WITHOUT the scope kills exactly the wire-flip row (it degrades to scope-mismatch instead of bad-signature - the precise difference between a signed scope and a decorative one). Reviewer re-verification, independently: neutralizing the whole scope gate reds 5 rows across the verifier and both binders, restored green. Typecheck 0 errors; oxlint 0 errors / 77 warnings; multiplayer+types battery 169 suites / 2207 tests; full suite 2440 suites / 33327 passed / 0 failed; 30/30 CI. RESIDUAL, named where the acceptance happens and here: a token with NO scope is still accepted on the sockets, because production REST minting still issues unscoped identity tokens. That is transition state, not a missing clause - scoped minting exists and is what connections now use; the HARDENING cutover (reject scopeless socket tokens once nothing legitimate presents one) is future work, recorded in the verifier's comment. 6.4's Playwright cold-reload proof remains that box's own open half.

  - Progress (2026-08-24, second step — THE CREDENTIAL IS OUT OF THE URL; box STAYS OPEN for 'scoped'): the wire token now travels in `Sec-WebSocket-Protocol` as `mekstation.token.<base64url>` beside a plain `mekstation.v1`, and `server.js` echoes ONLY the version marker - selecting the credential subprotocol would put the token straight back into a response header and undo the reason it left the URL. Base64url because RFC-6455 subprotocol names use the RFC-7230 `token` production, which excludes `/` and `=`; the transform is on the base64 TEXT, so what the verifier receives is byte-for-byte what the client held. A QUERY TOKEN IS NOW REFUSED, NOT TOLERATED: accepting both would leave every caller free to keep logging the credential, so the header would be an option rather than a guarantee. CORRECTION TO THE PREVIOUS NOTE'S INVENTORY, which overstated the surface: `matches/index.ts`, `matches/[id]/spectate.ts`, and `multiplayerPage.helpers.ts` stamp only `matchId` - they never carried the token. The real credential-in-URL surface was THREE production spots (`client.ts` `buildSocketUrl`, `campaignSyncTransport.ts`, and `server.js`) plus the hand-dialled `authority-privacy-three-context` e2e, which is why this fitted one PR after all. LIVE EVIDENCE, not inferred from green tests - `node server.js` booted on 3611 and dialled with curl: credential in the HEADER with an invalid token answers **401** (so the header was read and verification ran; an ignored header would have been 400 'missing token'); a query token answers **400**; and header PLUS query token still answers **400**, which discriminates the refusal from the generic missing-credential path - the URL is what is being refused, not the absence of a credential. The `ws` negotiation itself was verified against the real library in this worktree before the wiring was trusted: the server sees `mekstation.v1,mekstation.token.ab-c_de-fg_hij` and the client negotiates `"mekstation.v1"` with the credential absent from the response. THAT SMOKE IS NOT COMMITTED AS A TEST, and the reason is worth recording: the `unit` jest project runs jsdom, where `ws` resolves to its browser stub and `WebSocketServer` is not a constructor, and a `@jest-environment node` docblock then fails on the shared `jest.setup.js`, which assumes `window`. Adding a third jest project for one test is scope this task does not justify; the regression surface is instead the e2e spec, which now dials with the subprotocol and would fail to open if `handleProtocols` regressed. `server.js` cannot import the TS module (CommonJS at the repo root), so the constants and decode are inlined and pinned by `serverCredentialMirror.test.ts`, which reads both files - drift there would be silent and total, every valid token failing to authenticate. That mirror test normalises line endings, because `server.js` is CRLF in this checkout and an exact-match assertion passed on one platform only. WHY THIS BOX STILL STAYS OPEN: 'scoped session tokens'. Today's credential is a self-derived PLAYER IDENTITY token that authenticates the same principal to every match; making it session-scoped is a design decision about lifetime and audience, not a transport change, and it should be taken deliberately rather than smuggled in behind this one. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; FULL unit suite 2413 suites / 33100 passed / 0 failed.
  - Progress (2026-08-24, STAYS OPEN — one of three clauses is unbuilt): THE MISMATCHED-IDENTITY CLAUSE IS NOW PROVEN AT UNIT LEVEL ON BOTH SOCKETS. It held before this pass, but the only proof was `authority-privacy-three-context.spec.ts`, a Playwright spec that PR CI never runs - so the guard was one careless edit away from silently disappearing between nightly runs. Three rows now pin it: a combat `SessionJoin` claiming another player replays nothing and closes 1008/`player-mismatch`; a combat `Intent` claiming another player after an HONEST join dispatches nothing (guarded separately from `SessionJoin`, so passing the first tells you nothing about the second); and a campaign envelope claiming another player gets `AUTH_REJECTED`/`player-mismatch` with NOTHING but the refusal on the wire - no snapshot, no event, no pending-proposal list. That last assertion is the 'before payload disclosure' half stated as a property rather than a hope. VERIFIED BY READING, then pinned: `handleCampaignHostIntent` authorises on `envelope.playerId !== entry.hostPlayerId` - a CLIENT-SUPPLIED field - which is safe ONLY because the campaign socket guards every inbound kind at one upstream place (`handleInbound`, line 285). The safety is entirely borrowed; a proof of the downstream comparison would prove nothing without the upstream one, which is why the row is written against the upstream guard. Falsified 1:1 three times, each neutralisation reddening exactly one row: `false &&` on the combat `SessionJoin` guard, on the combat `Intent` guard, and on the campaign guard. THE OTHER TWO CLAUSES, HONESTLY: 'cold reauthentication from account/vault identity plus durable membership' is structurally satisfied by work already receipted - the upgrade derives `verifiedPlayerId` from the vault-signed token (`derivePlayerId(publicKey)`), durable membership then admits without the invite (6.2), and revoked identities are refused (6.1 third receipt) - but it is claimed only as far as those receipts go, and no cold-reauth-specific proof is added here. 'MINT SCOPED SESSION TOKENS OUTSIDE URLs' IS NOT BUILT AT ALL, and it is the reason this box stays open. Surface inventory measured in this session so the next pass does not rediscover it: `server.js` reads the credential from `parsedUrl.query.token` in the upgrade handler; URL builders that stamp it are `client.ts` `buildSocketUrl`, `campaignSyncTransport.ts`, `pages/api/multiplayer/matches/index.ts` (line 66), `pages/api/multiplayer/matches/[id]/spectate.ts` (line 69), and `pages-modules/multiplayer/multiplayerPage.helpers.ts` (line 14), plus e2e specs that build socket URLs by hand. Proposed mechanism: carry the wire token in `Sec-WebSocket-Protocol` as `mekstation.token.<base64url>` alongside a plain `mekstation.v1`, with `handleProtocols` echoing ONLY the version protocol so the credential never appears in the response headers; base64url because RFC-6455 subprotocol names exclude `/` and `=`, which raw base64 uses. That is a five-surface client sweep plus a `server.js` change plus its inline-mirror test, which exceeds the per-PR cap on its own - it is a separate seam, not a corner of this one. Also unaddressed: 'SCOPED' session tokens as a concept. Today's credential is a self-derived PLAYER IDENTITY token, not a session-scoped one; making it session-scoped is a design decision, not a refactor, and it should be taken deliberately rather than smuggled in with the transport move. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; both binder suites 28 tests; multiplayer battery 69 suites; FULL unit suite green.
- [x] 6.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindMultiplayerSocketConnection.test.ts src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.test.ts src/lib/multiplayer/server/__tests__/auth.test.ts` and a focused Playwright cold-reload test.
  - Receipt (2026-08-29 - CLOSES this box, and the cold-reload clause found TWO product defects before its first green): the named jest command runs green (3 suites / 63 tests). The focused Playwright cold-reload row now exists inside the live vault-auth spec: after host/join/launch/advance, the guest hard-reloads mid-match and must land back in the LIVE Movement phase seeing the opponent's unit - not a lobby, a prompt, or a dead end. FIRST DEFECT: the minted wire identity lived only in React state, so every reload fell back to the vault-password prompt; fixed by persisting MultiplayerTokenState in sessionStorage keyed by room code - the exact custody class and pattern the co-op campaign token already uses (the store #1357 moved credentials INTO when they left the URL). SECOND DEFECT, one layer deeper: invites deliberately stop resolving once a match goes active, and the lobby page derived its matchId from the invite on every mount - so even an authenticated reload dead-ended at 'Lobby not found' (screenshot-verified). Fixed by riding the resolved matchId beside the stored identity: a party to the match still knows which match it is, a stranger with the dead code and no stored identity still sees not-found, and the socket still authenticates the wire token server-side. FALSIFICATION, run live: clearing the guest's sessionStorage before the reload reds the row. Gates: tsc 0; oxlint 0 errors / 77 warnings; vault-auth + p2p-sync together 13 passed (the reconnect flows sharing the page stay green).

  - Progress (2026-08-24, STAYS OPEN — the Playwright half is not written): the three named suites pass together under `--runInBand` (3 suites / 29 tests). Writing the cold-reconnect proof found an AUTHORIZATION DEFECT that predates this program's work. The guest join path resolved the invite as `envelope.roomCode ?? entry.roomCode`, so a client sending NO room code had the session's OWN correct code substituted and the join always succeeded - omitting the invite was equivalent to presenting the right one, and any authenticated player who knew a match id could join a campaign they were never invited to. The room code protected nothing against them. Introduced in `27e1ee448` ("Prove live co-op campaign play through server transport"), found now only because a CONTROL row asserted that a stranger presenting no code is refused, and it was not. Fallback removed: a newcomer must actually present a code, and a durable member never reaches that line because the membership path admits them first - which is what makes the removal safe rather than a regression for people who legitimately have no code to present. The paired rows are the point: one proves a member can cold-reconnect on a brand-new socket with NO room code (the situation after a page reload or client restart, which before durable membership was simply a rejected join), the other proves a stranger doing the same is refused. Without the second, the first would pass equally against a server that had stopped checking anything at all. Falsified 1:1: restoring the fallback reds the stranger row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 16/16; multiplayer battery 69 suites / 498 tests; FULL unit suite 2411 suites / 33080 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task names a focused PLAYWRIGHT cold-reload test and that is not written. Stated honestly, a browser cannot observe the property that matters here - a real client re-presents whatever room code it cached, so the durable path and the invite path are indistinguishable from outside. A meaningful browser version needs the invite to be invalidated between joins (room-code rotation or launch-time clearing), which is task 6.2's remaining "preserve invite expiry" work; the Playwright row should be written once that exists, rather than written now against a scenario that cannot fail.


## 7. Durable Publication, Heartbeat, and Backpressure

- [x] 7.1 Publish committed results from durable outbox rows after transaction commit and resume pending publication after restart, implementing `Commit Precedes Recipient Publication`.
  - Receipt (2026-08-30 - CLOSES this box): committed results now publish from durable outbox rows, and restart recovery drains what a dead process never sent. The live intent path commits each command as ONE atomic batch - events plus their mp_match_outbox rows in the same transaction (the store half shipped with 3.x; this closes the host half, whose commitThenPublish passes 0/3 were honestly documented as inert because the legacy path still appended event-at-a-time). Pass 2 publishes the batch's frames by reading its OWN rows back (listPendingPublications filtered to the batch), the literal reading of 'only from durable publication records created in the same transaction'; each row is marked published one at a time after its own send, so a mid-fanout death leaves unsent rows pending. Restart: MatchRecovery drains pending publications AFTER restoring viewer delivery cursors, through a new undelivered-only broadcast mode (publishEvent's existing onlyUndelivered arm, backed by deliverySequenceOf) - a drained frame never assigns a fresh delivery number to a viewer whose cursor already records it, which resolves the file's flagged cursor-corruption unknown; the same cursor-safe mode now serves the journal-authority duplicate path's resume. Command identity: intentId when the client sent one, first-event-id fallback that never dedupes (fresh ids per re-dispatch - kept honest in a comment). Failed transaction: typed STORE_FAILURE, no committed frame, no rows, nothing to resume - proven against the _setFailAtHeadUpdateForTests seam. Proofs on a real seated host over SQLite: receipt-probe distinguishing the batch path from the old one (red first: probe answered committed, no receipt existed); crash-sim orphan batch delivered exactly once on recovery with the drain idempotent on second call; bootstrap recovery settles all pending rows; failure row. Two suites re-anchored honestly for the path change: the serialized-executor GatedStore now gates appendCommandBatch (the ascending-starts law transfers), and the shadow NO-DUAL-AUTHORING invariant became 'shadow performs EXACTLY the writes mode-off performs' since zero-batch-appends no longer means what it did. Wire frames for players carry no authority sequence (the 11.x redaction), so the proofs match by event identity. Mutations (reviewer-authored, no worker this seam - codex lane capped, orchestrator built it): mark-without-send dies at two rows; recovery-drain no-op dies at two rows; publish-from-memory-bypassing-rows dies at three rows. Gates serial and clean-tree: tsc 0; oxlint 0 errors / 77 warnings (an intent-module max-lines breach was fixed by extracting shadowAudienceInput to its natural home in journalAuthorityShadow, not by suppression); full multiplayer tree 112 suites / 871 passed. Scope note: the box's 'resume publication after restart' is the event-outbox half; the combat-outcome outbox half shipped in 13.1-13.3.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, THE OUTBOX EXISTS AND RESTART RESUME WORKS; box STAYS OPEN for the live intent path and boot-time drain): #1397 added `mp_match_outbox` written INSIDE `appendCommandBatch`'s existing transaction (the review's central blocking finding was that this exact claim had ZERO coverage - moving the INSERT out of the transaction left all 657 tests green; the remediation added the row that reds when the INSERT leaves the transaction, and a second that reds when the `published_at IS NULL` idempotency guard is deleted); #1403 extracted the commit/publish passes from the intent gates; #1404 drains records a previous run never delivered, on the next command after restart. STILL OPEN, stated plainly: (1) the LIVE intent path still commits event-at-a-time through `appendEvent` and publishes from memory - `Commit Precedes Recipient Publication` holds at the store layer but not yet on the intent path; routing the commit pass through `appendCommandBatch` is owned by the combat-journal leaf (wave-map order 4); (2) no boot-time drain - `recoverActiveMatches` runs before any socket attaches, so the resume deliberately fires on the next command instead; a true boot-time resume needs a hook where a recovered host first gains a live recipient; (3) delivery is fail-open at the last hop: a drain marks a record published when `broadcastEvent` returns, and that resolves normally having reached nobody - closing it needs per-viewer delivery cursors on the outbox record.
  - Progress (2026-08-24, STAYS OPEN — there is still no outbox and no restart resume): the ORDERING half is done and it was a real defect. The intent path appended and broadcast in the SAME pass, so a command failing partway through had ALREADY told every client about the events that landed before it - recipients applied half a command, the match then closed underneath them, and no reader afterwards could tell that half-state from a command that legitimately produced fewer events. THE FAILURE BRANCH HAD NO TEST ANYWHERE (grepped: the only `STORE_FAILURE` reference outside production was the protocol schema's own test), which is exactly how the optimistic path survives. Extracted as `commitThenPublish` in two steps so the evidence is honest: first a pure extraction with the interleave preserved, then the row 'publishes NOTHING when an event partway through fails' written and watched RED, then the split into a commit pass and a publish pass. Nothing reaches a recipient from inside the commit pass; the publish pass is reached only when the whole command is durable. The failure returns at the FIRST failed append rather than pushing on, because further appends only deepen a commit the caller is abandoning. Falsified 1:1: restoring the interleave reds two rows - the defect row, and the success row, whose event count doubles because the interleaved version publishes in both passes. WHAT IS NOT CLAIMED, PRECISELY: (1) ATOMICITY. Events still go down one at a time, so a mid-command failure still leaves the earlier ones COMMITTED - it is only their PUBLICATION that is now withheld. That boundary is the store adapters' `appendCommandBatch`, which exists and is tested (#1343) but is not wired into this path; task 3.1 owns it. (2) THE OUTBOX. This publishes from the in-memory event objects after their appends return, NOT from durable publication rows written in the same transaction as the batch, which is what the requirement's first sentence asks for. (3) RESTART RESUME. Nothing replays pending publications on boot. The nearest thing that exists is reconnect-and-replay - a client's `SessionJoin` carries `lastSeq` and `sendReplay` resumes from durable storage without re-executing the command - which arguably satisfies the second scenario's shape, but it is client-driven rather than a server-side resume, and it is deliberately NOT claimed as one. Closing this box needs the outbox table, its same-transaction insert, and a boot-time drain; that is a separate seam from this ordering fix and larger than the per-PR cap. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 4/4; multiplayer battery 73 suites / 532 tests; FULL unit suite 2415 suites / 33114 passed / 0 failed.
- [x] 7.2 Complete the bidirectional heartbeat contract in `Protocol.ts`, `ServerMatchSocketLifecycle.ts`, and `client.ts`; prove quiet healthy sessions remain connected and dead peers transition through reconnect.
  - Receipt (2026-08-24 — CLOSES this box): the contract was declared but only half-built, and the missing half was a LIVE DEFECT. `Protocol.ts` already documents Heartbeat as 'sent in BOTH directions' and already carries `HeartbeatSchema` in `ClientMessageSchema`, and the combat dispatcher already had a `case 'Heartbeat': return;` - so the server was ready to receive one. The CLIENT never sent one; `client.ts` handled the inbound ping with the comment 'clients do not need to echo'. But the server's liveness check is `Date.now() - lastInboundAt > HEARTBEAT_TIMEOUT_MS` and `lastInboundAt` moves only on INBOUND traffic, so a player who was WATCHING rather than acting - a spectator, or anyone waiting out an opponent's turn - was reaped after 60s of their own silence and reconnected into the same silence. Written failing-test-first: the row asserting a quiet client emits heartbeats was RED before the change. FIX IS CLIENT-ONLY, which is the honest scope of this box: `startHeartbeat` fires at `HEARTBEAT_INTERVAL_MS` (the server's own interval, so a single dropped frame still leaves two more inside the timeout), guards on `runtime.state.socket === socket && readyState === 1` so a reconnect's swapped-out socket is not written to forever, and swallows send failures because a failed keepalive is not worth surfacing when `onclose` reports the death properly. Cleared from BOTH close paths (`handleSocketClose` and `closeClient`) - a timer outliving its socket keeps the process alive and writes to a corpse. NEITHER `Protocol.ts` NOR `ServerMatchSocketLifecycle.ts` NEEDED A CHANGE, and that is stated rather than invented: both were read, and the server ping, the reaper, and the schema were already correct. 'QUIET HEALTHY SESSIONS REMAIN CONNECTED' is proven at both ends: the client row asserts a session idle for a full server timeout still emits more than one heartbeat, and a new lifecycle row drives the client's actual cadence against the reaper for FOUR consecutive timeout windows. That second row is what binds the two constants together - raise the client interval above the server timeout and it reds while every other row stays green. 'DEAD PEERS TRANSITION THROUGH RECONNECT' was already covered and is claimed only that far: the reaper closes the socket (pinned by 'detaches idle sockets after the heartbeat timeout window', which asserts `closeCount === 1`), and the client schedules a reconnect on close (pinned by 'schedules a reconnect on close (exponential backoff)'). Falsified 1:1 twice: neutralising the `startHeartbeat` call reds the client row and nothing else; removing the `noteInbound` from the cadence loop reds the cadence row, which is what shows it is not passing vacuously - the socket really would have died. A PAIRED ROW IS DELIBERATELY WEAK TODAY: 'stops heartbeating once the caller closes the client' passes trivially against a client that never heartbeats at all, and only became meaningful once the timer existed; it is kept because it pins the teardown, not the send. OUT OF SCOPE, NAMED: the CAMPAIGN socket has no liveness timer and its transport sends no heartbeats, so nothing there is reaped for silence - this box names the three combat files and is claimed no wider. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 10/10, lifecycle suite 6/6; FULL unit suite 2413 suites / 33103 passed / 0 failed.
- [x] 7.3 Add bounded per-connection queues to `ServerMatchBroadcaster.ts`; on saturation mark only that participant behind, continue healthy delivery, and resynchronize from the durable viewer cursor.
  - ERRATA (2026-09-02, finding #18, fixed by Wave A2b - box stays closed, its claim is now true on the fact path): the 2026-08-24 receipt's bound covered ServerMatchBroadcaster.broadcast ONLY. The per-viewer authorized event fan-out (ServerMatchHostEvents.publishEvent) resolved recipients from lifecycle.attachedSockets() and pushed each frame through broadcaster.safeSend, which applied no cap and consulted no behind set; during a live match broadcast carries typed errors, pause/resume and host migration - never command traffic - so a stalled viewer was handed authorized facts without limit and never entered the behind state at all. The bound now lives in ONE place, ServerMatchBroadcaster.admitForSend, and both fan-out paths ask it. Placement is load-bearing and was corrected against the orchestrator's first instruction: the per-viewer path asks AFTER deliveryCursors.assign, because ViewerDeliveryCursors distinguishes a frame never owed (consumes no number) from a frame owed and lost (consumes its number so the hole is a true loss signal) and firstMissedAuthoritySequence resumes the rejoin from exactly that number - the before-assign variant, run as a mutant, re-delivered already-applied frames (15 frames / 12 distinct). Proven on a real ServerMatchHost + real ViewerDeliveryCursors + the real replay path (ServerMatchHostEventsBackpressure.test.ts: saturated viewer refused while healthy viewers stay contiguous; refusal sticky until reaped; rejoin resumes at the first missed frame, no gap, no duplicate) - two of four rows RED on the pre-fix tree. Mutants killed: before-assign, behind socket not skipped, >= at the cap boundary. Contract preservation live: exactly-once-pack 4/4, restart-pack 2/2, resilience-pack 1/1 twice.

  - Receipt (2026-08-24 — CLOSES this box): THERE WAS NO BOUND AT ALL. `broadcast` called `socket.send` for every socket and swallowed failures; `ws` buffers what it cannot flush, without limit. One stalled client grew the server's memory for the length of a match, and every other recipient waited behind its `send` on the way past. THE QUEUE IS `bufferedAmount` - there is no queue of our own to bound, because `send` hands the payload straight to `ws`, so the bound is a check on the buffer `ws` already keeps. `MAX_BUFFERED_BYTES` is 1 MiB: far more than a healthy client ever holds (frames are small and drain in milliseconds) and far less than a stalled one reaches in a long match. ON SATURATION the connection is marked behind and skipped, and it STAYS skipped even after its buffer drains. That is deliberate and is the subtle half: resuming mid-stream would leave a HOLE in that connection's event sequence, and a hole it does not know about is worse than a gap it does. RESYNCHRONIZATION IS RECONNECT-AND-REPLAY, and the box is closed on that basis: the lifecycle's existing heartbeat tick now reaps a behind connection exactly as it reaps an idle one, the client's `onclose` schedules a reconnect (both already pinned), and its `SessionJoin` carries `lastSeq`, which `sendReplay` resumes from out of durable event storage. RESUMING FROM THE CLIENT'S CURSOR IS THE MORE CORRECT CHOICE, not a shortcut: the server's `deliveredCursor` records what was successfully SENT, while the client's `lastSeq` records what actually ARRIVED, and a frame can be sent into a dying socket and never parsed. The server-side cursor is preserved and exposed (`deliveredCursor`, `behindSockets`) because the spec asks for it and because 7.4's convergence gate is its first real consumer - stated plainly, NOTHING CONSUMES IT YET. A PROPERTY WORTH RECORDING: the bound applies to LIVE fan-out only. The join/replay path goes through `safeSend` (`ServerMatchHostReplay` lines 89-93), which has no saturation check, so a legitimate catch-up burst cannot trip the bound and put a client into a reconnect loop for the crime of needing a big replay. Verified by reading both paths in this session, not assumed. Side-tables are cleared on `unregister`, or they leak one row per connection for the life of the match. `IMatchSocket.bufferedAmount` is OPTIONAL: absent means 'no backpressure signal' and reads as healthy - every test double in the suite lacks it, and inventing saturation for a socket that cannot report it would disconnect all of them. Falsified 1:1 on each seam: neutralising the skip-if-behind reds 'keeps a behind connection behind even after its buffer drains'; neutralising the `unregister` cleanup reds 'forgets a connection entirely'; neutralising the lifecycle reap reds 'reaps a connection the broadcaster gave up on'; neutralising the saturation check reds FIVE rows, which is honest rather than tidy - it is the check the rest of the mechanism hangs off. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; broadcaster + lifecycle suites 13/13; FULL unit suite 2414 suites / 33110 passed / 0 failed. Two of the three spec scenarios hold outright ('Slow player enters behind state', 'Healthy recipients continue'); the third ('Progression remains convergence-gated') is 7.4's and is NOT claimed here.
- [x] 7.4 Add tests for one socket-send failure, slow-client isolation, bounded queue/memory behavior, and convergence-gated progression; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchSocketLifecycle.test.ts src/lib/multiplayer/server/__tests__/reconnectPersistence.integration.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts`.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 7 at 4/4): evidence-mapped close - every named test exists from the waves that built the machinery, and the box's literal command runs green in THIS session: `npx jest --runInBand` over ServerMatchSocketLifecycle.test.ts + reconnectPersistence.integration.test.ts + hardenedTransport.test.ts = 3 suites / 23 tests passed. Clause map: ONE SOCKET-SEND FAILURE -> hardenedTransport 'keeps delivering when one socket throws on send' plus 'does not advance a cursor for a send that threw' (the throw neither stalls the fan-out nor lies about delivery); SLOW-CLIENT ISOLATION -> 'skips a saturated connection and keeps delivering to healthy ones' plus 'keeps a behind connection behind even after its buffer drains' (behind is a fact about missed frames, not current buffer level); BOUNDED QUEUE/MEMORY -> the MAX_BUFFERED_BYTES saturation rows - a connection past the cap gets NO further queued sends (marked behind, cursor preserved at the last received event), which IS the memory bound: nothing accumulates for a dead reader, recovery rides the replay path from the preserved cursor; CONVERGENCE-GATED PROGRESSION -> CampaignSyncSession 'allows the launch once every retained participant has converged' plus the blocked-launch-must-not-lock-out-joining delivery row (9.4's machinery, verified 9.4 checked). Lifecycle suite additionally pins heartbeat-idle reaping, reap-on-broadcaster-giveup, and no-timer-leak reattach; reconnectPersistence pins the durable resume side those clauses depend on. Nothing needed adding - the box's tests were authored by the M2/M3 transport waves and 9.4, and this box's own command is the proof they run together; the honest scope note is that 'bounded queue' is implemented as drop-past-cap-with-cursor (no unbounded per-client queue exists to bound), which the spec's slow-client scenario is satisfied by.

  - Progress (2026-08-24, STAYS OPEN — convergence-gated progression does not exist to test): THREE OF THE FOUR named test subjects are now covered. ONE SOCKET-SEND FAILURE: the `catch` inside `broadcast` was pure optimistic-path code with nothing behind it - two rows now pin it, one proving a socket that throws mid-fan-out does not take the event away from everybody iterated after it, and one proving a throwing send does NOT advance that connection's cursor. That second row matters more than it looks: the cursor is what ARRIVED, and recording a sequence whose send threw would tell a later resynchronization to resume PAST an event the connection never received, skipping it forever. Falsified 1:1 by moving the cursor write above the `send`, which reds exactly that row. SLOW-CLIENT ISOLATION and BOUNDED QUEUE/MEMORY were delivered and falsified under 7.3 (#1359) - 'skips a saturated connection and keeps delivering to healthy ones', 'keeps a behind connection behind even after its buffer drains', 'forgets a connection entirely on unregister'. THE THREE NAMED SUITES RUN GREEN TOGETHER under `--runInBand`: 3 suites / 23 tests. WHY THIS BOX STAYS OPEN: convergence-gated progression. Grepped this session - the words `convergence` and `converged` appear NOWHERE in `src/lib/multiplayer/`, production or test. The spec scenario ('a player remains behind at a scenario or correction boundary -> delivery to healthy clients continues but launch of the next scenario remains blocked until convergence or audited GM removal') describes a gate that has not been built, and a test for it would either be vacuous or would be the feature itself smuggled in as a test. The inputs it needs now exist and are proven - `behindSockets()` and `deliveredCursor()` from 7.3 - but nothing consumes them, and the scenario-launch path has no gate to hang them on. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; broadcaster suite 8/8; the three named suites 3 suites / 23 tests; FULL unit suite 2415 suites / 33116 passed / 0 failed.

## 8. Durable Campaign Journal Migration and Cutover

- [x] 8.1 Replace `CampaignHostRegistry.ts`'s unconditional in-memory event-store selection with an environment-aware durable server campaign journal while preserving a loud dev/test adapter.
  - Receipt (2026-09-02, Wave B2 PR 1 of 2 - CLOSES this box; premise corrected): on the pinned base the selection was NOT unconditionally in-memory - CampaignHostRegistry already read sqliteReady() ? JournalCampaignEventStore : createDefaultCampaignEventStore() - so what was genuinely missing was the ENVIRONMENT half and the LOUD half: a process that promised durability and lacked it degraded silently to a projection-only log. selectCampaignEventStore (src/lib/multiplayer/server/getCampaignEventStore.ts) states the choice in getDefaultMatchStore's shape with three arms: SQLite open -> JournalCampaignEventStore; durability promised (NODE_ENV=production, or CAMPAIGN_EVENT_STORE=durable) and absent -> CampaignDurableStoreUnavailableError, fail-closed, no in-memory substitute accepted as durability evidence; dev/test -> the kept ephemeral adapter, announced once per process (InMemoryMatchStore's dev-only warning idiom). CAMPAIGN_EVENT_STORE=memory is honoured everywhere including production - the design Migration Plan's rollback lever; an operator who names the downgrade has accepted it. server.js initialises SQLite at boot and routeHelpers per API route, so no live path relied on the silent downgrade. CampaignHostRegistry uses the selector and carries eventStoreDurability on the entry so a recovery/diagnostic path cannot mistake an ephemeral log for authority it can rebuild from. Red-first: module-not-found on the selector suite; the two registry rows asserting eventStoreDurability were red (undefined) against the unmodified registry via git stash. Mutant M6 (production fail-closed throw deleted) killed by 'refuses to substitute memory in production'; orchestrator re-ran it as the silent-fallback rewrite (1 red). Gates: tsc 0; oxlint 77-pin; the 8.5 command 3 suites / 44; campaign+multiplayer 281 / 3585. No migration added.

- [x] 8.2 Add idempotent backfill from existing materialized campaign snapshots into a genesis branch, preserving snapshots during migration and blocking ambiguous ownership for GM remapping.
  - Receipt (2026-09-02, Wave B2 PR 2 of 2 - CLOSES this box): backfillCampaignFromSnapshot (src/lib/campaign/authority/campaignSnapshotBackfill.ts) is built ENTIRELY on the merged prior art - 5.2's importCampaignBaseline still does the appending onto ROOT_EVENT_BRANCH_ID at sequence 0, 1.4's ICampaignMarkerIo is still the marker seam - no second journal, migration ledger, or genesis path. Order is load-bearing: marker check, then the ownership gate, then append. Idempotency comes from the RECORDED MARKER, not the append, because the journal REPLAYS an identical retried command rather than refusing it (mutant lesson: the first idempotency row used differing inputs, so the journal's own command-digest guard was what protected it and the marker check was untested; the byte-identical-retry row is the one that can only pass through the marker - orchestrator re-ran the marker-read-ignored mutant, 1 red). Ambiguous ownership FAILS CLOSED before anything is written: mapCampaignForceOwnership's taxonomy is force-unclaimed, force-owner-not-a-player (GM seat deliberately excluded from tactical players; revoked members too), force-claimed-by-several (the claim table is per-mission, so a force can carry two holders across missions - orchestrator mutant dropping this case killed by 'blocks a force two players claimed on different missions'), and unit-in-several-forces; evidence is read from 9.1's durable campaign_session_participant rows plus the new listCampaignSessionForceClaims. An audited GM remapping is the only way through, may name only one of the two tactical slots, and cannot resolve a unit-level collision. Snapshots are PRESERVED, proven not asserted: a real envelope saved through saveCampaign, backfilled, re-read identical. campaignAuthorityMigration gains one additive optional field, importedBaseline.ownershipRemapping, distinguishing a mapping READ from records from one a GM DECIDED. Worker mutants killed (6/6 incl. the M1 rewrite). NOT claimed: nothing in production calls the backfill - maybeBackfillCampaignFromSnapshot is flag-gated and inert, activation wiring belongs with 5.7 cutover; CAMPAIGN_JOURNAL_AUTHORITY_ENABLED stays false; an ambiguous campaign does NOT persist a 'blocked' cutover marker (that arm is typed for shadow-parity evidence; a campaign that never began migration is honestly legacy - the typed refusal re-evaluates on retry, self-healing once the GM remaps); a roster unit in NO force is unowned, not ambiguous. No migration added (campaign_authority_migration #9, participants, force claims #19 already exist). Gates: tsc 0; oxlint 77-pin; campaign+multiplayer 281 / 3585; api campaigns 8 / 57.

- [x] 8.3 Make campaign activity a role-scoped authoritative projection rather than a browser-global log and remove whole-envelope 409 retry as a conflict strategy.
  - CLOSED (2026-09-02, Wave B5 8.3 part B, the UI cutover): the dashboard and the log page render useCampaignActivityFeed(campaignId) instead of the browser-global FIFO. The activity card and the log table take display rows { id, campaignDay, category, message } mapped by a pure campaignActivityDisplay module from ICampaignActivityEntry (ordinal / occurredAt, no payload), plus the feed state: loading; forbidden renders the server's message verbatim (never blank, never the FIFO); error renders a truthful message; needs-identity renders a rejoin prompt with no FIFO; local renders the FIFO rows under the honest source label; authoritative empty says the shared log has nothing yet. activityLog is REMOVED from IDashboardSummary and from the summary selector's return, so the FIFO cannot compile back into the card (the compiler named every consumer; each was switched). The client never filters rows by role - viewerSeat is echo only - and never calls getCoopLocalPlayerId. Rows: the display mapping (ordinal -> id, day/category/message, stable order); a seated participant sees the authoritative entries and a FIFO-only message seeded in the store is ABSENT; a guest sees exactly what the feed returned; the log page and the dashboard render the same rows for the same feed state; a 403 renders the server's message and no FIFO rows; solo renders the FIFO under the honest label; co-op without identity renders the rejoin copy; the summary no longer exposes activityLog (a ts-expect-error pin plus a runtime absence check). Orchestrator mutants in the PR body. The 409 half closed with 8.4/8.5 (one PUT, no whole-envelope retry). NOT CLAIMED: no audit-feed merge (ViewerHistory stays separate); no journal rows for day-advance / medical / travel / battle (solo still writes only the FIFO); the FIFO writer is untouched; no solo participant backfill.

  - PROGRESS (2026-09-02, Wave B5 8.3 part A, the client half's foundation): the browser can now name itself to the activity route without inventing a role. useCampaignViewerIdentity answers none (no coopSession - solo), needs-identity (a co-op session but no participant id in hand), or pair: sessionId = getCoopMatchId(coopSession) (the session id the route binds IS the match id, bindCampaignSyncConnection writes sessionId: matchId), participantId = the live sync transport's playerId, else the stored co-op token's playerId - NEVER getCoopLocalPlayerId (it returns the role labels host/guest, and a role is not a participant), never the campaign owner, never hostPlayerId, never a scraped grant token. useCampaignActivityFeed(campaignId) returns local (the Zustand FIFO with the honest label 'This browser's campaign log (not shared)' and NO request - the solo day pipeline still writes only the FIFO, and an empty 'authoritative' feed would lie), needs-identity (no request; rejoin copy), or authoritative with loading / ready / forbidden (403 carries the server's error string, never empty-ready) / error (never silently empty); refresh on mount, identity change, window focus and the live transport's CampaignEvent only - no interval, the journal does not tick; AbortController on identity change and unmount; one in-flight request per (campaignId, sessionId, participantId) so the dashboard and the log page share it; viewerSeat is echo only, the client never re-filters by role. Rows (18): the six identity rows including 'never uses the host/guest role label as a participantId', and the nine feed rows (local makes no request; needs-identity makes no request; pair fetches with both params; 403 -> forbidden with the message; 500 -> error; two consumers share one request; the cache key includes participantId; one CampaignEvent -> exactly one refetch; unmount aborts). Orchestrator mutants in the PR body. NOT CLAIMED: the dashboard and the log page still read the FIFO (part B switches them and drops activityLog from the summary so the FIFO cannot compile back into the card); the FIFO writer is untouched; no solo participant backfill; 409 work is 8.4/8.5's and closed.

  - Progress (2026-09-02, Wave B2c seam 1 - the ACTIVITY half; box stays OPEN on the 409-retry half, which ships WITH 8.4 by design): the browser-global log characterised, not guessed - useCampaignStore.types' 200-entry FIFO in Zustand, persisted per device, appended by that device's own day pipeline, read by useCampaignDashboardSummary and the /log page: three participants hold three logs that need not agree, none outlives the browser, role never enters. Replacement: projectCampaignActivityForViewer (src/lib/campaign/activity/campaignActivityProjection.ts) derives rows from the committed campaign stream, folding through applyCampaignEvent over the VISIBLE set only - admission delegated wholesale to campaignViewerVisibleEvents with the caller's predicate (campaignScopeAdmits) injected, NO second role filter; and the one thing admission cannot express: ParticipantRemoved.reason is the audited GM rationale on a campaign-scoped fact, so every player was admitted to the GM's reason through a public fact - the removal renders for everyone, the rationale only for a viewer holding GM private detail. The seat is READ from campaign_session_participant, never asserted; unbound or revoked -> not-a-participant refusal (serving the campaign tier to a stranger would widen 'every participant in this session' to 'everyone'). Ordinals are per-viewer and gapless - never journal sequences (a concealment side channel). Reachable via GET /api/campaigns/:id/activity (403 non-participant, 400 no participant named). Durability proven on a real SQLite FILE with the store discarded and the DB reopened between write and read (spec scenario 'GM activity survives restart'); 'player activity omits private facts' pinned. Worker mutants killed (6): admission dropped (4 red), rationale rendered for all, ordinal from the sequence, seat asserted (exactly 1 red - the row targets it), unbound participant served (3 red), streamRevision leaked (killed ONLY by the leakScan row - the scanner is not decorative). Cut decision, ratified: the 409 whole-envelope retry (useCampaignPersistenceStore's re-PUT of the same stale envelope at the server's version, and resolveConflictKeepLocal = the same overwrite behind a button) is the ONLY conflict strategy a user has today besides losing their work, so removing it BEFORE 8.4's typed same-field rejection with a recovery action exists would leave a window with no strategy at all - the removal and 8.4 ship as one seam. Additive-only PR (7 new files, 1079 lines, ~8% over the cap, accepted as one seam - the amputation alternative leaves the read with no production caller). NOT claimed: no UI reads the feed yet (the dashboard/log page still render the browser log; the cutover needs a proven browser-side viewer identity - participantId + sessionId - not an asserted role); the audit half of 'journal and audit facts' is not reprojected (ViewerHistoryService already splits GM/player audit rows - a second derivation would be the second audience policy this module refuses; a surface wanting both composes the two feeds); CampaignDayAdvanced / CampaignSnapshotPublished produce no rows (the browser log's own convention); a solo owner with no participant row gets 403 fail-closed until the client cutover. Gates: tsc 0; oxlint 77-pin; 8.5 command 3 suites / 44; campaign+multiplayer+stores 320 suites / 4022; ten sweeps unmodified.

- [x] 8.4 Implement command-based conflict handling: revalidate disjoint stale commands, reject same-field stale commands, and return current branch/revision plus safe recovery action.
  - ADDENDUM (2026-09-02, Wave B3 seam 2c-iii, findings #78 FIXED / #59 CLOSED): the reconcile path is one critical section. CampaignMatchHost exports runBatchExclusive, which takes the single-writer lock ONCE and hands the walk UNLOCKED door bodies (the six *Locked bodies moved to CampaignMatchHost.doors.ts so the host stays an acquisition facade under the 400-line rule; runExclusive itself stays private); reconcileCoopBattle's non-inbox three-door walk (funds, salvage, roster) runs inside it, so its events are contiguous and a racing writer cannot land between funds and salvage; the inbox path is untouched. bindCampaignSyncConnection's ReconcileBattle handler RECORDS the battle before the first await - emit is synchronous, so a duplicate frame's handler starts while this one is suspended on the walk, and a record on the far side of the await let both frames pass the check and both walk (five events for a three-event battle; the funds door dedupes on intentId, salvage and roster carry no identity) - and CLEARS it whenever the walk does not succeed, a not-ok result OR a throw (try/finally), so a reconnect retry is admitted. Rows R1 a duplicate ReconcileBattle frame commits nothing twice (exactly 3 events, each door once; RED today with 5), R2 an interleaved applyHostIntent cannot land mid-walk (contiguous), R3 the batch door does not deadlock on the doors it wraps, R4 a not-ok walk leaves the battle un-recorded so a retry is admitted, R5 a THROWN walk leaves it un-recorded too. #59 needs no code: commitCampaignEventBatch has exactly one call site (CampaignMatchHost.commitEvents) behind the lock-held assert, and every commitEvents caller enters through runExclusive; the function is exported, so a future bypassing module supplying its own ICampaignBatchCommitHost would not be protected by that assert - no such caller exists. Orchestrator mutants in the PR body. NOT CLAIMED: the WALK becomes atomic; the salvage and roster doors do not become idempotent (a direct creditSalvagePool twice still credits twice); exclusion is per host OBJECT - a second host instance or the HTTP command route still races, and the store's duplicate-command check covers identity-carrying doors only; the defect disappears at the 5.7 cutover when the inbox path (outcomeId + version dedupe) takes over.

  - ADDENDUM (2026-09-02, Wave B2c seam 2c-ii-typed, findings #40 closed / #77 fixed): the socket host answers a genuine cross-writer lost race with the SAME typed shape the HTTP route returns - CampaignIntentResult gains an additive CAMPAIGN_STALE_HEAD arm (ICampaignHeadRef + ICampaignStaleHeadRefusal; the 19 existing ok:false sites keep their shape), runMechanicalCheck narrowed to ICampaignMechanicalRejection (validation touches no head), GuestProposalResult gains a fifth 'stale-head' arm so CampaignGmArbiter.commitProposal cannot fold a lost race into 'mechanically-rejected' (useGuestProposals' exhaustive switch renders it as 'the campaign moved on'), ErrorCodeSchema gains CAMPAIGN_STALE_HEAD and ErrorMessageSchema optional conflictHead + recoveryAction, toIntentError SPREADS the rejection (hand-picking three fields is how head+action were dropped), the binding emits the code with head and action; the post-race head is RE-READ via host.nextSequence() after the failed append (the ICampaignEventStore conflict arm is structural and carries no sequence, so the designed source would not compile - post-race by construction on every adapter). #77: commitCombatOutcomeConsequences is now a locked door, proven on a real journal-backed store (InMemoryCampaignEventStore has no outcome inbox). Red-first at the frame. Mutants 7/7 killed after M2 (pre-race head carried) SURVIVED first - the row asserted revision: expect.any(Number) and could not tell pre from post; now the exact post-race value (M7a's lesson, repeated); orchestrator mutants in the PR body. max-lines held at 400 by dedup (one stale-head builder shared by host + arbiter, outcomeInboxHostFrom, one resultOfCommit). LIVE_WIRE_CODES untouched (its derivation lives in #1545); campaignRefusalFromServerErrorCode returns null for CAMPAIGN_STALE_HEAD so the admitted set stays one member. NOT CLAIMED: #78 reconcile check-then-act + #59 runExclusive batch door = 2c-iii; in-process mutex only.

  - ADDENDUM (2026-09-02, Wave B2c seam 2c-i, finding #40 fixed): the campaign host now serializes its own writers - a per-host promise chain (runExclusive, commitChain + lockDepth, rejection-proof) acquired at the six public doors (open, handleIntent, applyHostIntent, creditSalvagePool, applyRosterUnitChange, _commitEventsForTests) spanning validate->commit (the invisible half: validateCampaignIntent read this.state before the await, so two spends of 60 against 100 were both approved; a commit-only lock leaves that race and the M2 mutant dies on exactly that row); commitEvents never acquires and ASSERTS the lock is held - the enforcement of the host's own 'single-writer' comment at :629. Door bodies moved to *Locked siblings; the D10 batch pipeline extracted to campaignHostBatchCommit.ts and the subscriber fan-out collapsed to one publish() to hold max-lines at 400 without suppression (77 pin held). #66 fixed: the pipeline's blocked arm carries recoveryAction (rebuilding.action) through the route's 409. Red-first: 50 unwaited intents -> 198 CampaignEventSequenceCollisionError rejections on shipped code; two spends both approved; arbiter + reconcile doors collided with the intent door; the open read-then-act row was GREEN before (open sets this.opened synchronously) and is a regression guard, not a fix. Mutants 8/8 killed (lock removed x46 rows; lock narrowed to commit x3 - the one that matters; chain not rejection-proof - strengthened with a door-throws row; assert removed - strengthened with a white-box row; salvage/roster/open doors unlocked; rebuild action dropped); orchestrator mutants in the PR body. Harness: bindCampaignSyncConnection.reconcile's flushAsyncHandlers drain raised 32->200 because each lock acquisition costs a promise hop (verified by raising it in a scratch run first). FINDINGS: #77 a SEVENTH writer door, commitCombatOutcomeConsequences (CampaignMatchHost.ts:272), bypasses commitEvents with its own nextSequence()/setState across an await - still unserialized, reachable only on an inbox-capable store (found by the assert; fixed in 2c-ii); #78 bindCampaignSyncConnection.ts:708-716 checks hasReconciledBattle and records AFTER the await - two ReconcileBattle frames for one battle both commit and the host lock does not save it (2c-iii). NOT CLAIMED: the typed lost-race answer (CampaignIntentResult arm + frame + a fifth GuestProposalResult arm so the arbiter cannot fold a lost race into 'mechanically-rejected') = 2c-ii; in-process only - two host instances or the HTTP route still race and the typed conflict covers that.

  - CLOSED (2026-09-02, Wave B2c seam 2a, campaignCommandFieldSet.ts + campaignConflictDecision.ts + campaignCommandPipeline.resolveCommandBase): server-side contract complete - base reconstructed from the client's expectedRevision (first R events replayed and folded), touchedFields derived by the SERVER from the intent against that base, interveningFields = diff(base, head) with keyed collections diffed by member; disjoint -> revalidate through the ordinary path against priorState; same-field -> typed conflict with head {branchId, revision}, conflictingFields and a recovery action; declaredFields is a checkable claim compared order-independently against the derivation, never an input (undeclared-field-set / declared-field-set-mismatch / base-revision-unknown / same-field-stale; actions EXPECTED_HEAD_RESYNC_ACTION + one addition rebase-onto-active-head); the lost-race exit enriched to the SAME shape (head read AFTER the failed append, reason 'lost-race' keeps it distinguishable; the unconverted-sequence variant is equivalent because next-sequence == revision on a campaign stream); nothing appended on any refusal, asserted by counting the stream. Route maps conflict at commands.ts. Red-first: 5 of 6 rows red BEHAVIOURALLY on shipped code (a stale command simply committed); the disjoint row was green pre-change and is load-bearing only once refusals exist (stated). Mutants killed: worker M1-M7b (same-field accepted, recovery omitted pre-append / lost race, declaration trusted, unknown base revalidated, base +1 event, pre-race head, branch omitted); orchestrator M6 re-run, same-field handed the rebase action, lost race misreported as same-field-stale. Gotcha: a NUL byte in an absent-sentinel literal made the source file diff as BINARY and every gate passed it - replaced with the bare word 'absent' (no canonical form can collide; canonicalizations are valid JSON). Cap: 1053 changed lines (+5.3%) accepted as one seam because splitting the lost-race enrichment out would ship one union with two shapes. Finding #40 (host intents racing on one connection close the GM socket) is NOT addressed here - it is host-side serialization, owned by the 2b/host seam. NOT CLAIMED: the client still retries whole envelopes and keeps local on conflict (seam 2b).

- [x] 8.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignHostRegistry.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.
  - CLOSED (2026-09-02, Wave B2c seam 2b - section 8 sealed): the named command re-run green on the cutover tree (3 suites / 44) plus the wide sweep src/lib/campaign src/lib/multiplayer src/stores src/components/gameplay src/__tests__ src/pages-modules (1218 suites / 14527 passed, 0 failed) and the wire sweeps unweakened. Client cutover: runSave's second putLiveCampaign (the whole-envelope resend at the server's version = an OVERWRITE the server accepts because the version now matches) deleted; resolveConflictKeepLocal removed from interface, factory and wiring; retryOnConflict/retriedConflict gone; new saveConflict state carries the server's typed refusal; co-op rollback on the FIRST 409. Server: PUT /api/campaigns/[id]'s stale-write 409 returns the same taxonomy as the command boundary - reason 'base-state-unavailable' (added to CampaignConflictRefusalReason - one taxonomy), recoveryAction EXPECTED_HEAD_RESYNC_ACTION, conflictingFields [], currentVersion (the campaigns-table write counter, deliberately NOT dressed as a journal head - the launch-head route's documented trap) and `current` for take-server. Card: one button, the dangerous one was the primary. Red-first: FOUR overwrite-pinning rows inverted, two of them unlisted (coopPersistenceRegression asserted the stale envelope re-sent at baseVersion 2 after an external change - a pinned live data-loss path; campaigns.test.ts titled the overwrite 'recovers via keep-local'); the route row is kept as an executable statement of the danger (resending the stale envelope still returns 200/version 3 - the server cannot see the body is stale). Gate-set lesson: src/__tests__/api was outside the earlier sweeps (three route rows were red unseen). Mutants: 3 SURVIVED first (store tests mock fetch so the route shape was unproven; nothing rendered the card's conflict block) - closed with a route assertion + CampaignSaveStatusCard.test.tsx; re-run 6/6 killed (retry reintroduced x5 rows, typed conflict dropped, co-op rollback deferred, recovery action omitted, bare record returned, reason blanked); orchestrator mutants in the PR body. Finding #40 (host-intent race on one connection) is seam 2c, its own PR.

  - PROGRESS (2026-09-02, Wave B2c seam 2a): the named command passes (3 suites / 44 tests) plus the wide sweep src/lib/campaign src/lib/multiplayer src/stores/campaign (327 suites / 4082) and the wire-leak sweeps unweakened (10 / 58). Box stays open until seam 2b (client cutover: delete runSave's whole-envelope retry and resolveConflictKeepLocal, surface the typed conflict on CampaignSaveStatusCard) lands and the command is re-run on it.


## 9. Non-Playing GM and Two-Player Membership

- [x] 9.1 Extend campaign membership to one non-playing GM and two tactical player slots with durable role, ownership, readiness revision, branch, acknowledgement cursor, and revocation state, implementing `Co-op Campaign Has One Non-Playing GM and Two Tactical Players`.
  - DEFECT FOUND AND FIXED (2026-09-03): the co-op host was seated in a tactical human seat. The matches route seated every host on the first free human seat, so a GM creating a co-op match occupied one of the two player seats it is meant to watch over, and the membership source read the seat kind rather than GM identity. Fix: the route resolves a host seat kind (explicit hostSeatKind, else spectator for any co-op body, human otherwise), builds the catalog with defaultSeatsWithSpectatorHost for spectator hosts, and seats the host on that kind, ready; the co-op entry panel sends hostSeatKind spectator explicitly; MatchSeatMembershipSource derives role gm ONLY from the durable campaign_session_participant row at seat gm (the same row 9.3's probe reads) - spectator occupancy is membership, not identity, and a missing port, unreadied store, non-co-op match, or non-gm row all stay player so tactical matches are untouched; unit bootstrap walks the layout, not the occupants, so both playing units still materialize. Worker rows (RED against shipped code): both human seats empty with the GM on spectator-1 and two playing bootstrap units; role gm with no forces for the durable GM; the co-op panel's hostSeatKind pin. Orchestrator mutants: M1 co-op default seat reverted to human SURVIVED (every co-op body in the suite sent the kind explicitly) - directed row 'seats the GM as a spectator when a co-op body names no host seat kind' deletes the field and asserts the spectator occupant, now KILLED; M2 any membership row reads gm SURVIVED (no non-gm row was ever seeded) - directed row 'reads role player when the durable row seats this principal as a player', now KILLED; M3 absent gm row reads gm KILLED; NOVEL N1 host seat find ignores the resolved kind KILLED (2 red). Gates: tsc 0; oxlint 77-pin; multiplayer authorization + lobby + checkpoint + co-op page suites 125/125. NOT claimed: a GM already seated human in a match created before this fix is not migrated (in-memory matches only, none survive a restart)
  - ERRATA (2026-09-03, same audit): the requirement's headline SHALL - 'The GM SHALL NOT occupy or inherit a tactical player seat' - is unmet on the seat surface. The only shipped co-op creation site posts layout '1v1', which Lobby.ts defines as exactly two human seats ('a player-controlled seat that owns game units'), and buildLobbyPresentation occupies the first free human seat with the host reference, which lands on IMatchMeta. MatchSeatMembershipSource then returns role 'player' on every branch with ownedForceIds set; there is no 'gm' role there. The durable membership row this box added IS correct (the participants migration constrains seat to gm or player and the creation checkpoint binds seat 'gm'), so the box's own mechanism holds - but the seat surface contradicts the requirement it closes, and no receipt reconciles the two.
  - Receipt (2026-08-28, EVERY NAMED FIELD IS NOW DURABLE; box CHECKED): #1418 closes the last two clauses the fifth step left open. Migration 20 adds `readiness_revision` (integer, NOT NULL default 0) and `active_branch` (text, NULL = the genesis/default branch, said in the migration comment) to the campaign session record; ladder pins moved together MAX 19->20 / COUNT 18->19. The registry persists on `advanceRevision` / `setActiveBranch` in the same method as the in-memory update — skipped when SQLite is not initialized so host tests stay database-free — and `register` / `getOrCreate` restore both instead of defaulting. A session with NO stored row keeps the log-head fallback, so unmigrated campaigns do not acquire a reset-to-zero bug on their first restart after this ships. THE DESIGN DECISION, recorded so the next reader does not re-open it: readiness itself stays a COMPUTED projection — #1381 made it derived from live state deliberately, and persisting the flag would invite divergence from its own sources. What a restart REMEMBERS is the revision counter and the branch id; what it RECOMPUTES is readiness, against the remembered revision and branch. The full ledger of the box, each with its migration: role + revocation (17), acknowledgement cursor (16), force ownership (19, the insert-is-the-decision primary key), seat cap + GM seat decided by durable membership with the claim ordered before resume (fifth step), readiness revision + active branch (20). EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-first — the restart row read revision 0 before the wiring. Re-verified independently: M1 (recovery blinded to the store) reds exactly the restart row across a 4407-test sweep; M2 (drop the persist on advance) reds exactly the advance row; typecheck clean; oxlint 0 errors / 77 warnings; persistence+multiplayer battery 103 suites / 800 tests; full suite 2439 suites / 33311 passed / 0 failed; 30/30 CI. STATED PLAINLY, and deliberately NOT a reason to hold the box: `advanceRevision` and `setActiveBranch` still have no production caller. Advancing the revision when authoritative force or membership state changes is task 9.2's open readiness clause (its receipts have tracked that linkage since campaignParticipationFreshness documented the frozen revision); the branch setter's first caller is the history-branches leaf (wave-map row 7). This box named DURABILITY of the fields, and that is what exists: the values survive restart for the day they move.

  - Progress (2026-08-27, fifth step — THE GM SEAT IS NOW DECIDED BY DURABLE MEMBERSHIP; box STAYS OPEN for readiness revision and active branch): the fourth step listed the GM's discarded `gm-seat-taken` answer as a remaining clause. It is closed, and the ORDERING turned out to matter as much as the refusal. TWO AUTHORITIES THAT CAN DISAGREE. `role === 'host'` is decided by comparing the verified principal to the registry's OWN in-memory `hostPlayerId`. Durable membership is what actually decides, and its single-active-GM partial index answers `gm-seat-taken` when a different identity already holds the seat for that session — the case where an in-memory registry rebuilt with a different host (a recovered match, a campaign adopted by another account) disagrees with the durable record. The spec is explicit that the host SHALL use durable membership for validation; this path used it for nothing. AND THE SESSION WAS RESUMED FIRST. `noteGmConnected()` ran BEFORE the bind, so a contested GM unpaused a live campaign on its way to having its refusal ignored — then got the snapshot and a subscription too. The fix moves the claim ahead of everything: nothing resumes, attaches, or subscribes until the seat is ours. A refusal that fired after the resume would have been decoration. THE TEST ASSERTS ADMISSION, NOT JUST THE ERROR. My first attempt asserted the session was still paused, which was WRONG as a probe — a fresh session is not paused, so the assertion could never have distinguished the fix from the bug. The shipped rows check the thing that actually differs: an admitted GM is sent a `CampaignSnapshot`, a refused one is not. Falsified 1:1, and the control row (the GM whose seat IS theirs) proves a guard that refused every host would fail rather than pass. STILL OPEN, and now down to two named fields: `Membership survives process restart` requires READINESS REVISION and ACTIVE BRANCH to be recoverable from durable authority, and neither is persisted. Everything else this box names is now durable — role and revocation since migration 17, the acknowledgement cursor since migration 16, owned forces since migration 19 (#1390). Readiness in particular is a design question rather than a schema one: #1381 made it a COMPUTED projection from live state rather than a stored flag, deliberately, so persisting it means deciding what a restart should recompute versus remember.
  - Progress (2026-08-26, fourth step — FORCE OWNERSHIP IS NOW DURABLE; box STAYS OPEN for readiness revision and active branch): the third step's rule (first claim on a mission owns the force) lived entirely in the registry's IN-MEMORY participation records, so a restart handed every force back to whoever asked first next. This gives it somewhere to live: migration 19, `campaign_session_force_claim`, whose PRIMARY KEY (campaign, session, mission, force) IS the rule — a second participant's insert conflicts rather than being talked out of it by application code, the same instinct as the partial unique index enforcing one active GM. THE INSERT IS THE DECISION, not a read-then-write. `claimCampaignSessionForce` returns `claimed` / `already-held` / `held-by-other` with the holder named, so two participants racing for the same force are separated by the database rather than by two application checks that could both pass. That is the same reasoning as the seat cap in the first step, and it is why the claim is written rather than merely consulted. WIRED, NOT JUST BUILT. An earlier note in this file warned about building a mechanism with no producer; the claim is taken on every accepted non-idempotent participation through an optional `ICampaignForceClaimPort`, and `held-by-other` refuses `INVALID_INTENT`/`foreign-force` WITHOUT publishing — a broadcast would have told the GM this player is deploying a force they were just denied. The port is optional and supplied from `server.js` for exactly the reason the membership port is: a default would reach for SQLite from every test that binds a socket, and those tests have no database. Absence keeps the pre-change behaviour. FIVE STORE ROWS AND ONE WIRING ROW, each falsified 1:1. Neutralising the ownership discriminator reds precisely the refusal row and the restart row while leaving three controls green — first claimant wins, the holder's own re-send stays idempotent, and the rule does not bleed across forces or missions (a guard keyed too broadly would refuse a player their SECOND force, or refuse the same force on a later mission). Neutralising the socket-side refusal reds only the wiring row. A FIXTURE LESSON WORTH KEEPING: the wiring test first tried to add a second force to the shared `makeRegistry` fixture so it could refuse one force and admit another. That reds TEN unrelated rows, because the baseline snapshot is asserted on. The shipped test varies the FAKE's answer by call order instead — the same force, two worlds — and touches no shared state. When a test needs a new world, changing a shared fixture is the expensive way to get one. STILL OPEN ON THIS BOX, and now genuinely short: `Membership survives process restart` also names READINESS REVISION and ACTIVE BRANCH, neither of which is persisted (the acknowledgement cursor already is — `campaign_participant_cursor.acked_sequence`, migration 16 — and seat/revocation have been durable since migration 17). The GM's own `gm-seat-taken` answer is still discarded at admission. Those are the remaining clauses.
  - Progress (2026-08-26, third step — `Player edits foreign force` now holds; the FORCE half of `Client role claim is ignored` is closed; box STAYS OPEN): the previous note said this half could not be enforced until force ownership existed, and that was half right — it could not be enforced from DURABLE ownership, which still does not exist. It could be enforced from the claims the session already holds, and that is what shipped. THE REFUSAL WAS ALREADY NAMED FOR OWNERSHIP AND DID NOT DO IT. `admitCampaignParticipation` read `forceUnits[payload.forceId]` and returned `{reason: 'foreign-force'}` when the lookup missed. But a campaign has ONE SHARED ROSTER, so `forceUnits` knowing a force only proves the force EXISTS — it says nothing about whose it is. The refusal fired only for a force nobody had. A player could file participation naming their TEAMMATE's force and be admitted, taking it out of their hands. THE FUNCTION COULD NOT HAVE CAUGHT IT EITHER, which is the structural half. It was handed `existing` — the caller's OWN record, found by `.find(record => record.playerId === verifiedPlayerId)`. Ownership is a fact about the OTHER claims, so a function given only its own record has nothing to compare against. The signature now takes the mission's whole record set and derives `existing` from it, which is the shape the question actually has. THE RULE IS FIRST CLAIM ON A MISSION OWNS THE FORCE, and it is deliberately modest. Nothing upstream records force ownership — the campaign's forces are shared, and the ONLY owner map in the codebase is built inside `composeCoopEncounter` and never leaves it. So this does not pretend to be a GM assignment mechanism, which would need an authority that does not exist. It stops a player taking a force out of a teammate's hands, which is exactly the scenario `Player edits foreign force` names. Falsified 1:1, with TWO controls that both stay green: another player claiming an UNclaimed force is admitted, and a participant re-sending their own claim still returns idempotent — so a guard that refused whenever any claim existed, or that made a participant a stranger to their own force, fails. STILL OPEN ON THIS BOX, and the list is now short and specific: the claims live in the registry entry IN MEMORY, so this ownership does not survive a restart — `Membership survives process restart` needs owned force ids, readiness revision, active branch and acknowledgement cursor persisted on `campaign_session_participant`, which today stores only seat, bound_at and revoked_at. The GM's own `gm-seat-taken` answer is still discarded. Those are the remaining clauses, and they are durability work rather than guard work.
  - Progress (2026-08-26, second step — `Client role claim is ignored` now holds for the PROPOSAL path; box STAYS OPEN): the inbound guard proves `envelope.playerId` and closes any socket whose envelope claims an identity the connection did not authenticate as. The PROPOSAL carries its own author one level down as `proposal.proposingPlayerId`, and that field was never compared to the proved one, so a player could file a request under another player's name. THIS IS NOT COSMETIC, which is worth stating because the field does no authorization work server-side. `HostGmReviewSurface` renders `entry.proposal.proposingPlayerId` verbatim, so it decides WHOSE NAME sits beside a request the GM is about to approve or veto. In `host-review` mode — the default the co-op entry panel picks — an unchecked string was choosing what the deciding authority believed about who was asking. A guest could make an expensive request appear to come from their teammate. THE FIX compares the claimed proposer to the proved identity and refuses `AUTH_REJECTED`/`campaign-proposal-attribution` on mismatch, before the arbiter sees it, so nothing is queued for the GM to look at. Falsified 1:1, and the control row (a player attributing a proposal to themselves) stays green so a guard that refused every proposal would not pass. A TYPING NOTE WORTH KEEPING, because jest hid it: the first version of this guard read `envelope.proposal.proposingPlayerId` directly and ALL 30 ROWS WENT GREEN — then `tsc` rejected it, because `CampaignProposal.proposal` is deliberately `unknown` on the wire (the arbiter owns parsing it) and babel-backed jest type-checks nothing. The shipped version reads the one field defensively and returns null on absent-or-wrong-shape, so a MALFORMED proposal stays the arbiter's typed `malformed-intent` rejection instead of being relabelled an attribution error. Green tests were not evidence the code compiled. STILL OPEN ON THIS BOX: the other half of `Client role claim is ignored` is a client claiming another player's FORCE, which cannot be enforced until force ownership is durable — the same missing per-participant ownership that 9.2 is blocked on. Durable role, owned force ids, readiness revision, active branch, acknowledgement cursor and revocation across restart remain untouched.
  - Progress (2026-08-26, first step — THE THIRD TACTICAL PLAYER IS NOW REFUSED; box STAYS OPEN, this is one scenario of several): the seat limit was already written and already correct — `bindCampaignSessionParticipant` counts active `player` seats inside its bind TRANSACTION and returns `{kind:'tactical-seats-full', limit: TACTICAL_SEAT_LIMIT}`. What was missing is that nobody listened. `campaignSessionMembershipPort` discarded the result and said so in a comment: "Enforcing seat limits at admission time is task 9's job, where the refusal has somewhere to go." This is task 9, and the refusal now has somewhere to go. SO A THIRD PLAYER WAS FULLY ADMITTED. The guest path called `bind` AFTER `joinGuest` had already hydrated them, streamed the campaign baseline and tail, and left them subscribed — then threw the refusal away and finished the admission. Two tactical slots was a rule the database enforced and the socket layer did not implement. THE FIX returns the store's answer through the port and acts on `tactical-seats-full` by undoing the hydration: `join.disconnect()`, an `AUTH_REJECTED`/`campaign-tactical-seats-full` frame, and no `addSocketToMatch` or acknowledgement. Undoing rather than pre-checking is deliberate — the store's transaction stays the single authority on seat count, which is what makes it safe against two newcomers racing for the last seat, where a read-then-admit check lets both through. THE TEST is what makes this provable, and the interesting part is the FAKE. `fakeMembership` described itself as "the store's contract without a database" while omitting the one rule under test, so it always said yes and no test could see the socket layer ignoring a refusal. Teaching the fake the cap is what turned the row red. Falsified 1:1: neutralizing only the refusal branch reds exactly the new row. The row is self-controlling — it asserts the first two players are still bound, so a guard that refused everyone fails it too. RESIDUALS, named rather than left implied. (1) A refused newcomer HAS ALREADY RECEIVED the baseline snapshot before being turned away, because the invite check and the hydration both live inside `joinGuest`. Closing that means splitting invite validation from streaming so the seat check can happen before any campaign data moves — a larger change than this refusal, and a privacy question rather than a membership one. (2) The GM bind still ignores its own `gm-seat-taken` answer; that is the single-active-GM rule and a separate clause. (3) This closes ONE scenario of this box. The requirement also demands durable role, owned force ids, readiness revision, active branch, acknowledgement cursor and revocation state recovered across restart, plus `Client role claim is ignored` — none of which this step touches, which is why the box stays open.
- [x] 9.2 Validate unit, force, proposal, readiness, and launch commands against durable ownership; clear only affected readiness when an authoritative force or revision changes.
  - Receipt (2026-08-28, THE FINAL ACCOUNTING; box CHECKED - every clause is either enforced or has no surface to enforce, and the difference is stated per clause): UNIT - enforced. Cross-side unit commands refuse `AUTH_REJECTED`/`unit-not-owned` at the intent seam (#1384), the shared co-op roster refuses duplicate deployment (#1380), and the co-op intra-side grain is delegated scope recorded in the fourth/sixth steps below. FORCE - enforced durably. `campaign_session_force_claim` (migration 19, #1390): the INSERT is the decision, first claim owns the force per (campaign, session, mission), `held-by-other` refuses without publishing. PROPOSAL - vacuously satisfied, and the accounting is the point: ALL FIVE campaign intent kinds (HirePilot, AcceptContract, SpendFunds, AllocateSalvage, AdvanceDay) act on the SHARED campaign ledger and carry NO unit or force reference in their payloads - verified against the arbiter's exhaustive `describeProposalEffect` switch, whose compile-time exhaustiveness guard is the TRIPWIRE: a future intent kind that names a force cannot be added without tripping it, and whoever adds one owes it an ownership check at the same commit. Proposal AUTHORITY meanwhile is enforced twice over: only the GM decides (#1382), and AdvanceDay proposals are convergence-gated at every commit route (#1411). READINESS - enforced, by the design the module argues for in its own header: `campaignParticipationFreshness` filters each participation record at read time against the current roster, so exactly the AFFECTED player's choice goes stale when their unit dies and nobody else's is touched - a computed projection cannot drift from the state it projects, where a stored cleared-flag rots with the first path that forgets to set it. `advanceRevision` remains uncalled and the module documents why that refusal is dead code rather than pretending it guards anything; the durable `readiness_revision` (migration 20, #1418) is the restart-surviving counter the projection is validated against when branching lands. LAUNCH - enforced at both existing gates: participation admission takes the force claim and refuses `foreign-force` before publishing (#1390), and materialization refuses a roster containing a blocked unit at launch. The deeper launch question - that co-op COMBAT has no shared server host to validate against - is the sixth step's finding, recorded there as delegated program scope, not an ownership check this box can add. WHY THIS CLOSES NOW: the box was held open through six dated steps until every clause had either an enforcement receipt or a proven absence of surface. Nothing here is new code; the closing evidence is the accounting itself, grounded this session by reading the arbiter's intent switch and the freshness module rather than remembering them.

  - Progress (2026-08-26, sixth step — TRACED THE CO-OP COMBAT ROUTE THE FOURTH STEP ASSUMED; it does not exist, and that reframes this box): the fifth step said the next work was to settle how a co-op encounter becomes a `ServerMatchHost` match, because the fourth step had assumed a route it never verified. Traced end to end by reading, and the honest answer is that there is no such route today. WHAT THE READING SHOWS. `launchCoopMission` composes the encounter and hands it to `launchCampaignEncounter`, which contains NO multiplayer references at all — it creates an encounter and a game session, never a match. The launch page then routes the deploying player to `campaignEncounterHref(...)`, which builds `/gameplay/encounters/<encounterId>?campaignId=…&missionId=…` — note the absence of any match id. And the destination itself carries no binding to recover one: `src/pages/gameplay/encounters/[id].tsx` and every page under `[id]/` (`pre-battle`, `select-force`, `sim`) contain no reference to `matchId`, `multiplayer`, or `coop`. The only multiplayer match anywhere in the co-op flow is the one `CampaignCoopEntryPanel` POSTs at session start, and that one is the CAMPAIGN-SYNC vessel — it carries `coopCampaign` and drives the arbiter, not a combat engine. SO THE CO-OP UNIT-OWNERSHIP QUESTION HAS NO HOST TO BE ASKED OF. That is almost certainly why `ownsCoopUnit` was never called and why `coopSeats` never needed to leave `composeCoopEncounter`: there is no shared combat host on the other end to wire them to. Two co-op players who launch a mission each land on their own local encounter page. The gap this task names — validating unit commands against durable ownership — is downstream of a much larger one, which is that co-op COMBAT is not a shared server-authoritative session at all. STATED CAREFULLY, because this is a negative claim from reading rather than from execution: what is established is that the launch path passes no match id, that `launchCampaignEncounter` has no multiplayer surface, and that the destination page tree has no match binding. What is NOT established is that no path whatsoever could produce a shared co-op combat host — I found none, and I looked at the launch call chain and the destination tree, but a negative is only as good as the search behind it. Anyone acting on this should re-run that search before treating co-op combat as unbuilt rather than merely unwired. WHAT THIS MEANS FOR THE BOX. The two shipped steps stand on their own: cross-side unit commands are refused for versus matches (#1384), and the co-op exemption that keeps a shared-side roster reachable is correct regardless of whether anything currently sends those intents (#1385). But 'validate unit commands against durable ownership' cannot be finished for co-op by adding a check — it needs the shared combat session to exist first. That is a scope question for the umbrella, not a guard to write, and it should be settled before anyone opens this box again.
  - Progress (2026-08-26, fifth step — REGRESSION FROM THE FOURTH STEP, FOUND AND FIXED; box STAYS OPEN): the fourth step shipped a side-granularity ownership guard and its note claimed the co-op grain was merely left uncovered. That was too generous: the guard did not leave co-op alone, it BROKE it, and #1384 was already merged when this surfaced. WHAT WENT WRONG. Co-op pools every deploying player's units onto the SHARED `player` side — `composeCoopEncounter` says so in as many words — while the guest still occupies a `bravo` seat, and `lobbySideAssignments` resolves the second side name to `opponent`. So a co-op guest is `opponent` holding a roster that is entirely `player`-side, and a side check reads them as an intruder against EVERY unit. Measured on a co-op-shaped match (coopCampaign present, guest on bravo, all units Player-side): `refused=true, moved=false` — the guest was locked out of their own mechs. Not a narrowing of the guard's reach; a total lockout of one of the two players. HOW IT GOT PAST THE FOURTH STEP, which is the part worth keeping. The full suite was green — 2430 suites, 33209 tests — and stayed green, because NOTHING exercises a seated multiplayer host with real movable units end to end. The one test that dispatches Move through the host stubs `InteractiveSession` entirely and asserts only routing. The fourth step's own test was the first of its kind, and it covered the shape I had been probing (versus) rather than the shape the task was about (co-op). A green full suite said nothing about either. The lesson is not 'run the suite' — it was run — but that a guard keyed on a proxy MUST be tested against every shape where the proxy's meaning changes, and co-op was the shape whose meaning changes. THE FIX exempts co-op matches from the side check, because side is not an ownership signal there by construction. That restores the guest's pre-#1384 behaviour exactly, keeps the measured versus hole closed, and leaves co-op unit commands unconstrained — which is where they were before either step, and is honestly the state this task still has to close. Falsified 1:1: neutralizing only the co-op exemption reds exactly the co-op row and leaves the versus row green. The test now boots both shapes from one factory whose ONLY difference is the co-op campaign, so the next person changing this guard cannot cover one shape and miss the other the way I did. STILL OPEN, unchanged by either step: co-op per-unit ownership. `ownsCoopUnit` remains uncalled and `coopSeats` still never leave `composeCoopEncounter`, so the per-unit owner map still never reaches the host. Wiring it is a real slice, not a guard tweak — the co-op combat path does not create the match through `launchCampaignEncounter` (that function has no multiplayer references at all), so where the owner map would have to be persisted is itself unestablished. That trace is the next piece of work here, and it should start by settling how a co-op encounter becomes a `ServerMatchHost` match, because the fourth step assumed a route it never verified.
  - Progress (2026-08-26, fourth step — CROSS-SIDE UNIT COMMANDS ARE NOW REFUSED; box STAYS OPEN, the co-op grain is not covered): the previous note said a properly-seated co-op match had to be built before anything could be concluded, and that the earlier probe was unrepresentative. Both were right, and building the seated match turned up a live defect that is broader than the co-op question that prompted it. MEASURED, NOT INFERRED. On a properly-seated 1v1 — `meta.seats` occupied, server-derived ownership resolving to `ownedForceIds=["alpha-1"]` for one player and `["bravo-1"]` for the other — the player holding the BRAVO (opponent) seat issued a Move for `unit-A2`, a PLAYER-side mech, and the host committed it: `movement_declared` + `movement_locked`, position `(-1,5)` -> `(-1,4)`. The control in the same run (the owner moving their own unit) also committed, which is what makes the result readable: the harness is faithful, so the refusal is the thing that was missing. NEITHER EXISTING LAYER COULD HAVE CAUGHT IT, and this is structural rather than a missed branch. A Move names `unitId`, never `forceId`, so `commandRequestFromIntent` builds an EMPTY force claim and the subset check against `ownedForceIds` passes trivially — the comment right above it ("unit ids are not force ids") records the decision that left it that way. The engine cannot catch it either: `EngineIntentHandler` is `(session, intent) => void`, so the dispatch layer never learns who sent the command. Ownership had nowhere to be checked. WHY IT SURVIVED THIS LONG, which is the part worth keeping: `ownsCoopUnit` opened by asserting that "`ServerMatchHost` already validates unit ownership for any seated match". That was false when it was written. It made the co-op helper look like a refinement of a working base case instead of the only ownership check in the codebase, and the base case was never built. The comment is corrected in the same commit, because leaving it would re-seed the same mistake in the next reader. THE FIX is a side-granularity refusal at the intent seam, before rate-limiting and before any engine dispatch: resolve the actor unit's side from engine state, resolve the caller's side from `meta.sideAssignments`, refuse `AUTH_REJECTED`/`unit-not-owned` when they differ. Both halves are records the server already owns, so nothing new is threaded through the wire. The actor is `unitId`, or `attackerId` for Attack/Physical; `targetId` is pointedly NOT read, since shooting a unit you do not own is the entire game. Server-internal callers are exempt — the host acting is not a principal claiming scope. Falsified 1:1 (#PR): neutralizing only the side comparison reds exactly the refusal row and leaves the owner-control row green, and the row also asserts the unit DID NOT MOVE, so a guard that refused loudly while committing anyway would not pass. WHAT THIS DOES **NOT** CLOSE, stated plainly so the box is not read as done. (1) It separates OPPONENTS, not TEAMMATES. In a co-op match both players are assigned side `player`, so caller side and unit side match and the guard returns without refusing — player B commanding player A's mech is still accepted. That intra-side grain is exactly what `ownsCoopUnit` expresses, and it is STILL uncalled, because `coopSeats` still never leave `composeCoopEncounter` and so the per-unit owner map still never reaches the host. Nothing in this step changed that. (2) A match whose meta carries no `sideAssignments` resolves no caller side and is left exactly as it was rather than newly refused — deliberate, since making unresolved-ownership matches unplayable is a bet this slice did not verify across every creation path. (3) The proposal / readiness / launch clauses of this task are unaffected by this step. CORRECTION TO THE PREVIOUS NOTE, which got the reason right but the evidence wrong: it reported that the earlier probe saw own/teammate/enemy all "commit". They did not. Re-run with the effect measured instead of the frame kind, all three were `movement_invalid` events — engine refusals for `NoMovementCapability`, because the probe's units were `as IGameUnit` casts with no movement stats — and no position changed. `kind: 'Event'` was never evidence of success; an engine REFUSAL is also an Event. The empty `ownedForceIds` was real, but it was not what made that probe unreadable.
  - Progress (2026-08-26, third step — DECISION AUTHORITY closed; a LAUNCH-side gap is recorded but NOT claimed as an exploit): the decision clause is done and was a real hole - `handleCampaignDecision` never checked the sender, so in `host-review` mode (the default the co-op entry panel picks) a guest could submit a proposal and immediately approve their own, committing it with the GM never consulted. `handleCampaignHostIntent` had always compared against `entry.hostPlayerId`; the decision path fifty lines away simply never did. Fixed with the same comparison and `AUTH_REJECTED`/`campaign-decision-requires-gm`; the row also asserts the proposal is STILL PENDING after the refusal, so a guard that complained while deciding anyway would not pass. Falsified 1:1 (#1382). ON THE LAUNCH CLAUSE, stated carefully because I could not finish verifying it: three facts ARE established by reading. (1) `ownsCoopUnit` - the per-unit co-op ownership predicate - has NO production caller; only its own tests reference it. (2) `coopSeats`, which `composeCoopEncounter` tags with `ownerPlayerId`, never leave that module: `launchCoopMission` forwards only `composition.encounter` onward, so no per-unit ownership data reaches `ServerMatchHost` at all. (3) Unit-scoped intents carry no force identity - `MoveIntentSchema` is `unitId`/`to`/`facing`/`movementType` - while `commandRequestFromIntent` builds its authorization request from `claimedForceIds` and `claimedParticipantId`, so FORCE-SCOPE authorization has nothing to constrain a unit-scoped command with. WHAT IS **NOT** ESTABLISHED, and must not be read into the above: that a co-op player can actually command a teammate's or an opponent's unit in production. I probed it and the probe was NOT REPRESENTATIVE - the harness created a match with `sideAssignments` but no seats or server-derived force ownership, so `ownedForceIds` was empty and an empty claim passed trivially; all three of own/teammate/enemy committed, INCLUDING the enemy case, which is itself the signal that the harness rather than the product was permissive. `HumanActionAuthorization` has real coverage that contradicts a naive reading of that result ('command force scope must be a subset of server-derived ownedForceIds', 'an admitted member whose membership was revoked cannot command', 'a mismatched participant claim is scope-escalation, never a fallback'). ANYONE PICKING THIS UP SHOULD BUILD A PROPERLY-SEATED CO-OP MATCH FIRST - real seats, real server-derived force ownership, both players on the same side - and only then decide whether unit-scoped commands are constrained. The probe was deleted rather than merged, because a test that cannot tell its own subject apart from its harness is worse than none.
  - Progress (2026-08-26, STAYS OPEN — the UNIT half is done; proposal/readiness/launch are not): found and fixed a live defect on the way. A co-op campaign has ONE SHARED ROSTER, so both players pick from the same units, and `composeCoopEncounter` guarded `duplicate-player` but NOT duplicate unit - overlapping forces produced two `coopSeats` entries for one unit id carrying two DIFFERENT `ownerPlayerId` values. The same mech on the map twice, and an ownership question with two answers, in exactly the flow this program exists to make playable: two people share a roster and both take the good lance. Written failing-test-first (both duplicate rows RED before the guard, both controls already green). CHECKED ACROSS DEPLOYING CONTRIBUTIONS ONLY - a command-HQ player puts nothing on the map, so their force overlapping a deployer's is not a double-deployment and must not block the launch; there is a row for that, because a check that rejected it would be indistinguishable from a correct one on the two obvious cases. REFUSED RATHER THAN DE-DUPLICATED: silently dropping the second copy would quietly discard part of somebody's contribution, and the launch surface branches on a typed cause so it can say what happened. The exhaustive switch in `launchCoopMission` refused to compile until the new rejection carried a player-facing message - the type system asking the right question. Falsified 1:1: neutralising the guard reds exactly the two duplicate rows and leaves the controls green. WHAT REMAINS ON THIS BOX, stated so it is not mistaken for done: proposal, readiness and launch commands are NOT yet validated against durable ownership, and 'clear only affected readiness when an authoritative force or revision changes' is untouched. Also worth recording for whoever takes those: `admitCampaignParticipation`'s `foreign-force` refusal checks only that the force EXISTS in shared `forceUnits` - it does NOT check the sender owns it, which is defensible while the roster is genuinely shared but stops being so the moment per-player ownership is introduced. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; coop + encounter suites 9 suites / 54 tests; FULL unit suite green.
- [x] 9.3 Implement durable GM-loss pause with no implicit player promotion; allow resume only by the same reauthenticated GM and participant removal only through an audited GM command.
  - DEFECT FOUND AND FIXED (2026-09-03, audit completeness pass at a98e70e01): this box was CLOSED while the match tier did the exact thing its letter forbids. onLastSocketDropped calls handleSocketDropped, which awaited migrateHostIfNeeded FIRST; when the dropped player held meta.hostPlayerId, HostMigration picked the longest-connected human seat occupant, PERSISTED updateMatchMeta({ hostPlayerId: successor }) and broadcast HostMigrated - and its docstring said the migration stays migrated even if the original host reconnects. Because commandAuthorityProjection derives viewerRole and authority 'host-gm' and the NETWORK_HOST_CONTROLS set (approve, gm-correction) purely from hostPlayerId === playerId, and NetworkedGameSurface gates the GM control block on that role, the drop really handed GM powers to a tactical player and never gave them back. The GM is the host of a co-op campaign match (matches/index.ts passes gmParticipantId: hostPlayerId and sets the creating GM as host), so this was reachable on every co-op session. The original receipt reasoned only about bindCampaignSyncConnection's host branch; the match channel is a second door and was ungated. THE FIX: an optional ICampaignGmHostProbe on the migration context, bound at ServerMatchHost construction, reads the DURABLE seat - activeCampaignSessionMembership for (campaignId, sessionId = matchId, participantId), whose row the creation checkpoint binds as seat 'gm' under a migration that constrains seat to gm or player - and migration refuses with { migrated: false, reason: 'campaign-gm' } when the dropped host holds it. The pause path still runs, and because hostPlayerId never changes the same GM reconnecting is the host again, which is the letter's 'same reauthenticated GM' clause. Absent signal is byte-identical to before: no coop campaign on the meta, no probe bound, an unready participant store, or no gm row all fall through to today's migration, which ordinary tactical matches still need. The durable seat was chosen over the easy 'skip every co-op match' test because the seat is the GM identity; the host id is only where it happens to sit. Rows: a dropped GM leaves hostPlayerId unchanged, broadcasts no HostMigrated and answers campaign-gm; the same GM reconnecting still projects host-gm with approve and gm-correction; a GM drop pauses the match; a non-GM host still migrates (control); a co-op match with no gm row still migrates (absent-signal pin). Orchestrator mutants in the PR body, including one that restores the original defect. NOT CLAIMED: the projection and the surface are unchanged (correct given a correct host id); no GM field was added to IMatchMeta; the campaign-tier session pause is out of scope; and 9.1's separate finding - that the GM occupies one of the two tactical human seats - is filed as errata, not fixed here.

  - ERRATA SUPERSEDED (2026-09-03): this box's audit errata is folded into the DEFECT FOUND AND FIXED receipt above, which landed first and states the same finding with the fix that closes it. The errata is recorded here only so the sequence is legible: the audit's completeness pass over the CLOSED boxes is what found the violation, and it found it precisely because it read closed boxes rather than open ones.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 9 at 5/5): the third clause - participant removal only through an audited GM command - now exists; the first two (durable GM-loss pause with no promotion; same-reauthenticated-GM-only resume) were already shipped and stand untouched. RemoveParticipant is a new host-only campaign intent: validateCampaignIntent rejects any non-host author with typed 'host-only' appending nothing, and the committed ParticipantRemoved event - actor, participant, reason, timestamp, append-only through the normal one-batch commit path - IS the audit record the spec scenario names. In the same operation the durable seat is revoked (revokeCampaignSessionParticipant, previously ZERO production callers) and the participant leaves the sync session's retained convergence map, so the launch acknowledgement requirement updates with the batch: a launch blocked on a departed player's convergence proceeds after removal (the exact scenario SHALL). A live removed socket detaches on its next frame; rejoin gets the existing membership-revoked refusal (readmission is a decision, not a retry). Crash-window semantics chosen commit-first and PROVEN: a death between event commit and durable revoke leaves the committed removal event, and the next authenticated join rescans committed removals and heals both the revocation and the retained set. Union ripple enumerated and complete: CampaignEventType 7->8 with the runtime-list completeness pin, zod baseline schema pack + strict mutant matrix (missing-participant / extra-property / non-string-reason all reject), campaignEventScope ('campaign' - a roster change is session-public), reducer, provenance manifest (NONE - pure command input), grant projection switch, provenance/exhaustive-switch consumers, membership-port revoke. HONEST CAP EXCESS: 21 files (worker BLOCKED at the 15-file bound after compile-time exhaustiveness demanded five more mechanical consumers; splitting would have left main broken between PRs, so the orchestrator completed the six - two intent-label switches, one projection row, one provenance member, the replay-registry count pin (retitled to the discriminant heads per the migration-head law), test fakes - and the excess is recorded here rather than hidden). REVIEWER RE-RUN CAUGHT A VACUOUS MUTANT CLAIM: the worker's guard-removal mutant 'killer' was a pre-existing TRANSPORT-layer test (AUTH_REJECTED at the socket) that never reaches the validator - the guard was real but unproven; a new CampaignMatchHost row drives the guest-facing handleIntent path with a RemoveParticipant intent and now reds when the guard is removed. Mutants, all reviewer-re-run: guard-removed dies at the new validator row; retained-delete-removed and durable-revoke-removed both die at the revoke/detach/launch-unblock integration row. Gates: tsc 0 across the 21-file ripple; focused 4 suites / 84; worker evidence multiplayer 112/873 + oxlint 77-pin, orchestrator serial re-run across multiplayer+campaign+events trees recorded in the PR. +~530/-11.

  - Progress (2026-08-25, second step — THE PAUSE NOW SURVIVES A RESTART; box still OPEN for audited removal): the previous note named non-durability as reason (1) for leaving this box open. That is closed, and WITHOUT a stored flag, which is the part worth recording. A stored `paused` column can disagree with reality - it is a claim about the world that has to be kept in step with it. What replaced it states the reality directly: a REBUILT session has no GM connection attached, therefore the GM is absent, therefore the campaign is paused. `CampaignHostRegistry.register` takes a `rebuilt` option and `getOrCreate` - the path that runs precisely when the entry was already gone, after a restart or an eviction - passes it. A freshly CREATED session deliberately does NOT: the GM is the one creating it and their socket follows immediately, and starting that case paused would refuse a guest who arrived in between. Three rows: a rebuilt campaign is paused, a fresh one is not, and the returning GM clears a rebuild pause (a pause nothing can lift is an outage, not a pause). Falsified 1:1 - neutralising the rebuilt branch reds exactly the rebuild row. WHAT REMAINS OPEN on this box, unchanged: AUDITED PARTICIPANT REMOVAL is not built - revocation exists in the participant store but no GM command performs it and no audit record says who removed whom - and the spec's 'MAY resume after cursor convergence' is still immediate rather than gated, which needs 9.4's convergence gate. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; registry suite 10/10; FULL unit suite 33125 passed / 0 failed.
  - Progress (2026-08-24, STAYS OPEN — the pause is not DURABLE, and audited removal is unbuilt): GM LOSS DID NOTHING AT ALL BEFORE THIS. `CampaignSyncSession.hostDisconnected()` existed and read like the handler for it, but grepped this session it has NO production caller - only two tests - so a GM's socket dropping left the campaign running as if they were still there. It is also TERMINAL by design and by its own test ('pauses the session and closes the host' asserts `host.isClosed()`), so wiring it up would have made GM loss unrecoverable rather than paused. A recoverable pause now exists beside it: `noteGmConnected` / `noteGmDisconnected`, wired from the campaign socket's HOST branch and its cleanup. Only the GM's own connection can reach either, because `role` is host precisely when the connection's VERIFIED principal matches the registered host - a tactical player cannot claim it, which is what makes 'no implicit promotion' structural rather than a rule someone has to remember. PRESENCE IS A COUNT, NOT A FLAG, and the falsification is what forced that: with a flag, the `!gmPresent` guard was unreachable and neutralising it reddened nothing - which turned out to be hiding a real defect. A GM holding TWO connections (a second tab, or a reconnect landing before the old socket's close is processed) paused a session they were still sitting in the moment either one closed. Now paused only when the LAST GM connection goes. THE PAUSE BITES: `CampaignProposal`, `CampaignDecision`, and `CampaignHostIntent` are refused while paused with `MATCH_PAUSED` / `campaign-paused-gm-absent` - a code that already means exactly this, so none was minted. Join, grant-join, and acknowledgement stay OPEN on purpose: those are how a participant gets back and how the GM returns to resume, and refusing them would make the pause unrecoverable. Five rows pin the behaviour: the GM dropping pauses; a player's command is refused while the GM is absent; a player RECONNECTING does not resume it; the same GM reconnecting does; and a non-GM connection closing does not pause at all. Falsified 1:1: dropping the pause assignment reds four rows, dropping the resume reds one, and forcing the refusal helper to admit reds one. WHY THIS BOX STAYS OPEN, EXACTLY: (1) DURABILITY. The paused state lives on the in-memory session, so a process restart forgets it - a restarted server rehydrates the campaign unpaused with no GM connected. Making it durable means a column on the session row and a read on rehydration, which is 9.1's schema work. (2) AUDITED PARTICIPANT REMOVAL. Not built at all: revocation exists in the participant store (6.1) but there is no GM command that performs it and no audit record of who removed whom. (3) 'MAY resume after cursor convergence' from the spec scenario is NOT implemented - the resume here is immediate on the GM's return, and gating it on convergence is 9.4's gate, which does not exist. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; bind suite 25/25; the three suites 9.5 names run green together under `--runInBand` (3 suites / 43 tests); FULL unit suite 2415 suites / 33122 passed / 0 failed. The spec's second scenario ('Player cannot inherit GM authority') holds independently of this work and was pinned in #1356 - `handleCampaignHostIntent` compares against the registered host, and the upstream identity guard binds that comparison to the verified principal.
- [x] 9.4 Gate scenario progression on convergence of all retained participants while allowing eligible committed delivery to healthy clients.
  - DEFECT FOUND AND FIXED (2026-09-03): scenario launch is now gated. POST /api/campaigns/:id/launch-authority consults campaignLaunchProgressionGate before force materialization and refuses 409 CAMPAIGN_NOT_CONVERGED with the behind list (participants read from durable cursor rows, a seat with no row fail-closed) and the same reason vocabulary the day-advance socket path emits; the day-advance gate is unchanged. Rows, mutants, and gates are recorded under 17.3's receipt of this date (Wave E17 17.3-b), which carries the fix for both boxes
  - ERRATA (2026-09-03, same audit): convergence gates ONLY AdvanceDay; scenario launch is never gated. The requirement names scenario launch explicitly ('next-scenario launch SHALL remain blocked with a visible reason'), and the gate function is even called evaluateScenarioLaunch - but its only production caller is refuseUnconvergedProgression, whose own docstring says progression only and that other intent kinds are deliberately never gated. Grep over campaignLaunchAuthorityRoute, launchCoopMission and materializeCampaignMissionEncounter finds no call at all. The receipt enumerates three call routes, all of them AdvanceDay, and never states that launch is outside the gate. 17.3 extends what the gate CHECKS; wiring it to an actual launch remains open there.
  - Receipt (2026-08-28, ENFORCED — the gate now refuses real progression; box CHECKED with its delegated remainder named): #1395 built the ledger, #1402 made 'behind' mean behind-on-DELIVERY, and both merged enforcing nothing — zero production callers, `retained` empty in every live session, a finding the adversarial review put in capitals. #1411 is the wiring. Every session-world join path — `joinMember`, `joinGuest`, and a new resync branch for a rejoiner quoting `lastSeq` (current room code required, exactly the newcomer rule) — carries the connection's verified playerId, so participants are RETAINED. The client acks every applied `CampaignEvent` over a new `CampaignAck` frame in the session's own event-log revision space — deliberately never the delivery-epoch grant cursor, which numbers a privacy-FILTERED view; feeding one number space into the other is the replay off-by-one class (#1399) rebuilt an abstraction higher, and the wiring charter forbade it by name. Envelope identity is safe because `handleInbound` closes any socket whose envelope playerId is not the connection's verified principal, before dispatch. PROGRESSION IS GATED AT EVERY ROUTE TO A COMMIT: the host's own `AdvanceDay` intent, the GM's approval of a pending `AdvanceDay` proposal, and — in auto-approve mode, where submission IS the commit — the submission itself, read off a new one-line `arbitrationMode` accessor. Refusal is typed `CAMPAIGN_NOT_CONVERGED` naming who is behind and the revision they must reach, and the day does not advance (asserted on state, not on the error frame). THE OTHER HALF OF THE TASK'S SENTENCE IS ALSO PINNED: non-progression intents commit fine while a participant lags (`SpendFunds` control row), so eligible committed delivery to healthy clients keeps flowing. A REVIEW FINDING CLOSED IN THE SAME PR, found by the landing reviewer rather than the implementing worker: the large-gap resync baseline is stamped `sequence: -1` and the client acked only `sequence >= 0`, so a rejoiner past `RESYNC_SNAPSHOT_GAP` could NEVER converge until an unrelated live event happened to land — AdvanceDay deadlocked with nothing anyone could do. After applying a snapshot the client genuinely holds the state at `payload.revision`, so it now acks that revision; `resyncGuest` still never reseeds `acknowledged`, so convergence remains something only the participant's own ack can claim. EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-test-first — six rows red for the stated reasons before any wiring; reviewed, extended, and re-verified independently. Falsified 1:1 (neutralizing the gate consult reds exactly the five gate rows, controls green) plus a five-mutation matrix each killing exactly its named row: drop `joinMember`'s id; disable the decision-route consult; disable the auto-approve-route consult; disable the snapshot ack; neutralize the gate. Typecheck clean; oxlint 0 errors / 77 warnings (the post-merge-wave pin); multiplayer+campaign battery 258 suites / 3355 tests; full suite 2438 suites / 33302 passed / 0 failed; 30/30 CI. WHY THE BOX CLOSES WITH TWO THINGS STILL NAMED OPEN IN THE FILE: the HOST is not retained (snapshot+subscribe path) — the host is the author of every commit this gate protects, so 'retained participants' excluding the author is the design, not a gap; and grant-served guests (`handleRoomCodeGuestJoin` with a replicaStore) never enter this ledger — that join path belongs to the delivery-epoch world, whose convergence story the wave map delegates to `design-campaign-authority-and-sync` (campaign portions of section 9, wave-map row 5). Per wave-map rule 3 the umbrella acceptance still gates the program: if the leaf's convergence lands weaker than this, section 21-25 acceptance fails the program, not this receipt.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, THE GATE IS BUILT AND CORRECT; box STAYS OPEN because NOTHING CALLS IT): #1395 added the convergence ledger and `evaluateScenarioLaunch` to `CampaignSyncSession` - retained participants, per-participant acknowledgement cursor, behind-list with required revision. The review found a NaN acknowledgement slipped BOTH guards and permanently self-converged a participant (unrepairable, because Math.max(x, NaN) is NaN); the remediation added the `invalid-revision` outcome (NaN/Infinity/1.5/-1 all refused, probed live) and DELETED the Math.max seed that made a bad value permanent - a provably equivalent mutant replaced by a plain assignment and an invariant comment. #1402 replaced the commit-head ceiling with a per-participant DELIVERED watermark, closing the review's honesty finding that 'ahead-of-delivery' never checked delivery (probed live: a participant sent literally nothing could converge by naming the head; now refused), including a frame committed DURING hydration (race constructed in a test with a mid-hydration commit). STILL OPEN, and the next PR: `evaluateScenarioLaunch` and `noteParticipantAcknowledged` have ZERO production callers, and neither production join site passes a participantId, so `retained` is empty in every live session and the gate is unconditionally open. THE WIRING SEAM, verified so the next author does not repeat the review's finding 3: the ack must quote the baseline event's `payload.revision` (log-head space) - NOT `entry.revision`, which is sampled once at registration, never advanced in production (`advanceRevision` has no callers), and would make the gate refuse every launch forever. KNOWN HAZARD for the wiring, verified live: an ID-LESS rejoin (exactly what production does today) leaves the delivered watermark stale and fail-closes later acknowledgements - the wiring must pass the verified playerId on EVERY join path, including reconnect and `resyncGuest`.
- [x] 9.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/CampaignGmArbiter.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts`.
  - Receipt (2026-08-29, run on main b63808758 - CLOSES this box on its literal command): 3 suites / 56 passed / 0 failed. HONESTY NOTE, per the 3.4 precedent: sibling 9.3 (durable GM-loss pause without implicit promotion) remains open and is NOT covered by these suites' current rows - this box verifies what its named command verifies, and 9.3 carries its own future receipt. The section's shipped halves (9.1 durable membership fields, 9.2 readiness, 9.4 convergence gate) are what these suites pin today.


## 10. Awaited Campaign Creation and Canonical Unit Adoption

- [x] 10.1 Make campaign creation/adoption await authoritative campaign, genesis branch, GM membership, player-slot placeholders, force ownership, and unit-reference commit before returning success, implementing `Campaign Creation Has an Awaited Authority Checkpoint`.
  - Receipt (2026-09-02, Wave B3 PR B of 2 - CLOSES this box; ~8% over the per-PR line cap, accepted as one seam because the checkpoint's unit-references stage is 10.2's commit point): commitCampaignCreationCheckpoint (src/lib/campaign/authority/campaignCreationCheckpoint.ts) runs the six authority stages the requirement names, in order, each awaited, first refusal short-circuiting: authoritative record, genesis branch, GM membership, player-slot placeholders, force ownership, unit references; production ports in campaignCreationCheckpointPorts.ts (structural-flag wiring: null when SQLite is not initialised, route unchanged then). The co-op matches route awaits it BEFORE CampaignHostRegistry.register and inside the existing try, so a refusal takes the shipped rollback - the match closes, creation answers 500, nothing navigates. Already-awaited pieces PINNED, not re-implemented: the authoritative record (wizard + co-op panel await the persist before navigation, #1254) and the genesis branch (PUT /api/campaigns/:id awaits maybeAppendCampaignGenesisOnCreate, #1263 - the checkpoint verifies the journal-native marker, never re-appends). Genuinely un-awaited before and proven RED against shipped code: a 201 with no durable GM seat, a 201 with no durable slot ownership, and a 201 for a campaign the server had never persisted. No migration: placeholders and creation-time ownership ride migration 19's campaign_session_force_claim under the reserved mission id campaign-creation with reserved participant ids campaign-player-slot:1|2 (inside that table's declared additive contract; keyed by mission so it cannot collide with real missions); deliberately NOT participant rows, which would consume TACTICAL_SEAT_LIMIT and break section 9's sealed seat behaviour. Slot->force derivation: the campaign's own forces in stable id order when no explicit map is supplied (no GM assignment command exists yet; playerSlotForceIds is the input for when it lands). Worker mutants killed: route await dropped (201 instead of 500), ownership commit skipped for slot 1 (4 red); orchestrator re-ran the dropped await (1 red) and confirmed the genesis-verdict-ignored mutant is killed by 'refuses at genesis-branch when journal authority has no marker' (the refusal object is swallowed into genesisBranch and the checkpoint reports committed having bound the GM and claimed both slots). Equivalent mutant recorded, not counted: the un-awaited GM bind survives because the shipped stores are synchronous; ports are typed T | Promise<T> so a future async store cannot make it a silent defect. NOT claimed: a force-less player slot has no durable row (needs a schema this seam may not add - named touchpoint); the GM cannot yet reassign a force between slots; custom units remain blocked at launch admission (10.3); client creation surfaces unchanged (the co-op panel's post-registration updateCampaign is still local-only until the debounced autosave - real, adjacent, flagged); genesis under journal authority verified by marker, not exercised end to end (flag false). Gates: tsc 0; oxlint 77-pin; 10.4 command 25/25; blast radius 332 suites / 4259.

- [x] 10.2 Preserve customized-unit identity, weight, tech base, engine, gyro, armor, equipment, critical slots, and temporal metadata through campaign adoption and player ownership.
  - Receipt (2026-09-02, Wave B3 PR A of 2 - CLOSES this box): adoptRosterUnitReference (src/lib/campaign/authority/campaignAdoptedUnitReference.ts) resolves a customized design against its durable custom_units record through UnitRepository and carries every named clause through adoption and player ownership with NO default anywhere in the module: identity (chassis/model/variant/unitType/configuration), weight (tonnage), tech base, engine {type,rating}, gyro, structure, armor {type, allocation incl. front/rear pairs}, equipment, critical slots, and temporal metadata (sourceCreatedAt/sourceUpdatedAt/sourceRecordVersion/era/year/adoptedAt); an unresolvable custom reference is a typed refusal, never a template substitution. Proven field by field off the READ-BACK envelope of a real round-trip (UnitRepository.create -> buildSerializedCampaign -> saveCampaign -> readCampaign -> re-adopt) on temp-file SQLite, not against a fixture the expectation was copied from. Worker mutants killed: gyro silently defaulted (1 red), gyro substituted outright (3 red). ORCHESTRATOR-FOUND HOLE, fixed red-first: swapping sourceUpdatedAt for record.createdAt SURVIVED all 19 rows - UnitRepository.create stamps created_at and updated_at from ONE clock, so the existing temporal row asserted against two byte-identical strings and could not discriminate. New row 'carries created-at, updated-at and record version apart from each other' drives the repository clock to two instants, edits through the shipped UnitRepository.update (which also moves current_version off 1), round-trips, asserts the three fields separately, and carries an anti-decay guard (createdAt !== updatedAt) so a fixture that stops separating them fails there instead of going vacuous again; red under the mutant (Expected 3025-06-15 / Received 3025-01-01), green on revert (77/77). NOT claimed: the custom-unit launch gate (admitCanonicalExactReference still denies unitSource 'custom' with roster_source_custom) - launch admission, not adoption, belongs with 10.3; TacticalPlayerSlot is exported here and consumed only by PR B's checkpoint. Gates: tsc 0; oxlint 77-pin; 10.4 command 25/25; campaign+multiplayer 281 / 3579.

- [x] 10.3 Materialize both tactical player forces from the active campaign branch/revision and reject stale force or ownership revisions before encounter creation.
  - ERRATUM TO THE FIX RECEIPT BELOW (2026-09-03, same day, before merge): the receipt's "all KILLED" was VOID. My typing fix to the worker's suite had turned the single-player row's save result into skipped (a real union member the page reads as a failed checkpoint), so that row was red at baseline and every mutant run showed the same one failure; CI caught it (and six rows of the pre-existing mission-launch.coop page suite, whose store stub predated clearLaunchConflict, so the page's first call threw and the failure handler rendered that TypeError). Repaired: the row answers a real saved result; the page suite's stub carries launchConflict/clearLaunchConflict/reportLaunchConflict. Matrix RE-RUN on a green baseline: M1 was EQUIVALENT under the fixture as first written (it read a runtime property the fake never sets) and is rewritten to the exact removed code (nextSequence() - 1) - KILLED by the co-op row; M2 stale refusal swallowed KILLED; M3 co-op arm with no head KILLED (2 red); N1 single-player arm skips the head read SURVIVED honestly (no row drove a single-player launch with a real head) - directed row "reads the server head for a single-player launch and asks the launch door with it" (head fixture, POST launch-authority with that head and no session id, materialize with ownedForces) now KILLS it. Gates re-run: tsc 0; oxlint 77-pin; launch page + co-op page + requestLaunchAuthority suites 21/21. Lesson recorded as LAW 85: babel-jest never type-checks, so a typing-only edit to a suite can change runtime semantics - re-run after every edit, and ladder every pre-existing suite that consumes the touched module.
  - DEFECT FOUND AND FIXED (2026-09-03): the launch page never consulted the server head. The co-op arm materialized both player forces against the runtime host's in-memory sequence (a replica's view, not the authoritative revision), and the single-player arm materialized them with no head at all, so the stale-revision refusal this box promises could not fire on the one screen that launches encounters. Fix: resolveLaunchForces and classifyLaunchFailure (src/lib/campaign/encounter/requestLaunchAuthority.ts) are the single force-materialization path both pages share; missionLaunchPage.launch.ts reads GET /api/campaigns/:id/head first, resolves forces against that head, and derives the launch's catalog revision from it - a stale refusal surfaces as a typed launch conflict on the persistence store instead of being swallowed. CampaignDashboardPage.launch.ts becomes a facade over the same helpers (byte-identical behaviour, proven by its existing suite). Orchestrator mutants, all KILLED: co-op arm sends the in-memory sequence again (1 red); stale refusal swallowed (2 red); co-op arm resolves forces with no head (3 red); NOVEL single-player arm skips the head read (1 red). Gates: tsc 0; oxlint 77-pin; launch page + requestLaunchAuthority suites 12/12. NOT claimed: custom units remain blocked at launch admission (unchanged); the head read is one extra GET per launch, accepted
  - ERRATA (2026-09-03, same audit): the co-op mission-launch page does not gate on the server's active campaign branch or revision. The two production callers of requestLaunchAuthority are the dashboard launcher and the fast-forward runner; the shipped mission-launch page mounts missionLaunchPage.launch, which names no expected head, no launch authority and no session id. Its co-op arm passes a revision read from the in-memory coopRuntimeSession (whose own header says it is intentionally in-memory and which binds the in-memory campaign event store while the journal authority flag is false), and its single-player arm passes no head at all. So the letter's 'reject stale force or ownership revisions before encounter creation' is enforced on a browser-local number on that route.
  - Receipt (2026-09-02, seam 3 PR B of 2 - CLOSES this box, SEALING SECTION 10 at 4/4; PR A was the head route #1513): the live paths are wired. POST /api/campaigns/:id/launch-authority (src/pages-modules/api/campaignLaunchAuthorityRoute.ts + thin shell) answers for a browser that has no SQLite: the client sends the head it HOLDS and gets one of four actionable answers - materialized (both tactical slots from the owned-force decision #1509 over durable ports), current (head fine, no co-op session), no-authoritative-stream (no journal to gate against - proceed ungated exactly as today), or 409 refused carrying the head that IS current plus the resync action; the journal-head reader moved into the launch-head module so the two endpoints share ONE definition of the head. The dashboard (CampaignDashboardPage.launch.ts, extracted for max-lines; useCampaignLaunchHead) reads the head ONCE at hydration and holds it on launch state - re-reading at launch would always match and prove nothing; the hydration-to-launch window is exactly where another client advances the campaign. fastForwardCampaign reads it once at the start of a run and stamps every FastForwardBridgedScenarioHandoff so the gate spans the run (with the head dropped the stale multi-battle run did not merely miss the refusal - it FOUGHT the battle, deep enough to trip the #998 zero-armor guard); an unreachable authority THROWS rather than running ungated (unavailable is explicitly not permission). Both gates are opt-in by the presence of their input so no existing caller changes shape; the in-process API router now serves both authority routes so the gated fast-forward path drives the REAL handlers. Order: admitCampaignLaunch (local, offline readiness) first, then the authority round-trip. Refusals reach the materializer's existing gate (throws before its first POST) and surface on CampaignSaveStatusCard as a typed conflict with the current head + resync-to-active-head (mapped to resolveConflictTakeServer - one definition of 'catch up'), a SEPARATE block from the save conflict on purpose (two versions vs one honest move; 'Keep My Version' for a branch head would invite insisting on a world that no longer exists). Red-first: a stale launch RESOLVED with real forces, encounter and session. Worker mutants killed: literal root head sent, refusal flattened, runner ignores the refusal, unavailable silently ungated, driver drops the head; orchestrator: route ignores the head verdict, non-JSON answer -> ungated, driver drops the head re-run. DESIGN OUTCOME, not a gap: the single-player dashboard launch is head-gated but NOT owned-force gated - force claims are keyed by session and an SP campaign has none, so gating it would refuse every SP launch UNOWNED_SLOT. Residual carried to the acceptance packs (21.x/22.x), e2e-shaped: no test yet drives a GM launching a real two-player co-op campaign end to end through the browser (the route proves both slots materialize for a session; the clients send sessionId when present). Gates: tsc 0; oxlint 77-pin; pages-modules + encounter + fastForward + dashboard + stores 44 suites / 461; campaign + pages-modules + dashboard + stores 210 suites / 3118; prefix i alone 185 suites / 2807.

  - Progress (2026-09-02, Wave B3 seam 2 landed as two PRs - box stays OPEN on one clause: no production caller supplies the expected head yet): materializeOwnedPlayerForces (campaignOwnedForceMaterialization.ts) is a pure decision over injected ports (the browser launch path has no SQLite): head FIRST through the branches leaf's validateExpectedBranchHead (#1498) - the branch id is READ, never named; production is genesis-only so today it is the backfilled root at its effective generation, and a hard-coded 'root' would materialize the wrong lineage on the day a candidate activates - then ownership per slot (per-mission claim by the slot placeholder wins, else the creation-mission claim the checkpoint wrote; new reverse read readCampaignSessionForcesHeldBy), then units. A force another participant now holds for this mission is a typed STALE_OWNERSHIP refusal carrying the current head + resync action; UNOWNED_SLOT and UNRESOLVED_SLOT_UNIT refuse rather than substitute; every path is a read, nothing appended. The materializer calls the gate as the FIRST statement in its try, ahead of the roster preflight and every fetch; both slots field ONE player side in slot order (composeCoopEncounter doctrine; IEncounter carries a single playerForceId; per-slot attribution kept for seat validation) and the OpFor is sized off the union. Red-first: a stale revision launched silently and slot 2's Marauder never reached the map. A test the worker wrote was WRONG and the module right (one slot taking the other's force is a conflict, not an override) - split into an override row with a spare force plus a STALE_OWNERSHIP row. Mutants killed: stale-head verdict ignored, slot 2 under slot 1's ownership (5 red), unresolvable unit skipped, gate call dropped post-extraction (added because the max-lines extraction MOVED the gate); orchestrator re-ran the head and gate mutants and added stale ownership accepted + unowned slot fielded empty. CUSTOM-UNIT LAUNCH GATE NOT LIFTED, with evidence: resolveNodeCanonicalUnitStats answers null for a custom id and force assignment stores unitId opaquely, so lifting it today fields a player's design at BV 0 / tonnage 0 (the #998 armor-seed class); the stats resolver becoming custom-aware is a named touchpoint in another owner's file. Extractions forced by max-lines: the gate to .ownedForces.ts, the REST builders to .rest.ts (directory convention). NOT claimed: CampaignDashboardPage and fastForwardCombatRunner still call the single-force form - wiring them to supply the head is the next slice and what closes this box; force-less slots still have no durable row; GM reassignment between slots uncommanded.

- [x] 10.4 Verify with focused campaign creation, serialization, customizer-handoff, and encounter-materialization tests plus `npm.cmd test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts`.
  - Receipt (2026-09-02 - CLOSES this box): the exact command `npm.cmd test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts` passes 2 suites / 29 tests (25 before seam 2); focused creation (campaignCreationCheckpoint 13 + route 3), serialization/customizer-handoff round-trip (campaignAdoptedUnitReference 20 incl. the temporal-metadata discriminator row), and encounter-materialization (campaignOwnedForceMaterialization 18 + materializer) suites green; campaign+multiplayer+events 322 suites / 4396; full services sweep 611 suites / 8795. Verified at PR B's head by the orchestrator.

  - Progress (2026-09-02, seam 3 PR A of 2 - the head becomes askable; box stays OPEN until the launch paths pass it): the wiring was blocked on a PRECONDITION the worker verified rather than guessed past - no client-facing campaign type carries a branch id, no route exposed a head, and the two revision-shaped numbers a client holds are both wrong (SerializedCampaign.version is the campaigns-table write counter; the co-op snapshot's revision is a campaign event SEQUENCE, one below the journal revision per JournalCampaignEventStore). Owner decision: Option 1, a dedicated read - GET /api/campaigns/:id/head (logic in src/pages-modules/api/campaignLaunchHeadRoute.ts, thin shell in src/pages/api/campaigns/[id]/head.ts; resolution in campaignLaunchHead.ts behind ports) resolves the effective branch from the branch store (readEffectiveHead, #1497) plus event_journal_stream_heads.stream_revision for the stream AND that branch, returning exactly the number validateExpectedBranchHead compares against; a branch with no head row is revision 0 (genesis), not a missing stream. A campaign with no journal stream answers 200 {kind:'no-authoritative-stream'} not 404 (flag off = every campaign today; 404 would fire on every production launch and be indistinguishable from a deleted campaign - the typed answer lets the launch proceed ungated on a FACT). The client reader (readCampaignLaunchHead) never fabricates: malformed payload / non-OK / transport failure -> 'unavailable'; the head is never written into SerializedCampaign (no schemaVersion bump). Red-first: module-not-found; every row builds a world where write version (2) and journal revision (1) DIFFER so the wrong counter cannot pass by coincidence. Worker mutants killed: write version returned as revision (2 red), genesis head invented for a stream with none (2 red), reader default-head fallback (5 red). NOT claimed: nothing calls either module yet (PR B wires CampaignDashboardPage + fastForwardCombatRunner and surfaces STALE_* on the persistence store's conflict surface); no auth on the endpoint (matches the sibling campaign read route); a FOURTH reader of event_journal_stream_heads now exists (writer, recovery, C2's private lease-store reader, this route) - consolidation is a named touchpoint for the journal layer.

- [x] 10.4 Verify with focused campaign creation, serialization, customizer-handoff, and encounter-materialization tests plus `npm.cmd test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts`.
  - CLOSED (2026-09-02, leftover duplicate box): the checked 10.4 above already carries the receipt for this verification; this second copy was left over when the section was renumbered. Re-run fresh on main 15b35341 with the named command (npm test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts): 2 suites, 29 passed. No code.


## 11. Pre-Serialization Viewer Projection

- [x] 11.1 Introduce one server-side viewer projector used before `JSON.stringify` for live match and campaign frames, implementing `Viewer Projection Occurs Before Serialization` and private authority-sequence separation.
  - Receipt (2026-08-29 - CLOSES this box; the campaign half its receipts named is delivered): CAMPAIGN frames now pass a per-recipient scope boundary BEFORE serialization. The finding that drove the shape: design D3 has stamped an access scope (gm | campaign | team | player:<id>) on every campaign event since it shipped, the emission table explicitly anticipates GM-hidden facts - and NOTHING on the delivery side ever read it: CampaignSyncSession fanned every envelope to every sink, so the FIRST gm-scoped producer would have broadcast its secret to the whole session. (No leak exists on today's wire - every current producer stamps scope campaign, grep-verified - the boundary is the prevention the vocabulary always assumed.) campaignScopeAdmits is the pure guard (GM sees all; campaign flows to everyone; player:<id> targets its player plus the GM; team fails closed to GM-only until a membership source exists; an unproven viewer gets only campaign scope), applied by admitToWire at the session's sink boundary - joinMember hydration and live subscribe, resyncGuest tail, baseline, and live - so nothing reaches JSON.stringify unguarded. A withheld frame still advances delivery bookkeeping, deliberately: convergence must not wait forever on a fact scope holds back, proven by the ack row ('applied', where skipping the recording would answer ahead-of-delivery). Integration rows drive the REAL commit/hydration/fan-out via a _commitEventsForTests seam (the anticipated GM-hidden facts have no producer yet) and were RED before the wiring - gm-scoped withheld from players live AND in resync tails, player-scoped targeting exactly its player plus GM, campaign flowing to all. Mutations: bypassing the boundary at ONE site (resyncGuest) dies at the resync row - the single-application-point law; stripping the GM too dies at six GM-keep rows across the pure matrix and integration. Gates: tsc 0; oxlint 0 errors / 77 warnings; campaign+bind suites 4/96; multiplayer tree 110 suites / 852 passed. DEFERRAL, DOCUMENTED IN THE MODULE ITSELF: the envelope's authority `sequence` stays on player campaign frames - the legacy campaign client resumes by that number, and the delivery-epoch mapping that replaces it is delegated to design-campaign-authority-and-sync (wave map row 5), exactly as this box's own receipts anticipated. Until it lands, concealment arithmetic over sequence gaps becomes possible the moment something is actually withheld - stated here rather than silently shipped, and the match half's separation (delivery-space frames, sequence stripped per viewer) remains fully in force.

  - Progress (2026-08-28 later, THE ENVELOPE STOPPED COUNTING TOO; box still open only for campaign frames): #1429 closed the negative sweeps' second finding - ReplayStart/End carried authority-space bounds, so `(toSeq - fromSeq + 1) - totalEvents` handed a player the exact number of fog-hidden events in the span, the same arithmetic the inverted guard kills at the event level surviving one envelope higher. Envelope projection now rides the SAME publication boundary as event fields: player envelopes carry delivery-space bounds only, where span equals items delivered BY CONSTRUCTION and the arithmetic yields nothing; GM and authority envelopes keep `fromSeq`/`toSeq`, and over-stripping them is a caught failure. The sweeps' finding row flipped `it.failing` to `it` - red first with the honest number (expected 0, received 1). The client needed no authority bounds already; a new row proves recovery completes when the envelopes carry none at all. Reviewer re-run: re-emitting authority bounds to players reds four rows. Full suite 2443/33360/0. STILL OPEN here: campaign frames outside the boundary, and the sweeps' schema finding (`EventMessageSchema` tolerating `event.sequence` via `z.unknown()`) stays a deliberate `it.failing` until the protocol decision is taken.
  - Progress (2026-08-28, THE CONCEALMENT LEAK IS DEAD; box STAYS OPEN for the campaign half): the authority-sequence separation this task names is now enforced, in two slices plus a review addition (#1422/#1423 and its predecessor). SLICE A made the client delivery-first and dual-keyed, proven against a real fog server whose frames were post-processed to DELETE event.sequence - exactly-once by id, gap report, resync, recovery, all without the number. Its most valuable output was naming the protocol gap: ReplayStart/End bounds were authority-space and could neither number replay items nor prove a delivery hole filled. SLICE B closed that gap and cut over. Replayed events ARE deliveries: each per-player replay item carries the viewer's deliverySequence - reusing the existing index for events already in the record, assigning through the SAME cursor for events never delivered, so the record stays gapless and later resumes still answer. ReplayEnd carries a delivery-space bound for pin release. `sequence` joined AUTHORITY_ONLY_EVENT_FIELDS at the single projection point, PER-VIEWER: GM and authority projections keep it, and over-stripping them is a caught failure (the mutation that strips GM too reds the authority-keep row). `viewerSequenceConcealmentLeak.test.ts` is INVERTED per its own header: what measured the violation now fails the suite if any player-projection frame carries event.sequence or exposes an authority gap. THE REVIEW ADDITION, because the worker's honest stop surfaced it: `isGameEvent` rightly demands a numeric sequence (engine events always carry one), so a real player's mirror would have hydrated at 'loading match...' forever against the stripped stream - while every hook test stayed green on locally-built events that still carry sequences, the suite-green-product-broken shape this program keeps finding. The bridge is the client: within a player's mirror, `sequence` already means position-in-the-local-log (locally-appended events use events.length), and the delivery number IS that position, so the client stamps it onto sequence-less wire events at emission - a local number, never an authority one, and the code says so. The live-shape row (real client, real fog server, sequence-stripping duplex, mirror hydrates) was red before the stamp. One premise corrected on the way: emitted events do not carry NO number, they carry the local stamp - duplicate-free and contiguous once a late recovery lands, arrival order deliberately not ascending because the recovered frame keeps its original number. EVIDENCE: grok-4.6 worker on a bounded charter, failing-first (the inverted guard red before the strip); reviewed, extended, re-verified independently. Worker mutations: fresh-assign on replay reuse reds the resume rows (the #1406 defect class, guarded); strip-GM-too reds the authority-keep row; dropping the ReplayEnd bound reds the pin-release row. Reviewer re-runs: neutralizing the mirror stamp reds the hydration and stripped-stream rows; re-stripping GM reds authority-keep. Typecheck 0; oxlint 0 errors / 77 warnings; battery 95 suites / 734; full suite 2440 suites / 33337 passed / 0 failed; 30/30 CI. Scope disclosed in the commit: 21 files, over the 15-file convention, 7 production - the assertion sweep of an atomic cutover, declared rather than silent. STAYS OPEN, and it is the task's own second noun: CAMPAIGN frames do not route through the publication boundary - `CampaignSyncSession` / `bindCampaignSyncConnection` serialize outside the projector. That half is this box's remaining work; the wave map's delegation of campaign portions to design-campaign-authority-and-sync is the likely home for its delivery-epoch side, and whichever change takes it must run this same per-viewer separation there.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, ONE PROJECTOR NOW DECIDES WHAT LEAVES THE SERVER for live-match frames; box STAYS OPEN for the sequence removal and campaign frames): #1398 introduced `ViewerFrameProjector` behind the publication boundary as the single application point before serialization, with `AUTHORITY_ONLY_EVENT_FIELDS` as the declared removal list. The review's blocking finding - an always-true `redacted` flag collapsed replay pagination from 64-event chunks to ONE frame - was fixed and the remediation added the row that reds when pages re-collapse, plus a by-reference identity-fast-path pin (removing the fast path had left all 44 tests green while the docblock promised the original object back). #1400 added wire-level proofs that a removed field is absent on all three surfaces (live, replay, resync). STILL OPEN, stated plainly: (1) `event.sequence` is STILL on player frames - the concealment leak is not closed, `viewerSequenceConcealmentLeak.test.ts` still passes and must be INVERTED when the removal lands; the removal needs the client resuming from its per-viewer deliverySequence first (dedupe and fork detection re-keyed off the same number) - the delivery-cursor half of that landed in the 5.3 wave, the client re-key has not; (2) campaign frames (`CampaignSyncSession` / `bindCampaignSyncConnection`) do not route through the publication boundary at all - the task says live match AND campaign, and only the live-match half is done; (3) the removal list has one entry (`payload.intentId`).
  - Progress (2026-08-26, first slice — the per-viewer sequence EXISTS and is consumed; box STAYS OPEN): this box has two halves and the projector half was already built - `MATCH_WIRE_PUBLICATION_BOUNDARY.guardLiveEvent` runs per recipient BEFORE `safeSend` does the `JSON.stringify`, so `Viewer Projection Occurs Before Serialization` holds today. What did not exist was PRIVATE AUTHORITY-SEQUENCE SEPARATION: the wire carried only the global authority sequence, so a fog viewer's slice of it was full of holes. `ViewerDeliveryCursors` now assigns each viewer their own gapless `deliverySequence`, and the number is taken AT SEND TIME, after every `continue` that fog or the guard can take - which is precisely what makes it gapless for them while the authority sequence in the same frame is not. A send that FAILS does consume its number, deliberately: the resulting hole is a true signal that something was lost, which is the signal the authority sequence could never give. THE SLICE IS CONSUMED, not shipped inert - the client tracks the number and reports a `delivery-gap` on a hole, while STILL APPLYING what arrived. That last part is the whole lesson of the reverted regression: holding events behind a gap stalls a fog client forever on a sequence it may never receive, so the signal is advisory and resync is the response, not a stall. Three server rows (gapless per viewer while the authority sequence is not; the two viewers numbered independently; a withheld frame consumes no number) and three client rows (reports a gap and still applies; does NOT cry gap over a sparse authority sequence; says nothing about pre-rollout frames that carry no number). Falsified 1:1 on both halves: removing the server stamp reds all three server rows, and making the client's detector always return false reds the gap row alone. TWO TEST-AUTHORING MISTAKES WORTH RECORDING, both caught before merge and both of the same family. The first control asserted the two viewers received DIFFERENT NUMBERS of frames - they received the same count (10 each), losing a different event apiece, so it was comparing the wrong thing; it now compares what actually arrived. The second was worse: the client harness sent `ReplayStart`/`ReplayEnd` without their required fields, so the frames failed schema validation, the client never left its replay window, and both control rows passed because NOTHING WAS PROCESSED AT ALL. They now assert the applied count too. WHY THIS BOX STAYS OPEN, EXACTLY: the concealment leak is NOT closed. The authority sequence is still on the wire beside the new number, so `viewerSequenceConcealmentLeak.test.ts` still passes - a player can still count what was hidden from them. Closing it means REMOVING the authority sequence from player frames and resuming replay from the delivery cursor rather than `lastSeq`, which touches replay, mirror hydration, and reconnect together. That is the next slice, and doing it in the same step as this one would have been a change nobody could review. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suites 6/6; FULL unit suite green.
- [x] 11.2 Store GM previews and private reasons in separately authorized server-only records; ensure player-safe facts contain no private fields or correlatable private identifiers.
  - Receipt (2026-08-30 - CLOSES this box; the evidence map had shown the consumer machinery real and the PRODUCER wiring absent, and the producer now exists): GmPrivatePreviewRecordWriter is the server-only ingress - a GM preview persists as a gm-draft private record (content + derived summary) and a private reason as a separate gm-reason record, both through authenticated repository writes with the access audit recording write/granted; a player write is DENIED, creates nothing, and records write/denied. No public marker, frame, or history row is emitted by the writer; finalized-correction ROUTING stays with 14.1 by boundary, and this is the ingress it will call. NO CORRELATABLE IDENTIFIERS: the projector now strips privateRecordRef from player projections recursively (top-level AND nested) while the GM projection retains it, and the 11.4 raw sweep gained the planted-opaque-ref row proving a 32-hex ref appears in no player frame. DEFECT FOUND AND FIXED EN ROUTE: the private-access audit storage rejected the new write purpose, and its broad constraint classifier mislabeled that failure as an opaque-ref collision - additive migration 22 extends the purpose set with an append-only trigger, and collision handling is narrowed to actual PK conflicts. Red-first throughout; mutations killed in both runs: disabling recursive ref-stripping dies at the sweep row (reviewer re-run confirming); removing GM-role enforcement dies at the GM-only-write row. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused privacy/history suites 6/51; full multiplayer tree 110 suites / 857 passed. 10 files.

- [x] 11.3 Implement sealed Player 1/Player 2 choices and authoritative reveal while keeping ordinary public combat facts on immediate committed publication.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 11 at 5/5): sealed choices exist on the wire. The seal set - MovementDeclared, AttackDeclared, PhysicalAttackDeclared - is declared solely in MatchWireAudienceCatalog (the single policy point) as SEALED_TO_ACTOR_UNTIL_REVEALED, evaluated by MatchWireSealedChoices, satisfying the spec letter: a tactical player's sealed choice SHALL be visible only to that player and the GM until an authoritative reveal or phase-finalization event. Reveal semantics per finalizer: weapon attacks reveal on AttacksRevealed; movement and physical attacks finalize on the PhaseChanged out of their phase (movement has no dedicated reveal marker - found, not assumed). The public finalizer publishes FIRST, then the withheld originals are DELIVERED to newly eligible viewers in committed order through the SHIPPED delivery machinery - ViewerDeliveryCursors identity mapping, normal delivery numbering, exactly once - and replay/resume applies the same audience policy, so E2E-22's reveal-from-committed-delivery-streams letter is the mechanism, not a paraphrase. Pre-reveal the sealed-out player's frames carry neither the choice nor a delivery-number gap (the fog no-gap law extended); ordinary public facts keep immediate committed publication (PhaseChanged control row). Proven on a real seated 1v1: actor-immediate delivery, opponent omission with no destination leak and no number gap, exactly-once full originals after finalization. Failing-first throughout, including a REAL finding: the actor-delivery path first keyed on a lobby owner id that mismatched the authoritative principal id - red until corrected. Mutations: leaking a declaration at commit dies at TWO boundary rows (reviewer re-run - the first reviewer probe hit the movement-only integration suite and survived, the boundary suite is where attack coverage lives, both suites now cited); skipping reveal delivery dies at the exactly-once row; sealing a public fact dies at the control. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 5 suites / 31; full multiplayer tree 111 suites / 860 passed. 9 files, +762/-68.

- [x] 11.4 Add schema/object tests and raw-frame negative tests for GM-private reason, hidden metadata, private IDs, authority sequences, and inferable gaps; fail closed on any projection error.
  - Receipt (2026-08-28, PR #1427; box CHECKED - the sweeps exist for all five categories, the fail-closed clause was already load-bearing, and the two truths the sweeps surfaced are pinned as failing rows rather than papered over): one suite drives real hosts and asserts on the RAW WIRE STRINGS. Authority sequences: absent on every player surface, PRESENT on the GM stream - the discrimination is part of the property, and the mutation that strips GM too reds the GM-keep row. Private ids: an explicit documented allowlist, anything id-shaped outside it fails (a stamped `rowid` reds exactly that row). GM-private reasons: a sentinel planted on a fog-concealed event appears in no player frame. Hidden metadata: the concealed event's literals swept against every frame. Inferable gaps: every NUMBER in every player frame swept - a stamped derived authority position reds the sweep. Five deliberate-leak mutations, five named deaths; the reviewer independently re-ran keep-sequence-everywhere (three rows died, restored green beside the inverted guard). FAIL CLOSED ON PROJECTION ERROR is the #1398 boundary's standing law, not new work: a projector throw returns a typed failure with NO raw fallback (ViewerPublicationBoundary's own contract and rows), so a projection error suppresses the frame rather than leaking the unprojected one - re-read this session, not assumed. TWO FINDINGS, as `it.failing` rows that flip green when fixed, deliberately not fixed in a test-only slice: (1) `EventMessageSchema` admits `event.sequence` through `z.unknown()` - the wire schema tolerates what the projector strips, so schema validation alone cannot catch a projector regression; tightening it is a protocol decision. (2) THE REPLAY ENVELOPE STILL LEAKS A COUNT: on a player resync, `(ReplayEnd.toSeq - ReplayStart.fromSeq + 1) - ReplayStart.totalEvents` equals the number of fog-hidden events in the span (measured 1). The per-event sequences are gone (#1423), but the authority-space bounds let a player COUNT what was concealed - the same arithmetic the inverted guard kills at the event level, surviving one envelope higher. The fix direction is already named on the schema fields: player replay bounds fully into delivery space. That fix is 11.1-remainder work and the next production slice. Gates: typecheck 0; oxlint 0 errors / 77 warnings; multiplayer battery 96 suites / 743 tests; one file, 778 lines.

- [x] 11.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.fogOfWarIntegration.test.ts src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts`.
  - Receipt (2026-08-29, run on main 7d3a9040b - CLOSES this box on its literal command, per the 3.4/9.5 precedent): 3 suites / 40 passed / 0 failed - fog integration, the campaign sync session (now carrying the 11.1 scope boundary), and the match host (now carrying the serialized executor). HONESTY NOTE: siblings 11.2 (GM-private server-only records) and 11.3 (sealed choices) remain open and are not covered by these suites' current rows; each carries its own future receipt. What this section HAS delivered and these suites plus the 11.4 sweeps pin: the projector before serialization on both frame families, per-viewer sequence separation for matches, the campaign scope boundary, and the five-category raw-wire negative sweeps.


## 12. Replay Snapshot Timeline and Export Parity

- [ ] 12.1 Reuse the pre-serialization projector for replay, resync, snapshot, cold recovery, audit timeline, and export, implementing `Visibility Is Equivalent Across Surfaces` and `Timeline and Export Use the Same Viewer Projection`.
  - NOTE (2026-09-02, Wave E8): the timeline and export now have HTTP routes sharing one viewer chain and one digest law (see 21.2); 12.1 itself still waits on the baseline-kind field (#20).

  - Progress (2026-09-02, seam 2 - timeline/export half DELIVERED; box stays OPEN on one named, owned clause): the audit timeline and export now consume the shared contract (see 12.2). FINDING #20, probed for real and NOT papered over: seam 1's header claimed the grant projector implemented 'the conservative form of the same law' - it does not. campaignGenesisArmParity.test.ts drives one logical committed stream through BOTH arms (real CampaignMatchHost + CampaignSyncSession vs the real SQLite grant projection harness for a ['campaign']-scoped grant): both withhold the gm-scoped fact from the same player (control), but the legacy arm folds the opening ledger into the restricted player's baseline while the grant arm withholds it (projectCampaignStreamForGrant drops EVERY full-state snapshot for a partially-scoped grant) - the same player gets two different opening ledgers. Neither arm is simply wrong: a stored baseline can be a pre-scope GENESIS (minted at open from the shared starting ledger) or an IMPORTED campaign's whole state (which CAN carry withheld material - campaignGrantSnapshot.storedBaselineLeak.test proves that threat), both are CampaignSnapshotPublished stamped 'campaign' with identical shape, so relaxing the grant rule re-opens a proven leak and adopting it on the legacy arm loses facts nothing withheld. The distinguishing fact is a SCHEMA ADDITION on the baseline event (baseline kind) owned by the campaign-journal migration work (design-campaign-authority-and-sync D10 / task 5.2 authority migration states). Pinned as it.failing (house precedent 11.4) so it flips green from either reconciliation and cannot change silently; seam 1's contract header corrected. `Visibility Is Equivalent Across Surfaces` is therefore NOT yet fully true: every surface can prove what it shows, and one pair provably disagrees for a named, owned reason. This box closes when the baseline-kind field lands and the failing row flips.

  - Progress (2026-09-02 - OWNER DECISION ENACTED, seam 1 of 2, Wave B1; box stays OPEN until timeline/export consume the contract in seam 2): the shared-contract slice the 2026-08-30 inventory asked for is approved and built - campaignViewerProjection.ts is the per-viewer projection CONTRACT the six surfaces agree on without sharing a projector instance: the visibility SET for one viewer (campaignViewerVisibleEvents; caller injects its own admission predicate - campaignScopeAdmits for the legacy session, grantAllowsScope for the grant channel - so the module imports neither authority model), the fold of exactly that set through the ONE reducer (applyCampaignEvent), and a per-viewer digest in two halves (factsDigest for surfaces carrying events, stateDigest for surfaces carrying only a fold, projectionDigest binding both) so another surface can prove agreement. The digest strips `sequence` deliberately: the same fact carries a journal sequence on the legacy surface and a per-grant delivery number on the scoped one, so a digest keeping it could neither let surfaces agree nor avoid becoming the channel the scoped policy closes (row: renumbered facts digest the same; orchestrator mutant N3 keeps the sequence - killed). THE INVENTORY'S ONE UNPROJECTED SURFACE IS FIXED RED-FIRST: CampaignSyncSession.buildBaselineEvent serialized host.buildSnapshotPayload() - the full folded authoritative state - to every admitted viewer (its own comment conceded it), so a gm-scoped hire or an authority-only balance move was withheld from a player's live stream and handed to them in the join baseline anyway; five rows on the real host and session were RED (Expected 500000 / Received 499999; the withheld pilot present in the fold) and are green with the baseline projected for the joining viewer (both the GM and member arms post-#1493, plus the large-gap resync). Only the STATE is projected; matchId and revision are untouched (the sequence deferral in campaignWireScopeBoundary stands). Snapshot law stated once and refined vs the grant projector's conservative form: a full-state snapshot is folded only while no withheld fact precedes it - genesis qualifies (the shared opening ledger survives), a mid-stream re-baseline does not (cannot smuggle back what the filter removed); both directions pinned. Cold recovery cannot share (CampaignHostRegistry.register rebuilds a host + session over the surviving log and serializes nothing itself) and is PINNED instead: a real process-restart rebuild whose full-state initialState is the trap reaches the same guarded path. Worker mutants killed: predicate bypassed (6 rows), digest blind to scope, snapshot latch removed, GM given the restricted fold; orchestrator re-ran the predicate bypass (6 red) and added N2 baseline ships the host's cached state at the exact old leak site (6 red) and N3 (1 red). Wire sweeps run unchanged: 7 suites / 36 green. Named harness change: bindCampaignSyncConnection.test's flushAsyncHandlers drain 8 -> 32 ticks (matching its .reconcile sibling) because the baseline now reads the log; no assertion changed. NOT claimed: audit timeline and export do not yet consume the contract; the grant projector is untouched; a live-vs-grant DISAGREEMENT read from the code and not yet probed - the grant path drops genesis for a restricted grant while the legacy path keeps it, so the same player may see the opening balance on one arm and an empty ledger on the other (seam 2's first parity target); the per-join full-log read is unbenchmarked. Suites: multiplayer+campaign 282 / 3587; replay-recovery jest half 3 / 18; tsc 0; oxlint 77-pin.

  - Progress (2026-08-30, six-surface projection inventory by a fresh-context worker, suites re-verified 110/856 - box STAYS OPEN, and the inventory says its letter needs an owner decision before code): the surfaces do not share one projector BY DESIGN, and forcing them onto one would break proven contracts. LIVE/REPLAY/RESYNC: the wire publication boundary, proven (11.x). GRANT-CHANNEL SNAPSHOT: buildScopedCampaignSnapshot - a deliberately SEPARATE policy that excludes journal sequence/revision/digest fields so a restricted snapshot cannot leak identities; replacing it with the wire projector would assign sequences to withheld identities (projectCampaignStreamForGrant states this in its own contract). COLD RECOVERY: rehydrates a host and serializes nothing itself - reconnect reaches the guarded replay path. AUDIT TIMELINE: audit-row metadata mapped per viewer role, not event projections - no event identity or digest exists to unify. EXPORT: the stream half uses ViewerProjectionService; its embedded timeline uses the audit mapper. FINDING, the one genuinely unprojected surface: the LEGACY campaign socket CampaignSnapshot serializes entry.host.buildSnapshotPayload() directly (bindCampaignSyncConnection ~759) - full folded campaign state to any admitted viewer, no projection; the 11.1 scope boundary filters EVENTS, but folded state in a baseline can carry the effects of scope-withheld facts. The scoped-grant snapshot machinery exists precisely for this; routing the legacy baseline through it (or its policy) is the fix direction and belongs with the delivery-epoch delegation (design-campaign-authority-and-sync) beside the sequence deferral already recorded there. 12.2 cross-surface parity digests are blocked on the same fork: audienceDigest speaks IGameEvent+IGameState and cannot digest campaign snapshots or audit rows - a shared projection/digest contract is a DESIGNED slice, not a test-authoring afternoon. WHAT WOULD CLOSE 12.1 as written: either the owner narrows its letter to per-surface-policy-proven (mostly true today, legacy snapshot excepted) or approves the shared-contract slice. Recorded here rather than guessed at.

- [x] 12.2 Add per-viewer projection digests and parity tests across live/replay/snapshot/cold-recovery/timeline/export for GM, Player 1, and Player 2.
  - Receipt (2026-09-02, Wave B1 seam 2 - CLOSES this box): per-viewer projection digests exist on every surface and the parity is ASSERTED per family, not pretended away. viewerProjectionHash (campaignViewerProjection.ts) is the ONE hash law every digest goes through in every vocabulary (campaign events, folded state, action-audit rows). campaignSurfaceParity.test.ts drives ONE committed stream through GM x Player 1 x Player 2 (20 rows): the facts family (live fan-out, resync replay tail) compared on factsDigest; the state family (join baseline, large-gap resync baseline, cold recovery - a fold with no events) compared on stateDigest; a binding row proves the facts family FOLDS to the state family's digest per viewer so a surface cannot satisfy one half while its fold diverges; and a non-constancy row proves three viewers get three different numbers. Timeline/export arm: viewerTimelineDigest consumes the contract at the layer it can (the hash law - an audit row has no scope stamp, sequence, or fold) and exportForViewer derives IViewerHistoryExport.timelineDigest from the timeline it actually RETURNS, so 'the export contains the same authorized facts and no additional private fields' is a number two surfaces match on (4 rows, GM/P1/P2 + three-different). Worker mutants killed (6): live admission bypassed, replay diverging from live (the answer to comparing a surface to itself), baseline falling back to unprojected state (13 red), timeline digest blind to redacted fields, state digest ignoring the fold, export digest over raw audit rows; orchestrator: hash law blind to material (6 red), export digest over an empty timeline (3 red), resync tail to the raw sink re-run (4 red). NOT claimed: the timeline arm shares the hash law, not the campaign visibility SET (no common vocabulary exists yet); exportForViewer.stream reads the combat journal and is not compared to campaign surfaces; timelineDigest has no production caller (no HTTP route added); the timeline/export parity rows are proof rows over existing behaviour, non-vacuity rests on the mutants, not a red-first. IViewerHistoryExport gained one field (no envelope key-allowlist sweep exists; the digest is a hash of rows the viewer already holds).

- [x] 12.3 Add authorization tests proving a player cannot query a private GM audit identifier or infer hidden authority events from delivery gaps.
  - Receipt (2026-08-29, reconciled against existing shipped evidence on main 61d862840, suites re-run 3/20 green - CLOSES this box; both named properties were already proven by the history/audit suites and the 11.4 raw-wire sweeps, and this receipt cites the rows rather than re-authoring them): CANNOT QUERY A PRIVATE GM AUDIT IDENTIFIER - ViewerHistoryService.test rows: unknown principal refused on every entrypoint before any store read; wrong-scope refused with a CONSTANT message and no foreign load; the audit timeline hides other principals and committed revisions from a player while the GM sees both; private records return PAYLOAD-FREE shapes to a player even when the player asks includePrivate, with the DENIAL RECORDED in the access audit; a typed failure carries no partial facts and no private content. ThreeContextPrivacyEvidence adds the indistinguishability law: a player presenting ANOTHER player's cursor matches a fabricated-unknown-id response byte-for-byte, and cross-principal scope escalation is typed with pinned audit and journal counts. CANNOT INFER HIDDEN AUTHORITY EVENTS FROM DELIVERY GAPS - the service pages visible facts with GAPLESS durable sequences across hidden events; player replay envelopes carry delivery-space bounds where span equals items BY CONSTRUCTION (the 11.1 envelope receipts); and the 11.4 sweeps assert every number in every player frame against inferable-gap arithmetic, with the stamped-derived-position mutation dying there. The three-context evidence suite's export rows extend the same guarantees to timeline and export buffers.

- [x] 12.4 Verify with focused replay, recovery, fog, audit, and export suites and `npm.cmd run verify:qc:replay-recovery`.
  - Receipt (2026-09-02 - CLOSES this box; run by the orchestrator in the e2e worktree at the 12.2 head b6d602455, merged as #1505): the exact command `npm run verify:qc:replay-recovery` passed whole - jest half (replay-determinism integration, useGameplayStore.recover, InteractiveSession.recovery) 3 suites / 18 tests; Playwright leg 1 e2e/active-session-recovery.spec.ts 3 passed (2.3m); Playwright leg 2 e2e/replay-player.spec.ts 12 passed (2.3m) incl. the audit timeline page, replay library round-trips, and the timeline export download. Focused fog / audit / export / leak-sweep suites ran green inside #1499 and #1505 (7 suites / 36; multiplayer+campaign+events 321 suites / 4390). What this box verifies is delivered; what it cannot make true is 12.1's remaining clause - the legacy-vs-grant genesis divergence (finding #20) stays pinned as a failing row until the baseline-kind field lands.


## 13. Durable Combat Outcome Receipt Boundary

- [x] 13.1 Replace process-local terminal publication as the authority boundary with a combat outcome outbox written in the terminal combat transaction, implementing `Terminal Combat Outcome Uses Durable Outbox and Inbox`.
  - Receipt (2026-08-29 - CLOSES this box, and the two 4.5-review residuals it structurally owned): the terminal journal-authority command writes a write-once mp_combat_outcome_outbox row (outcomeId, outcomeVersion, canonical outcome) INSIDE the same batch transaction that commits its events - the started-fact pattern applied to the outcome, in both stores behind the IMatchStore contract. Post-verify publication reads the ROW, fires CombatOutcomeReady from it, and marks published_at; recovery drains an unpublished terminal row before accepting new work (the resumePendingPublications pass-0 pattern). The bus remains the in-process notification; the ROW is the authority boundary. RESIDUALS CLOSED: the enabled path's missing post-verify publication (the #1455 suppression removed the scratch's accidental pre-commit publish; the row is its truthful replacement) and the stale constructor-session publisher (publication no longer touches the session the journal path replaced). The legacy event-at-a-time path keeps its engine-primary publish unchanged - it has no terminal transaction to join; the boundary upgrade rides the cutover path like the rest of section 4's machinery, stated in code. Proofs on real SQLite: reopen shows both-or-neither for events+row (out-of-transaction mutant dies there); write-once second insert throws; non-terminal batches write no row; live publication exactly once; crash-before-publication then recovery publishes exactly once from the row (skip-resume mutant dies there); shadow/scratch paths write nothing. The reviewer's NOVEL never-mark-published mutant dies at two rows - the suite watches the marking, not just the firing. Spec SHALLs quoted in the PR. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 8 suites / 79; full multiplayer tree 110 suites / 856 passed. 13 files, +451/-13. The campaign inbox and (outcomeId, outcomeVersion) receipt are 13.2's own work - the consumer lands separately.

- [x] 13.2 Add a campaign inbox and unique `(outcomeId, outcomeVersion)` receipt that commits campaign consequences atomically.
  - Receipt (2026-08-29 - CLOSES this box): campaign_combat_outcome_inbox (migration 21, ladder pins moved 20->21 together) holds one receipt per outcomeId with its version, consuming command identity, stream revisions, and commit positions. Consumption is ATOMIC in the journal's own extension transaction: check-receipt -> append the consequence events -> insert the receipt, one transaction - a receipt-insert failure rolls the consequences back (its own test row). The receipt is the dedupe authority: a second delivery of the same (outcomeId, outcomeVersion) answers 'duplicate' with the original receipt and applies NOTHING; a DIFFERENT version of the same outcomeId answers a typed outcome-version-conflict, never a silent apply. The consumer topology found and wired: co-op battle reconciliation (reconcileCoopBattle -> CampaignMatchHostOutcomeInbox.commitCampaignOutcomeConsequences) routes SQLite-backed server hosts through the durable inbox; CoopBattleConsequences carries outcomeVersion on the wire (optional for compatibility). Worker-begun (codex, capped mid-task before gates - next window Aug 30 1:50 AM), orchestrator-finished. Mutations, reviewer-run: skipping the receipt check dies at the duplicate AND restart-idempotency rows; skipping the receipt INSERT dies at FOUR rows including rollback-on-receipt-failure. Gates: tsc 0; oxlint 0 errors / 77 warnings; multiplayer+campaign+persistence trees 285 suites / 3603 passed. 13 files.

- [x] 13.3 Prove duplicate delivery, crash-before-receipt, restart, and campaign ingestion retries apply one consequence batch.
  - Receipt (2026-08-29 - CLOSES this box, proven by the 13.2 suite's own rows on real SQLite): DUPLICATE DELIVERY commits one consequence batch and one durable receipt (second delivery answers duplicate, applies nothing); CRASH-BEFORE-RECEIPT rolls back the consequences with the failed receipt insert and a redelivery applies exactly one batch; RESTART plus outbox replay stays idempotent against the durable receipt; INGESTION RETRIES are the same redelivery path, receipt-answered. Each proof drives the real journal extension transaction; the mutation runs above are the falsifications.

- [x] 13.4 Enforce `Combat-Only Rewind Stops at Campaign Receipt` with a typed closed-boundary response.
  - CLOSED (2026-09-02, Wave C5 3b-iv-b): Combat-Only Rewind Stops at Campaign Receipt is enforced on the wire. The commit module reads campaign_combat_outcome_inbox BEFORE any mutation (3b-iv-a) and the route answers the typed closed-boundary refusal campaign-receipt-delivered as a 409 body the E6 phrasing already carries; the preview answers the same reason, so a GM is told before and refused after, and the storage census across the refusal is byte-identical. The coordinated post-receipt correction that would follow such a refusal is 17.x, not this box.

  - PROGRESS (2026-09-02, Wave C5 3b-iv-a): the closed-boundary read is the FIRST step of the rewind commit module, before any lease or branch is written, and its refusal is the typed campaign-receipt-delivered arm - the same reason the preview answers - so a delivered campaign receipt can never be followed by a partial rewind; the row pins a byte-identical storage census across the refusal. The route that surfaces it is 3b-iv-b.

  - PROGRESS (2026-09-02, Wave C3 3b-iii, the preview ROUTE): POST src/pages/api/matches/[id]/rewind-preview.ts returns GmCombatRewindPreviewResult VERBATIM (no envelope; kind present = domain union, absent = transport error {error, reason?}): 200 preview; 403 gm-role-required / actor-mismatch / state-not-owned (a refused brand - HumanActionAuthorizationError / AuthorizedViewerError - maps to 403 state-not-owned in the union rather than a 500: a non-member is not a fault, and the gate's constant id-free message is kept so a refusal cannot probe which matches exist); 404 no-authoritative-history (NEW module arm via readEffectiveHead, campaignLaunchHead precedent, so a real match with no stream-head row answers typed instead of throwing - the #48 consequence 3b-ii carried); 409 for the rest. Order: initializeApiDatabase -> query id -> POST only -> body guard (replacementEvents REFUSED at 400, the commands.ts precedent - the same reason name also exists as the module's 409 domain arm for direct callers, a two-shapes-one-name wart recorded in the header) -> authenticateRequest -> authorizeHumanAction({kind:'branch', streamType:'match'}) -> isAuthorizedViewer brand recheck -> IMatchMeta -> GM = meta.hostPlayerId (finding #55 stands: 'GM' on this route means the match's current host; teaching MatchSeatMembershipSource to emit 'gm' is its own seam because projectEventForViewerClass keys on that role) -> previewGmCombatRewind with matchStoreBranchSegmentReader + combatViewerProbe(gmPlayerId = hostPlayerId). #81 in the route: priorHeadRevision is read readEffectiveHead-first and then AND branch_id = <effective> (campaignLaunchHead precedent) - qualified from birth; mutant P6 (unqualified read) made #81 EMPIRICAL: with a candidate-1 head row at revision 2 present the unqualified read returns it over root at 4. NEW refusal member fog-preview-unsupported (409, in the union, emitted by the ROUTE only): IMatchConfig.fogOfWar exists, filterEventForPlayer dereferences engine state the route does not hold, and a fabricated state would hand the GM a blast radius computed against fog that is not the real fog; the only cast in the route (FOG_DISABLED_STATE) is guarded by that refusal and its comment says removing the refusal makes the cast a lie. Worker mutants 6/6 killed (actor from body, GM check skipped, replacementEvents stripped, 404 collapsed to 409, readEffectiveHead -> requireEffectiveHead, unqualified head read); orchestrator mutants in the PR body. NOT CLAIMED: truncation only; 13.4 is receipt-presence; fog off and now explicitly refused; the route creates, seals and activates nothing (3b-iv); route rows seed a stream-head row standing in for what the combat cutover will write (#48/#53), the 404 row exercises its absence.

  - PROGRESS (2026-09-02, Wave C3 3b-ii, the preview's two real deps): matchStoreBranchSegmentReader (IBranchSegmentReader over mp_match_events, per #48 the journal holds no match events) and combatViewerProbe (IViewerProjectionProbe composed from the shadow's audienceDigest; audiencesFor EXPORTED from journalAuthorityShadow so the probe and the shadow name the same 'gm' / 'player:<id>' audiences rather than two vocabularies). Pinning contract, every clause measured: mp_match_events sequences start at 0 (DurableMatchStore COALESCE(MAX(sequence)+1, 0)); branch revision 0 means nothing yet; revision = sequence + 1 (the same off-by-one JournalCampaignEventStore documents); segments are (fromRevision, throughRevision] (EventHistoryBranchResolver, verifySegment); the first event chains from null. Digest chain computed from the START of the stream then sliced, never from the window - a truncation is a prefix of history, not a rewrite. Unknown viewer REFUSED typed (neither default is safe: 'player' hands the most permissive projection to whoever was not recognised, 'gm' leaks authority-only fields). Worker mutants 6/6 killed (constant digest, player-as-gm, unknown-viewer default, non-reproducible probe, window ignored, revision = sequence); a malformed first M1 (constant prefixed onto a varying digest) passed 10/10 and was caught and rewritten - reported because a mutant that 'survives' by construction error is a false green. Orchestrator mutants in the PR body. FINDING (#48 consequence, carried for 3b-iii): event_journal_stream_heads has exactly one writer (the journal append), so a real match has no stream-head row, backfillGenesisBranches never gives it a root branch, and requireEffectiveHead would THROW for a production match before the reader is reached - 3b-iii answers a typed no-authoritative-history via readEffectiveHead (campaignLaunchHead precedent) rather than throwing; 3a's readRebuildRefusal is safe but INERT for combat today (readLiveLease first, and no lease can exist without an effective head) - not live combat protection until cutover. NOT CLAIMED: fog is OFF - viewer CLASSES (gm vs player) separate, not the two players from each other (state threaded so fog is a config flip, not a signature change); revision = sequence + 1 and the (from, through] window hold only until the combat journal cutover, whose reader swap must re-prove both; the route is 3b-iii; create/verify/seal/activate untouched.

  - PROGRESS (2026-09-02, Wave C3 seam 3b-i): enforced on the PREVIEW surface - a combat-only rewind is refused typed 'campaign-receipt-delivered' when the match's outcome (mp_combat_outcome_outbox, match db) appears in campaign_combat_outcome_inbox (mekstation.db). Receipt PRESENCE is the precise rule, not a compromise: the outcome is derived from the match's final state, so once a campaign has taken delivery no revision of that match survives a combat-only truncation. The inbox carries no match id and its revisions are campaign revisions, so the outbox is the only bridge and the consult is a two-database read, not a join. Commit-side enforcement lands with 3b-iv; box stays open until then.

- [x] 13.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.reconcile.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts` and focused `InteractiveSession.outcome` tests.
  - Receipt (2026-08-29, run on main eb500af3a - CLOSES this box on its literal command plus the focused outcome suite): reconcile + CampaignMatchHost + InteractiveSession.outcomeBus, 3 suites / 16 passed / 0 failed - the reconcile path now routing through the durable inbox, the host carrying the outcome-consequence commitment, and the bus suite pinning the engine-side publish guard beside the suppression law. HONESTY NOTE: sibling 13.4 (Combat-Only Rewind Stops at Campaign Receipt) remains open - its enforcement point is the rewind command surface owned by add-authoritative-history-branches (wave map row 7), and the typed closed-boundary response lands with it; the receipt table it will consult exists as of 13.2.


## 14. Immutable Combat Correction and Rewind Kernel

- SPLIT RECEIPT (2026-09-04, owner ruling on the 2026-09-03 inventory-and-sync council; "move it to a successor, separate and document the specs separately"): GM combat rewind leaves this change. Successor `adopt-combat-journal-cutover-and-gm-rewind` created (proposal naming its relationship to the archived `adopt-combat-event-journal-authority` and to `add-authoritative-history-branches`; design carrying the six cutover seams S1-S6, per-viewer fog rebuild, activation; 17 task boxes; delta spec); `openspec validate` passes for both changes, successor 4/4 artifacts. Delta split of specs/gm-combat-interventions/spec.md: MOVED verbatim `Combat Intervention Has Distinct Preview and Commit Phases`, `Superseded Combat Commands Are Rejected`, `Combat Rewind Preserves Viewer-Specific Hidden State`; SPLIT `Combat Rewind Is GM-Only and Append-Only` (stays: GM-only commit + player request, scenarios Player requests / Player commits; moves: the replacement-branch clause + GM commits rewind) and `Combat Rebuild Gates Commands and Activation` (stays: the PROJECTION_REBUILDING clause + Command during rebuild rejects; moves: the activation clause + Valid rebuild activates / Invalid rebuild remains blocked); KEPT `Applied Campaign Outcome Requires Coordinated Correction`. DEVIATION from the council text, documented in the delta's split notice: requirement 3 was to move whole, but 14.3 closed 2026-09-02 with the refusal live at both admission sites, so only its activation half moves. The successor carries the two split requirements as MODIFIED with the full final text so its archive replaces the reduced headers. Verified mechanically (verify-split.py against origin/main): three whole moves verbatim, one kept verbatim, two splits partitioned with the umbrella SHALL text a prefix of the original and every scenario unaltered (SPLIT-VERIFIED). proposal.md capability line amended with the withdrawal; BLOCKED lines under 14.1, 14.2, 14.4, 14.5 naming `journal-cutover` and the successor boxes that close each; NOTE under 25.4 (34 of 80 reachable at the split). Rows: E2E-40, 41, 42, 43, 44, 76 now EXIST in e2e/gm-two-player-rewind.pack.spec.ts (+ helpers/gmTwoPlayerRewind.ts), group `rewind-pack` (GROUP_CATALOG, SPEC_BY_GROUP, qc pins; bare `rewind` free), each a real body to the letter under a strict test.fail mark tagged @until-journal-cutover; ORCHESTRATOR FIX: Playwright APIResponse type in the helper and a closure-narrowed refusal in E2E-41. LIVE (l89, production build): 6 passed (42.8 s) as six expected failures; the server log shows six matches, six rewind-preview POSTs, six 404 no-authoritative-history (finding #48) - every row reaches the letter's door. Live mutant NOVEL N1 (E2E-41 mark stripped): E2E-41 red on the letter (preview answered refused / no-authoritative-history where the row expects 200), the other five rows stayed expected failures (1 failed, 5 passed, 33.1 s) - KILLED; marks restored. Gates: tsc 0; oxlint 77-pin; oxfmt clean; qc pin suite 9/9. NOT claimed: no SHALL discharged; no product code; the rows go red on their own the day the cutover lands, and the successor's box 3.2 removes the marks one row per seam.

- [ ] 14.1 Route finalized combat corrections through server-authored authoritative commands while keeping preview non-mutating and GM-private, implementing `Combat Intervention Has Distinct Preview and Commit Phases`.
  - BLOCKED on `journal-cutover` (2026-09-04, owner ruling): the requirement `Combat Intervention Has Distinct Preview and Commit Phases` moved verbatim to successor `adopt-combat-journal-cutover-and-gm-rewind` (its box 2.1 closes this one by receipt). The preview route, commit route and commit module exist, but on a live match both routes answer 404 no-authoritative-history because the match stream is never journaled in production (finding #48); nothing here can discharge the letter until the writer ships there. Not closed, not skipped.
  - NOTE (2026-09-02, Wave E7b): the E6 dialog's confirm now reaches the commit route (19.3 closed). 14.1 stays open on the letter's deterministic replacement data and authorized player projections (replacement events are refused by both routes; fog is refused) - gated on the journal cutover and the fog product decision.

  - PROGRESS (2026-09-02, Wave C5 3b-iv-b, the commit ROUTE): POST /api/matches/:id/rewind-commit mirrors the preview route step for step - initializeApiDatabase, the id guard, 405 with Allow, the body guard (the same targetRevision + four expected* fields; replacementEvents refused 400 { error, reason }, transport, exactly as the preview does), authenticateRequest -> 401, authorizeHumanAction on the branch stream + the brand recheck, GM = meta.hostPlayerId, actor = the branded principal - then the commit module with the same deps the preview wires (branch-qualified head reads, the match-store reader, the combat probe aimed at the host, the outbox outcome id) and the CLOCK injected by the route (the one place allowed to touch it; the history module cannot). No envelope: the body is GmCombatRewindCommitResult verbatim - 200 committed; 403 gm-role-required / actor-mismatch / state-not-owned; 404 no-authoritative-history; 409 every other refusal including the three commit-only members (candidate-verification-failed, generation-exhausted, correction-lease-held); { error } for 400/401/405/500. Rows on the preview route's harness: the host commits (200, generation advanced); a seated non-host is 403 and NOTHING is written (storage census); an unauthenticated caller is 401 before any body complaint; replacementEvents is 400 and nothing derived; no authoritative history is 404; a delivered campaign receipt is 409 campaign-receipt-delivered with nothing written; the actor is the token, never the body; GET is 405. Orchestrator mutants in the PR body. NOT CLAIMED: the E6 dialog's onConfirmRewind is not wired to this route yet (a sibling of E7 does that); no coordinated correction; no fog; 'GM' = the match's current host (#55).

  - PROGRESS (2026-09-02, Wave C5 3b-iv-a, the COMMIT module): GmCombatRewindCommit.ts performs an approved preview's correction in an order no step may leave: (1) actor = the match's GM (host, #55) re-checked as the preview does; (2) the 13.4 closed boundary read of campaign_combat_outcome_inbox BEFORE ANY MUTATION - a delivered receipt refuses campaign-receipt-delivered with nothing written, no lease, no branch; (3) the correction lease acquired through acquireCorrectionLease binding the four expected* values - this OPENS the rebuild window the shipped 14.3 gate already watches (refuseDuringHistoryRebuild via readMatchStreamRebuild), so no new gate is added; (4) the candidate cut with createCorrectionCandidateBranch at baseRevision = targetRevision (C1c-i); (5) verifyCandidatePath over matchStoreBranchSegmentReader - failure refuses candidate-verification-failed and activates nothing; (6) deriveAndSealCandidateImpact with combatViewerProbe; (7) activateCandidateBranch, one transaction that re-proves the lease, re-checks the head and refuses an unsealed manifest; (8) the lease released in a finally so a refusal at any step reopens the stream. Result union GmCombatRewindCommitResult = committed (matchId, activatedBranchId, priorBranchId, effectiveGeneration, invalidations from IActivationResult) | refused (reason, detail), where reason REUSES the preview's refusal vocabulary verbatim plus three commit-only members: candidate-verification-failed, generation-exhausted, correction-lease-held. Rows on a real DurableMatchStore: R1 commits and activates (generation = prior + 1, effective head names the candidate); R2 refuses past a delivered receipt with a byte-identical storage census before/after; R3 live intents are refused PROJECTION_REBUILDING while the window is open; R4 a failed verification activates nothing and reopens the stream; R5 the lease is released on every refusal path. Orchestrator mutants in the PR body. NOT CLAIMED: no route yet (3b-iv-b); no coordinated post-receipt correction (17.x); no replacement events (truncation only); no fog; no new 14.3 gate; the E6 dialog's onConfirmRewind stays unwired until the route lands.

  - PROGRESS (2026-09-02, Wave C3 seam 3b-i, GmCombatRewindPreview.ts + deriveImpactBetween extracted from EventHistoryImpactDerivation): the PREVIEW phase is non-mutating and GM-private - previewGmCombatRewind materialises the prior branch twice off the same branch (full, and read only to the target: a truncation needing no candidate row), runs the pure derivation the sealing caller also runs (row: preview reports exactly what deriveAndSealCandidateImpact reports for the same truncation, non-circular - it mints the real candidate and seals), writes nothing (storage census byte-identical); refusal order authorization (evaluateGmInterventionAuthority reused with kind 'undo', actor from the branded context, probe never invoked before it passes - probeCalls asserted []) -> replacement-events-unsupported (refused, never trimmed) -> PROJECTION_REBUILDING via the same durable reader as the admission gate -> campaign-receipt-delivered -> expected-head verdict -> rewind-target-above-head / -below-branch-base -> derive. Mutants killed: authorization ignored, lease consult skipped, receipt consult skipped, derived from the untruncated prior, stale checkpoints read at the head (SURVIVED first - no fixture had a checkpoint between target and head; row added). STILL OPEN: the server-authored authoritative COMMIT (create/verify/seal/activate = 3b-iv) and the route (3b-iii), which waits on an honest viewer half - FINDING #48: no production path writes match events to the journal (ServerMatchHostPublication appends IGameEvent to mp_match_events; combat is journal-authority-CAPABLE - baseline, branches, leases, admission key on ('match', matchId) - while its events live in the match store), so 3b-ii adopts the combat IViewerProjectionProbe over a match-store-backed IBranchSegmentReader (audienceDigest consumed, never edited; fog off -> viewer classes only, per D1's finding).

- [ ] 14.2 Add immutable combat branch, supersession, trusted-checkpoint, and stale-branch command semantics without changing existing combat reducers.
  - BLOCKED on `journal-cutover` (2026-09-04, owner ruling): the requirement `Superseded Combat Commands Are Rejected` moved verbatim to successor `adopt-combat-journal-cutover-and-gm-rewind` (its boxes 1.2 and 2.3 close this one by receipt). The branch admission consult (14.2-a) is live but both STALE_BRANCH arms are structurally dead while readEffectiveHead answers null (finding #48). Row E2E-41 is a strict expected failure tagged @until-journal-cutover.
  - PROGRESS (2026-09-02, Wave C7 14.2-a): the four illegal command shapes are refused on the LIVE intent path through the shipped branch port, with no combat reducer edits. A consult (ServerMatchHostBranchAdmission) runs in handleIntent after the rebuild refusal and before authorization, ONLY when the store exposes the branch port (hasHistoryBranchStore); without the port it is a no-op and every existing test is byte-identical (pinned). CI found two ripples the seam's own rows could not: the durable store binds its branch port lazily on the campaign database, so a host whose SQLiteService was never initialized threw 'Database not initialized' at the first consult - the port now exposes a readiness probe, the guard isHistoryBranchStoreReady consults it, the consult is inert until the database is up, and a row pins that inert case; and the RewindRequest kind is one more out-of-scope row in the combat catalogs (148->149, actions 29->30, leaf 145->146; the scope matrix carries its row; its source ref cites the dispatch exclusion like its siblings). (1) a player intent whose stream's effective head is not the live-path identity is refused STALE_BRANCH with conflictHead and recoveryAction resync-to-active-head (the shipped expected-head vocabulary); (2) an intent arriving while the effective branch is superseded is refused the same way; (3) any non-request payload that carries targetRevision (a rewind cut) from a non-host is refused GM_ONLY (reason gm-role-required) while the host's passes admission and is handled as today - the commit route remains the only door; (4) a player MAY submit a non-mutating RewindRequest (targetRevision + reason): it is answered accepted-for-gm-review and derives no event (journal and match-store census). LAW-40 tripwires bumped: ErrorCodeSchema 14->16 (STALE_BRANCH, GM_ONLY), wire intent kinds 21->22 (RewindRequest, excluded from engine dispatch), a two-member admission phrasing Record. Rows: player intent on a non-effective branch refused STALE_BRANCH with head + resync; intent on a superseded effective branch refused; a targetRevision-bearing intent from a player refused GM_ONLY while the host's passes; the player's RewindRequest is accepted-for-gm-review and derives no event; with an in-memory store that lacks the branch port every refusal is inert. Orchestrator mutants in the PR body. NOT CLAIMED, why 14.2 stays open: no expected-head WIRE field (#38 is a separate product decision - live intents still name no branch, so (1) protects the stream, not a client's stale belief); the rewind cut is still targetRevision on the match store, not 'from a trusted checkpoint'; the RewindRequest is answered but NOT recorded as a GM-review private record from the host (the writer is not wired there; the route-side preview record is the GM's review trail).

- [x] 14.3 Reject commands during rebuild with retryable `PROJECTION_REBUILDING`; do not queue them invisibly.
  - CLOSED (2026-09-02, Wave C3 seam 3a, leaf 2.2 adoption): PROJECTION_REBUILDING is now a real server refusal at both live admission sites (combat ServerMatchHostIntent after the rollback gate; campaign executeCampaignCommand third blocked arm -> 409), retryable (a lapsed lease admits again), never queued. ErrorCodeSchema gains the member; no exhaustive pin exists and LIVE_WIRE_CODES already carried it (the earlier wave's landing strip); campaignLifecycleState comments corrected; both reserved-posture guards survive unchanged because they assert unreachability from live client/sync signals, not from a server refusal. journalAuthorityAdmission is MATCH-CREATION admission, not this gate. Client-side gating on the code is 19.2 (Wave E2 seam 3).

- [ ] 14.4 Rebuild combat state, RNG provenance, fog, sealed choices, and viewer projections; activate the replacement branch atomically only after verification.
  - BLOCKED on `journal-cutover` (2026-09-04, owner ruling): the replacement-branch clause of `Combat Rewind Is GM-Only and Append-Only`, the activation clause of `Combat Rebuild Gates Commands and Activation`, and the whole of `Combat Rewind Preserves Viewer-Specific Hidden State` moved verbatim to successor `adopt-combat-journal-cutover-and-gm-rewind` (its boxes 2.2, 2.4, 2.5 close this one by receipt). The live-host rebuild (14.4-a) and logical truncation (14.4-b) stay as shipped infrastructure; there is no journaled branch to activate until the writer ships (finding #48) and fog is refused at preview. Rows E2E-40, E2E-42, E2E-43, E2E-44 are strict expected failures tagged @until-journal-cutover.
  - PROGRESS (2026-09-02, Wave C9 14.4-b): the sequence-collision gap named by 14.4-a is CLOSED - a rewound match can be extended. The match file gains logical truncation as a MOVE: supersedeFrom(matchId, fromSequence, at) moves the tail of mp_match_events / mp_match_outbox (sequence >= cut) and mp_command_receipts (last_revision >= cut) into sibling tables mp_*_superseded (same columns + superseded_at, rowid identity because a second rewind can reuse (match_id, sequence)) in ONE transaction; the live tables keep their original composite PRIMARY KEYs, so every live read and write is unchanged by construction and the legacy importer's FOREIGN KEY, prune and the journal-authority admission keep the parent key they attach to (a first cut rebuilt the live tables around a partial unique index and CI showed the importer, prune and admission suites broken by the dropped parent key - LAW 67). The migration only creates the siblings (idempotent, PRAGMA user_version=1). supersedeFrom is called after activation from the live rebuild (ServerMatchHostRewindRebuild, from the effective revision, stamped with the host clock) and from the boot fold (MatchRecovery), never from the commit module; the in-memory store drops the tail with the same observables. Superseded bytes stay readable through readSupersededMatchEvents for the 15.2 checkpoint law (unchanged: an old-head checkpoint is already unattested by digest); prune deletes sibling rows before the match row. Rows: appendCommandBatch at the cut commits after supersedeFrom; appendEvent at the cut sequence does not throw; getEvents length equals the prefix plus the new tail; getLastCommandReceipt answers the prefix's last receipt; a pending outbox row of the superseded tail is never drained; a second rewind after new events moves the new tail again; the migration is idempotent; the first command after a committed rewind PERSISTS and is delivered with a deterministic wait (the 14.4-a non-claim, now a row); the importer, prune and admission suites stay green. CONTRADICTION FOUND AND RESOLVED by the first-command row: the 14.2 admission refused every live intent whose effective head was not the root/baseline identity, and after an activation the effective head IS the activated candidate, so a rewound match could never be extended (deterministic; CI showed it, and locally the row was red once the fixture initialized the campaign database). The live path is now the branch the host SERVES: ServerMatchHost keeps servedBranchId (null = root/baseline), set by the live rebuild only AFTER replaceSession returns (a failed rebuild claims nothing) and adopted by the boot fold from the folded branch; the admission admits a head that is a live-path id OR the served branch, still refuses a superseded effective branch, and still refuses a host serving root while the store activated a candidate (the 14.2 unrebuilt row, unchanged). Row added: a host rebuilt onto the activated branch admits live intents on it; the first-command row flips green with no edit. Not claimed: a second rewind re-pointing the served branch (one candidate per fixture; the same setter runs). Orchestrator mutants in the PR body. NOT CLAIMED: no branch_id on the match event table (this is truncation, not the journal cutover - the journal still holds no match events, #48); E2E-05/06 restart atomicity unchanged; fog / sealed choices / offline-rejoin hidden facts stay 14.5's and remain gated on fog, which is why 14.4 stays open.

  - PROGRESS (2026-09-02, Wave C8 14.4-a): after a COMMITTED rewind the live host is rebuilt from the activated branch, with engine ownership kept out of the commit module. The commit route reaches the live host through the registry (get only, never getOrCreate - no host is fabricated); ServerMatchHostRewindRebuild holds the 14.3 correction lease, walks the activated path through the branch-aware reader (never the untruncated store log), folds it with foldMatchSession, builds the InteractiveSession through fromSessionAsync with the host's own journal seeds (never Math.random), replaceSession, resets the intent window and broadcast cursor, DISCARDS every pre-rewind viewer delivery and marks the seats for resync, sets a replay ceiling at the activated head so the single ReplayChunk gather never surfaces a superseded event, and releases the lease LAST. With nobody connected the route still answers committed and the next boot's recovery folds the activated branch (MatchRecovery). Rows: the live session answers the revision-N phase, not the pre-rewind head; the next die roll equals a fresh host seeded with the same seeds; a viewer past N is resynced from the new head and its cursor discarded (bounded by event identity - the wire strips the authority sequence); the rebuild lease is released only after replaceSession; no live host: commit still answers committed and boot folds the activated branch. The first harness seeded the branch from unrelated probe rows and the commit was refused candidate-verification-failed - the product was right, the seed was rebuilt from the R1 recipe. Orchestrator mutants in the PR body. NOT CLAIMED, and why 14.4 stays open: (1) the durable store is NOT cut over - activation leaves the superseded mp_match_events rows in place, so the store's next free sequence is still old-head+1 and the first PERSIST after a rewind would hit the sequence collision guard (the rebuilt engine numbers from the cut); until the store appends onto the activated branch (the journal cutover, 5.7) a rewound match can be read, replayed and resynced but not extended - fail-closed, and named here rather than hidden; (2) fog stays refused at preview; sealed choices are not rebuilt; offline-rejoin hidden facts are 14.5's letter and stay gated on fog; (3) 'replay, state validation, fog, hidden-state and viewer-projection checks pass' is proven for replay and viewer projection only.

- [ ] 14.5 Verify with GM intervention suites, deterministic replay tests, fog integration tests, and `npm.cmd run verify:qc:gm:campaign-ledger`.
  - BLOCKED on `journal-cutover` (2026-09-04, owner ruling): verification of the moved requirements is successor `adopt-combat-journal-cutover-and-gm-rewind` box 3.1, which closes this one by receipt. The GM-only / player-request clauses and the PROJECTION_REBUILDING refusal that stay in this change are verified by their own receipts (14.3 closed 2026-09-02; #1628).

## 15. Cache-Only Checkpoints and Branch Recovery

- [x] 15.1 Implement immutable checkpoints keyed by branch, authority head, reducer version, and digest, satisfying `Checkpoints and Compaction Are Cache-Only`.
  - Receipt (2026-09-02, Wave D1 seam 1 - CLOSES this box): the checkpoint CONTRACT (ReplayCheckpointCompatibility, ReplayEquivalenceHarness.recoverState with the digestsVerified law, SQLiteReplayCheckpointRepository, migration 10) had NO producer - the harness header and #1518's candidate verification both say so. src/lib/events/checkpoints/BranchCheckpointCache.ts is the producer: a checkpoint's id is DERIVED from the key it binds (streamType+streamId encoded the way ReplayQuarantineRegistry scopes keys, branch, authority head revision, projectorId + projectorVersion, schema pipeline fingerprint), so the same key is the same primary key and a second write is a collision the writer answers for - the identical claim is 'already-recorded' (proven through selectRecoveryBase's digest law, not by trusting the row), a different claim is the repository's typed duplicate-checkpoint refusal, nothing is ever overwritten; a bumped projector version writes a NEW row beside the old (the superseded one stays for the manifest). The reader hands out an ICandidateCheckpointOffer - no state field exists on the return type; expected.sourceTailDigest is the LIVE chain digest at that revision (never the row's own claim - that would forge digestsVerified), expected.stateDigest comes from the row only because hydrate already proved the stored bytes hash to it. Only recoverState turns an offer into state, and because the accelerated path reads only the TAIL, the kernel's full-replay fallback would fold a partial history - a base that verified and then failed is a typed accelerated-recovery-rejected, never a state (the load-bearing rule of the seam). CACHE-ONLY proven: with the table emptied, recovery takes the reference path and returns the same state and the same canonical digest, differing only in appliedRevisions. Earlier trusted base: the reader walks candidates newest-first and passes over a row live history cannot attest. Red-first: module-not-found. Worker mutants killed (5): row overwritten in place, key without the projector version, state handed out bypassing recoverState, deleting checkpoints changing an observable, reader ignoring the branch; one documented survivor (discovery dropping the projector version - selectRecoveryBase re-filters; a cost, not a hole; stated in code). NOT claimed: a checkpoint on a non-root branch (migration 10 pins branch_id='root' mirroring event_journal_events; lifting one without the other would cache branches whose events cannot exist - both belong to the branches leaf's activation, its 3c cut in flight; the key/writer/reader are branch-generic and the branch keying is proven behaviourally); no production call site records or recovers yet (session-recovery wiring is 15.2/15.3); viewer digests (15.2) and quarantine (15.4) are later seams. No migration (head 25 unchanged). Gates: tsc 0; oxlint 77-pin; events+persistence 55 suites / 941; openspec validate clean.

- [x] 15.2 Prove compatible checkpoint-plus-tail equals full replay for authoritative state and all three viewer digests.
  - CLOSED (2026-09-02, Wave D3 15.2 match half; the campaign half closed earlier the same day): the match door becomes the FIRST production caller of checkpointRecoveryPort (finding #49 closed for combat). MatchSessionProjector.ts is the ReplayProjector for IGameSession (id match.authoritative, version 1; every GameEventType applies through foldMatchSession, which returns the base on an empty tail so hydrate is never called with [], never calls appendEvent (its updatedAt is not byte-stable), and normalizes the hydrated session through a JSON round-trip because the session carries undefined-valued optionals that the JCS canonicalizer refuses outright - the cache can only ever hand back the JSON shape, so the live fold and the cached fold are the same bytes; completeness over GameEventType only, not the baseline registry that includes campaign types; an identity schema registry over the whole IGameEvent envelope). MatchCheckpointHistory.ts reads the match store as an IBranchHistoryReader at revision = sequence + 1, reusing the exported matchEventChainDigest so there is one chain. MatchRecovery.recoverActiveMatches uses the checkpoint port when SQLite is initialized (cache on mekstation.db, head = revisionForMatchSequence(last.sequence), parse re-hydrates cached events) and records a checkpoint at the live head after a recovered outcome; InMemory keeps referenceRecoveryPort byte-identical. The read is now EXCLUSIVE (getEvents(id, fromExclusive), revisionOf = sequence + 1): 15.3's documented survivor A5-match (inclusive +1) is observable and dies - a checkpoint at revision R must be followed by a tail starting at R + 1, which evaluateReplayTailContinuity demands. Rows on real SQLite with a real GameCreated log (the fixture is JSON round-tripped because the store only ever hands the reader parsed events): R1 checkpoint-plus-tail path taken, stateDigest byte-equals full replay, appliedRevisions = head - base; R2 combatViewerProbe digests for gm / player:p1 / player:p2 equal across both paths with fog ON (fog off makes p1 = p2 and proves nothing); R3 a version-1 checkpoint is ignored at pipeline version 2; R4 a checkpoint recorded under another branch is not offered on root. DOOR rows (matchRecoveryCheckpointDoor.test.ts), added because two orchestrator mutants on MatchRecovery SURVIVED the equivalence rows - those rows drive the cache with their own reader and never reach the door: D1 recovers through the checkpoint door and reads only the tail (a spy pins getEvents called with the checkpoint's revision as the exclusive start and NOT a second time from 0 - the inclusive +1 skips the first tail event, continuity rejects the tail, and the door full-replays, which LOOKS correct and is why equivalence could not see it; revisionOf = sequence fails the same continuity check); D2 records a fresh checkpoint at the live head after recovery; D3 falls back to the reference port when SQLite is not initialized. Orchestrator mutants in the PR body. NOT CLAIMED: no journal cutover; no per-append checkpoint producer; no viewer ReplayProjectors (R2 probes the authoritative session); recoverActiveMatches is not a new boot door; InMemory has no checkpoint durability; both authority flags stay off.

  - PROGRESS (2026-09-02, Wave D1 seam 2b, viewerDigestEquivalence.test.ts): four distinct projector identities (campaign.authoritative + viewer.gm/p1/p2) each write their own checkpoint at the same head on the same branch and each recovers through BranchCheckpointCache.recover to a stateDigest BYTE-EQUAL to its own full replay, with path and appliedRevisions asserted so a silent full replay cannot pass; non-vacuity controls: the three viewer digests differ pairwise and the injected combat audienceDigest probe (called exactly as journalAuthorityShadow calls it, that file untouched) separates GM from players. RESIDUE keeping the box open: the combat probe separates viewer CLASSES, not the two players (fog off -> filterEventForPlayer is a passthrough; per-player separation is proven on the campaign pipelines, which discriminate on scope) and the match door has no accelerated read at all (no ReplayProjector for IGameSession), so checkpoint-plus-tail equals full replay is proven for the campaign authority only. Closes when a match-side checkpoint producer exists (same fork as the two-doors ruling).

- [x] 15.3 Prove reducer-version or digest mismatch rebuilds from an earlier base or blocks truthfully without publishing partial recovery.
  - CLOSED (2026-09-02, Wave D1 seam 2b): both doors adopted. Campaign (seam 2a): reducer-version or digest mismatch = no offer -> earlier trusted base or full replay; an admitted base with a tail hole -> blocked, no fallback. Match (this seam, MatchRecovery.recoverActiveMatches through referenceRecoveryPort<IGameEvent, IGameSession>, fold = hydrateGameSessionFromEvents, read fromExclusive + 1 byte-identical to today): a match whose history cannot be rebuilt returns typed `blocked` {matchId, reason, evidence} with NO host registered and no partial session published; `failed` keeps its exact old shape; one blocked match does not stop the sweep. emptyHistory is 'corrupt' on the match door and 'empty-state' on the campaign door - per call site, never defaulted. Red-first: empty log blocked by name with nothing registered, un-foldable log blocked as replay-failed, blocked match does not stop the sweep (3 red on shipped code). Mutants killed: A1-match (paper over the block and rebuild anyway), A4 (empty active log called fresh), M2/M5/N1 re-runs on the match tree, orchestrator A4 re-run, N1 evidence dropped, N2 blocked match stops the sweep. DOCUMENTED SURVIVOR: A5-match inclusive read (+ 1 dropped) is unobservable because the match door has no accelerated read yet - killable the day a match-side checkpoint producer exists. NOT CLAIMED: no healthy-match control in the blocked suite (that pairing is 15.4's letter and lands there).

  - PROGRESS (2026-09-02, Wave D1 seam 2a, AuthorityRecoveryPort.ts + campaignEventLog.reconstructState): the campaign door is adopted. AuthorityRecoveryVerdict = recovered (state + path checkpoint-plus-tail | full-replay + appliedRevisions) | blocked (reason + evidence, NO state field on the variant - partial recovery is unpublishable by type). checkpointRecoveryPort asks BranchCheckpointCache.offer (digest law, live chain-digest binding, earlier-base fallback all hold where written) and checks the tail with the shipped evaluateReplayTailContinuity; a stale/unattested row is no offer -> the SAME reference function runs (full replay), so reducer-version or digest mismatch rebuilds from an earlier base or replays; exactly one thing blocks: an ADMITTED base handed a tail with a hole (partial-history) - no fallback, because full replay would hide a journal missing events. emptyHistory ('empty-state' | 'corrupt') is stated per call site, never defaulted. reconstructState routes through the port (default reference = byte-identical), blocked throws AuthorityRecoveryBlockedError composing into CampaignProjectionDivergenceError's refusal (both live callers assign straight to host state). Red-first at the adoption point: accelerated read (reads [4] not [0]) and refuse-partial were red on the shipped log. Mutants killed: M2/M5 re-runs on the adopted tree, N1 self-attesting row (live digest never consulted), A1 block papered over with full replay, A2 inclusive tail read, A3 empty campaign log called corrupt (SURVIVED first - row added), orchestrator N1 prefix applied twice (tail read from history start), N2 continuity off by one. TWO DOORS by ruling: the campaign fold has no ReplayProjector binding, so it composes offer + continuity rather than recover(); they collapse when the 5.7 cutover registers a projector whose fold is applyCampaignEvent (acceptance text in the module header at seam 2b). STILL OPEN: MatchRecovery adoption (~118, `blocked` replacing untyped failed.push for the corruption classes) - seam 2b.

- [x] 15.4 Add per-session corruption quarantine for broken sequence, lineage, receipt, or digest while a healthy control session remains available.
  - DEFECT FOUND AND FIXED (2026-09-03): the quarantine had no production binder. recoverActiveMatches took the ReplayQuarantineRegistry as an optional argument and its sole production caller, MatchHostRegistry.recoverActiveMatches, never passed one, so quarantineAuthorityCorruption never ran outside tests; the registry also discarded result.blocked, and getOrCreate fell through to ServerMatchHost.create for any match id without a host, including one refused a moment earlier at boot; the socket binder called every missing host UNKNOWN_MATCH. Fix: MatchHostRegistry owns one ReplayQuarantineRegistry for its life (instance-owned so injected-store tests never share a ledger, replaced on _reset so an empty new registry stays absent-signal identical), passes it to recovery, retains the blocked list from boot, and getOrCreate returns null for a quarantined match id BEFORE it builds a bootstrap; reads for section 19: isQuarantined, quarantinedMatchIds, blockedMatches; bindMultiplayerSocketConnection answers a quarantined match with MATCH_QUARANTINED / match-quarantined (close 1008) and every other missing host with the existing UNKNOWN_MATCH refusal (a registry without the read keeps the old path). Worker rows (RED against shipped code, predicted and confirmed): a refused match is recorded quarantined, getOrCreate answers null and create is never called; the blocked list is visible after boot; the socket distinguishes MATCH_QUARANTINED from UNKNOWN_MATCH; GREEN controls: a healthy match of the same boot is still served, a never-recovered id still mints, an empty store with an empty ledger still mints. Orchestrator mutants on a proven-green baseline (ladder first: 141 suites / 1096 tests), all KILLED: M1 binder dropped; M2 getOrCreate skips the consult; M3 blocked list discarded (2 red); NOVEL N1 socket refusal ignores the ledger. Gates: tsc 0; oxlint 77-pin; multiplayer server + protocol + security boundary 141/141. Worker note: first launch of this charter sat 46 minutes at 5.5 s of CPU with no edits and was killed and relaunched (delivered in 10 minutes). NOT claimed: no UI surfaces the quarantine; the ledger is process-scoped, a match quarantined in one process is not quarantined in another
  - ERRATA (2026-09-03, same audit): the per-session quarantine has NO PRODUCTION BINDER. recoverActiveMatches takes the ReplayQuarantineRegistry as an OPTIONAL argument, and its sole production caller in MatchHostRegistry passes none, then discards result.blocked; grep finds ReplayQuarantineRegistry constructed only in two test files. Worse than an unrecorded quarantine: MatchHostRegistry.getOrCreate, which the socket attach path calls, falls through to building a fresh host from meta for any matchId with no recovered host - so a corrupt session is re-served rather than blocked, against the spec's 'only that session SHALL enter a quarantined blocked state'. The box's NOT CLAIMED list names the lifecycle posture and the campaign binding, but never says the registry itself is unbound.
  - CLOSED (2026-09-02, Wave D1 seam 3, AuthorityQuarantine.ts + referenceRecoveryPort + ReplayQuarantineRegistry vocabulary): detectAuthorityCorruption runs BEFORE the fold on the reference path - sequence-gap, broken-lineage (chain link), duplicate-receipt, missing-digest each produce a blocked verdict, so a corrupt history never reaches the reducer; quarantineAuthorityCorruption maps the verdict onto the EXISTING registry keyed {authorityType, authorityId} (scope model + assertScopeOperational + first-diagnosis-wins + release(scope, recoveryAction) reused; reason vocabulary extended - '50% fit' stated; the registry's BRANCH-ERA DEFERRAL header marked SPENT with the condition met; QUARANTINABLE satisfies readonly ReplayQuarantineReason[] so the vocabularies cannot drift). DERIVED, NEVER STORED by ruling: no table, no migration (head 25) - a durable quarantine row would be a second source of truth about the journal; operator cost (no query without a sweep; the sweep is boot) stated in the module header. Digests conditional: if no event carries one the lineage/digest checks do not apply, if any does all must and the chain must link; empty-history and replay-failed are refused but NOT quarantined (release-and-retry must not be a lie). Healthy control: one InMemoryMatchStore, one recoverActiveMatches sweep, two matches from the same real ServerMatchHost.create log - match-gapped blocked with sequence-gap, no host, quarantined; match-healthy recovered, not quarantined, assertScopeOperational passes, admitSocket non-null. Red-first: module absent, then the sweep on shipped code REBUILT the gapped match as whole (a gapped log was not corruption to anything before). Mutants killed: Q1 global keying (the healthy session goes dark), Q2 first-diagnosis-wins inverted (SURVIVED first - the claim lived in a suite comment with no row; row added), Q3 lineage removed, Q4 receipt removed, Q5 sequence gap unchecked on the reference path, Q6 missing-digest removed; orchestrator Q4 re-run and N2 missing digest misreported as broken lineage killed; orchestrator N1 (empty-history added to QUARANTINABLE) SURVIVED FIRST - the report's claim that non-corruption refusals are never quarantined had no row behind it (same shape as Q2); fixed red-first with one row per non-corruption reason (empty-history, replay-failed: no quarantine record, scope stays operational). NOT CLAIMED: the lifecycle posture is not lit from a quarantine record (the blocked posture and the registry read exist; the component edit is 19.2's); the campaign door supplies no integrityOf yet so its per-event digests light nothing here - a one-object binding owned by the 5.7 cutover; match authorities validate sequence + event identity only (no digests on their events).


## 16. Retroactive Campaign Rebuild

- [ ] 16.1 Extend campaign intervention preview to declare affected date, missions/contracts, finances, transactions, loans, reputation, rewards, salvage, repairs, inventory, roster, unit/pilot/personnel, markets, receipts, scenario artifacts, activity, audit, and viewer-projection families.
  - RESIDUAL FROM 18.3 (2026-09-02): the campaign rewind-impact preview that lists the letter's domain families (cutoff vs current through CAMPAIGN_ROOT_FIELDS + declareAffectedFamilies; never projectDays; persisted through GmPrivatePreviewRecordWriter; GM-gated POST under campaigns; not mounted on EventTimeline) is owned HERE, alongside the ten still-underivable families. 18.3 closed for the combat stack only.
  - PROGRESS (2026-09-02, Wave E11 16.1-a): the campaign rewind-impact preview exists and is auditable but non-mutating. previewGmCampaignRewind folds the campaign journal to the cutoff (events up to the cutoff) and to the current head (the full log) - there is no past-state materializer, the journal fold is the only reconstructible past - projects both onto CAMPAIGN_ROOT_FIELDS and lets declareAffectedFamilies (16.1's own classifier) name the families; the ten still-underivable families are NAMED as underivable, never silently omitted; a forward time cascade is not a rewind, so projectDays is never called. Typed refusals: not-gm, unknown-campaign, cutoff-not-in-history, cutoff-is-current. A GM-gated POST /api/campaigns/:id/rewind-preview (the campaign analogue of the combat preview route: GM gate, host-as-GM remap for the private writer, clock injected by the route) returns the result verbatim (200 / 403 / 404 / 409) and, only after a successful preview, stores ONE server-only record through GmPrivatePreviewRecordWriter; refusals store nothing; no read route; not mounted on any timeline. Rows: a preview declares the families its own diff found and names the underivable ten; a cutoff equal to the current revision is refused cutoff-is-current; a preview writes nothing (census over campaigns, participants, journal events/batches, private_record); the GM gets the preview and one private record; a non-GM is refused 403 with no record; a refused preview stores nothing. Orchestrator mutants in the PR body. STILL OPEN on 16.1: the ten letter families stay UNDERIVABLE_AFFECTED_FAMILIES (dates, missions, contracts, loans, salvage, repairs, inventory, markets, scenario artifacts, audit projections need their own derivations); the preview names families, not per-artifact lists; no cascade, no commit, no UI.

  - PROGRESS (2026-09-02, Wave D1 seam 16A, GmCampaignAffectedFamilies.ts + GmTimeCascadePreview.ts): the preview DECLARES affectedFamilies derived from the changedStateRefs it already produces - deduped, vocabulary-ordered, empty in = empty out; CAMPAIGN_ROOT_FIELDS moved beside the map (no import cycle) and CAMPAIGN_FIELD_FAMILY satisfies Record<CampaignRootField, CampaignAffectedFamily> so an unmapped root field is a compile error. 'location' declared as an extension beyond the letter (currentSystemId has no named family; not folded into date). FINDING #44: 10 of the 18 named families (transactions, reputation, rewards, salvage, roster, receipts, scenario-artifacts, activity, audit, viewer-projection) are NOT derivable from a field diff - pinned as UNDERIVABLE_AFFECTED_FAMILIES with a test asserting residue == vocabulary - derivable, so the gap fails the day a family is added without being derived or admitted; the box stays OPEN until those sources are read (they arrive with 16.2's replay-based derivation, not a field diff). FINDING #45 (third instance of the weak-assertion class): jest toEqual accepted [undefined] as [] so the unknown-field row passed with the check removed - fixed with toStrictEqual + an explicit no-undefined assertion (LAW: pins use strict equality). Red-first on the shipped single-valued family. Mutants killed: every family declared, dedup removed, campaign-id prefix dropped, unknown-field check dropped (survived first), preview declares nothing, repairQueue misfiled; orchestrator mutants in the PR body. Backward time does NOT exist today (GmTimeCascadePreview refuses days <= 0) - the letter's 'not a negative loop' is a warning, not a description.

- [ ] 16.2 Implement backward time and retroactive correction as replacement-branch replay from a trusted base, not a negative forward-day loop, satisfying `Retroactive Campaign Changes Rebuild Declared Families`.
  - PROGRESS (2026-09-03, Wave G1 16.2 + the WRITER half of 16.4): retroactive correction is now a single commit from a trusted base. commitCampaignRetroactiveCorrection (src/lib/campaign/rebuild/CampaignRetroactiveCorrectionCommit.ts) takes the validated cutoff from previewGmCampaignRewind as baseRevision, acquires the campaign-stream correction lease (SQLiteEventHistoryCorrectionLeaseStore.acquireCorrectionLease), mints the candidate through createCorrectionCandidateBranch, drives replayCampaignReplacement over the caller-supplied retained events, runs verifyCandidatePath, then deriveAndSealCampaignImpact (the CAMPAIGN sealer at GmCampaignImpactDerivation.ts:175, not the combat-generic one) against the candidate, and STOPS before activation (activation is 16.3 clause B). Every exit is typed - correction-lease-held, base-ahead-of-head, replay-divergence, candidate-verification-failed, sealer-failure - and the lease is released in finally on every path. No day is forward-looped anywhere. Port fix folded in: ICampaignEventStore.appendCommandBatch gains optional expectedRevision and branchId (ICampaignCommandBatchInput); JournalCampaignEventStore threads both, InMemoryCampaignEventStore accepts and ignores them, campaignHostBatchCommit passes the first event's sequence explicitly - the same number the journal already used when the field was absent. Rows on temp-file SQLite (red-first, module absent): H1 candidate holds the replayed events, manifest sealed, source head unchanged, lease released; B1 base ahead refused typed, no candidate; L1 nested commit refused correction-lease-held; D1 non-contiguous retained group refused replay-divergence with the effective head still root; C1 an append without the new fields still lands sequence 0 at revision 1. Mutants: M2 replay divergence swallowed KILLED; M3 lease never released KILLED (4 red); NOVEL N1 batch commit passes an off-by-one expected revision KILLED; M1 early base-ahead guard dropped SURVIVED and is EQUIVALENT - createCorrectionCandidateBranch throws EventHistoryBranchError for the same condition before minting and mapOuterRefusal already turns it into base-ahead-of-head, so the early guard was a second owner of one rule; it is REMOVED (5/5 green after, B1 still asserts the typed reason and zero candidates). Gates: tsc 0; oxlint 77-pin; 57 store/port/replay consumer suites 526/526. NOT claimed (both named by the audit): sealing the manifest does not satisfy "declare every affected family before activation" - the manifest carries artifactKind, artifactId, sourceRevision and no family field; retained-set selection is UNOWNED - the commit takes retainedEvents as an explicit input and IRetainedSourceEvent (CampaignReplacementReplay.ts:57) still has no producer. Box stays open on those two clauses plus activation.
  - PROGRESS (2026-09-02, Wave D2 C1c-ii, finding #70 CLOSED): the replacement replay onto a candidate branch. JournalCampaignEventStore.appendCampaignCommandBatch gains an OPTIONAL expectedRevision that flows into toJournalBatch - absent, today's value (the first event's sequence) so every existing caller is byte-identical; present, the journal revision the batch must land on. campaignBranchSegmentReader reads a segment's events from ITS OWN branch (branch-qualified, never root by default). CampaignReplacementReplay replays RETAINED source events (given - choosing what to drop is not this module's decision) from the parent onto a candidate: grouped by stored commandId (the D10 batch boundary a bare event list has lost), each group one atomic batch, expectedRevision read from readCampaignBranchAnchor and RE-READ after every group lands (never sequence, never base + n), and a DERIVED command id `${candidateBranchId}:${sourceCommandId}` - the original would collide with the source command still on the parent and the collision does NOT throw: the journal's command-identity check fires before the revision guard and replays the cached commit as a plausible success. Rows: R2 a committed append lands on a non-root branch at baseRevision + 1 chained to the base digest (RED before: sequence-conflict, the branch rule admitted the append but expectedRevision was branch-unaware); R3 sequence differs from revision WITH A DROP and the multi-event group NOT last (C1 seq 1, C2 seq 2 = base, C3 DROPPED, C4 seq 4+5 -> revisions 3,4, C5 seq 6 -> revision 5: one stored row carries sequence 6 at revision 5; drops are at command granularity because toJournalBatch refuses a non-contiguous batch; with every group one event, 'anchor read once + base + groupIndex' computes the right answer by accident, which is why the two-event group sits before C5); R4 the client door still refuses a direct append to the building candidate; R5 materializeBranchPath accepts the replayed candidate (#80); R6 the reader reads the candidate, not root; R7 one receipt per command whose CONTENTS are the candidate's, not the source command's cached receipt; plus a wrong expectedRevision is refused with the store's typed sequence-conflict. Orchestrator mutants in the PR body. NOT CLAIMED: no activation (stops at a verified, materialisable candidate); one candidate per stream while the head-seed limitation stands; no family derivation, no manifest, no checkpoint on the candidate; campaignJournalReads stays root-pinned; both flags off; no production site constructs any of this; a replayed candidate surviving a BOOT waits on #96/#97 (recovery reconcile + chain start from the base).

  - PROGRESS (2026-09-02, Wave C3 3c-heads, finding #86 FIXED): event_journal_stream_heads is keyed (stream_type, stream_id, branch_id) and since migration 26 a stream may hold several head rows; C1a seeds one per correction candidate at revision = baseRevision, BELOW the effective head. Two readers selected without naming a branch, so SQLite returned an arbitrary row through the PK index - 'candidate-...' sorts before 'root', so the lower revision won and acquireCorrectionLease / activateCandidateBranch refused a caller's TRUE head as STALE_REVISION or STALE_DIGEST permanently. One helper, EventHistoryEffectiveStreamHead.readEffectiveStreamHead(db, branches, stream), resolves the effective branch through readEffectiveHead (NEVER requireEffectiveHead - absence is an answer) and reads stream_revision AND event_digest with WHERE ... AND branch_id = ?; no effective branch or no row keeps today's answer (revision 0, EVENT_HISTORY_GENESIS_DIGEST). Both unqualified readers use it (EventHistoryDurableRebuild.readStreamRevision, SQLiteEventHistoryCorrectionLeaseStore.readJournalHead via this.branches - no constructor change); the four already-qualified readers, the recovery scan (reads every row on purpose) and the INSERT sites are untouched; the stale prose in SQLiteService.historyBranches.migration.ts that still claimed the journal keeps its root pin is corrected. Rows seed TWO head rows (root at 4, candidate-1 at 2): R1 readDurableStreamRebuild reports activeHead.revision 4 (RED today: 2); R2 a lease naming the true head is ACCEPTED (RED today: STALE_REVISION); R3 a lease naming the candidate's digest is refused STALE_DIGEST (the digest half moved with the revision half); R4 a candidate that IS the effective branch reads 2 (not red on the original bug - exists to kill the 'always root' and 'wrong branch' mutants). Orchestrator mutants in the PR body. NOT CLAIMED: only which row is read changes, not what any caller does with it; replay_checkpoints untouched; finding #96 (below) is probed, not fixed.
  - NOTE (2026-09-02, Wave C4): boot recovery now reconciles C1a's seeded candidate heads against event_history_branches and starts a non-root chain at its base (#96/#97 fixed; see 21.1); the stored-event branchId root pin is lifted. A replayed candidate surviving a boot still needs #98 (writer) from C1c-ii.

  - PROGRESS (2026-09-02, Wave D2 C1c-i, finding #87 FIXED): ICandidateBuildRequest gains an optional baseRevision, separating two numbers that were one: where a correction is FENCED (the lease's expectedRevision, still demanded current by assertExpectedHeadIsCurrent, untouched) and where the replacement is CUT. Absent, every caller behaves as before (cut at the fenced head). Present, resolveCandidateBaseRevision validates rather than trusts: a safe integer >= 1 (invalid-branch-record), <= the fenced head (branch-integrity: a base above the head is history that does not exist yet), and requireBaseEvent must find a real event on the PARENT branch at that revision (never a sibling at the same number); the mint anchors at the resolved base. Rows B1 default cut at the fenced head; B2 a named earlier base accepted (head 4, base 2 - C1a's seed writes the head there); B3 base above the fence refused; B4 base 0 refused by name; B5 parent-branch event, not a sibling at the same revision. A2b rewritten to carry BOTH halves: a lease fenced at 1 while the head is 2 still throws STALE_REVISION, and the same 1 passed as baseRevision under a live-head lease is ACCEPTED - without the second half the suite pinned #87 as correct. Orchestrator mutants in the PR body. NOT CLAIMED: no replay onto the candidate, no committed non-root append (#70 stays open until C1c-ii resolves the expected revision from the branch head), no activation; split at the pre-declared prefix-green boundary - C1c-ii (store expectedRevision, campaignBranchSegmentReader, CampaignReplacementReplay with a candidate-scoped command id, rows R2-R6 with the multi-event group NOT last) follows.

  - PROGRESS (2026-09-02, Wave D2 seam C1a, findings #80/#86/#87): the candidate branch gets its journal head. Measured by a scratch probe: createCorrectionCandidateBranch wrote event_history_branches ONLY, so a first append onto a candidate anchored at base N committed at revision 1 chained to null (SQLiteEventJournalWriter derives revision and previous digest from the head row) and verifySegment refused the path - a candidate was unmaterializable from its first event (#80). Fix: seedCandidateJournalHead inside the mint transaction from the branch record's own (baseRevision, baseDigest) - the head the writer reads and the base the resolver verifies are one pair (EventHistoryCandidateBuild.ts, the ruled four-line carve-out). CampaignBranchAnchor.readCampaignBranchAnchor: head row if present else the branch record's base, no offset arithmetic, typed refusals for an unknown branch and a head below its base. Rows A0-A5; M1 (seed removed) SURVIVED first because the anchor's fallback returns exactly the pair the seed writes - fix and absence observationally identical above the table - A0 now reads event_journal_stream_heads directly (the writer's only surface). Mutants 6/6 killed; orchestrator mutants in the PR body. TWO LIMITS RECORDED: #86 SQLiteEventHistoryCorrectionLeaseStore.readJournalHead is branch-blind, so once ANY second head row exists (the seed, or the replay's own appends) it returns the lower revision and acquireCorrectionLease / activateCandidateBranch refuse STALE_REVISION forever - invisible on main because nothing ever wrote a second head row; fixed by C3's 3c-heads (one shared branch-qualified reader), which is now BLOCKING for C1c. #87 a candidate can only be cut AT the head (assertExpectedHeadIsCurrent requires expectedRevision == head and the candidate anchors at lease.expectedRevision) - fence and base conflated; pinned by A2b as today's behaviour and rewritten in C1c when ICandidateBuildRequest gains baseRevision (a named earlier base accepted, an arbitrary one refused). NOT CLAIMED: no committed append onto a non-root branch (#70 stays open, gated on 3c-heads); no replay, no rewind command; both flags off; replay_checkpoints pin untouched (C1b).
  - PROGRESS (2026-09-02, Wave D2 C1b, checkpoint pin lift): migration 27 (SQLiteService.replayCheckpointsBranch.migration.ts) widens replay_checkpoints' CHECK from branch_id = 'root' to length(trim(branch_id)) > 0 - migration 26's rule character for character - so a candidate-branch checkpoint is storable. Numbered 27, NOT the planned 28: SQLiteService.migrations.test.ts asserts COUNT(*) === MAX(version), so versions are contiguous by test and numbering follows LANDING order (C3's artifact-kinds migration takes the next number when it lands; recorded in the migration header so the gap is not re-reserved). PLAIN rebuild, stated as such: replay_checkpoints has no FK children and no explicit indexes (only the _no_update write-once trigger and the UNIQUE tuple's implicit index), so no parking, no defer_foreign_keys, no foreign_key_check - a check over a table no key touches would be a vacuous guarantee. Kept from B1: DDL derived from sqlite_master's own text with only the CHECK swapped (asserted - a no-op replace would rebuild with the pin still on and report success); trigger recreated from stored text; the UNIQUE's implicit index (sql IS NULL) is deliberately not captured because the derived DDL recreates it. Rows R1 candidate-branch checkpoint storable (RED on a v26 tree: CHECK constraint failed), R2 blank id still refused (narrowed, not removed), R3 root-era rows byte-identical across the rebuild + column set, R4 _no_update trigger body byte-identical (toStrictEqual name->sql) and still aborts an UPDATE, R5 UNIQUE identity tuple still binds, R6 explicit head pin .toBe(27) (#54). BranchCheckpointCache: stale NOT-claimed header retired; D1's write-key mutant (pipeline.branchId dropped from checkpointIdFor) was EQUIVALENT under the pin (only root was storable, no second branch to collide) and is now a KILL by the row 'keys the WRITE by branch: two branches cache the same head apart', which records why it used to be equivalent. Worker mutants 5/5 killed; orchestrator mutants in the PR body. NOT CLAIMED: nothing produces a candidate-branch checkpoint yet - recording one is the rebuild replay's act (C2); both flags still off; readJournalHead untouched; C1a (#1551) is a separate branch.

  - GATE LIFTED, HALF TWO (2026-09-02, Wave D1 seam B2, finding #70): EventBranchId (EventJournalContract.ts:5) widened from the 'root' literal type to string and EventJournalSchemas.ts:53 from z.literal(ROOT_EVENT_BRANCH_ID) to DurableIdSchema (the same non-empty-id rule every durable id carries) - the named two-line exception; the four field declarations followed the alias unchanged. The RULE replacing the pin: campaignBranchRule.ts resolveCampaignBranchId(campaignId, requested, branches) refuses typed CampaignStaleBranchError (code STALE_BRANCH) for any branch that is not the stream's current effective branch, nothing written; blank id still refused (pin narrowed, not removed); JournalCampaignEventStore takes an optional branches store DEFAULT-INERT (no store -> root, byte-identical batch; store handed in -> id derived from the effective head), branchId threaded through appendCommandBatch -> appendCampaignCommandBatch -> toJournalBatch optionally. Rows R1-R5 green; R4 uses the REAL activation path (lease + candidate + sealed manifest + atomic swap). FINDING #70 (R4 narrower than briefed, nearly missed twice - a not-throwing assertion passed on a promise that RESOLVED with a conflict): after activation, naming the new effective branch passes the rule and naming the superseded root is refused, but the append returns sequence-conflict rather than committing, because a campaign sequence is stream-global while a candidate branch is anchored at a base revision - expectedRevision is branch-unaware; resolving the expected REVISION from the branch head (as this seam resolves the branch ID) is 16.2's replay-onto-branch work, not this seam's. A fourth root pin (EventJournalSchemas.test.ts 'fork-1' rejected) legitimately invalidated -> blank-id row + pointer. max-lines fixed by extraction (rule module), 77 pin held. Mutants killed: rule removed, literal kept with a store, wrong stream, blank id accepted, default path resolves anyway (makes R1 load-bearing); orchestrator: arbitrary-id re-run and no-store-trusts-request killed, 'refusal carries the wrong code' SURVIVED first (nothing pinned the STALE_BRANCH discriminant the client's command door keys on) - fixed red-first with a toStrictEqual row on the code. The B1->B2 window is closed. NOT CLAIMED: no committed append onto a non-root branch; no production site hands in a branch store (running behaviour unchanged); CAMPAIGN_JOURNAL_AUTHORITY_ENABLED still false. Remaining gate for 16.2 itself: the replay-onto-replacement-branch machinery (Seam C) + revision resolution + the artifact-kind migration for 16.4.

  - GATE LIFTED, HALF ONE (2026-09-02, Wave D1 seam B1, SQLiteService.branchPin.migration.ts = migration 26): the three journal CHECKs pinning branch_id = 'root' (event_journal_batches ~32, event_journal_events ~54, event_journal_stream_heads ~88 - the brief named two; the batch CHECK fires FIRST because a batch is written before its events) are widened to length(trim(branch_id)) > 0. The canonical 12-step rebuild is IMPOSSIBLE in this runner (SQLiteService wraps every migration in a transaction and PRAGMA foreign_keys = OFF is a no-op inside one; defer_foreign_keys works but a rename alone does not re-satisfy the deferred check; writable_schema surgery is refused) - three probes recorded in the header. Shape: defer_foreign_keys ON, drop the _no_delete immutability triggers on entity_refs/causations, park their rows in TEMP tables, rebuild the three tables with DDL DERIVED FROM sqlite_master's own text (only the CHECK clause swapped - the chain CHECK rejected a test fixture mid-development, proving a retyped body would have silently dropped it), reinstate, re-execute every trigger/index from captured sql (bodies identical, not names), foreign_key_check must be empty before the transaction closes. Fidelity suite replays the ledger on a real v25 tree: trigger+index bodies byte-identical (toStrictEqual over name->sql), parked-table content digests identical, dangling reference refused in-txn, injected refusal rolls back to a byte-identical v25 tree (schema, digests, ledger head, pin present). Explicit head pin .toBe(26) added - FINDING #54: both existing head constants (MIGRATIONS.length - 1 and MIGRATION_HEAD = max(version)) are self-adjusting and cannot kill a dropped migration. Two shipped pins legitimately invalidated (eventJournal.migration.test.ts 'fork' throws at CHECK -> blank-branch assertion + pointer; artifactManifest.migration.test.ts 'is the head' -> 'is applied'). Mutants killed: changed trigger body, foreign_key_check skipped, reinstate dropped, batches CHECK left, triggers not recreated; orchestrator: fk-check re-run, widened CHECK admits blank, defer_foreign_keys never enabled. WINDOW (corrected by the worker's own prep): the storage CHECK was the THIRD line of defence, not the only one - EventBranchId is the literal type 'root' (EventJournalContract.ts:5) and EventJournalSchemas.ts:53 is z.literal(ROOT_EVENT_BRANCH_ID), so between B1 and B2 no non-root append can even be attempted; the sole production writer also hard-codes ROOT_EVENT_BRANCH_ID. B2 widens the type + the Zod literal (7 non-test references across 3 files), adds the optional branch store + expectedBranchId resolution + the STALE_BRANCH rule + row (c). Second gate (CAMPAIGN_JOURNAL_AUTHORITY_ENABLED = false) untouched.

- [ ] 16.3 Reject commands during rebuild with `PROJECTION_REBUILDING`; activate only after every declared state, artifact, receipt, audit, and viewer projection verifies.
  - PROGRESS (2026-09-03, Wave G2 16.3 CLAUSE A; clause B waits on 16.2's activation): a command refused during a campaign rebuild now names the active head, and the LIVE co-op socket refuses during a rebuild instead of committing into the history the correction is replacing. Before: campaignCommandPipeline.ts's blocked result (112-125, returned at 327-336) carried no head; the HTTP door (src/pages/api/campaigns/[id]/commands.ts 168-173) emitted PROJECTION_REBUILDING without one; bindCampaignSyncConnection.ts dispatched CampaignProposal (549), CampaignDecision (558) and CampaignHostIntent (564) before any rebuild consult. Fix: the pipeline's blocked result carries activeHead {branchId, revision} from the lease's StreamRebuildRefusal.activeHead (readDurableStreamRebuild over SQLiteEventHistoryCorrectionLeaseStore's expected_branch_id + expected_revision, resolved by readActiveBranchHead); the HTTP door answers 409 { kind:'blocked', reason:'PROJECTION_REBUILDING', recoveryAction:'retry-after-rebuild', activeHead }; new campaignSyncRebuildAdmission.ts (refusedWhileCampaignRebuilding, mirroring ServerMatchHostBranchAdmission.refuseDuringHistoryRebuild at 191-206) is consulted before each of the three arms and sends the typed Error { code:'PROJECTION_REBUILDING', reason and recoveryAction 'retry-after-rebuild', conflictHead, activeHead, intentId? } instead of dispatching; Protocol.ts gains the optional activeHead. Cites the CHANGE folder's campaign-persistence spec 80-85 (not the root spec). CampaignGmArbiter.commitProposal is private and reachable only through submitProposal and decide, both under those arms, so gating the three arms is sufficient. Rows RED against shipped code (real temp-file SQLite lease): the pipeline's blocked result carries the head; the route's 409 carries it; a CampaignHostIntent, a CampaignProposal and a CampaignDecision each answer PROJECTION_REBUILDING with the head and the stream revision is UNCHANGED; control: with no lease the same frames dispatch as before. Mutants, all KILLED: M1 the live door never refuses (3 red); M2 the refusal frame is sent but dispatch continues (3 red); M3 the blocked result drops the head (2 red); NOVEL N1 one of the three arms loses its consult (1 red). Gates: tsc 0; oxlint 77-pin; 29 consumer suites (pipeline, route, binder, protocol) 239/239. UNTRIGGERABLE TODAY, stated plainly: nothing in production acquires a correction lease on a campaign stream yet (16.2's commit does, but no route calls it), so the gate is correct but not reachable by a user until 16.2 is wired to a door. Clause B (activate only after every declared state, artifact, receipt, audit and viewer projection is verified) stays open.
- [ ] 16.4 Invalidate stale scenario drafts, force selections, outcomes, and other externalized artifacts explicitly and reject their later use.
  - PROGRESS (2026-09-02, Wave E14 16.4-b): the reject-later-use half exists as a consult and is wired into the doors as an opt-in port. GmCampaignArtifactUseGuard reads the effective head, then that branch's sealed manifest only (a sealed-but-never-activated candidate blocks nothing), matches kind AND id, and answers null or a typed refusal { kind: invalidated-artifact, artifactKind, artifactId, branchId, revision }; it reads, never writes. materializeCampaignMissionEncounter (launch + materialize, scenario and encounter ids), applySalvage (matchId) and acceptContract (contract id) each take an optional consultArtifactUse and run it BEFORE any write, answering the refusal as a new discriminated member, never a thrown Error. Rows: an id the effective branch sealed is refused with that branch and revision; an id sealed only on a never-activated candidate is usable; an unknown id is usable; a stream with no effective head blocks nothing (orchestrator row, mutant N1); the same id of another kind is usable (kind AND id); launching an invalidated scenario draft, materializing an invalidated encounter, a salvage roll for an invalidated matchId and accepting an invalidated contract each append nothing and answer invalidated-artifact; a valid id still passes each door (four pins). Orchestrator mutants in the PR body. HONEST GAP, why 16.4 stays open: no production caller BINDS the consult yet - the campaign doors run in the browser/host process without the manifest store in hand, so the guard is enforced only where a caller passes it; binding it at the server-side campaign command sites (the co-op host's materialize/salvage/accept paths that do hold SQLiteService) is the remaining seam (16.4-c), and until then the letter's 'later use SHALL be rejected' holds by port, not by default. Also not claimed: replay/export kinds stay underivable; the ten underivable families are 16.1's; the journal cutover stays off; no UI surfaces the refusal.
  - PROGRESS (2026-09-02, Wave E14 16.4-c): the consult now holds BY DEFAULT on the server. readDurableCampaignArtifactUse(stream, artifact) answers null while the SQLite service is not initialized (an in-memory journal has no manifest table) and otherwise consults the branch + manifest stores on the initialized database; ICampaignCommandDeps.artifactUse is optional with the 1.x rebuild-reader discipline (absent means the durable reader, so the shipped route, which still passes { journal, authority } only, is gated without naming it). executeCampaignCommand derives the artifact an intent names after the conflict decision and before validation: AcceptContract names { contract, payload.contract.contractId }; HirePilot / SpendFunds / AllocateSalvage / RemoveParticipant / AdvanceDay name none and never consult. A refusal is a new result member { kind: invalidated-artifact, artifactKind, artifactId, branchId, revision } - not a string reason, because the client needs the branch and revision to resync - and nothing is appended; the commands route answers 409 with that body the way it answers conflict. ReconcileBattle (the host-only frame) consults { salvage, matchId } in handleCampaignHostIntent (bindCampaignSyncConnection, server-only) BEFORE record/walk, and refuses on the existing Error frame (INVALID_INTENT, reason invalidated-artifact) carrying the typed fields - no new message kind; the reader is an optional bind dep with the durable default bound in the binder because reconcileCoopBattle is value-imported by the client graph (LAW 62). Rows: a sealed contract id is answered invalidated-artifact with that branch and revision and the journal head is unchanged; the same id sealed only on a never-activated candidate commits; an unknown id commits; AllocateSalvage / HirePilot / SpendFunds never consult (reader asserted not called); with the dep absent and SQLite not initialized the command commits; a reconcile naming an invalidated matchId is refused and applies nothing. Orchestrator mutants in the PR body; orchestrator fixes: the durable rows pass rebuild: () => null because activation leaves the stream under the 1.x PROJECTION_REBUILDING gate that its own suite pins. WHY 16.4 STAYS OPEN: launching or materializing an encounter (scenario / encounter kinds) is a CLIENT-side door (CampaignDashboardPage, missionLaunchPage, fastForwardCombatRunner) that reaches no server consult - the client still passes nothing, so a stale scenario draft is rejected only when a caller binds the port; force selections have no consult at all; the campaign-side salvage/contract doors of 16.4-b are still client-side too and are covered only where the server command path (AcceptContract, ReconcileBattle) is the door. Not claimed: no UI surfaces the refusal; replay/export kinds stay underivable.

  - PROGRESS (2026-09-02, Wave E13 16.4-a): the invalidation manifest can now name CAMPAIGN artifacts by id. Migration v30 rebuilds event_history_artifact_manifest_entries (DDL derived from sqlite_master, the kind CHECK widened from the four combat kinds to nine, INSERT..SELECT, DROP, RENAME in the runner transaction, triggers recreated from stored text, idempotent, existing combat rows untouched, unknown kinds still refused); the five new kinds are the ones that already have ids today: scenario (scn-<contract>-<date>-<force>), encounter (enc-<scenario>), salvage (matchId), contract (contractId), external-effect (the opaque ref string); EVENT_HISTORY_ARTIFACT_KINDS carries a LAW-40 exhaustiveness pin and the migration ledger pin moved 29->30. GmCampaignImpactDerivation walks the campaign journal fold past a cutoff (ContractAccepted -> contract at its stream revision) plus the keys the journal types cannot carry (scenario drafts, encounters, salvage rolls, unprojected externalEffectRefs), names { artifactKind, artifactId, sourceRevision } entries and seals them through the existing manifest store against a C1c-ii correction candidate - IDS ONLY; families remain 16.1's. Rows: CHECK admits the five campaign kinds and still refuses an unknown kind; idempotent re-apply leaves existing combat rows untouched; seals scenario, contract and salvage ids after the cutoff with their source revisions and names nothing at or before it; an empty tail seals an empty manifest, not a family list; names unprojected externalEffectRefs and ignores projected ones; activation over a campaign candidate returns the sealed campaign artifact ids. Orchestrator mutants in the PR body. STILL OPEN on 16.4 (16.4-b): the reject-later-use half - launch, materialize and salvage do not yet consult the sealed set and refuse with branch/revision; replay/export kinds remain underivable (the combat deriver's existing limit); the ten underivable families stay 16.1's; the journal cutover stays off.

  - PROGRESS (2026-09-02, Wave D1 seam 16A): the reject-later-use half has NO surface to build against - grep over src/lib and src/types finds no scenarioDraft / forceSelection / encounterDraft; the only externalized-artifact surface is externalEffectRefs / externalEffects on the time-cascade types, and EVENT_HISTORY_ARTIFACT_KINDS (replay, export, checkpoint, projection; SQL CHECK) carries no campaign kinds, so the manifest half needs a migration. Re-scoped: both halves land with Seam C (after the pin lift) against externalEffectRefs + a campaign artifact-kind migration, never invented ahead of the surface.

- [ ] 16.5 Verify with campaign day-pipeline, campaign intervention control-plane, mission-launch, and time-cascade tests plus `npm.cmd run verify:qc:gm:time-cascade`.
  - PROGRESS (2026-09-03, Wave G2 16.5, ADVANCES - cannot close while 16.1 to 16.4 are open): the verify:qc:gm:time-cascade lane now EXECUTES the Section-16 suites and its validator fails if that coverage is ever dropped. Before: the lane (package.json:201) ran none of the rewind impact preview, affected-family declaration, impact derivation, artifact-use guard, command-pipeline rebuild and artifact-use gates, the day pipeline, or the history activation guard; the validator's requiredSurface.commandIncludes held 8 tokens naming none of them. Fix (red-first, in the order the audit prescribed): scripts/qc/validate-gm-time-cascade-ledger.mjs gains the eight suite tokens (dayPipeline, GmCampaignRewindImpactPreview, GmCampaignAffectedFamilies, GmCampaignImpactDerivation, GmCampaignArtifactUseGuard, campaignCommandPipelineRebuild, campaignCommandPipelineArtifactUse, EventHistoryActivation) and scores suite tokens against the LIVE npm script (isTimeCascadeLaneSuiteToken) rather than the registry copy, so a lane that drops one goes red; package.json:201 names the eight paths; the validator's own suite (scripts/__tests__/gm-time-cascade-qc.test.ts) gains an it.each over the eight tokens that strips each from a copy of package.json (MEKSTATION_PACKAGE_JSON_PATH) and expects status 1 / command-missing. Deliberately NOT done (audit): no fourth active-head-preservation guard (EventHistoryActivation.test.ts already holds it at 258, 338, 359) and nothing in GmCampaignInterventionBoundaries.test.ts; processors and marketProcessors were already in the lane. Mutants, all KILLED: M1 the lane drops one Section-16 suite (live validator exit 1, errors=1); M2 the validator stops requiring a token (its suite 1 red); NOVEL N1 the suite-token predicate matches nothing (2 red). Gates: tsc 0; oxlint 77-pin; qc suites 22/22; the validator live: surfaces 1/1 domains 1/1 families 1/1 processors 6/6 roots 12/12 anchors 17 errors 0; the LANE ITSELF: 16 suites / 143 tests green.

## 17. Coordinated Post-Receipt Outcome Correction

- [ ] 17.1 Implement a distinct higher-version coordinated outcome-correction command for the post-campaign-receipt boundary, satisfying `Coordinated Outcome Correction Supersedes Across Journals`.
  - PROGRESS (2026-09-03, Wave G2 17.1-b, the expectedDigest hole - the audit's "ship this as its own small PR first"): the correction command's expectedDigest is now ENFORCED. Before: CoordinatedOutcomeCorrection.ts:99 declared readonly expectedDigest on the intent and admitCoordinatedOutcomeCorrection compared only branch, revision and generation (validateExpectedBranchHead), so any string was admitted - a binding advertised and not enforced. Fix (option a, chosen because the inbox already persists command_digest on campaign_combat_outcome_inbox in the same db the version check reads): readDeliveredOutcomeVersion becomes readDeliveredOutcomeReceipt returning {outcomeVersion, commandDigest} for the outcome at its delivered version; admission compares intent.expectedDigest with that stored digest by the same string equality the inbox uses for expected-versus-applied, refusing typed expected-digest-mismatch (new member of the refusal vocabulary, 9 to 10) when the caller's digest is empty or differs; the check sits after version-not-next so a stale version is still named first. Rows (RED against shipped code): a mismatched digest is refused typed and nothing is written; an empty digest is refused; control: a matching digest is admitted exactly as before. Mutants, all KILLED: M1 digest check always passes (2 red); NOVEL N1 the refusal reason drifts onto version-not-next (2 red); NOVEL N2 the receipt no longer carries the digest so even a matching caller is refused (3 red). Gates: tsc 0; oxlint 77-pin; 6 consumer suites (saga, run, target steps, correction) 83/83. NOT claimed (the rest of 17.1, unchanged by this PR): no wire route dispatches the command; outcomeJson, candidateBranchId and artifacts still have no producer; no two-database span; computeCampaignStateDigest was deliberately not recomputed here (it needs the campaign post-state, which is not on the inbox row).
  - PROGRESS (2026-09-02, Wave C6 17.1-a): the DISTINCT command exists as a typed module (CoordinatedOutcomeCorrection.ts): kind CoordinatedOutcomeCorrection carrying the delivered outcomeId, the NEW outcomeVersion as a version DISCRIMINATOR (it must be exactly delivered+1, never a free number), targetRevision, the same four expected* head fields the rewind commit carries, and the actor. Admission order is the contract: GM authority (post-combat fix) -> the inbox's MAX(outcome_version) for the outcome, refused outcome-not-delivered when nothing was delivered (the mirror of 13.4: this door exists only after delivery; before it the plain rewind is the door) -> version-not-next unless delivered+1 -> no-authoritative-history -> the shipped expected-head validation (STALE_BRANCH / STALE_REVISION / STALE_GENERATION) -> accepted-pending-saga. Admission WRITES NOTHING; the typed result is the receipt 17.2 will consume. A nine-member refusal Record is phrased (LAW 40). Rows (host harness on the rewind-commit SQLite style): the GM correction at delivered+1 is accepted-pending-saga and writes only the admission (census); version delivered+2 is refused version-not-next; an undelivered outcome is refused outcome-not-delivered; a non-GM is refused gm-role-required; a stale expected revision is refused STALE_REVISION; the plain rewind onto a delivered receipt is STILL refused campaign-receipt-delivered (pin). Orchestrator mutants in the PR body. NOT CLAIMED, and why 17.1 stays open: the command is not yet on the wire (no Protocol intent kind, no host/route door - the intent pipeline dispatches every admitted combat intent to the engine and this command must not be dispatched), so no live actor can send it; that door lands with the saga (17.2) so an admitted command is never left with nothing to consume it. No replacement outbox, no N+1 gate (17.3), no GameEvent.

- [ ] 17.2 Execute coordinated correction as a durable saga: atomically record combat supersession, invalidation manifest, and replacement outcome outbox in the source authority, then idempotently record the target-scoped campaign replacement receipt and deterministic consequence batch in the target authority; expose pending/blocked state and do not claim a cross-store transaction.
  - PROGRESS (2026-09-02, Wave E17 17.2-a): the SOURCE half of the saga exists as a typed module (CoordinatedOutcomeCorrectionSaga.ts) with its durable state. A saga table on the MATCH STORE database, mp_coordinated_correction_saga (PRIMARY KEY (match_id, outcome_id, outcome_version) = the admission's version discriminator; state source-recorded | manifest-sealed | target-pending | completed | blocked with blocked_reason and per-step timestamps; idempotent CREATE migration in the supersession style). Step 1, recordCoordinatedCorrectionSource, is ONE match-store transaction: the saga row in source-recorded, the tail moved by 14.4-b's supersedeMatchStoreFrom from the first superseded sequence (revision = sequence + 1, the rebuild's rule), and the match's ONE outcome slot in mp_combat_outcome_outbox (match_id is its PRIMARY KEY - finding #103: the worker's first cut inserted a second row and would have collided on any real match; its suite had seeded no delivered outcome) REPLACED by the accepted higher version as a fresh pending publication (published_at NULL); an injected failure on the slot write leaves the live table intact and no saga row. A retry with the same key is a no-op that answers the recorded row (one supersession, one slot write); a slot already at the accepted version with the same payload is adopted; the same version with a different payload, or a newer correction already holding the slot, is refused typed replacement-immutable. Step 2, sealCoordinatedCorrectionManifest, is on the JOURNAL database (sealArtifactManifest on the correction's candidate branch, skipped when already sealed) and then advances the saga row to manifest-sealed only from source-recorded. The two steps are separately durable and no cross-store transaction is claimed: a crash between them is a saga row that still reads source-recorded, which is the exposed pending state; blockCoordinatedCorrection answers blocked with its reason. Rows: source record moves the tail, writes the slot and the saga row together; injected slot failure leaves no partial state; a delivered outcome (version N, published) is replaced by N+1 pending; a slot already at N+1 with the same payload is adopted; a slot at N+2 refuses; sealing a blocked saga does not advance it; same-key retry is a no-op; different payload at the same version refused typed; the replacement row is pending and is NOT a match publication (listPendingPublications reads mp_match_outbox only - combat replacements ride getCombatOutcomeOutbox / markCombatOutcomePublished); read after the source step only answers source-recorded; the seal advances the saga and a second seal is equal; block answers blocked with the reason. Orchestrator mutants in the PR body. NOT CLAIMED, why 17.2 stays open: the TARGET side (17.2-b: the target-scoped campaign replacement receipt and the deterministic consequence batch in the campaign authority, idempotent, advancing target-pending -> completed) is not built - those two states are union members only; nothing routes the admission's accepted-pending-saga into this saga yet (no route, no intent kind); the scenario N+1 gate is 17.3; retry/reconnect/restart proofs are 17.4.
  - PROGRESS (2026-09-03, Wave E17 17.2-b): the TARGET half now exists, so both authorities of the saga are written. recordCoordinatedCorrectionTarget runs only on a saga the source half left at manifest-sealed (any other state, or none, answers typed not-ready and writes nothing), and its first act is to look for an existing replacement receipt keyed (outcome_id, outcome_version): a hit returns that receipt with no writes, which is the retry law made observable. The ordered steps and their keys: acquire the campaign correction lease on campaignStreamRef(campaignId); mint the correction candidate; PERSIST candidate_branch_id ON THE SAGA ROW BEFORE ANY JOURNAL WRITE (the branch id is random and not derivable, so a crash after the mint would otherwise leave an orphan branch and a retry would mint a second - the saga column is added by a table_info-guarded ALTER so a match store that already ran 17.2-a keeps its rows); replay the retained source commands onto the candidate; append the deterministic consequence batch under the candidate-scoped command id ${candidateBranchId}--outcome-correction:${outcomeId}:${outcomeVersion}; INSERT OR IGNORE the replacement receipt into the new append-only campaign_combat_outcome_replacement table (keyed (outcome_id, outcome_version), carrying the campaign, the candidate branch, the command and its revision range) - the ACCEPTED INBOX ROW IS NEVER UPDATED, because 21.3's immutable-correction letter and D6/D7 say a correction supersedes by a new fact and prior history stays auditable; seal the campaign impact if it is not already sealed; advance the saga to target-pending. The campaign inbox is deliberately NOT the door for the replacement: it is keyed by outcome_id with one accepted version and answers outcome-version-conflict for any other, and it is not branch-aware (its batch defaults to root), so pushing N+1 through it would either be refused or land on the wrong branch. Retryability is separated from failure: a lease held by someone else answers not-ready with the reason (a retry can succeed), while a non-retryable refusal blocks the saga with its reason. A replayed group whose candidate-scoped command id already landed is treated as convergence rather than an error, so a re-run of the whole target step is a no-op. Rows: a manifest-sealed saga mints one candidate, replays, appends the consequence batch ON THE CANDIDATE (root untouched), inserts one replacement row, seals, and answers pending with the saga at target-pending; a retry after success answers the same receipt with one candidate, one replacement row and one consequence batch; an injected failure after the mint leaves the candidate id on the saga and the retry REUSES it (no second mint); the accepted inbox row is byte-unchanged after the whole flow; a saga not yet manifest-sealed answers not-ready and writes nothing; a foreign lease answers not-ready, not blocked. Orchestrator mutants in the PR body. NOT CLAIMED, why 17.2 STAYS OPEN: nothing in production COMPOSES the two halves - there is still no route and no intent kind that carries an admitted correction from accepted-pending-saga through source, seal and target, so the saga is executable only from a test or a future driver; that driver and its retry/reconnect/restart proofs are 17.4, and 'completed' remains a reserved state that only 17.3's gate will retire. Also not claimed: activation of the campaign candidate (16.3's verify-before-activate owns it), and any UI surface.

- [x] 17.3 Gate scenario N+1 on the active outcome receipt, replacement artifacts, active branch, and convergence of every retained participant.
  - Receipt (2026-09-03, Wave E17 17.3-b - CLOSES this box; DEFECT FOUND AND FIXED): the launch gate had never gated a scenario launch. CampaignProgressionGate checked all four clauses (candidate branch, target-pending correction, unverified replacement, participants behind the head) but the only caller was the day-advance path; the one server-authoritative launch door, POST /api/campaigns/:id/launch-authority, never consulted it, so box 9.4 was closed while scenario launch stood outside the gate it named. Fix: campaignLaunchProgressionGate.ts (src/pages-modules/api) evaluates the three durable clauses through the shared campaignProgressionReaders.durable readers and derives participant convergence from the durable rows - each retained campaign_session_participant seat's MAX(campaign_participant_cursor.acked_sequence), a seat with no cursor row reads 0 (fail-closed) - instead of the session's in-memory map; the route runs the gate after the head read and before force materialization, refusing 409 { kind:'refused', code:'CAMPAIGN_NOT_CONVERGED', clause, reason, activeHead, resyncAction:'resync-to-active-head', requiredRevision, behind } in the same reason vocabulary the socket path emits. With the database uninitialised the route answers exactly what it answered before. Rows (RED against shipped code, real SQLite): behind participant refused with the behind list; candidate head refused; target-pending correction refused; converged launch still materializes; uninitialised DB unchanged. Orchestrator mutants: M1 clause consult ignored KILLED (2 red); M2 missing cursor treated as converged KILLED; M3 refusal code changed SURVIVED on the worker's rows (every row compared against the imported constant, so a drift moved both sides) - the first refusal row is now pinned to the literal 'CAMPAIGN_NOT_CONVERGED', the string campaignLifecycleState matches to keep commands enabled on a refusal, now KILLED; NOVEL N1 co-op launches bypass the gate KILLED (3 red). Suite fix on the way: jest.spyOn on an ES-module namespace import is non-configurable under this config ('Cannot redefine property') - replaced with a module mock wrapping the real materializer. Gates: tsc 0; oxlint 77-pin; route suite 17/17; production build exit 0. NOT claimed: the socket-path day-advance gate is untouched; the client's launch button does not yet pre-check convergence (it learns from the 409)
  - PROGRESS (2026-09-03, Wave E17 17.3): the gate now evaluates all four clauses instead of convergence alone. evaluateCampaignProgressionClauses runs BEFORE the retained-participant check, in this order: (1) the campaign stream's effective head must name a branch whose status is 'effective' (the contract's live-head word - there is no 'active' status), else branch-not-active carrying branchId and the status it actually holds; (2) a coordinated correction saga for this campaign must be absent or completed, else correction-pending carrying the saga key and state; (3) if that saga names a candidate branch, its artifact manifest must be sealed AND verify, else replacement-artifacts-unverified carrying the branchId; (4) the existing participants-behind law, unchanged. Every refusal keeps requiredRevision, and bindCampaignSyncConnection answers the three new reasons on the SAME CAMPAIGN_NOT_CONVERGED frame the behind case uses - no new message kind. The readers are an optional port (ICampaignProgressionReaders) whose durable default createDurableCampaignProgressionReaders is bound where CampaignHostRegistry builds the session; each reader answers null when SQLite is not initialized, and an absent port skips clauses 1-3 entirely, so every existing suite is byte-identical (pinned by a row). The campaign has no match id of its own, so the saga is found by reading the campaign's latest combat-outcome inbox row and looking the saga up by outcome id; the saga row still carries match_id, so the refusal names a complete key. Rows: a candidate effective head refuses branch-not-active with branch and status; a saga at target-pending refuses correction-pending with key and state while a completed saga does not; a sealed-but-unverifiable manifest refuses replacement-artifacts-unverified; with every clause satisfied the behind law still refuses; with no readers and SQLite uninitialized the answer is exactly today's; one wire row per new reason. Orchestrator mutants in the PR body, including a precedence mutant proving the clauses are consulted BEFORE convergence. NOT CLAIMED, why 17.3 STAYS OPEN: the gate is still only consulted on AdvanceDay. Verified this session: the one production caller is bindCampaignSyncConnection's refuseUnconvergedProgression, and grep over campaignLaunchAuthorityRoute, launchCoopMission and materializeCampaignMissionEncounter finds no call at all - so a function named evaluateScenarioLaunch does not yet gate a scenario launch, which is the letter's subject. That wiring is the remaining seam (and the same gap makes the closed box 9.4 overstate its own clause - filed as errata). Also not claimed: 'active outcome receipt' is read as the correction saga being settled, not as a separate receipt-status check; nothing activates a campaign candidate here (16.3 owns verify-before-activate); no UI surfaces the three new reasons.

- [ ] 17.4 Prove retry, reconnect, and restart cannot apply the coordinated correction twice and preserve cross-journal audit causality.
  - PROGRESS (2026-09-03, Wave E17 17.4): the correction is now RE-ENTRANT and proven so. runCoordinatedCorrection reads the saga first and skips every step it already shows done - any saga row skips the source step, manifest-sealed and later skip the seal, target-pending and later skip the target - and answers which of the three ran or was skipped. That is the retry law made executable: a reconnect or a restart re-enters the same function. Two one-shot fault kinds open the crash windows the letter names (E2E_FAULT_KINDS 5 -> 7): correction-exit-after-source fires after the source transaction COMMITS and before the manifest seal, and correction-exit-after-target-mint fires after the candidate id is durable on the saga and before the replay, with the target's catch rethrowing so a crash is not mistaken for a blocked saga. Both consume the in-graph arm and the sentinel together, the shipped consume-both-arms rule. In unit form the fault THROWS; the e2e process-exit form is deliberately not wired, and the module says so. Rows: RETRY - crash after the source commit, then a second run finishes with exactly one supersession, one outbox slot at N+1 with published_at NULL, one manifest seal, one candidate, one replacement row and one consequence batch; RECONNECT - two runs back to back, the second reports every step skipped and writes nothing; RESTART - a FILE-BACKED store closed and reopened re-runs to no effect and the 14.4 boot fold sees the superseded tail exactly once; MINT-RETRY - crash after the mint, and the retry reuses the persisted candidate rather than minting a second; CAUSALITY - the campaign replacement row and the match saga row name the same candidate, and the consequence command id embeds it; plus the fault-kind count pin. Orchestrator mutants in the PR body, including two novel ones that MOVE each crash window to the wrong side of its commit. Orchestrator fix on the worker's draft: the RETRY row counted rows in the replacement table before the target step creates it, so the pre-crash assertion threw instead of reading zero - the helper now treats an absent table as zero, which is what 'no replacement recorded' means at that point. NOT CLAIMED, why 17.4 stays open with 17.2: nothing in production calls this driver - there is still no route and no intent kind carrying an admitted correction into it, so retry/reconnect/restart are proven at the module boundary, not through a live socket. The e2e process-exit variants of both kinds are unwired. Deployment gap filed, not folded: the saga table is created lazily by its own entry points and is absent from the DurableMatchStore schema list.


## 18. Role-Scoped Audit Timeline

- [ ] 18.1 Add branch/effective-head/supersession rendering and cross-journal outcome causality to the audit timeline, implementing `Timeline Preserves Branch and Supersession Lineage`.
  - PROGRESS (2026-09-02, Wave E10 18.1): lineage is derived SERVER-SIDE (ViewerHistoryLineage.ts) from the branch store (listBranches, readEffectiveHead, readSupersessions) and the sealed artifact manifest, projected per audience, and attached as a SIBLING top-level `lineage` block on BOTH the timeline and export HTTP bodies - the timeline entries, the viewer key allowlists and timelineDigest (still over the audit entries only) are untouched. Shape: effectiveHead { branchId, revision (the installed branch's base), generation } or null when the store answers null (no invented genesis); transitions [{ fromBranchId, toBranchId, baseRevision (the cutoff), actorRole, supersededAt, invalidatedArtifacts [{ artifactKind, artifactId }] }] with `reason` (supersession row) and `createdBy` (replacement branch) present ONLY in the GM projection; a player sees the same transition ids, cutoff and actor role, no reason key, and only its own projection artifacts (other kinds by kind+id without the GM annotation). Rows: root-only stream; store answering null; GM projection lists the activation reason and invalidated artifacts; PLAYER projection omits the reason key; a superseded-then-superseded chain lists two transitions in order; the timeline body carries lineage and timelineDigest ignores it; the export body carries the same lineage for the same viewer; a player bearer has no reason key while the GM does; the strip renders a line per transition and the reason only when present. Orchestrator mutants in the PR body. STILL OPEN on 18.1: the letter's 'rebuild result' is not on the block (activation outcome is not persisted per transition yet); the MatchHistoryLineage strip is NOT mounted - no page consumes the timeline route today (the /audit/timeline page lists IBaseEvent[] from EventStoreService and is not this surface); cross-journal outcome causality and campaign-side lineage wait on the journal cutover; fog stays in the audience projector.

- [x] 18.2 Add separate authorized GM-private audit lookup and player-safe history/export; record private-record access and append exactly one private rejection-audit row before each terminal rejection without creating gameplay journal, outbox, cursor, replay, or export facts.
  - Receipt (2026-09-02, Wave E1 seam 1 - CLOSES this box; evidence-mapped clause by clause): SEPARATE AUTHORIZED GM-PRIVATE LOOKUP holds - SQLitePrivateRecordRepository.lookupPrivate gates through privateRecordAccess.requireGmViewer (fresh authorizeHumanAction kind private-audit, then a hard role !== 'gm' refusal); separate private_record table (migration 12), separate repository, no FK into action_audit. PLAYER-SAFE HISTORY/EXPORT holds - ViewerHistoryService.readHistory/readTimeline/exportForViewer with the player key allowlist omitting committed revisions and the 12.2 timelineDigest (#1505). PRIVATE-RECORD ACCESS RECORDED holds - privateRecordAccess inserts a payload-free private_access_audit row on every path, granted and denied. EXACTLY ONE PRIVATE REJECTION-AUDIT ROW BEFORE EACH TERMINAL REJECTION was MISSING and is delivered: action_audit (append-once, PK command_id, reserved safe class command-rejected) shipped in the authority-audit leaf's PR 4 with NO production caller - recordLifecycle had test callers only; CommandRejectionAudit.recordCommandRejection now runs inside ServerMatchHostIntent.rejectCommand at the seven post-gate terminal sites (RATE_LIMITED, DUPLICATE_INTENT, MATCH_PAUSED, match-already-completed, client-rolls-forbidden, engine-rejected, unit-not-owned) BEFORE the Error frame is built, with the gate's already-resolved viewer handed forward so the actor is server-derived without a second resolve. 'Private' = server-only, reachable only through the role-redacting projection; action_audit chosen over private_record because private_record's PK is a random opaque_ref with a nullable non-unique command_id and cannot enforce append-once by command identity without a migration (none added; head stays 24). NO GAMEPLAY/OUTBOX/CURSOR/REPLAY/EXPORT FACT proven by a total-table census over real SQLite: only action_audit moves (+1). Red-first behavioral (4 rows red against shipped code: 0 rows, no row before the Error, census empty). Worker mutants killed: row keyed on a fresh id (append-once broken), record moved after the broadcast, insertGranted dropped from the shipped lookup, dice guard removed so the 'rejection' commits. Reason granularity deliberately collapsed to command-rejected (finer detail would be a side channel about authority state through the player-readable timeline; belongs in a private_record kind nobody writes yet). Recording is best-effort (skipped/conflict never blocks the refusal - a colliding command id must not become a DoS lever). NOT claimed: pre-gate refusals (principal mismatch, spectator, rollback-blocked, unauthorized) are unaudited by design - no server-derived actor exists; journal-authority-path rejections untouched; no HTTP route for the GM-private lookup; retention wiring. Extraction forced by max-lines: ServerMatchHostIntentClaims (pure move). Gates: tsc 0; oxlint 77-pin; multiplayer+persistence 132 suites / 1067; ten leak/parity sweeps 89 green, none weakened.

- [x] 18.3 Add rewind-impact preview showing affected domains and artifacts without mutating state.
  - ERRATA (2026-09-03, same audit): this box closes a CAMPAIGN-scoped requirement with a combat-only delivery. The requirement's only normative scenario is the GM previewing a CAMPAIGN rewind and seeing affected dates, missions, contracts, finances, transactions, loans, roster, units, pilots, salvage, repairs, inventory, markets, outcome receipts, scenario artifacts, activity and audit projections. This box's own closing receipt says the preview holds end to end FOR COMBAT and that the campaign-side listing does not exist, parking it in open box 16.1. A checked box therefore carries a requirement whose only scenario is admittedly unimplemented.
  - CLOSED (2026-09-02, Wave E9 18.3-a, combat stack): Rewind Impact Preview Is Auditable but Non-Mutating holds end to end for combat. NON-MUTATING: previewGmCombatRewind is a pure consult (storage census of journal, branches, heads, leases, manifests, checkpoints unchanged - GmCombatRewindPreview.test.ts, 3b-i) and the E6 dialog commits nothing on cancel (GmRewindPreviewDialog.test.tsx, NetworkedGameSurface.rewind.test.tsx). AUDITABLE (new here): the preview ROUTE, after a successful preview and only then, stores one server-only review record through GmPrivatePreviewRecordWriter (private_record, record_kind gm-draft, joined to private_access_audit purpose write) carrying the token actor, the stream + branch + target revision, the affected entries and changedViewerIds, with the clock injected by the route; refusals (403/404/409) store nothing; the response stays the union verbatim; the record is on no player frame, snapshot, history read, or export, and there is no read route. The writer's authorized create requires role gm while seat membership only ever emits player for the host (#55), so the route remaps the match host to gm FOR THAT WRITE ONLY (HostAsGmMembershipSource) - the live wire still sees player. Rows: records exactly one private preview whose actor is the token principal; records nothing when a seated non-host or missing history is refused; omits the private preview record from the public preview body; records two private previews for two successful calls. Orchestrator mutants in the PR body. RESIDUAL, named into 16.1 not 18.1: the campaign-side rewind-impact preview listing the letter's domain families (dates, missions, contracts, finances, ... audit projections) does not exist - the preview names artifact kinds (replay/export/checkpoint/projection), ten families are still UNDERIVABLE_AFFECTED_FAMILIES, and GmTimeCascadePreview refuses days <= 0 (a forward cascade is not a rewind). 18.1 stays post-commit lineage; a pre-commit preview there would mix what would break with what did break.

- [x] 18.4 Add GM and player timeline/export parity tests plus negative privacy scans for private fields and hidden-event identifiers; prove rejected-command retries retain one authorized audit row while all player surfaces and gameplay authority remain unchanged.
  - Receipt (2026-09-02, Wave E1 seam 2 - CLOSES this box; honest framing: these are PINS over behaviour that already held plus one live-path gap closed, not red-first regressions - the trail must not overclaim): PARITY holds and is cited (ViewerHistoryService.test rows seeding a rejected audit row: player key allowlist, actorPrincipalId null for a non-actor, GM sees both principals and revision ranges; three-different-digests; #1505 export parity). The one gap closed: those rows seed the repository directly and prove the MAPPER, not the system - rejectionAuditViewerSurfaces.test drives a REAL refusal through handleIntent on a real host and reads it back through a real ViewerHistoryService over the same SQLite file: the GM sees the actor, the other player learns only that a command was refused, both player arms carry exactly the allowlisted keys, each viewer's export is its own timeline. NEGATIVE SCANS: ONE scanner - the three copies (two byte-identical JOURNAL_LEAK_KEYS + walkers + leakScan, and the snapshot variant a rename away) folded onto leakScan(value, markers, extraKeys?) in campaignGrantChannel.test-helpers with the base list extended in the one place (AUTHORITY_ONLY_EVENT_FIELDS spread in rather than restated, plus raw column names that can only surface if a repository row were serialized instead of projected: opaque_ref, payload_state, command_digest, correlation_id, campaign_session_id, actor_principal_id, actor_participant_id, lifecycle_state, safe_reason_code, committed_first/last_revision); keys forbidden only for players (the camelCase committed revision range the GM legitimately carries) ride at the call site as PLAYER_ONLY_KEYS the way the snapshot scan passes ['revision'] - one vocabulary correct across surfaces, not a lowest common denominator. Fold proof: all seven consuming suites held at 14 tests before and after; no existing call site went red on the stronger base. Sweep over timeline / export envelope / replay stream / live frames for P1 and P2 with TWO positive controls (a hand-built object carrying what the sweep hunts; the GM's real export tripping the player key list while clean against the base). RETRY PROOF: three retries of the same refused command leave ONE row (updated_at == created_at), total-table census {} (nothing moved), every player surface byte-identical per viewer, journalHead + effectiveHeads pinned; the only red on arrival was each replay frame's own envelope send-clock, normalized in a named helper with every match fact still compared. Worker mutants killed: opaque_ref dropped from the base (only the positive control died - the vacuity trap the control exists for), row keyed on a fresh id, player timeline naming the actor, player row carrying the revision range; orchestrator re-ran the actor-naming mutant and added walker-skips-arrays and extraKeys-ignored. NOT claimed: the GM viewer is minted by a test membership source (MatchSeatMembershipSource returns player for every seat - the GM arm proves the projection, not that a match can seat a GM); campaign-grant surfaces keep their own scans (now on the shared scanner). No production code in this seam. Section 18: 18.2 + 18.4 closed; 18.1 + 18.3 stay gated on the branches leaf.


## 19. Accessible Lifecycle and Recovery UX

- [x] 19.1 Add stable UI states and locators for pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, and blocked across campaign, combat, and GM surfaces.
  - Receipt (2026-09-02, Wave E2 seam 1 - CLOSES this box; the tactical half shipped 2026-08-31, the campaign and GM halves land here): ONE vocabulary - src/lib/lifecycle/lifecycleState.ts (LifecycleState, LifecycleProjectionSignal, LIFECYCLE_TONE) with a shared strip src/components/common/LifecycleStateBanner.tsx; the tactical module ALIASES its types onto it (TacticalLifecycleState = LifecycleState) so a posture added or renamed moves on all three surfaces or fails to build - reconciliation is compiler-enforced, tactical derivation/banner/DOM untouched (its two shipped suites are the proof). Campaign guest: toCampaignLifecyclePosture rendered by CampaignSyncStateBanner - locator campaign-sync-state KEPT (the two-browser journey spec walks it) with data-state added beside data-sync-state; postures blocked/syncing/reconnecting/behind/live from the real mirror status, pending from a guest proposal awaiting the GM, finalized from the last resolved proposal committed. GM: deriveGmLifecyclePosture rendered by HostGmReviewSurface - locator gm-lifecycle-state + data-state; pending/live from the queue depth, blocked from a REAL wire CAMPAIGN_NOT_CONVERGED (campaignSyncTransport dispatches every frame incl. Error to the host's onFrame; the connected route surface captures the code and clears it on the next CampaignEvent) and from a typed STALE_BRANCH (guarded unreachable). rewound / rebuilding on campaign + GM: defined, wired to the typed PROJECTION_* signals, guarded UNREACHABLE from live signals by a sweep over 16 wire codes x 6 sync states x 4 fact combinations plus a row proving they light when a typed signal arrives - the branches leaf lights them without UI work (same discipline as the tactical half). sealed is ABSENT on campaign/GM by construction (no declare-then-reveal phase; stated in code). Every posture carries a polite live region including blocked (a11y rows in the shipped suite shape). Red-first 13 rows. Mutants killed: data-state dropped (10 red), the gate letting a command through while not converged, live region silent on blocked (4 red), mapper admitting PROJECTION_REBUILDING from the wire (2 red). Tactical's own banner keeps its pre-existing TONE map (aliased type only) - available cleanup, not worth touching shipped rendering mid-wave.

  - Progress (2026-08-31 - box stays OPEN, TACTICAL HALF SHIPPED): the tactical surface now carries a stable lifecycle posture - data-testid tactical-lifecycle-state with data-state in pending/sealed/finalized/syncing/reconnecting/behind/blocked - derived from the live client's real signals (pending-intent tracking, own-sealed-declaration-awaiting-reveal, gap recovery, reconnect backoff, pre-ready resync, sequence-collision block) and mirrored on the shipped CampaignSyncStateBanner pattern (same TONE map discipline, polite live-region INCLUDING blocked, its a11y suite shape); wired into NetworkedGameSurface for lobby and spectate routes. rewound/rebuilding are DEFINED postures wired to the typed PROJECTION_REWOUND/PROJECTION_REBUILDING signals and guarded UNREACHABLE from live client signals - the branches leaf lights them without UI work; the guard began as a single sample, the reviewer's routed-live-signal mutant SURVIVED it, and it is now a sweep over every live-signal combination (the recurring vacuous-guard lesson). Honest scope note from the worker: no server 'behind' marker exists, so behind truthfully derives from the client's pre-ready/resync state. The campaign half shipped in a prior wave (CampaignSyncStateBanner); the box CLOSES when the branches leaf makes rewound/rebuilding live and the two surfaces' state names are reconciled against the letter's full list.

- [ ] 19.2 Gate command surfaces during rebuild, stale branch, blocked recovery, and required convergence; provide actor-safe typed conflict and recovery actions.
  - PROGRESS (2026-09-03, Wave G1 19.2 CAMPAIGN half): a GM who loses a head race now sees the server's named resync action and cannot approve on the stale head. Before: campaignLifecycleState's wire door (WIRE_REFUSALS) admitted only CAMPAIGN_NOT_CONVERGED and PROJECTION_REBUILDING, so campaignRefusalFromServerErrorCode('CAMPAIGN_STALE_HEAD') answered null, the door hardcoded recoveryAction null even for admitted codes, and Approve stayed live; two header comments (the "thirteen members" sentence and the claim that the union lacks STALE_BRANCH) were false against the real 17-member ErrorCodeSchema (Protocol.ts 746-793; the audit brief said 16, the worker counted 17). Fix: CAMPAIGN_STALE_HEAD is admitted onto the EXISTING blocked posture (no new LifecycleState member, no new gate reason); the door takes the frame's recoveryAction and carries it (resolveRecovery already copied a non-null action onto serverAction); ORCHESTRATOR FIX on top of the worker: the worker's own non-claim was the box's observable - CampaignCoopRouteSurfaceConnected stored only message.code, so a live GM would still have seen no action; it now holds lastRefusalAction from the Error frame (both handlers, cleared with the code), CampaignCoopRouteSurface accepts it and hands it to the door at both mapper calls, and a wire-level row in CampaignLifecycleGate emits a CAMPAIGN_STALE_HEAD frame with recoveryAction and asserts Approve disabled plus 'Server instruction: resync-to-active-head' rendered verbatim. Headers corrected to the real count. Mutants: M1 stale head dropped at the door KILLED (5 red); M2 recovery action nulled KILLED (3 red); M3 the worker's extra blocked-posture guard in decisionRefused removed SURVIVED and is EQUIVALENT - the next line (!commandsEnabled) already refuses regardless of state, so the guard was dead code and is REMOVED, the existing rule withholds Approve; NOVEL N1 connected surface drops the frame's action KILLED (1 red, the pre-fix production behaviour). Gates: tsc 0; oxlint 77-pin; 12 lifecycle-consumer suites 168/168 before the orchestrator fix, 85/85 on the six touched suites after. Named remainder: Preview, Manual and GM Fix stay pressable unwired no-ops (defect #25), not fixed here. Tactical half lands separately (same wave).
  - PROGRESS (2026-09-03, Wave G1 19.2 TACTICAL half): a live combat STALE_BRANCH refusal now blocks the tactical controls and shows the server's own recovery action. Before: the server's typed refusal (conflictHead + recoveryAction, emitted by ServerMatchHostBranchAdmission through the shipped branch port) reached the client and died three times over - src/lib/multiplayer/client.ts:395 re-emitted only {code, reason} (a drop the audit did not name; the worker found it), useMultiplayerSession.helpers.ts:275 mapped every non-PROJECTION code to null, and tacticalLifecycleState's projectionSignal channel was typed to LifecycleProjectionSignal (the two PROJECTION_* strings only), so a toast fired and every control stayed enabled. Fix: client.ts forwards conflictHead and recoveryAction; TacticalLifecycleProjectionSignal widens to LifecycleProjectionSignal | ITacticalBranchRefusal ({code:'STALE_BRANCH', conflictHead, recoveryAction}) with the PROJECTION_* signals unchanged as bare strings; projectionSignalFromServerError reads the frame; the derivation yields the EXISTING blocked posture (no new GATE_REASONS entry, no new LifecycleState member; commands already gate on blocked through tacticalCommandGate) and carries recoveryAction/conflictHead; NetworkedGameSurface.actionbar renders the server's wording verbatim through BranchRecoveryInstruction. Rows RED against shipped code: lifecycle maps a STALE_BRANCH frame onto blocked with the server action; helpers no longer map it to null; the hook lights the blocked signal from a server refusal; the action bar shows the action and disables commands. PROJECTION_* controls stay green. Mutants, all KILLED: M1 client strips recoveryAction (1 red); M2 branch refusal not blocked (3 red); M3 STALE_BRANCH mapped to null at the door (3 red); NOVEL N1 action bar renders no instruction (1 red). Gates: tsc 0; oxlint 77-pin; 15 consumer suites (lifecycle, session hook, surface, client) 156/156. Campaign half landed as #1619. Box stays open on the remaining letter (rebuild, blocked recovery and required convergence on the tactical surface).
  - PROGRESS (2026-09-02, Wave E4c-B2, findings #79/#85 FIXED): the P2P divergence REBUILD. applyReplayStream branches on the verdict: diverged -> every received event persisted through a new persistReplayEvent dep (default matchLogStorage.appendEvent, which never short-circuits - #85 was appendReplayEventToActiveSession returning before its storage write when an interactive session existed) -> adoptRebuiltSession(await hydrateFromMatchLog(matchId)) replacing the INTERACTIVE session too (the authority the dock commits through) -> useP2PMirrorStore.resolveDivergenceAfterRebuild(matchId), match-keyed and the store's ONLY clear; the clean path is byte-identical. InteractiveSession.fromMatchLog hydrates from STORAGE (InteractiveSession.persistence.ts:89-99), so the durable write lands first by construction. Four shipped rows in useP2PReconnectSession.test.tsx pinned the defect (diverging stream lands on the in-memory appender) - appender moved, intent and delete/callback assertions untouched; COLD-RELOAD's tail now asserts a later replay takes the ordinary path (the rebuild is for the diverging replay only). Hook split: production edges to useP2PReconnectSession.defaults.ts (re-exported), 77 pin held. Mutants 6/6 killed after M11 (resolver ignores its match id) SURVIVED first - every B2 row resolved the matching id; row added: a rebuild for ROOM99 leaves ROOM01's flag standing; M7 never clears, M7-inverse clears without a rebuild, M8 short-circuiting appender (#85), M9 hydrated but never adopted, M10 every replay rebuilds; orchestrator mutants in the PR body. NOT CLAIMED: the rebuild fires on the reconnect replay path only (a divergence detected elsewhere keeps B1's sticky refusal); adoptRebuiltSession discards in-flight selection/composer state - intended trade, PLAYTEST NOTE; refusal wording local until 8.4's recoveryAction reaches P2P. With this the tactical half of 19.2 (networked + P2P) is complete; the campaign guest arm (E5 cut B) remains.
  - PROGRESS (2026-09-02, Wave E5 cut C, findings #92/#93 FOLDED): the campaign recovery vocabulary tells the truth about what pressing the control does. campaignLifecycleState's RECOVERIES table labels the staleness family (STALE_BRANCH / PROJECTION_REWOUND) 'Check again' - the handler on both surfaces is onClearLifecycleRefusal -> setLastRefusalCode(null), which clears a local hint so the server answers afresh; 'Resync to active head' overclaimed because no client calls the commands route and no resync exists to wire it to (#93). resolveRecovery copies the server's recoveryAction into a separate serverAction field, never into the button label, so the verbatim rule holds without a movement promise on a control that only clears a hint; HostGmReviewSurface and GuestProposalSurface both render 'Server instruction: <verbatim>' beside the control and split actionable ('Check again') from non-actionable ('Wait for rebuild', no button). #92: the cut A pin that constructed the commands route's 409 without a recoveryAction now carries REBUILD_RETRY_ACTION, matching the real body since B2c's #66 fix; the ICampaignCommandRefusal comment that still claimed the route drops the action is rewritten. Rows: 'a recovery label says what pressing it does' (4), host + guest twins - offers a stale-branch host/guest the one thing the control actually does, never puts a movement promise on a control that only clears a hint, shows the server's own recovery action verbatim, renders no recovery at all for a posture that has none, tells the host/guest to wait out a rebuild with no button to press; CampaignLifecycleGate's refusedPosture fixture carries label 'Check again' + serverAction null. Worker mutants (stale label promises a resync again; server action put back on the button; description re-promises the resync; and the rest of the cut C matrix) plus orchestrator mutants in the PR body. NOT CLAIMED: no real resync action - that is the cutover's; the host and guest recovery banners duplicate markup (labels are shared in RECOVERIES, so the copy is presentation, not a second vocabulary); nothing under src/components/multiplayer.

  - PROGRESS (2026-09-02, Wave E5 cut B, the campaign GUEST arm): GuestProposalSurface withholds every proposal control while the projection is rebuilding or the branch is stale, with the same reason and the same recovery vocabulary the host surface shows (guest-command-blocked-reason / guest-lifecycle-recovery / -wait, all through cut A's toCampaignLifecyclePosture); the two load-bearing rows are the WIRING ones - the guest control stays disabled once the server refuses on the wire (a real Error frame) and the block lifts once a committed CampaignEvent proves the server took a write - because before this seam the guest had NO refusal channel at all (the posture rows went red only on the missing reason; withholding given a posture was already right). Re-derived against merged main: #66 was fixed upstream by B2c (campaignCommandPipeline returns recoveryAction on the rebuild block, commands.ts forwards it) and the door reads the field generically, so behaviour needed no change - see #92 for the fixture it left stale. Worker mutants 8/8 killed (gate collapses to sync-only, disabled control without a reason, reason never rendered, refusal channel bypassed, refusal never cleared, route re-hardcodes refusal null, recovery always pressable, reason text swapped for a generic); orchestrator mutants in the PR body. NOT CLAIMED: the tactical half is E4's; PROJECTION_REWOUND still has no server producer and cut A's derived sweep still proves nothing reaches 'rewound'; the command door still has no production caller (the guest channel wired here is the wire Error frame, the 409 body path is reachable-when-called); the guest refusal clear is OPTIMISTIC exactly like the host's (no 'you may command again' frame exists; the server re-refuses if the condition holds); defect #25 (route mounts the GM surface with no onPreview/onManualTakeover/onGmCorrection) untouched.

  - PROGRESS (2026-09-02, Wave E4c-B1, findings #62/#79): the P2P DIVERGENCE half. The reconnect hook's onMirrorPrefixDivergence (already producer-tested) was passed nothing by GameSessionPage.lifecycle.ts (#62); worse, on a non-match verdict reconcileMatchLogMirror DELETES the durable log (matchLogPrefix.ts:92-96), applyReplayStream appends the peer's events onto a still-live in-memory session (:332 rehydrates only if no interactiveSession) and :335 calls setLive() - the client tells itself everything is fine (#79). Now: p2pMirrorStore (zustand, keyed BY MATCH ID - an unkeyed sticky flag would refuse the player's NEXT battle; one field, one row), recordDivergence(matchId, verdict) where a 'match' verdict records nothing AND does not clear an existing divergence (the false-clear trap: after divergence the log is empty and the next reconcile returns {kind:'match'}), NO clear() in this seam (a clear the UI could call would be a button that lies; a row pins that the gate does not clear within the session); p2pCommandAvailability(status, divergence) checks divergence FIRST (peer-loss may resolve by waiting; divergence does not) with its own no-digits wording; useP2PCommandGate SUBSCRIBES to the mirror store (E4b's M7 lesson applied first); the lifecycle passes the callback at the redirectToLobby call site. Why not localMatchStatus: deriveLocalMatchStatusFromAwareness returns 'live' whenever the counterpart is PRESENT and E4b's detector resets it every second, and divergence is detected while the peer is present - a 'diverged' arm would be wiped in the state it was set in. Wiring row in two halves with no unmeasured hop (useP2PMirrorDivergenceWiring drives the REAL useGameSessionLifecycle, captures the mounted options, fires the callback, asserts the store; page rows carry store -> gate -> layout). Mutants 6/6 killed (M5 false-clear first; callback passed but write dropped; gate ignores divergence; precedence inverted; peer-loss wording borrowed; only 'replaced' refuses); orchestrator mutants in the PR body. Stacked on E4b (#1547). NOT CLAIMED: #79's hazard itself (stale in-memory events + setLive) is NOT fixed - E4c-B2 lifts the rehydrate guard for the divergence path, rebuilds from the reconciled log and clears on the rebuild; no recovery ACTION (the 'rejoin' wording is local until 8.4's recoveryAction reaches this transport); rebuilding/rewound have no P2P meaning; detection untouched.

  - PROGRESS (2026-09-02, Wave E4b, findings #61/#62/#63/#71): the P2P half of the tactical gate. Measured: localMatchStatus had ZERO production readers, its only mid-match writer (deriveLocalMatchStatusFromAwareness via useSyncRoom's 1 s awareness poll) had no production caller (#63), and GameplayLayout.actionDockSlot passed no commandGate - a P2P guest whose host vanished applied every command LOCALLY (silent divergence, #61; worse than #37 because no server refuses). Now: p2pCommandAvailability(status) in src/lib/p2p/p2pCommandGate.ts (Option A - the dock keeps taking CommandAvailability, a gameplay type; no multiplayer import into gameplay; rejected Option B recorded in the header: six of the shared union's ten members are unreachable over P2P), useP2PPeerPresence(matchId) mounted as a page effect beside useP2PReconnectSession writing the same store field (one field, two writers; samples once at mount; a mount with the peer already absent writes nothing - the reconnect timeout's case), useP2PCommandGate returns undefined for a non-P2P match (single player ungated by construction); chain store -> page -> GameplayLayout -> dock slot -> TacticalActionDock pinned in halves plus a subscribe row (the page re-renders when the peer drops AFTER mount - every other row seeded before render and a non-subscribing read passed them all). FINDING #71 (closed here): #1535's 'refuses the dispatch itself' row could not fail - every dispatch surface is a disabled button and React delivers no click to one (removeAttribute and .disabled=false both tried); the dock's dispatch is now a module-level factory createTacticalCommandDispatcher (TacticalActionDock.dispatch.ts) the row calls directly with a refusing gate, with a vacuity control that the same command applies when the gate allows; the layout rows keep proving no reachable way to apply a command. #58 duplicate comment removed. Red-first at four layers. Mutants 7/7 killed after M3 (fixed via #71) and M7 (subscribe row added) survived first; orchestrator mutants in the PR body. NOT CLAIMED: rebuilding/rewound have no P2P meaning; #62 onMirrorPrefixDivergence (useP2PReconnectSession.ts:322 - the P2P rewound analogue) is still unwired by GameSessionPage.lifecycle.ts; 'aborted' has no P2P writer (reachable-when-emitted row); guestPending is now live (host-side).

  - PROGRESS (2026-09-02, Wave E4, findings #46/#47/#56/#57): the NETWORKED tactical half. Measured: NetworkedGameSurface never renders TacticalActionDock (its own NetworkedActionBar at :341 gated on `ownership.canAct && !paused`, paused = status paused | blocked) - five of six gated postures left every control enabled (red on shipped code). Now: the bar derives its gate inside NetworkedGameSurface through tacticalCommandAvailability(posture) (NetworkedGameSurface.actionContext.ts), all 7 controls disable in all six gated postures with the reason in an sr-only span whose id the controls' aria-describedby actually names (#42 mirrored; rows resolve the id), dispatch refuses at the same construction point; the paused arm is KEPT beside the gate (a paused match is a match fact that also drives the overlay; the gate asks whether this client's view is authoritative). PRODUCER: projectionSignalFromServerError (tacticalLifecycleState.ts) maps the server's PROJECTION_REBUILDING Error frame into projectionSignal in useMultiplayerSession.helpers.handleClientError (toast channel kept), both pages pass it; clears on the next authoritative match event (the client emits the inner event, kind === null) and on reconnect; an unrelated refusal mid-rebuild and campaign/host-migration envelopes do NOT clear it (pinned). FINDING #56: PROJECTION_REWOUND is NOT a member of ErrorCodeSchema (Protocol.ts:733-765), so the wire cannot carry it - the producer has one arm and 'rewound' stays reachable-when-emitted for that reason, said in the reserved-signal comments. FINDING #57: /gameplay/games/[id] does NOT host WebSocket sessions - its matchId feeds useP2PReconnectSession (P2P_MATCH_ID_PREFIX 'p2p-'), a different transport with no IClientLifecycleState, so #1535's dock commandGate is DORMANT there pending a P2P posture producer (own seam). Red-first at four layers (mapper absent; hook signal undefined after a rebuilding frame; 5/6 postures enabled on the bar; page wiring row found concede enabled); one playable row corrected before implementation (movement is disabled for want of a map selection, not the gate). Mutants killed: producer maps to null, gate dropped from the surface call (9 red), disabled-but-dispatching, no reason, rebuilding never clears, collapse to !commandsEnabled; orchestrator mutants in the PR body. Gate-set note: src/hooks/__tests__ and src/__tests__/pages/multiplayer must be in any path list for this seam. Cosmetic finding #58: TacticalActionDock.commandButton.tsx carries one JSX comment block twice (#1535). STILL OPEN: campaign guest + GM arms (E5), P2P posture producer, PROJECTION_REWOUND server producer.
  - PROGRESS (2026-09-02, Wave E5 cut A, findings #64-#69): the campaign vocabulary + the GM arm. TWO DOORS: ErrorCodeSchema (Protocol.ts:733-765, 13 members) carries PROJECTION_REBUILDING but NOT STALE_BRANCH (command-admission vocabulary, EventHistoryExpectedHead.ts:37-44) nor PROJECTION_REWOUND (#64), so campaignLifecycleState now has a wire door admitting exactly ErrorCodeSchema's members and a command door admitting the commands route's refusal bodies (reason-blind over all five conflict reasons - four in CampaignConflictRefusalReason plus 'lost-race' in the pipeline union); 'rewound' admitted by neither, proven by a sweep over every schema member + every refusal body, and one row proves it lights on a typed signal. GM surface: every proposal control withheld while rebuilding, approve withheld on stale for EVERY proposal (not only progression), veto stays live under both (server accepts vetoes), reason on the control as a real element with a real aria-describedby target (#69: it was a data- attribute - announces nothing), recovery banner offers the one action the refusal names (typed recoveryAction verbatim via the imported CAMPAIGN_CONFLICT_REBASE_ACTION; rebuild = text, not a pressable control). #68: the 19.1 sweep's LIVE_WIRE_CODES was hand-typed and had drifted (STALE_BRANCH, STALE_OWNERSHIP, PROJECTION_REWOUND - none deliverable) -> now ErrorCodeSchema.options. Red-first 11 red / 2 green on shipped code (rebuild posture threw in refusalState). Mutants 11/11 killed after two real survivors: M3 veto (equivalent as aimed twice - the veto never consults decisionRefused; only a genuinely disabled veto bites, row now also asserts no withheld marking) and M11 one generic reason string satisfied every anti-dead-button row while telling a stale-branch host to wait for participants (two rows: distinctness AND per-refusal correctness). 967 lines / 8 files. STILL OPEN (cut B, preserved byte-exact in the scratchpad): the GUEST arm - #65 the guest transport discards Error frames (Connected.tsx:202-221) while the host branch captures them and CampaignCoopRouteSurface.tsx:336 hardcodes refusal: null, so the guest has NO refusal channel today (the shipped guest gate was never 'sync-only' at the button - commandsEnabled was already ANDed with refusal === null; the cause is upstream). Non-claims: #66 campaignCommandPipeline.ts:165 returns {blocked, reason} and DROPS rebuilding.action ('retry-after-rebuild') - B2c's file, folded into 2c; #67 /api/campaigns/[id]/commands has no client caller (the command door is exercised against the route's real body shapes); defect #25 (GM surface mounted without onPreview/onManualTakeover/onGmCorrection) untouched - transport wiring.

  - PROGRESS (2026-09-02, Wave E2 seam 3a, finding #37/#42): tactical half. tacticalCommandAvailability(posture) in src/lib/multiplayer/tacticalCommandGate.ts maps the posture to CommandAvailability; TacticalActionDock takes commandGate?: CommandAvailability (a type its own layer owns - no multiplayer import into gameplay) and applies it ahead of each command's own check at all three sites INCLUDING the dispatch guard (a gate that only disables the button is the silent retry 19.2 forbids). Gated: blocked, rebuilding, rewound, syncing, reconnecting, behind; CARVE-OUT pinned by two rows: pending/sealed are the player's own declarations in flight and are NOT gated (the gate is not merely !commandsEnabled). FINDING #42 (pre-existing, found by the anti-dead-button row): every disabled dock command carried aria-describedby=command-disabled-reason-<id> and nothing ever rendered that id (CommandTooltip marks its reason with a data-testid), so the disabled reason reached nobody and hover cannot rescue a control that is out of the tab order - fixed with an sr-only span carrying the real id. Two lint findings real: dispatchCommand closed over a stale gate (exhaustive-deps - a refusal arriving after the callback's last recreation was invisible to dispatch) and max-lines (command button extracted to TacticalActionDock.commandButton.tsx along the file's existing seams). Red-first: 12 red / 5 green on the shipped dock + mapper (carve-out rows correctly green). Mutants killed: disabled control renders no reason (row 7), gate stops short of dispatch, carve-out collapsed into !commandsEnabled, gated posture loses its reason; orchestrator mutants in the PR body. Folded: package.json verify:qc:scenario-packs now enumerates the two co-op parity specs. REACHABLE-WHEN-EMITTED: rebuilding/rewound rows are built against the reserved postures; no live client signal produces PROJECTION_REBUILDING/PROJECTION_REWOUND yet (server emission landed in #1534 - lighting them is a change in the upstream derivation, tacticalLifecycleState), STALE_BRANCH has no client path. STILL OPEN: the NetworkedGameSurface wiring row (posture -> gate), the campaign guest + GM arms with recovery actions on the banners (reading 8.4's recoveryAction once B2c's 2a lands), rows 5-8 - seam 3b.

  - Progress (2026-09-02, Wave E2 seam 1 - two of four gates live, two typed-and-guarded; box stays OPEN): REQUIRED CONVERGENCE is gated PRECISELY, not blanket - the server refuses CAMPAIGN_NOT_CONVERGED for progression only (AdvanceDay, incl. host approval of a guest's AdvanceDay), so HostGmReviewSurface.decisionRefused disables Approve on AdvanceDay proposals and nothing else; vetoes and non-progression approvals stay live because the server takes them (two anti-over-gating rows pin this). The typed conflict is read off the wire the host already subscribes to; the recovery action is a real button that COMMITS NOTHING - it clears a local hint so the server answers again (actor-safe: safe to hand an actor whose last command was refused). The clear is deliberately OPTIMISTIC (on the next CampaignEvent - no 'converged' push frame exists; the server stays the enforcer and re-refuses) - the one judgment call a reviewer should push on. BLOCKED RECOVERY: the tactical blocked posture (sequence collision) and the campaign blocked posture are surfaced; STALE BRANCH and REBUILD are wired to the typed STALE_BRANCH / PROJECTION_REBUILDING codes and guarded unreachable until the branches leaf's admission gate (#1512) reaches a live command path (PR 3's adoption step) - they light then without UI work. Guest-side refusal capture is null by construction (no guest route raises AdvanceDay). MATCH_PAUSED left ungated (none of the four named gates). DEFECT #25 (campaign-side sibling of #15): CampaignCoopRouteSurface mounts HostGmReviewSurface with neither onPreview, onManualTakeover, nor onGmCorrection, so Preview / Manual / GM Fix are no-ops in production on the campaign host surface too; left alone here (a transport wiring change, not a gating seam; adjacent to B3's launch work). Box closes when the typed gates are reachable live.

- [x] 19.3 Add keyboard-complete rewind preview/confirmation, focus management after errors and decisions, persistent text, live-region announcements, and narrow-viewport primary actions.
  - CLOSED (2026-09-02, Wave E7b): the confirm half is wired. commitGmCombatRewind POSTs the SAME five-field request the preview was made with (the lobby keeps the previewed request in a ref and forwards it verbatim - it never re-derives expectedRevision, never reads the mirror) to /api/matches/:id/rewind-commit with the bearer, narrows the JSON to GmCombatRewindCommitResult, and throws a typed transport failure on a non-JSON reply. onConfirmRewind is now an optional producer returning that result; the dialog awaits it, phrases 'committed' (generation advanced, invalidations count) or the refusal (the phrasing Record already carried every commit-only member; the 16-reason tripwire is unchanged), stays OPEN on a refusal, closes only on committed, and disables confirm while in flight and after a committed result (an in-flight ref guards what the disabled attribute cannot). The lobby passes both producers to the host surface only; spectate untouched. Rows: POSTs the preview's body verbatim to the commit route with the bearer; narrows a 409 refusal body to the union; throws the typed transport failure on a non-JSON 500; confirm calls the producer once and phrases committed; a campaign-receipt-delivered refusal keeps the dialog open with its phrasing; confirm is disabled while in flight so a second click does not call the producer again; offers confirm to nobody else at the table; the lobby forwards the previewed body to the commit producer. Orchestrator mutants in the PR body. NOT CLAIMED: the live three-browser row (E2E-76) is 22.3's; fog previews are still refused; no coordinated correction.

  - PROGRESS (2026-09-02, Wave E7, the replaced-when-emitted half): the lobby page wires the rewind PREVIEW producer to the merged route. previewGmCombatRewind (src/lib/multiplayer/client, beside issuePlayerToken - presentational components do not fetch) POSTs /api/matches/:id/rewind-preview with the bearer wire token and the body the route names (targetRevision + the four expected* fields), returns a body carrying `kind` VERBATIM (preview or refused - a 409 refusal keeps its STALE_* sentence), and turns a thrown fetch or a kind-less transport body (400/401/405/500) into the dialog's unavailable outcome without ever smuggling error text into it; onPreviewRewind is widened to Promise<GmRewindPreviewOutcome> so the page binds the adapter directly instead of throwing as control flow. The client holds none of the four expected* values, so the page sends honest fallbacks, each with its reason: expectedRevision = the last mirrored event's sequence + 1 (revision = sequence + 1 per the match reader), expectedBranchId = the root branch id (the match baseline's 'main' would answer STALE_BRANCH against the genesis branch the reader sees), expectedGeneration = the baseline's first generation (after a correction the route answers STALE_GENERATION, which the dialog phrases), and the digest question is recorded in the PR body; targetRevision = one behind the believed head (no target picker in this slice). The spectate page stays unwired; guests still get no control through viewerRole. Rows: adapter - 200 preview passed through verbatim, 409 refused passed through verbatim, 401 {error} -> unavailable, thrown fetch -> unavailable, the POST carries all five fields and the bearer; page - the host GM's preview control is ENABLED with no unavailable reason (RED before: disabled with PREVIEW_UNBUILT_REASON), a guest token still gets no networked-gm-rewind-controls, the adapter is called with the last mirror sequence + 1. Orchestrator mutants in the PR body. NOT CLAIMED: no commit (onConfirmRewind stays unwired until 3b-iv-b); no fog; E2E-19/25 stay deferred - a preview creates no draft and finalize is 3b-iv; nothing under src/components/campaign.

  - PROGRESS (2026-09-02, Wave E6 part 2, the lobby MOUNT): NetworkedGameSurface.gmRewind.tsx mounts the rewind preview dialog behind viewerRole === 'host-gm' && !spectator (derived role, never the raw player id), with its own container testid networked-gm-rewind-controls so no locator resolves to two elements; the dialog opens only when a preview producer is wired (no page passes one yet, so the control renders disabled with its reason), stays open and phrases a refusal for the GM, treats a producer that throws as a preview it could not get, closes once on confirm and on cancel applying nothing. The two-button GM stub's no-op DEFAULTS are gone - NetworkedHostGmControls renders only when a caller supplies BOTH handlers - so the production lobby route shows no dead GM buttons (defect #15 closed on the lobby route) while the /e2e harness that actually wires them keeps its proof (finding #94). Rows: offers it to the host GM; to nobody else at the table; to no spectator even one holding the host id; mounts none of the inert stubs it replaced; keeps the GM-fix stubs for the harness that wires them; shows the blast radius the producer answered with; opens no dialog when no producer is wired; keeps the dialog open on a refusal; treats a throwing producer as unavailable; closes once on confirm; closes on cancel. Worker mutants M3 (closes on refusal) and M8 (mount gated on raw player id) killed by these rows; orchestrator mutants in the PR body. NOT CLAIMED: no live preview until 3b-iii's route is wired from the lobby page (replaced-when-emitted); confirm has no producer (3b-iv); the privacy pack's comment that the lobby stubs are no-ops is stale wording, not a red - its guest assertion (zero networked-gm-approve-btn) still holds because the guest is not host-gm.

  - PROGRESS (2026-09-02, Wave E6 part 1, the GM rewind preview DIALOG standalone): gmRewindPreviewPhrasing.ts - a total Record over the closed GmCombatRewindPreviewRefusal union yields the derived GM_REWIND_REFUSAL_REASONS list, so a member without a sentence breaks compilation and the sweep row (LAW 40); describeRewindRefusal returns only the reason's sentence - the server `detail` is never reachable as the accessible description; the blast radius is authored from BOTH typed rows (entries + changedViewerIds; the preview result carries no player-readable text at all), and zero entries says so; rewindPreviewArm / rewindConfirmArm / dispatchWhenArmed are exported choke points tested directly (LAW 38: a disabled DOM control cannot fail a row). GmRewindPreviewDialog.tsx follows E2's VetoConfirmationDialog shape (trapFocus reused, name on the role element, confirm focused when armed else the usable control, document-scoped Escape, focus restored to the opener with fallbackFocusRef); a REFUSAL while open swaps the body to the GM phrasing WITHOUT closing, moves focus to the tabIndex=-1 refusal region, and the primary becomes 'Ask the server again' (commits nothing); confirm renders DISABLED with a rendered reason while no commit producer exists (3b-iv). Digits are allowed for the identifiers and counts the GM is choosing between. Split at the prefix-green boundary because the whole seam measured 1414 raw lines: part 1 = phrasing + dialog + their suites (green standalone, touches nothing shipped); part 2 = the NetworkedGameSurface mount (replacing the two-button GM stub) with finding #94, stacked. Worker mutants 9/9 killed (one sentence for every refusal; detail as the phrasing; closes on refusal; focus never moves to the refusal; trapFocus dropped; Escape confirms; confirm armed with no producer; mount gated on raw player id; blast radius ignores changedViewerIds); orchestrator mutants in the PR body. NOT CLAIMED: no live preview (onPreviewRewind is injected and no page passes it; 3b-iii's adapter branches on the presence of `kind`, and a throw or an {error} body becomes 'unavailable'); confirm has no producer; nothing is mounted by part 1; the two route-only members no-authoritative-history and fog-preview-unsupported land with 3b-iii and need one phrasing line each plus the count tripwire 11 -> 13 (deliberately not hand-added - neither is on this tree).

  - PROGRESS (2026-09-02, Wave E2 seam 2b): live-region announcements landed - guest-decision-announcement carries the newest RESOLVED outcome only (deliberately not a region around the feed, which holds pending rows and would re-read the queue on every change); gm-decision-announcement on approve and confirmed veto, empty after a cancelled one; both say 'sent', never 'vetoed'/'committed' (the surface hands the decision to a callback and never sees an acknowledgement - the row leaving the queue is the commit evidence; one row pins the wording). Persistent text and the narrow-viewport contract were ALREADY correct on main (one setTimeout in the coop tree and it is a Storybook mock; the decision row already declares flex-wrap) - those rows are regression pins and their file header says so. VACUITY CAUGHT: the persistence pin passed with a 5-second auto-dismiss mutant in place because jest.advanceTimersByTime fires the callback but leaves React's state update unflushed; wrapped in act(), the same mutant now kills it (law: a pin asserting that something did NOT happen must prove the mechanism that would make it happen actually ran). Mutants killed: guest region loses aria-live/aria-atomic, oldest decision announced, host never records, refusal never renders, decision row scrolls, refusal auto-dismisses (survived once - fixed red-first), orchestrator N1 guest announcement drops the outcome (2 red), N2 host region muted with aria-live=off (1 red). STILL OPEN in this box: keyboard-complete rewind preview/confirmation (needs the combat rewind command path - Wave C PR 3) and focus management after errors on the rewind surface; the veto confirmation (2a) covers the decision half only.

  - Progress (2026-09-02, Wave E2 seam 2 cut A - box stays OPEN for announcements, persistent text and the narrow-viewport contract in cut B): KEYBOARD-COMPLETE CONFIRMATION on the veto (the only destructive GM authority genuinely wired today - manual takeover / GM fix fall through to no-op defaults in production, defect #25, so a shell over them would confirm nothing; the rewind preview body is 18.3): VetoConfirmationDialog - focus enters on Confirm, Shift+Tab reaches Cancel, Tab wraps both ways, Escape cancels, focus returns to the opener on close with a fallback to the surface when the opener's row is gone after a commit; FOCUS MANAGEMENT after refusals - when a refusal frame disables the Approve button the host stood on, focus moves to the recovery action rather than dropping to body, and never steals focus from a host working elsewhere. FINDING #31 (production bug, FIXED): src/utils/accessibility.ts trapFocus filtered focusables by offsetParent !== null, which is null for ANY position:fixed element (i.e. every modal) - the filter emptied itself and the trap silently did nothing; its five existing suites dispatch a key, call cleanup and assert NOTHING (accessibility.part01.test.tsx 'should trap Tab focus within element' and four near-duplicates - flagged, not fixed here). Replaced with hidden / aria-hidden filtering that means the same thing in the browser and under jsdom; 266 existing a11y tests unchanged. The copied DialogTemplate pattern lacks a focus trap AND a focus return (its aria-labelledby sits on the content div, not the role=dialog element - axe flags it) - both added here, recorded in the component header. GmReachableState narrows GM_MESSAGES and IGmLifecyclePosture.state to the five reachable postures; refusalState returns RefusalPostureState. One shipped row changed on purpose ('forwards a veto decision' -> 'once the host confirms it'; same onDecide arguments). Worker mutants killed (6): veto decides without confirming (12 red), focus never restored, rescue fires regardless of focus, accessible name removed (axe), Escape no longer cancels, offsetParent filter restored; orchestrator: Escape confirms, focus never enters, offsetParent re-run (2 red). The worker caught its own vacuous Tab row (dispatch on document never reached the element-scoped listener; the containment assertion was true before the key) and rewrote it to assert the wrapped-to element's identity. Gates: tsc 0; oxlint 77-pin (an exhaustive-deps hit fixed at the cause, not suppressed); campaign/common/multiplayer/lib/utils/a11y trees 707 suites / 10837.

- [x] 19.4 Verify affected component tests, `npm.cmd run verify:qc:viewport-sweep`, and accessibility assertions for every lifecycle state.
  - DONE (2026-09-03, audit Wave 0): the box's three clauses are satisfied and the last gap in it was one token. The affected component tests and the accessibility assertions already enumerate all ten lifecycle states (19.1 is closed and its suite covers them), and the cross-product sweep in campaignLifecycleState.test.ts drives both derivations over every refusal and proposal count. The one thing missing was that the sweep's RESERVED_STATES list named only 'rewound', so nothing asserted that a campaign surface never reports 'sealed' - a state the module's own comment says is absent by construction because a campaign has no declare-then-reveal phase. 'sealed' now joins the list. HONEST ABOUT ITS STRENGTH, because a receipt that dressed this up would be the thing this change keeps catching: the row lands GREEN on the day it is added and could not have been red. The host posture cannot name the state at all (its type is GmReachableState, which excludes it at compile time) and the guest producers are exhaustive over a narrowed union, so the sweep now confirms a property the type system already enforces rather than falsifying a reachable path. It earns its place as the guard that notices if a future producer widens either union. Verification: the viewport sweep ran green on a tree with a real install (6 + 12 + 51 rows passing; a first attempt in a worktree without node_modules died in Playwright's webServer boot, which is the tree and not the product). The 2026-08-24 blocker recorded on this box ('19.4 closes when 19.1 builds the remaining four') is discharged - 19.1 is closed and the suite enumerates all ten. Not claimed: no new component was covered and no accessibility assertion changed; this box closes on coverage that already existed plus the one reserved-state addition.

  - EVIDENCE (2026-09-02, post-#1540 `npm run verify:qc:viewport-sweep` on main 84d88784f): 51 collected, 50 passed; the two co-op titles RUN AND PASS - 'pack-seeded (coop-host-review) > campaign detail' (ok 34) and 'pack-seeded (coop-guest-proposal) > campaign finances' (ok 50) - so the co-op surfaces are swept for the first time. The one failure is the FIRST test of the run, 'Viewport layout sweep > dashboard', on page.goto Timeout 30000ms: the cold dev-server compile of the first route exceeded Playwright's 30 s test timeout (finding #60 - harness class, same as the guest parity spec's 30.8 s cold compile earlier; both pass on a warm server). The Wave E3c hotfix makes the first navigation pay the compile outside any measured test without hiding product slowness. Remaining opens on this box: the GM 'pending' posture is sweepable only when the sweep is two-browser (21.x/25.x); the accessibility half is complete (seam 2b).

  - EVIDENCE + FINDING #39 (2026-09-02, post-#1531 run on main e9e592762 + Wave E3b hotfix): manifest.guard.spec.ts + coop-host-review.parity.spec.ts + coop-guest-proposal.parity.spec.ts GREEN on main (4 passed; the guest spec failed once on a 30.8 s cold route compile exceeding page.goto's 30 s timeout and passed on rerun at 12.4 s - harness cold-compile flake, not product). `npm run verify:qc:viewport-sweep`: 49 passed in 3.5 min - BUT neither the campaign-detail nor the finances screen appeared in the matrix: viewport-layout-sweep.spec.ts iterates pack-seeded entries with `entry.pack === 'navigation'` inside a describe whose beforeAll loads navigation-briefing, so the two entries #1531 repointed at the co-op packs were iterated by NO block - the repoint LOST two routes from the sweep instead of adding surfaces, and nothing went red because no guard asserted that every PACK_SEEDED_SWEPT_ENTRIES entry is swept (a third #21-class miss on the same feature: inventory and spec each individually fine, the relationship untested). Hotfix E3b: grouping moved to a pure module (packSeededGroups.ts) that BOTH the spec and a jest guard import (one describe per campaign pack, each loading its own pack), guard rows red-first: every direct-goto entry's pack is in an iterated group; every short pack name maps to a registered manifest id; a mapping is bound to a route the group actually sweeps (added after the registration-only row let 'guest -> navigation-briefing' survive); an entry on an unmapped pack lands in NO group (added after the orchestrator's complement-filter mutant survived - the positive combat filter was a design intention with no row; LAW: a filter written as the complement of a map is vacuous on today's data, guard it with a synthetic unknown member). `playwright --list` collects 51 titles (main: 49) - the two new ones are 'pack-seeded (coop-host-review) > campaign detail' and 'pack-seeded (coop-guest-proposal) > campaign finances'; the collection count is the standing gate for the group loop. The post-hotfix sweep result on main is recorded here when it runs; also gap: package.json verify:qc:scenario-packs enumerates parity specs by name and omits the two co-op specs (E2 folds the two-line edit into seam 3).

  - PROGRESS (2026-09-02, Wave E2 seam 2c, findings #34/#35/#36): the co-op surfaces are now real sweep targets - coop-host-review pack (campaign detail route -> host-gm-review-surface, swept in its EMPTY state, posture live) and coop-guest-proposal pack (finances sub-route -> guest-proposal-surface + campaign-sync-state, posture blocked/missing-token). Shape forced by the inventory: an entry names exactly one pack and the guard requires exactly one classification per route, so the two existing entries are REPOINTED at the packs with the co-op surfaces added as overlap targets and the campaign-nav affordances kept; the other four mutation sub-routes stay on navigation (one route sweeps a surface; repointing all five swaps seeded state for no coverage). Two packs because coopSession.mode is single-valued (#34). No 'coop' subsystem tag: PACK_SUBSYSTEMS is derived from the subsystems registered flows exercise, so the guard refused it and no spec delta is needed (#35 - the seventh tag needs a registered co-op FLOW first). No minter can produce these packs (flow-checkpoint minter needs a registered flow; fast-forward minter never opens a co-op session), so both are hand-authored with honest provenance (genesisSource hand-authored:harden-gm-two-player-campaign-sessions@19.4-coop-{host,guest}, real mintedAt, real baseCommit) and a row pins the host pack does not inherit navigation-briefing's originDeviceId (#36). Red-first on main's pack set (HOST and GUEST rows red, directory-found row green). Mutants killed: host pack loses coopSession, guest flipped to host, host inherits another pack's identity. ORCHESTRATOR MUTANTS SURVIVED FIRST (#21-class miss - a guard that cannot see the wiring it guards): N1 guest manifest entry loading the HOST payload and N2 guest entry pinning schemaVersion 2 both left jest green because the coverage guard read pack files off disk and never touched the manifest. Fixed red-first with 'co-op manifest wiring' rows that walk SCENARIO_PACK_MANIFEST, load each co-op entry's payloadPath and assert mode-matches-id, pin-equals-payload-schemaVersion and distinct payloads (N1 -> 2 red, N2 -> 1 red, both re-run by the orchestrator). Writing that guard was the first time anything runnable LOADED the manifest: its module-load schema threw on the hand-authored parityAnchorJourney value, i.e. the 2c entries as first staged would have crashed the sweep on import - corrected to the legal anchor: form (genesisSource stays the honest hand-authored string). LAW: a test asserting a relationship between two artifacts must load both through the path production uses. NOT CLAIMED: the GM 'pending' posture stays jest-only (in-memory proposals arrive from a guest over the transport; no page.evaluate exception - sweepable only when the sweep is two-browser, 21.x/25.x); the Playwright guard + the sweep were NOT run in the staging worktree (no dev server) - the post-merge sweep result on main is recorded under this box when it runs. Box stays open until that sweep evidence exists.

  - PROGRESS (2026-09-02, Wave E2 seam 2b, finding #32): the accessibility half is complete - campaign covers pending/finalized/rewound/rebuilding derived through the real lifecycle functions (one row asserts the derivation itself so a posture the product cannot build cannot pass), GM covers pending, tactical has the no-digits parity row; 'sealed' is absent from campaign and GM BY CONSTRUCTION (no declare-then-reveal phase exists there), tactical covers it. The viewport-sweep half is NOT met: no scenario pack seeds a coopSession (grep -rln coopSession e2e/scenario-packs/ is empty), CampaignCoopRouteSurface returns null without one, so host-gm-review-surface, guest-proposal-surface and campaign-sync-state are unreachable in the sweep and the green sweep at PR A's head exercised none of them; the campaign-detail route itself IS swept, which is exactly what made the gap invisible. Recorded as a NOTE on the inventory entry, not a QuarantineEntry - a quarantine is a suppression that would have switched off the clickable check that currently passes on that screen. Closes at seam 2c: host + guest co-op packs (one per coopSession mode - finding #34), manifest entries, schemaVersion pin, real CheckTargets. The GM 'pending' posture stays jest-only by decision: proposals are in-memory and arrive from a guest over the transport, so a postLoadAction could only reach it via page.evaluate, which breaks the front-door promise the loader header makes (design D8); it becomes sweepable only when the sweep is two-browser (21.x/25.x).

  - Progress (2026-09-02, Wave E2 seam 1 - box stays OPEN for the a11y-per-posture half of seam 2): `npm run verify:qc:viewport-sweep` run by the orchestrator in the e2e worktree at the PR A head 74bd093a6 - 6 + 12 + 51 passed (4.0m), incl. every pack-seeded campaign route and the mission launch briefing; the affected component suites (src/components/campaign 27 suites / 200, plus common/multiplayer/lifecycle trees 148 suites / 1152) green at both heads. Accessibility assertions exist for every posture shipped so far (polite live regions incl. blocked, on all three surfaces); the keyboard-complete confirmation shell, focus management and narrow-viewport primary actions (19.3) and their assertions are seam 2.

  - Progress (2026-08-24, STAYS OPEN — see the blocker at the end): found and fixed a real accessibility defect while writing the assertions this task asks for. `CampaignSyncStateBanner` — the strip that tells a player whether to trust what is on screen — was a plain `<p>` with NO live region. A sighted player watches it flip from "up to date" to "reconnecting"; a screen-reader user got nothing at all, because a status that changes outside a live region is announced to nobody. It now carries `role="status"`, `aria-live="polite"`, and `aria-atomic="true"`. POLITE DELIBERATELY, including for `blocked`: assertive cuts off whatever the reader is currently speaking, a sync posture is never more urgent than the sentence the player is in the middle of, and the blocked state also disables the controls so it is discoverable by trying rather than only by hearing. ATOMIC because without it a reader may announce only the CHANGED words, turning "you are up to date" into a fragment that means nothing alone. New a11y suite (`CampaignSyncStateBanner.a11y.test.tsx`, 14 rows, jest `a11y` project): axe-clean for all SIX existing states rather than one representative, because the per-state tone classes are exactly the kind of thing that silently breaks contrast for a single posture - and the broken one would be a failure state, which is when the message matters most; live-region announcement asserted BY ROLE rather than by testid, so it pins the accessibility tree a screen reader actually walks instead of a hook only tests can see; an explicit row that the region is never assertive; and a row that no state's announcement contains a DIGIT, because a spoken "12 events behind" rebuilds the same inference channel tasks 3.2/3.4 closed for the printed one. Falsified 1:1: changing `role="status"` to `role="presentation"` turns 8 of the 14 rows red. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; a11y project 7 suites / 53 tests; campaign-coop + replica units 15 suites / 77 tests; **`npm run verify:qc:viewport-sweep` 51 passed (3.5m)**, which is the second of this task's three named gates; FULL unit suite 2410 suites / 33061 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task requires a11y assertions for EVERY lifecycle state, and 19.1 names nine — pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, blocked. Four of those cannot be asserted because they do not exist to render: a case-insensitive grep over non-test `src/components` and `src/lib/campaign` finds `sealed` in 0 files and `rewound` in 0 files, with `finalized` (1) and `rebuilding` (2) present only outside the lifecycle UI. Writing assertions for states nothing renders would be theatre. The six states that DO exist are now covered end to end; 19.4 closes when 19.1 builds the remaining four, and this note is the record of exactly which ones are owed.


## 20. Three-Context Sandbox Foundation

- [x] 20.1 Add `e2e/fixtures/gmTwoPlayerCampaign.ts` with isolated contexts reserved for the future non-playing GM, Player 1, and Player 2, plus a harness-owned server, per-run database, distinct identities, and deterministic seeds. The foundation proves isolation first; Task 9 durable membership later binds those contexts to actual roles and must pass `membership-smoke` before role admission is claimed.
- [x] 20.2 Add test-only fault controls guarded by `NODE_ENV === 'test'`, E2E run ID, explicit session scope, and one-shot consumption; make production startup reject enabled controls.
  - Receipt (2026-08-24): `src/lib/testing/faultControls.ts`. A fault-injection switch is a remote kill switch wearing a helpful name, so arming one requires FOUR things to line up and each closes a different leak: the build must be `NODE_ENV === 'test'`; a RUN id, so one Playwright run cannot arm a seam another run then trips over (which is what makes parallel runs safe); a SESSION scope, so the fault hits one session and a healthy control session stays available to prove the failure was CONTAINED rather than global; and ONE-SHOT consumption, because an armed-until-disarmed switch outlives the test that armed it and fails the next one for no visible reason. Arming outside a test build THROWS rather than no-opping - a silent no-op would let a fault test "pass" on a production build having proven nothing at all - and consumption returns false for every near miss (wrong run, wrong session, wrong seam, already consumed, not a test build), because a seam that fires on an approximate match is worse than no seam. A partially-scoped control is refused outright: it looks armed and its blast radius is whatever the missing field would have narrowed. PRODUCTION STARTUP REFUSES, and refuses at the REAL entrypoint: `server.js` now exits 1 with `FAULT_CONTROLS_IN_PRODUCTION` when `MEKSTATION_FAULT_CONTROLS` is set outside a test build. Exiting rather than ignoring is deliberate - a process that boots and merely declines to honour fault config is one refactor away from honouring it, while one that refuses to start cannot rot that way. The guard is a hand-written MIRROR because `server.js` is CommonJS and cannot import the TS module, so a sibling test SPAWNS `server.js` itself rather than trusting the mirror; drift between the two would otherwise be invisible. The guard is "not a test build", not "is production", so a dev server carrying fault config is refused identically. Environment is INJECTED rather than read globally, so exercising the production path cannot disturb other tests sharing the worker (it also removed a `NODE_ENV` readonly type error rather than casting past it). Falsified 1:1: neutralising the startup guard turns both rejection rows red while the no-config CONTROL row stays green - without that control every rejection row would pass against a server that simply fails to boot for an unrelated reason, which on a machine with no build it otherwise would. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; fault-control suites 19/19 (16 scoping + 3 real-process startup); FULL unit suite 2408 suites / 33037 passed / 0 failed. NON-CLAIM: this ships the CONTROL, not the seams. No production call site consumes a fault yet - wiring `store.commit`, `outbox.insert`, and head-update seams belongs to the tasks that build those paths (plan todo 28 and leaf-4 task 2.4), and `MEKSTATION_FAULT_CONTROLS` is currently read only by the startup refusal.

- [x] 20.3 Add a dedicated SQLite evidence reader opened `readonly: true, fileMustExist: true`; never instantiate the production store for read-only proof.
  - Receipt (2026-08-24): `e2e/fixtures/sqliteEvidenceReader.ts` opens its own connection with `readonly: true, fileMustExist: true` and imports NOTHING from the production persistence layer. Both options are load-bearing for a reason worth stating: the production store CREATES its file and runs migrations, so pointed at a database the run never produced it would manufacture an empty one and report zero rows - evidence of absence conjured from nothing, which is the exact shape of a false pass. `fileMustExist` turns that into a thrown `EVIDENCE_DB_MISSING`, and the row proving it asserts the file was NOT brought into existence, not merely that the open threw. `readonly` makes immutability a database-level guarantee rather than a convention, and the byte-equality row compares the file BEFORE and AFTER a full probe (count + select + tables) as bytes rather than row counts, because a probe that rewrote a page while leaving rows alone would still make the artifact describe a different file than the run produced - which is what the plan's evidence gate means by identical hash and schema. Two refusals are named rather than left to the driver: a non-SELECT statement is rejected as `EVIDENCE_WRITE_REFUSED` with the offending SQL (the connection would refuse it anyway, but as "attempt to write a readonly database", which does not say what was attempted), and a table name that could carry SQL is rejected outright because table names cannot be bound as parameters. WIRED, NOT SHELVED: the fixture exposes `openEvidence('app' | 'multiplayer')` over the run-owned paths `assertRunOwnedPath` already validated, and the `fixture-smoke` gate now reads its own database through it and asserts the hash is unchanged - so the reader is proven against REAL run databases end to end, not only against synthetic ones. Falsified 1:1: flipping `fileMustExist`/`readonly` off turns the missing-database row red. A SELF-CORRECTION worth recording: the guard proving no production dependency first banned the string `DurableMatchStore` anywhere in the file, and failed on the module's own comment EXPLAINING why it is not used; it now scans import lines only, so it fails on a dependency rather than on prose. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; reader suite 6/6; `fixture-smoke` and the 20.5 ownership spec both green on real builds; FULL unit suite 2405 suites / 33011 passed / 0 failed. NON-CLAIM: this ships the reader and one live use of it; the per-table durable-row assertions the program's evidence bundle needs (receipts, batches, outbox, participants, cursors, branches, outcome receipts) arrive with the tasks that create those tables.

- [x] 20.4 Add role-labeled traces, screenshots, raw socket transcript, pre-serialization projection capture, latency timestamps, durable-row export, state/projection hashes, environment manifest, and cleanup log under `test-results/gm-two-player/<run-id>/`.
  - Receipt (2026-08-24): `e2e/fixtures/gmTwoPlayerEvidence.ts` writes the declared bundle under `test-results/gm-two-player/<run-id>/`, role-labeled in the FILENAME so three contexts are distinguishable from a directory listing rather than by opening every file, with a sha256 and byte count per artifact. Two properties make such a bundle trustworthy and both are easy to lose silently. (1) IT CANNOT WRITE OUTSIDE ITS RUN - every path goes through the same `assertRunOwnedPath` the sandbox already uses, because a writer that escapes clobbers another run's evidence or a developer's files and the resulting artifact describes a run that never happened; a `../../../` name is refused. (2) IT CANNOT CARRY SECRETS - the bundle captures raw socket transcripts and pre-serialization projections, which is exactly where a bearer token or password lives, and an evidence file is written to disk, uploaded as a CI artifact, and read by people who were not in the run, so a leak there is a leak everywhere. The scan matches the SHAPE of a credential (password/token fields, `Bearer`, private keys) rather than known values, because a list of known secrets contains only the ones already known about; the refusal does NOT echo the match into its error, since an error message is itself logged and repeating it would defeat the refusal it just performed. A CONTROL row writes ordinary prose that merely mentions "token" and must still be accepted - a guard that refused every transcript containing the word would be switched off within a week. (3) THE MANIFEST DECLARES INTENT, not inventory: `recordMissing` puts an artifact the run could not capture into the manifest with a reason, so a missing file is a DETECTABLE gap; a manifest listing only what exists cannot distinguish "never captured" from "captured and lost", which is the difference between a gap and a bug. WIRED AND PROVEN LIVE: the fixture hands out `openEvidenceBundle()` and the `fixture-smoke` gate now writes a per-role artifact and finalizes a manifest; verified by reading the real bundle off disk after the run (3 role-labeled files plus `manifest.json`, every sha256 64 hex chars, environment carrying node version and run id). Falsified 1:1: neutralising the secret scan and the run-owned path check turns 6 rows red while the 4 structural rows stay green. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bundle suite 10/10; `fixture-smoke` green end to end on a real build; FULL unit suite 2409 suites / 33047 passed / 0 failed. SECTION 20 IS NOW COMPLETE (20.1-20.5). NON-CLAIMS: the bundle is the WRITER plus one live use - actually capturing traces, screenshots, socket transcripts, latency series, and durable-row exports happens in the tasks that produce those things (Playwright tracing/screenshots ride Todo 26's runner; socket transcripts and pre-serialization projections need the seams of sections 5 and 11; durable-row export needs the tables of section 2). The secret scan is a shape heuristic, not a proof of absence: it catches the credentials this program actually handles and would not catch an unrecognised encoding.

- [x] 20.5 Add ownership-scoped cleanup that preserves ambient browser tabs, Chrome processes, unrelated servers, databases, and user artifacts.
  - Receipt (2026-08-24): writing the proof found a REAL defect rather than confirming the implementation. The context half was already scoped correctly - `cleanup()` closes only the three contexts it opened - but the IDENTITY half was not, and nothing had ever asserted it. Seeding an identity through `POST /api/e2e/vault-identity` calls `repository.setActive`, whose SQL is `UPDATE vault_identities SET is_active = 0` across the WHOLE table; the sandbox seeds three, so it deactivated whatever identity the machine had, and then deleted its own three on teardown. End state: NO active identity at all. `getActive()` returned null and `/api/multiplayer/auth/token` answered `404 No vault identity configured` - a developer's own vault session silently stopped working after running this suite, which is exactly the "preserve ... user artifacts" this task forbids. FIX: the fixture now reads the prior active id BEFORE its first seed (new read-only `GET /api/e2e/vault-identity`, id-only because the caller needs to restore the identity, not learn about it) and hands it back through `DELETE`'s new `restoreActiveId`, which reactivates AFTER the deletes so the final state is the one the caller asked for even when the restore target was also in the delete list. A restore target that no longer exists is REPORTED (`restored: false`), not thrown - the harness cannot know whether something else removed that identity meanwhile, and failing teardown over it would turn a tidy-up into a test failure. PROOF (`e2e/gm-two-player-cleanup-ownership.spec.ts`, 2/2 on a real build): an ambient context and an ambient active identity survive a full fixture lifecycle; the fixture's own three contexts are gone and the bystander's is not; the ambient PAGE is re-navigated rather than merely found in the list, because a context object can outlive its pages; and a second `cleanup()` destroys nothing further (specs that clean up in both `finally` and an error path call it twice). A control asserts the fixture actually ADDED contexts, so the preservation rows cannot pass against a fixture that created nothing. Falsified 1:1 - dropping `restoreActiveId` turns both rows red. HONEST NOTE ON METHOD: the first version of this spec probed identity liveness by minting a token and asserted an "unrelated identity survives", which failed - and the failure was mine, not the fixture's: identities are a collection with ONE active row, so `getActive()`-backed auth answers about activation, not existence. Chasing that wrong probe is what surfaced the actual defect. Verification: tsc clean; oxlint 0 errors / 78 warnings, unchanged from main at `c7fba3db7` (the count moved 77 to 78 when PRs #1342-#1343 landed, not here); api/vault/scripts batteries 117 suites / 1784 tests; the endpoint's own unit suite grew 4 to 8 rows covering the GET, the null case, a successful restore, and an unrestorable target; the `fixture-smoke` gate still passes end to end; FULL unit suite 2404 suites / 33005 passed / 0 failed. NON-CLAIMS: Chrome PROCESS preservation and unrelated-server/database preservation are asserted only insofar as the fixture never targets them - there is no positive process-level assertion, and adding one needs the fault-control seams of task 20.2; this closes the browser-tab and user-artifact halves of 20.5 with evidence and leaves the process half named rather than implied.


## 21. Strict Acceptance Scenarios E2E-01 Through E2E-45

- [ ] 21.1 Implement and pass E2E-01 through E2E-18 for durable creation, order, atomicity, idempotency, replay/live recovery, cursor integrity, slow-client isolation, restart, reauthentication, active routing, and heartbeat.
  - DECISION D2 + PROGRESS (2026-09-03, fix wave, E2E-01/E2E-02 now EXECUTE): the council of this date ruled the journal-authority flag an engineering matter, not a product decision. CAMPAIGN_JOURNAL_AUTHORITY_ENABLED stays a hardcoded false in production; a resolver (src/lib/campaign/sync/campaignJournalAuthorityEnabled.ts, isCampaignJournalAuthorityEnabled) answers true ONLY when NEXT_PUBLIC_E2E_MODE === 'true' AND MEKSTATION_E2E_CAMPAIGN_JOURNAL_AUTHORITY === '1' together - a production process can never satisfy the first key and a test run has to ask for the second. All three marker producers read the resolver instead of the constant: PUT /api/campaigns/:id, the adopt route, and the co-op match creation route (ORCHESTRATOR-FOUND: the worker missed the match route, whose creation checkpoint skips its genesis-branch step whenever the flag it is handed is false, so a journal-native campaign would have created a match with no genesis branch to recover). The authority-recovery group starts its server with the variable set (e2e/core.cjs) and playwright.config.ts forwards it; the two test.fail marks on E2E-01 genesis-branch-recovers and E2E-02 effective-branch-remains-authoritative are removed (the mark is strict and would go red the moment the rows passed); no other expected-failure row is touched; the qc pin follows the group. Mutants, all KILLED: e2e-mode key dropped; env key dropped (4 red); match route back on the raw constant (SURVIVED the worker rows - new row in multiplayerCoopCreationCheckpoint arms both keys against a campaign with no genesis marker and expects creation refused 500, now killed); NOVEL PUT route back on the raw constant. LIVE PROOF (run d2-proof-1, l89, production build of this branch, --group=authority-recovery): 4 passed / 0 failed in 42.8s - E2E-01 durable campaign cold recovery, E2E-01 genesis branch recovers, E2E-02 participant ownership survives restart, E2E-02 effective branch remains authoritative, the last two GREEN for the first time. Gates: tsc 0; oxlint 77-pin; resolver + PUT genesis + checkpoint + qc suites 15/15. NOT claimed: E2E-03..18 unchanged; the production default is unmoved; combat rewind (14.x) does not ship in this change per the same council - that re-gate is the owner ruling, not enacted here. CI ADDENDUM (same day): once 9.1's two creation rows merged into the same suite, the six handler calls from one fake client crossed the route's 5-per-minute mutation limit and the last row (this one) was answered 429; the limiter's bucket map was module-private, so security.ts gains _resetRateLimitBucketsForTests beside the other _reset helpers and the suite clears it before every row - the production limiter is untouched.
  - PROGRESS (2026-09-02, Wave G 21.1 PR3-b, the E2E-14 BROWSER half): resilience-pack row `E2E-14 a viewer that stops acknowledging is bounded and recovers alone` (@E2E-14). Three contexts reach Movement through the shared launch drive; a routeWebSocket on Player 2's page swallows only its outgoing DeliveryAck frames; the GM and Player 1 keep commanding until Player 2's unacked window - read from the per-run sqlite as issued minus acked, exactly as the product computes it - reaches MAX_VIEWER_UNACKED; then the letter is asserted: the GM's and Player 1's delivery rows keep growing and their pages keep rendering the new phase while Player 2's issued freezes at or just past the cap and its page shows the behind posture; recovery lets the acks flow again and Player 2 catches up to the same phase with its ack cursor resumed contiguously from the first missed authority and each effect rendered once; no bearer in any URL. Live result in the PR body. Product mutants (first red): MAX_VIEWER_UNACKED = Infinity -> Player 2 never freezes; isolation applied to every viewer -> the healthy seats stop growing; isolation never cleared -> Player 2 never catches up; resume from the live head -> a gap in Player 2's ack table. Lesson recorded: the first live run failed for two reasons that were not the row's - the branch predated the fault-scope fix (#1594) and the launch helper clicked a generic Ready button instead of reusing the pack's drive - both fixed before this landed. STILL OPEN on 21.1: the genesis clauses of 01/02 (5.7) and SPEC_BY_GROUP.authority (21.4).
  - PROGRESS (2026-09-02, Wave G 21.1 PR3-c): E2E-01 and E2E-02 are authored TO THE LETTER in a new respawning pack e2e/gm-two-player-authority-recovery.pack.spec.ts (group authority-recovery, owner 21, in RESPAWNING_GROUPS because both rows kill the host with the scoped fault lever; registered and pinned). Each row is split in two: a PLAIN row asserting what the product already recovers after a process death and three reloads (E2E-01: the campaign id/name, the GM seat plus two player seats with revoked_at NULL, an authorized baseline; E2E-02: two player slots still bound, the same force owners on campaign_session_force_claim, the readiness revision in mp_matches.meta_json unchanged, the mp_viewer_delivery cursors covering the same players and never rewound), and an EXPECTED-FAILURE row tagged @until-journal-cutover carrying the genesis-branch clause (one event_history_branches row for the campaign stream, status effective, ancestor_depth 0, parent NULL / the effective branch unchanged) marked test.fail('red until the journal cutover (task 5.7): no production path writes campaign events to the journal, finding #48') so nightly CI - which runs every chromium spec and does not ignore gm-two-player* - stays green, and an UNEXPECTED PASS is the flip signal when the cutover lands. Neither the letter nor the product was weakened; no test.skip. Live result of --group=authority-recovery in the PR body. STILL OPEN on 21.1: the genesis clauses (5.7); the E2E-14 browser row (PR3-b, in flight); SPEC_BY_GROUP.authority stays NOT_IMPLEMENTED until every 01-18 title exists (21.4).

  - FINDING #100 (2026-09-02, found by running the packs alone under the new `all` selector; CLOSED in the same PR): restart-pack E2E-05/E2E-06 and resilience-pack E2E-15 armed the fault lever with { kind, mode } only, and since #1550 (the lever carries the session scope its own contract requires) the arm answers 400 'A fault arm requires a session scope: matchId is required' - the three rows were red on main from that merge on. Their arm helpers now send { kind, mode: 'once', matchId } for the launched match exactly as the fault/failure packs do; the lever is unchanged. Live alone after the fix: restart-pack 2/2, resilience-pack 1/1 (the first restart rerun died in the shared launch helper's cold-start token read, the known flake, and passed on the next run).

  - FINDING #99 (2026-09-02, found by the C1c-ii rows after #96/#97 landed; CLOSED in the same PR): ReadStreamQuerySchema still pinned branchId to root, so every candidate-branch READ was refused at the schema with 'Invalid input: expected "root"' while the SQL underneath already filtered by branch_id and the stored rows were legal after #97. Lifted to DurableIdSchema with the reason in the comment. The rows that died on it and now pass: R3 sequence differs from revision when a command is dropped; R5 materializeBranchPath accepts the replayed candidate; R6 the reader reads the candidate, not root. The in-memory journal already honored the branch on reads.
  - PROGRESS (2026-09-02, Wave G 21.1 PR1): the three authority.pack1 rows that already bore E2E-18 / E2E-03 / E2E-08 titles now also carry the row tag next to @authority-e2e (grep selection is the title tag, #76). NEW ROW E2E-17 on the token pack (@token-pack @E2E-17): drive launchOneVersusOne through LaunchMatch keeping the pre-launch room code; cold-reload the guest on the lobby URL; a third context joins with the stale code. Asserts the letter: mp_matches.room_code is NULL after launch (LaunchMatch clearRoomCode); status active with two playerIds; the reloader reaches the game surface with the SAME playerId/seat (stored matchId fallback + durable membership path); the stranger's invite GET is 404 'Invite code not found or expired', the lobby join alert refuses, and the campaign join wire answers the unknown-room-code refusal; status/playerIds/room_code unchanged after the row; no bearer in any URL (the E2E-16 sweep). Live result recorded in the PR body. Mutants (for the orchestrator's record, not run against product): clearRoomCode dropped -> first red is room_code NULL; membership path skipped -> first red is the guest's playerId after reload. STILL OPEN on 21.1: 01/02 (genesis branch + membership recovery; go red until the journal cutover), 04/07 (authority-order pack, PR2), 14 (a bounded per-viewer unacked cap does not exist; product seam, PR3). SPEC_BY_GROUP.authority stays NOT_IMPLEMENTED until every 01-18 title exists.
  - PROGRESS (2026-09-02, Wave G 21.1 PR2): new live pack e2e/gm-two-player-authority-order.pack.spec.ts, group authority-order (owner 21, NOT respawning - no row kills the server), registered in GROUP_CATALOG + SPEC_BY_GROUP and pinned by scripts/__tests__/gm-two-player-campaign-qc.test.ts (the pin went red first: authority-order absent, Expected 28 / Received 27). E2E-04 (@authority-order @E2E-04): AdvancePhase at Movement derives more than one event; BEFORE any phase poll the multiplayer sqlite shows exactly one new mp_command_receipts row with last = first + event_count - 1, event_count equal to the mp_match_events rows in that range and > 1, and the receipt plus the fixture file hash unchanged on a second read; only then do both pages show Weapon Attack (DurableMatchStore.appendCommandBatch: identity, events, outbox, receipt, head in one transaction). E2E-07 (@authority-order @E2E-07): two GoProne intents fired with Promise.all from the two player contexts; two receipts with distinct command_id and actor_id; the revision ranges wholly before or wholly after each other (no interleave); host, guest and spectator show the same Movement phase and each unit's effect exactly once (ServerMatchHost.intentChain serializes validate/reduce/batch/publish per match). Live result in the PR body. Mutants (product, first red): batch transaction split -> E2E-04 receipt census; intent chain removed -> E2E-07 wholly-ordered ranges; receipt event_count hardcoded 1 -> E2E-04 arithmetic. STILL OPEN on 21.1: 01/02 (genesis branch + membership recovery; red until the journal cutover), 14 (the bounded per-viewer unacked cap is a product seam). SPEC_BY_GROUP.authority stays NOT_IMPLEMENTED until every 01-18 title exists.
  - PROGRESS (2026-09-02, Wave G 21.1 PR3-a, the E2E-14 PRODUCT half): the bounded per-viewer delivery queue exists. The bound is UNACKED FRAMES, not bytes and not socket.bufferedAmount (#19: that stays 0 on match traffic): unacked = issued - 1 - lastAcked (issued when never acked); MAX_VIEWER_UNACKED = 64 on the cursors module (a healthy client acks within a handful of frames; the cap absorbs a hitch or one phase-advance burst; Infinity would never isolate). ViewerDeliveryCursors.admit runs once, before assign, in the assign/send loop: when a viewer's unacked window reaches the cap THAT viewer is isolated (no further live assign/send; the first refused authority is remembered so firstMissedAuthoritySequence names the gap) while every other viewer keeps being assigned; handleDeliveryAck clears the isolation when unacked drops under the cap and resumes the viewer through the SAME path a reconnect uses (handleSessionJoin from firstMissedAuthoritySequence) - never from the live head. Rows: unacked counts issued minus acked and trips the bound for that viewer only; a fresh ack below the bound clears the isolation; withholding Player 2's acks isolates Player 2 while Player 1's issued keeps growing (the harness had to learn that handleIntent rate-limits per connection - 20 intents per bucket - so the drive uses a fresh connection id per step and a turnLimit of 0; a guard now names the last frame kind if a drive ever stops issuing); one ack from the isolated viewer resumes it from firstMissedAuthoritySequence, not the live head; the GM is never isolated by another viewer's backlog. Orchestrator mutants in the PR body. NOT CLAIMED: the E2E-14 browser row (Player 2's DeliveryAck dropped by routeWebSocket while the GM and Player 1 keep rendering, then one ack recovers) is PR3-b on the resilience pack; no campaign channel; bufferedAmount untouched.

  - FINDING #96 (2026-09-02, Wave C3 3c-heads, CONFIRMED by an it.failing row, not inferred): boot recovery treats a C1a-seeded candidate head as journal corruption. SQLiteEventJournalRecovery.verifyHeads compares the head-row count to the number of distinct (stream, branch) event chains derived from stored events; a candidate head seeded at its base revision has a row but no events on that branch yet, so heads = 2 and chains = 1 and recovery answers corrupt('Stream head count differs'). Row: SQLiteEventJournalRecovery.test.ts 'finding #86-adjacent: a C1a candidate head with no events is not journal corruption' (it.failing until fixed). Latent today - nothing in production mints a candidate yet - but it MUST be fixed before 3b-iv ships a rewind commit, since the first rewind would make the next boot refuse the journal. Owner: its own seam (recovery must reconcile candidate head rows against event_history_branches - a branch whose head sits at its recorded base with zero events is a seed, not corruption); #86 CLOSED by this seam.
  - CLOSURES (2026-09-02, Wave C4 recovery reconcile): #96 FIXED and #97 FIXED, one seam. #96: SQLiteEventJournalRecovery partitions head rows into event-backed rows (compared to the event-derived (stream, branch) chains exactly as before) and SEED rows; a seed is legitimate iff its branch exists in event_history_branches AND the row's stream_revision equals that branch's recorded base revision AND its event_digest equals the base digest the seed copied; any other event-less head, or a seed that disagrees with its branch record, is still refused. #97 (found by the #96 seam's own row 'a candidate branch that later has events is treated as event-backed'): recovery seeded EVERY (stream, branch) chain from genesis (revision 0 / digest null), so a candidate whose first event legitimately carries streamRevision = base + 1 and previousStreamEventDigest = the base digest failed verification; a non-root branch with a branch record now starts its walk at (baseRevision, baseDigest), root / a missing record / a missing table keep the genesis start. The worker also found WHY that row threw a wrapped error before the walk ran at all: the STORED event and receipt Zod schemas still pinned branchId to the root literal (only expectedBranchId had been widened in B2), so hydrating a candidate-branch row rejected - the stored-schema pin is lifted to the same EventBranchId the contract already promised. Rows: the it.failing row is a plain passing row; an event-less head on an unknown branch, a seed at the wrong revision, a seed with the wrong digest are refused; two seeds on two candidates of one stream are admitted; a candidate with events is event-backed and its head must match its chain; a candidate whose first event chains from genesis instead of the base, or from the wrong digest, is refused; root still starts at genesis; an older database without the branch table behaves as today. Orchestrator mutants in the PR body. NOT CLAIMED: the WRITER side (#98 - verifyHead looks for the base event on the child branch while a seed's base event lives on the parent) is fixed on C1c-ii's branch, not here; recovery still refuses genuine corruption; how heads and events are written is unchanged.

  - FINDING #94 (2026-09-02, Wave E6 part 2): the 19.3 handover's premise that the GM correction stub was 'passed by no page' was FALSE - src/pages/e2e/networked-command-proof.tsx wires both stub props and three Playwright specs depend on the testids (e2e/networked-command-proof.spec.ts and e2e/playable-command-feature-screens.spec.ts CLICK them; e2e/gm-two-player-privacy.pack.spec.ts asserts their absence for the guest), plus a jest row in NetworkedGameSurface.test.tsx. The stub was not deleted blind; the defect was fixed at its cause by removing the no-op defaults so the block renders only when a caller supplies both handlers - the production lobby route passes neither (no dead GM buttons, #15 closed there) and the harness keeps its proof; a jest regression pin makes that relationship runnable rather than a comment.
  - CLOSURE (2026-09-02, Wave B3 2c-iii): #78 FIXED (record-before-await + clear-on-failure, batch door around the three-door walk). #59 CLOSED as receipt-only (every commitCampaignEventBatch caller holds the lock; the exported function's hypothetical bypass caller is noted, not guarded). Adjacent risk carried, not claimed: findings about per-host-object exclusion remain (cross-instance races are the store's duplicate-command check, identity-carrying doors only).

  - FINDINGS #40 CLOSED / #77 FIXED (2026-09-02, Wave B2c 2c-ii): the socket host no longer throws on a lost race (typed CAMPAIGN_STALE_HEAD with head + recovery on the frame) and the seventh writer door is serialized.
  - FINDINGS #92-#93 (2026-09-02, Wave E5 cut B): #92 a cut A pin describes a body production no longer sends - campaignLifecycleState.test.ts 'reads a rebuild out of the commands route's blocked body' constructs the 409 without a recoveryAction and pins recoveryAction: null, while since B2c's #66 fix the real body carries recoveryAction: 'retry-after-rebuild'; the door reads the field generically so behaviour is right, but the fixture is less real than the route (folded inline as cut C). #93 the staleness family's recovery label overclaims: RECOVERIES.STALE_BRANCH / PROJECTION_REWOUND read 'Resync to active head' but the handler on BOTH surfaces is onClearRefusal -> setLastRefusalCode(null), which clears a local hint and lets the server answer again - no resync exists to wire it to (no client calls the commands route); honest for 'Check again', overclaiming for staleness; ruling: truthful labels derived from the handler (cut C), a real resync action is the cutover's.
  - FINDINGS #88-#90 (2026-09-02, Wave E3 PR-A): #88 a phase advance cannot commit while a player is partitioned - ending a phase needs BOTH players (#23), so a partitioned guest structurally blocks the very commit E2E-66's letter requires to happen WHILE it is partitioned; a partition scenario must pick a command one player can commit alone (GoProne, the tactical packs' command). #89 a player's wire carries NO authority sequence: the post-reconnect frame is 'Event deliverySequence=5, event.type=movement_locked, event.sequence=undefined' - the viewer projection strips it by design (the privacy pack's E2E-27 asserts precisely its absence), so an exactly-once claim on a player's wire keys on deliverySequence, with a contiguity check so a GAP fails as firmly as a repeat. #90 (found by an orchestrator LIVE mutant that SURVIVED - resumeFrom = afterLastHeld, ServerMatchHost.ts:787 - because the row's counter read only the singular top-level deliverySequence and every ReplayChunk carries a PLURAL deliverySequences[] array): a reconnect-by-RELOAD replays the viewer stream from zero, re-sending delivery numbers the client already held, while the missed event is ALSO delivered live immediately before (measured frame order: Event deliverySequence=5, ReplayStart, ReplayChunk [0..5], ReplayEnd); a reconnect-IN-PLACE takes the live outbox path and never enters the replay code at all (zero ReplayChunks) - so a session-wide 'no delivery number repeats' claim is FALSE against correct behaviour, and the letter ('apply every authorized event once from the durable viewer cursor') is APPLY-side. RULING: E2E-66 splits into R66a (in-place: one new live delivery, contiguous, zero ReplayChunks, applied once) and R66b (reload: live Event then from-zero replay, no repeat WITHIN the replay stream, contiguous coverage, the missed event in both, applied ONCE); the apply-side choke point is admitLiveEvent (client.ts:870) with admitByDelivery / admitByIdentity, and ReplayChunk routes through the SAME admission as live traffic - measured: the client does NOT double-apply the overlap (no #91 defect); the observable is what the client emits to its consumer, because the durable match log is keyed ['matchId','sequence'] (a second apply overwrites, invisible to a count) and the networked surface renders no per-event list. LAW 47: a surviving live mutant is first checked for REACHABILITY by the row's own path.
  - FINDINGS #49-#52 (2026-09-02, Wave F1 23.3 preparation): #49 checkpointRecoveryPort (AuthorityRecoveryPort.ts:242) has ZERO production callers - every production construction takes referenceRecoveryPort, so checkpoint acceleration is built and proven (15.x) but never opted into; E2E-55's browser half is gated on a production caller and closes as an integration proof. #50 a GM (host) disconnect PAUSES the campaign session (design D6, CampaignSyncSession.ts ~726: room code stops resolving, host closed, no campaign-tier host migration) and no resume path exists - a GM-reconnect scenario would prove the pause, a different letter. #51 joinGuest re-converges a returning participant by PLAIN ASSIGNMENT of the current head (CampaignSyncSession.ts:496), so 'converged' after a reconnect asserts the join, not the client's applied state; a disconnected participant stays in `retained` and behind (only applyCommittedParticipantRemoval deletes it), so AdvanceDay is refused CAMPAIGN_NOT_CONVERGED naming them until they return (live probe: PROBE-D refused 'participants-behind <pid>:1; requiredRevision 2', PROBE-E committed sequence 3 after rejoin) - the 23.3 sequencing law follows: reconnect windows live INSIDE a scenario, the boundary AdvanceDay waits for everyone. #52 the CampaignSyncSession.ts:184-190 comment block ('NOTHING REMOVES ANYONE TODAY ... refused for the life of the process') is stale on both halves: removal is wired via applyCommittedParticipantRemoval from bindCampaignSyncConnection.ts:781 and a returner re-converges.

  - FINDINGS #40/#41 (2026-09-02, Wave F1): #40 two CampaignHostIntents in flight on one campaign connection both read the same next sequence in CampaignMatchHost.applyHostIntent; the second commit throws CampaignEventSequenceCollisionError and bindCampaignSyncConnection closes the GM's socket with dispatch-failed (reproduced at 50 unwaited intents, collision at sequence 222; a fast GM double-click reaches it) - real defect, the pack serializes rather than papering over it; owner: 8.4 conflict handling (host-side intents must serialize per connection, never drop the socket). #41 building an 800-command log beyond the fixture grows the server ~466 MB heap / ~851 MB RSS - ungated, archived; owner: #19's app-level backlog bound plan.

  - FINDING #79 (2026-09-02, Wave E4c): on a mirror-prefix divergence the P2P client deletes its durable log, appends the peer's events onto a stale in-memory session and calls setLive() - visible half fixed in B1 (refusal + reason), hazard fixed in B2 (rebuild from the reconciled log).
  - FINDINGS #80/#86/#87 (2026-09-02, Wave D2): #80 candidate branches had no journal head row; #86 the lease store's branch-blind head read closes the rewind flow once a second head row exists (consequence of #81); #87 candidates can only be cut at the head - lease fencing conflated with the candidate base.

  - FINDING #60 (2026-09-02, Wave E3c): the layout sweep's and parity specs' first page.goto on a cold dev server can exceed the 30 s test timeout (playwright.config.ts:105) because the first route compile is paid inside a measured test - a harness flake class, not a product one; fixed by a warm-up that keeps every test's own timeout meaningful.
  - FINDING #70 (2026-09-02, Wave D1): a campaign sequence is stream-global while a candidate branch is anchored at a base revision, so appendCampaignCommandBatch's expectedRevision is branch-unaware - the branch RULE admits an append on the new effective branch but the append conflicts on sequence; committing onto a non-root branch needs the expected revision resolved from the branch head (16.2 replay work).
  - FINDINGS #61/#62/#63/#71 (2026-09-02, Wave E4b): #61 P2P guest applies commands locally while the peer is gone; #62 onMirrorPrefixDivergence never wired; #63 the peer-drop detector's only caller has no production caller; #71 the shipped dock dispatch-refusal row was unfailable (disabled DOM controls receive no clicks) - fixed by an exported dispatch choke point + direct row.
  - FINDINGS #77/#78 (2026-09-02, Wave B2c): #77 commitCombatOutcomeConsequences is a seventh unserialized writer door on CampaignMatchHost (inbox-capable stores only); #78 the sync binding's reconcile guard is check-then-act across an await. #40 is fixed by 2c-i's serializer.
  - FINDINGS #72-#76 (2026-09-02, Wave F2): #72 the fault lever had no session scope (arms were process-wide one-shots) and repo-relative sentinels nothing cleared between runs. #73 E2E-46 is unreachable - the intervention day count is hardcoded to 2 (GmCampaignInterventionControlPlane.helpers.ts:136/:156) with no control. #74 the per-day cascade breakdown (daySummaries) is computed and has no DOM. #75 of E2E-63's three named failure points only the head update existed. #76 E2E-46..80 live only in the unmerged delta spec and every one was greenfield with zero @E2E-NN title-tag claims; the repo's binding convention is the title tag, not a docblock.

  - FINDINGS #37/#38/#39 (2026-09-02): #37 ITacticalLifecyclePosture.commandsEnabled is computed and never read - the tactical dock gates only on `blocked`, so syncing/reconnecting/behind/pending/rewound/rebuilding all issue commands (Wave E2 seam 3 wires it). #38 the combat wire carries no client-claimed expected head, so the STALE_BRANCH arm of admitStreamCommand cannot be adopted on the socket path without a new protocol field - its own decision, not folded into 3a. #39 the viewport sweep iterates pack-seeded entries by pack literal, so repointing an inventory entry at a new pack silently removes it from the matrix (E3b hotfix + guard).
  - FINDINGS #56/#57/#58 (2026-09-02, Wave E4): #56 PROJECTION_REWOUND is not in ErrorCodeSchema - the wire cannot carry it, the client mapper has one arm. #57 /gameplay/games/[id] is the P2P transport (useP2PReconnectSession), not the WebSocket client; the dock's commandGate has no producer there. #58 duplicated JSX comment block in TacticalActionDock.commandButton.tsx (cosmetic, from #1535).
  - FINDINGS #46/#47/#48 (2026-09-02): #46 NetworkedGameSurface does not render TacticalActionDock (only GameplayLayout.actionDockSlot on /gameplay/games/[id] does), so #1535's dock gate has no networked producer today and the live 19.2 tactical gap is NetworkedGameSurface.actionbar's `ownership.canAct && !paused` (E4). #47 projectionSignal is a prop on NetworkedGameSurface that no caller passes; rebuilding/rewound are unreachable because nothing feeds the prop, not because the derivation refuses - the client Error-frame path must carry PROJECTION_REBUILDING (E4). #48 combat journal authority is nominal: no production path writes match events to the journal (see 14.1).
  - FINDINGS #64-#69 (2026-09-02, Wave E5): #64 STALE_BRANCH and PROJECTION_REWOUND are not wire codes - two client doors (wire / command body). #65 the guest campaign transport discards Error frames; the guest has no refusal channel. #66 campaignCommandPipeline drops the server's rebuild recovery action on its blocked arm. #67 the commands route has no client caller. #68 LIVE_WIRE_CODES had drifted from ErrorCodeSchema. #69 the GM 'reason rides the control' was a data- attribute.

  - FINDINGS #35/#36 (2026-09-02, Wave E2): #35 the scenario-pack subsystem vocabulary is closed by derivation from registered flows, not by a spec list - adding a tag means registering a flow. #36 no minter can produce a co-op pack; hand-authored packs must carry honest provenance (own genesisSource/mintedAt/baseCommit), never a copied block.
  - FINDING #42 (2026-09-02, Wave E2): TacticalActionDock's aria-describedby pointed at an id nothing rendered since the dock shipped - every disabled command (engine-derived reasons included) was a dead button for assistive tech; a disabled control is out of the tab order so onFocus-based reasons can never fire. Fixed in seam 3a.

  - FINDINGS #32/#34 (2026-09-02, Wave E2): #32 no scenario pack seeds a coopSession, so every co-op lifecycle surface is outside the layout sweep while the route containing them passes (recorded as an inventory note; co-op packs at seam 2c). #34 coopSession.mode is single-valued (host mounts the GM surface, guest mounts the proposal surface + sync banner), so sweep coverage needs two packs, not one.

  - Progress (2026-09-02 - FINDING #21 was a REAL LEAK on an UNAUTHENTICATED ADMIN API, fixed; FINDING #23 E2E-12 fixed at its true cause; findings #26/#27, Wave A3c): the three-context blob assertion was catching Player 1's dashboard rendering EVERY participant's principal id in the share-grant roster, each with a working Revoke button. Root cause: campaignShareService.requireSourceCampaign called evaluateSourceMutationGate, which answers 'does THIS SERVER own the campaign' (a property of the campaign - ISourceCampaignAuthority is {role:'source'} and names nobody), standing in for 'is the caller the owner', which it never enforced; and GET/POST/DELETE on /api/campaigns/[id]/grants read NO Authorization header at all. Any client that knew a campaign id could read the roster, issue itself a grant with arbitrary scopes, or revoke another participant. Fix at the boundary that should have withheld it (the route via the service, never the UI): authenticateRequest with an expectedScope DERIVED SERVER-SIDE from the campaign's own coopSession.matchId (never read off the request) before the verb switch -> 401; requireCampaignGm keyed on the active 'gm' row of campaign_session_participant (migration 17, revoked_at IS NULL) -> 403 not-campaign-gm; callerId now REQUIRED on all three service entry points; the GM's share panel presents the co-op token it already holds. Room-code guest grants go through the store, not the service - unaffected (three-context-pack + proposal-pack green). Red-first: with the gate scaffolded but unenforced an outsider with no seat received the full grant list (5/6 red). Mutants killed: GM check accepts everyone (6 red), 401 dropped, revoked filter dropped, expected scope undefined. authority-privacy-three-context.spec.ts now runs in a CI-gated group (three-context-pack:30) - it ran in NONE, which is why an unauthenticated admin API went unnoticed. FINDING #26 (NOT fixed, wider seam): the rest of the campaign REST surface - adopt, commands, replica-sync, [id], index, redeem - still reads no Authorization header; and there is still no owner principal on the campaign record (the gate keys on the co-op gm seat, so a solo campaign now gets 403 and an empty share panel - a deliberate narrowing with no practical loss since grants are only issued through co-op). FINDING #23 diagnosed by instrumenting both action bars at the timeout: NOT the poll racing the toast - a collided client SELF-PAUSES (every control disabled incl. Concede), the D4 turn-ownership gate withholds the advance control from the non-active side, and ending a phase needs BOTH players, so once anyone is wedged the phase cannot advance at all (initiative-dependent ~40% of runs; wedging the passive side and driving from the active side still failed 4/4). The row only ever passed by clicking the wedged client's control in the instant before its pause rendered. Fix removes the dependency instead of winning the race: the collision targets the FIRST delivered event of the commit (phase_changed is the LAST event of its commit, proven 4/4) so the rest of that commit IS the later delivery, and the row now asserts POSITIVELY that a later-numbered frame arrived before asserting the cursor did not follow it - strengthened, not loosened; 8 green runs. FINDING #27 (product question, flagged not answered): a collided client stalling a match for both seats. Live under the orchestrator: exactly-once 4/4 x2, three-context 1/1, proposal 1/1, privacy 5/5, authority-pack1 3/3. Environmental note: three recurring dev-server flake modes at fixture setup on this machine (PageNotFoundError for a dynamic route, waitForURL timeouts, Network.getResponseBody missing) - `rm -rf .next` cleared a persistent bout; its own seam. Acceptance ledger: E2E-03/05/06/08/09/10/11/12/13/15/16/18/20/21/22/23/24/26/27/30 live (20) across nine runner groups.

  - Progress (2026-09-02 - E2E-14 STAYS OPEN for a MEASURED reason; findings #18/#19/#21, Wave A2b): the product half of E2E-14's letter is now true (see the 7.3 errata: the bounded queue covers the fact path, a stalled viewer enters a recoverable behind state and resumes at the first missed frame), but the browser scenario cannot be closed by the e2e cap override Wave A2 proposed. FINDING #19: the override seam was built and VERIFIED to reach the socket host's module graph (sentinel file; the broadcaster's admission check logged cap=1024 after arming) and still never trips, because ws.bufferedAmount is the Node stream's PENDING-WRITE backlog, not bytes the client has not consumed - it stays 0 until the OS send buffer refuses a write (O(100 KB) in flight): 0 on all 60 admission observations through a 45-second fully blocked renderer; a 37-second ~1 KB/s CDP throttle left the stalled client never behind, never reaped, finally holding all 37 frames contiguously. A turn-limited match puts ~37 frames / ~17 KB on one socket - an order of magnitude short - so lowering the cap compares against zero, and A2's premise (their 3.9 MB figure came from pushing megabytes, not match traffic) was wrong. The seam was REMOVED rather than shipped as dead code (reversal cost: route + shell + 9 route rows + 5 cap rows; one boundary row kept against the shipped constant). What closes E2E-14 honestly: an APPLICATION-level backlog bound (sent minus acknowledged over the existing DeliveryAck stream) alongside or instead of bufferedAmount - a design change with its own plan; the 10x-volume route guesses at an unmeasured absorption floor and is not recommended. Not claimed: no typed close code on reap (ServerMatchSocketLifecycle closes with no status). FINDING #21 (pre-existing, not caused here - fails identically with the fix stashed): e2e/authority-privacy-three-context.spec.ts is RED on main on a DOM leak-sweep assertion (a pid_ identifier present in page content); it is not in any CI-gated group; owner: the privacy pack (Wave A3b to diagnose after its current seam).

  - Progress (2026-09-02 - FINDING #12 RESOLVED: one live-delivery path for every CampaignJoin arm, Wave A1): CampaignSyncSession.attachLiveParticipant is now the ONLY host subscription any join arm takes - admitToWire-scoped per recipient through the 11.1 boundary and delivery-recorded where delivery happens. The host arm no longer hand-builds a CampaignSnapshot plus a direct UNSCOPED host.subscribe: it hydrates through joinMember like every other client (the GM baseline now carries matchId + revision), and joinMember/retainHydratedParticipant refuse to retain the GM (retaining the authority gates the GM's next command on the GM's own ack - a deadlock; the two binder rows that assert launch ok with the GM connected pin it). The grant arm retains the guest at the journal head FIRST, then attaches the unified sink BEFORE its hydration reads (a commit landing mid-hydration is buffered behind the snapshot gate, never lost between 'not in the snapshot' and 'not yet subscribed'; the rejoin path flushes that buffer instead of dropping it). roomCodeGuestLiveFanout is ingest-only: the grant channel keeps the durable replica current and emits nothing to the client wire. campaignEventWireFrame is the one event->frame mapping. Acceptance harness campaignLiveDeliveryUnification.test.ts (7 socket-level rows across all three arms sharing one registry entry) was captured RED-FIRST against pristine main by a second agent working the same finding - its two red rows (GM hydration still hand-built; grant guests received live frames with zero convergence bookkeeping, so evaluateScenarioLaunch read converged while they were arbitrarily behind and their CampaignAck was refused unknown-participant) are exactly the gaps the first implementation had, and are closed. Orchestrator mutants (all killed): retention seed removed from the grant arm (1 red); GM retained by joinMember (4 red); scope admission bypassed inside attachLiveParticipant (1 red - the gm-scope-withheld cross-arm row). NAMED WIRE-SHAPE CHANGE: room-code guests' live CampaignEvent frames now carry the JOURNAL sequence rather than the grant projector's per-grant delivery sequence (the key was always present; the number space changed to match the legacy arm and the ack gate, per the sequence-concealment deferral recorded in campaignWireScopeBoundary.ts); no leakScan expectation covered that arm, a harness row pins the change instead. E2E-03 and E2E-08 are REVIVED in authority-pack1 with eventSequence captured per frame; E2E-08 has each guest ack the post-advance revision before the verbatim resend and asserts no Error frame reached the GM, so it can only pass on idempotency, never on a CAMPAIGN_NOT_CONVERGED refusal. Named residual: a retained-but-unstreamed grant guest (hydration failure after retention) stays retained until their retry re-seeds - consistent with the binder's bound-but-unstreamed-member stance. Full suite 2467/33590 green; tsc 0; oxlint 77-pin.
  - Progress (2026-09-02 - TENTH SCENARIO LIVE: E2E-15 host restart recovers every authority clause; E2E-14 BLOCKED on volume, Wave A2): E2E-15 is green in the new resilience-pack group (behind the relaunching wrapper; RESPAWNING_GROUPS now names restart-pack and resilience-pack, the qc suite pins both plus a non-respawning group's plain server). Shape: a real 1v1 vault flow to Movement plus a server-persisted campaign; process-exit-after-commit armed; the advance commits and the process dies unpublished; BOTH browser contexts go setOffline before the respawn can answer them, so the post-restart evidence window carries zero client traffic (no pending-intent retry, no rejoin replay, no ack) and the ORDERING claim is assertable exactly: server.js awaits bootstrapMultiplayerServer (match replay, cursor restore, outbox drain) before listen, so the first 200 is already downstream of recovery. Seven clauses read at that instant: campaign (id, name, version, instanceId), match status + room code, participants, receipts (count grew and coverage reaches the log head), cursors (same player set, never rewound), pending outbox (total grew, pending 0 with no client attached), authorized projections (both still hold a stream); then connectivity returns, both surfaces reach a commandsEnabled posture with no test-driven reload, and a new command round-trips. ACTIVE BRANCH is deferred, not faked: the tactical client emits neither PROJECTION_REWOUND nor PROJECTION_REBUILDING today, so the clause lands with the branches leaf. Orchestrator re-ran the pack live: 1/1 green (11.7s), [mp-boot] recovered 1 active match(es) observed after the death. Would-be mutant (reasoned, src outside the seam): deleting resumePendingEventPublications in MatchRecovery leaves E2E-05/06 green and fails E2E-15 on outboxPending 0. Honest limit: the cursor clause proves the rows survived, not that the host reloaded them - a restorePersistedViewerDeliveries deletion is NOT caught here. E2E-06's carried question (does the client's automatic reconnect outlast a respawn) STAYS OPEN and this harness cannot answer it: the e2e server runs Next in dev mode and the dev HMR client hard-reloads the document when the server returns (measured in every run: a fresh document GET on the match route right after mp-boot), so the observed path is reload-equivalent; the test RECORDS which path carried each page (reconnect-path annotation) rather than asserting a reconnect it did not witness. Settling it needs the pack behind a production build. E2E-14 (slow-client isolation) is BLOCKED and the charter's stated obstacle was wrong: a standalone ws+Chromium probe measured CDP network throttling DOES reach the server socket's bufferedAmount (4.0 MB throttled, 3.9 MB with a blocked renderer, cap 1,048,576) - the stall lever exists; what is missing is VOLUME: mean durable event 470 B, 3 events per command, so ~590 commands must queue on one stalled socket before MAX_BUFFERED_BYTES trips, more than a turn-limited match contains. Unblocking = an e2e-mode-gated override of the cap in ServerMatchBroadcaster (the fault-kind seam shape); with a small cap the throttle lever reaches it in a handful of commands. Acceptance ledger: E2E-05/06/09/10/11/12/13/15/16/18 live across six runner groups (E2E-03/08 ride Wave A1).

  - Progress (2026-08-31 - box stays OPEN; FIRST SCENARIO LANDED + FINDING #12): E2E-18 is green and honest - three real browser contexts on the campaign channel sit QUIET for the full HEARTBEAT_TIMEOUT_MS plus a server period (a genuine ~75s hold; the first draft's expect.poll resolved on first success in 23s and was rejected as vacuous - survival BEYOND the interval is the letter), heartbeats flowing both ways, all three still connected after; registered as runner group authority-pack1 ('authority' stays reserved for the complete pack per 21.4). MARATHON FINDING #12, instrumented server-side across six live probe runs: a committed campaign command's event reaches NO connected campaign client. The campaign socket has THREE join arms with THREE different live-delivery behaviors - the host arm subscribes a direct unscoped sendCampaignEvent; the legacy newcomer arm subscribes via the session's admitToWire-scoped sinks; the grant arm attaches snapshot-gated epoch-PROJECTION subscribers that push no raw frames - and in the live fixture an AdvanceDay commit broadcast to 3 subscribers produced exactly ONE wire send (readyState 1) that the browser never received (raw message count stayed 1), zero admitToWire evaluations, zero errors anywhere. Which arm a client lands on depends on server state (hostPlayerId match, issuedInvite, grant channel), which is precisely the Visibility-Equivalent-Across-Surfaces violation the 12.x owner fork names; E2E-03/08 (their letters and their spec scaffolding retained in the pack file) are BLOCKED on this delivery unification, not on test authoring. The instrumentation lives in the session transcripts; the probe sequence (intent-arrival -> result-ok -> subscribers=3 -> one-send -> raw=1) is reproducible via MEKSTATION_E2E_SERVER_COMMAND log capture.

  - Progress (2026-08-30 - box stays OPEN): the acceptance drive's first authoring attempt opened with the campaign-socket subset (E2E-03/08/17/18) and the worker's STOP clause immediately earned its keep - E2E-08's letter could not pass because the campaign socket had NO intentId-keyed idempotency (command ids were sequence-derived, so a client retry after a lost ack double-committed; marathon defect #10, found by refusing to weaken the scenario). Fixed same-day: stable campaign-intent:<campaignId>:<intentId> command ids feeding the journal's existing duplicate detection, duplicate replays prior events without re-append/re-apply/re-broadcast, divergent reuse is typed intent-identity-conflict, and the flag-off production store carries the same receipt contract. Recon banked for the pack: HEARTBEAT_TIMEOUT_MS=60s makes E2E-18 a real ~70s idle; invite expiry is the registry's null-invite state from the 6.4 arc; evidence reads follow the readDurableParticipants read-only pattern. E2E-01/02 remain gated on the genesis-branch machinery (branches leaf), 05/06 on a scoped fault-injection harness, 09-12 were unblocked by the section-5 client work.

- [ ] 21.2 Implement and pass E2E-19 through E2E-30 for pre-serialization privacy, sealed choices, immediate public facts, fog projections, live/replay/export parity, unauthorized access, veto/timeout, and simultaneous proposal attribution.
  - PROGRESS (2026-09-03, Wave G2 21.2 E2E-29): one proposal vetoed and another timed out commit nothing and each guest clears only its own pending UI - proven live. Before: the proposals pack opened with a DEFERRED block claiming E2E-29 was blocked because both production wirings passed proposalTimeoutMs 0; FALSE on main - CampaignHostRegistry.ts:319 and coopRuntimeSession.ts:110 both pass PRODUCTION_PROPOSAL_TIMEOUT_MS (campaignGmArbiterTimeout.ts:10, 120 s, no env override), bindCampaignSyncConnection.ts:243 arms the timeout outcome broadcast, and the arbiter's timer is unref'd (CampaignGmArbiter.ts:519-520). Fix: the deferral is deleted and one row (@E2E-29, e2e/gm-two-player-proposals.pack.spec.ts) has guest A's proposal vetoed by the GM through the confirmation dialog (veto-<id> then veto-confirm, finding #101's shape) and guest B's left undecided until the watchdog auto-vetoes it past the production timeout; the head revision is read through GET /api/campaigns/:id/head (journal kind + revision) plus the campaign's HTTP version as the sibling fingerprint, before the proposals and after both resolutions, and must be equal; both CampaignDecision frames reach both guests' taps and each guest clears only its own proposalId (the pass/fail risk the worker named; it held). test.setTimeout(300_000) so setup cannot starve the 120 s wait. LIVE (l89, production build, proposal-pack): 2 passed, the new row in 2m06s (an earlier live run reported only E2E-30 because the spec edit was unstaged and the proof script copied only committed, staged or untracked files; the script now copies unstaged modifications too - LAW 89). Spec-level mutants, each a live Playwright run: N1 the veto confirmation click skipped KILLED (the veto never lands without the dialog, 1 failed); N2 the head check inverted to assert a change KILLED (proves the check executes and the head is genuinely untouched). Gates: tsc 0; oxlint 77-pin; qc pin suite 9/9. NOT claimed: no visibility group registered (the runner reserves it for the complete E2E-19..30 pack; E2E-19 and E2E-25 still have no row); spec-only, no product change.
  - FINDING #101 (2026-09-02; CLOSED in the same PR): proposal-pack E2E-30 waited for guest-proposal-vetoed after clicking veto-<id>, but the GM review surface now opens a confirmation dialog (VetoConfirmationDialog, veto-confirm) before the decision lands - the row's drive stopped one click short. The test id is composed from the tracked status and still renders after a real veto; the row now confirms. Live alone after the fix: proposal-pack 1/1.

  - PROGRESS (2026-09-02, Wave B4 arbiter timeout, defect #22 CLOSED): the co-op GM arbiter's proposal timeout was UNARMED in production - both construction sites (CampaignHostRegistry and coopRuntimeSession) passed proposalTimeoutMs: 0, so autoVetoForTimeout ('host-review-timeout'), unit-tested since 11.x, never fired for a real guest. One exported constant PRODUCTION_PROPOSAL_TIMEOUT_MS = 120000 (campaignGmArbiterTimeout.ts; the change specs require a timeout that commits nothing - E2E-29 - but name no duration; two minutes is long enough for a GM to decide and short enough that a guest is not stuck behind an absent host; the omitted-option fallback DEFAULT_PROPOSAL_TIMEOUT_MS stays 5 min; tests may still pass 0 to disable). Rows on PRODUCTION construction, not a hand-built arbiter: R1 an undecided proposal past the production timeout is auto-vetoed with host-review-timeout and the guest is told through the existing CampaignDecision path, ledger unchanged (fake timers with Date un-faked so heartbeat idle does not reap sockets); R2 a decision before the timeout returns the timer count to baseline and advancing the interval does not un-commit; R3 disposing the registry returns the timer count to baseline (no dangling timer keeps the process alive); R4 both production sites expose the same constant (a future site cannot quietly go back to 0); plus the runtime session's result waiters resolve on timeout. Orchestrator mutants in the PR body. NOT CLAIMED: E2E-29's browser row (a separate seam); reconnect after a missed timeout frame; a real 120 s wall-clock wait.
  - NOTE (2026-09-02, Wave E7): defect #15's 'lobby passes no GM callbacks' is superseded - the two-button stub was replaced by the rewind preview flow (E6) and the lobby now passes the preview producer; the GM-command letters (E2E-19/25) still wait on the commit half (3b-iv).
  - PROGRESS (2026-09-02, Wave E8, E2E-28's HTTP + export slice): main had NO timeline or export HTTP route - 12.x delivered the service half only (ViewerHistoryService registers no routes). Two GET routes now exist, /api/matches/:id/timeline and /api/matches/:id/export, through one shared chain (matchHistoryViewerChain: rejectUnexpectedMethod GET, initializeApiDatabase, id guard, authenticateRequest with the { kind: 'match', id } scope the preview route omits, then the SERVICE call - the route never pre-brands a viewer, because every service entrypoint re-resolves the branded viewer itself for kind 'timeline' / 'export') and a production factory (AuthorizedViewerResolver over MatchSeatMembershipSource on the default match store; SQLite audit, private-record and delivery-epoch repositories on the service database; ViewerProjectionService with the match-wire and history-proof projectors). The timeline body is { timeline, timelineDigest: viewerTimelineDigest(entries) } because the service returns a bare array and the invariant needs the digest on both bodies; the export body is the service's IViewerHistoryExport verbatim (application/json, no file wrapping); transport {error} for 400 (id / streamType), 401, 403 (constant, id-free), 405 (Allow GET), 500. INVARIANT pinned: a seated viewer's export timelineDigest equals the same viewer's timeline timelineDigest equals viewerTimelineDigest(export.timeline). Rows on real SQLite: seated participant, three equal digests; guest parity by SETS not counts (player export commandId set = player timeline set; player rows omit committedFirstRevision/committedLastRevision; the player's stream facts equal the timeline-visible public set and contain neither the GM-only hidden fact nor a private payload); no bearer 401; authenticated non-member 403 naming no match id; POST 405. Browser row @E2E-28 in the privacy pack: the seated bearer GETs both routes (200, digests equal), no auth 401, a stranger token 403, and no new outbox or domain event row appeared; the header says honestly that this is the HTTP API + export slice of the letter - the GM command stays defect #15's territory (3b-iv) and WS-unauthorized is not this row. Orchestrator mutants in the PR body. NOT CLAIMED: includePrivate; E2E-19/25/29; service signatures unchanged; finding #20 untouched.

  - Progress (2026-09-02 - DEFECT #14 FIXED, E2E-22/26/30 LIVE; errata on #17; findings #22/#23/#24, Wave A3b): the sealed reveal now republishes the event the authority COMMITTED. Root cause verified: stampIntentIdOnNewEvents / stampRollsOnNewEvents return NEW objects; commitAcceptedIntent hands the stamped copy to commitThenPublish (store + live wire) while the engine's session.getSession().events keeps the pre-stamp original, and broadcastEventInMode sourced reveals from the session. Fix decision (the correct one, not the easy one): committedDeclarations keeps SELECTION on the session log (type/turn/phase/sequence - fields no stamp touches) and resolves each PAYLOAD from store.getEvents by event id, failing CLOSED per declaration (a declaration with no committed row is not revealed; replay serves the same rows so it returns on the next resume); the alternative - making the session's copy the stamped one - needs an engine-wide mutation API at three stamping sites and would still leave the reveal structurally free to serve a cache copy. The shipped sealedChoiceDelivery row compared the reveal against the session's own event on both sides and was BLIND; it now compares against the committed row (strictly stronger). Mutants killed: session copy revealed (m1, 3 red unit + E2E-22/26 red live), fail-open on a missing row (m2); orchestrator re-ran m1 and added the committed index keyed by sequence (2 red). E2E-22 (reveal arrives from the committed delivery stream: none before finalization across a real hold, exactly one after with a re-read past the first poll success, payload byte-equal to the actor's own live copy, delivery run contiguous) and E2E-26 (hard reload -> full replay; the revealed declaration present in BOTH windows so the comparison is not vacuous; per-event payload equality + one ordered digest) are GREEN in the privacy pack; orchestrator re-ran the pack live 5/5. E2E-30 lands on a NEW campaign-channel proposal-pack (proposal-pack:30, three-context co-op UI rig): two concurrent proposals, two actor-attributed review items, and - because the GM's review queue is accumulated in the BROWSER from CampaignProposal frames and cannot see the arbiter's - the survivor is APPROVED after the veto, which only commits if the arbiter still holds it (mutant m3 'one decision drops every pending proposal' SURVIVED the DOM-only draft and is killed by that clause); orchestrator re-ran 1/1. DEFECT #22 (defers E2E-29): CampaignGmArbiter arms its auto-veto timer only if proposalTimeoutMs > 0 and BOTH production wirings (CampaignHostRegistry, coopRuntimeSession) construct it with 0 - no production surface ever times a proposal out (same shape as #15: live code, dead wiring). ERRATA on #17: NOT REPRODUCIBLE - measured five ways with the exit code read directly, run-playwright.mjs and the full runner relay a non-zero Playwright status in every case (nonexistent spec, deterministic failing spec, privacy-pack 5 failed, exactly-once 4 failed -> EXIT=1; green -> 0); the earlier observation almost certainly read $? after a pipeline. A regression pin was added instead ('relays a non-zero Playwright status instead of reporting success'). FINDING #24: intentId already reaches every attached viewer on every ordinary live frame (commitThenPublish broadcasts one stamped copy to all sockets; AUTHORITY_ONLY_EVENT_FIELDS is top-level and intentId lives in payload; client.ts settles the actor's pending intent from it) - withholding it from non-issuers is a systemic projector rule with no issuer identity to key on today; NOT fixed here, named. FINDING #23: exactly-once-pack E2E-12 ('collision blocks the cursor before later delivery can advance it') is RED on this machine at THREE bases - 7f0461fee (drive start, where it landed green in #1487), ecb2d8cad, and current main - always on the same 15s predicate poll inside advancePhase after the collision, with E2E-09/10/11 green each time: a timing flake under multi-worktree load, not a regression from any merge in this drive; needs a robust phase-advance wait (fold into the 21.x group-completion hardening). Finding #21 first hypothesis (not touched): the three-context blob assertion is player1's page content + storage AFTER a reload containing player2's playerId - likely a real leak via the post-#1493 retention/hydration bookkeeping persisting into the campaign store -> localStorage, second candidate HostGmReviewSurface rendering proposingPlayerId. Acceptance ledger: E2E-03/05/06/08/09/10/11/13/15/16/18/20/21/22/23/24/26/27/30 live (19) across eight runner groups; E2E-12 flaky-red; E2E-14 open (finding #19); E2E-19/25 (defect #15), E2E-28 (surfaces absent), E2E-29 (defect #22) deferred.

  - Progress (2026-09-02 - FIVE PRIVACY SCENARIOS LIVE + defects #14/#15/#16/#17, Wave A3): the privacy-pack group (privacy-pack:30) is green in Chromium on the TACTICAL match channel with every server->client frame recorded through routeWebSocket. E2E-20/21: a real sealed declaration (GoProne Intent on the actor's own socket) reaches neither the opponent's wire (no event id, no declared hex) nor their board (unit token present, data-token-map-position not the sealed hex), the opponent's deliverySequence run stays contiguous (no inferable gap), and the mirror row carries a positive control proving the actor's own wire DOES carry it - withholding is an audience decision, not a lost frame. E2E-23: the same command's public facts (movement_locked, phase_changed) reach the opponent immediately inside the sealed window with zero GM-approval controls mounted and nothing clicked. E2E-24: one authoritative event, two valid views - actor stream carries it, opponent stream does not, both gapless, a per-viewer digest over ordered (deliverySequence,eventId,type) differs, the actor's board shows the hex the opponent's does not (HONEST SCOPE: the wire carries no server-computed projection digest, so the digest is in-spec; this proves the sealed-visibility axis, not double-blind LOS). E2E-27: full sweep of both players' complete frame logs plus page.content() and history.state - no sequence/visibility/privateRecordRef on any event or ReplayChunk, no fromSeq/toSeq on replay envelopes. Role bound: the tactical channel mints no role:'gm' viewer (MatchSeatMembershipSource yields player for every seat), so the GM-MAY-see halves have no tactical counterpart. DEFECT #14 (blocks E2E-22 + E2E-26, authored/run/REMOVED rather than weakened): the sealed REVEAL republishes the SESSION's copy of the declaration, not the COMMITTED one - stampIntentIdOnNewEvents hands a stamped copy to commitThenPublish while ServerMatchHostEvents.broadcastEventInMode sources reveals from ctx.session.getSession().events (unstamped), so measured live the reveal differs from the actor's live copy by exactly intentId, and after a reconnect the REPLAYED copy carries intentId while the live reveal did not: live/replay payloads are not equivalent for that viewer (E2E-26's letter), and replay hands the opponent the actor's client-generated intent identifier the live path withholds. The shipped unit pin sealedChoiceDelivery.test.ts compares reveal vs the session's own event on both sides and cannot see it. Fix site: broadcastEventInMode must reveal the committed event. DEFECT #15 (defers E2E-19/25): src/pages/multiplayer/lobby/[roomCode].tsx mounts NetworkedGameSurface without onPreviewHostGmCorrection/onApproveHostGmCorrection, so the host's Preview/Approve GM Fix buttons are no-ops in production (only /e2e/networked-command-proof wires them) - dead GM controls ship to the host. DEFECT #16: the tactical client renders every unit at hex (0,0) until its first movement event (useHexMapStateFromEvents.tokens seedAccumulator seeds {q:0,r:0} because IGameUnit carries no coordinate, while the authority deploys at rows +-5 in applyGameCreated - a Move one hex north of the RENDERED token was refused InsufficientMP from {q:-2,r:5}; on a radius-4 map those rows are off the grid) - which is why the pack declares with GoProne. DEFECT #17 (harness): the qc runner exits 0 when Playwright tests FAIL (observed: 4 failed, exit 0) - the swallow is in scripts/playwright/run-playwright.mjs's exit normalizer; any CI gate on this runner's status is gating on nothing. DEFERRED with reasons in the spec header: E2E-29/30 (campaign-channel proposal/veto frames - unblocked by Wave A1's delivery unification, next), E2E-28 (spans WebSocket+API+export+GM-command; export has no HTTP route, no GM-command route here - a tactical proof would cover a quarter of the letter). Missing 19.x locators recorded: no per-hex click locator on the board, no data-testid on the fog-of-war toggle. Acceptance ledger: E2E-03/05/06/08/09/10/11/12/13/15/16/18/20/21/23/24/27 live (17) across seven runner groups.

- [ ] 21.3 Implement and pass E2E-31 through E2E-45 for player ownership, deterministic concurrency, server RNG, disconnect policy, outcome receipts, immutable correction, pre-receipt combat rewind, branch convergence, fog restoration, and coordinated post-receipt correction.
  - FIX + PROGRESS (2026-09-04, finding #104 closed, 21.3 E2E-34): the client-rolls-forbidden refusal is now reachable from the wire. Product: bindMultiplayerSocketConnection.handleInbound inspects the RAW parsed envelope before ClientMessageSchema.safeParse (rawClientRollsRefusal: kind Intent and intentHasForbiddenDiceField on the raw intent) and answers ONE Error INVALID_INTENT / client-rolls-forbidden carrying the envelope's intentId when it is a non-empty string, without dispatching and without closing the socket (a command refusal, like the host-level one); errorFrame takes an optional intentId. Protocol.ts: the dead .refine on IntentSchema is removed and the FORBIDDEN_DICE_KEYS comment now states the truth - z.object variants strip unknown keys, so the schema cannot see a dice key; the door is the wire defence and ServerMatchHostIntent's check stays as defence in depth. Jest: a binder row drives a raw Intent with roll 6 and intentId roll-1 through the socket and requires exactly one Error with the literal reason and that intentId, no handleIntent call, socket open; a Protocol pin proves IntentSchema.parse of a roll-carrying intent SUCCEEDS with the key stripped (why the door must read the raw payload). Mutants (jest): M1 door removed KILLED; M2 intentId dropped KILLED; M3 door refuses but does not return KILLED; NOVEL N1 roll removed from FORBIDDEN_DICE_KEYS KILLED (binder row + Protocol pin). E2E-34 restored to a REAL refusal: the GoProne carrying roll 99 must be answered by an Error with reason client-rolls-forbidden and the injected intentId, and the durable event count must be unchanged by it; the phase walk keeps injecting host AdvancePhase with rolls [99,99,99,99] and requires each to be refused, then advances through the UI; both viewers observe one initiative_rolled with identical playerRoll/opponentRoll that never contain 99. The tap helper (tapErrorFrames) now records intentId on Error frames. ORCHESTRATOR FIX: the first live run went red in the shared advancePhase helper - count() then isEnabled() races a phase transition (isEnabled auto-waits 10 s for a control the transition just detached and throws inside expect.poll); replaced by one non-waiting evaluateAll read (exactly one control, not disabled). LIVE (l89, production build, combat-pack1): run 1: 2 passed, E2E-34 red on that helper race (the server log shows both door refusals); run 2: 3 passed (14.9 s). Live mutant NOVEL N2: door removed in the live build - E2E-34 red on the refusal poll, E2E-31/32 green (2 passed, 1 failed) KILLED; restored. Gates: tsc 0; oxlint 77-pin; 159 multiplayer + types suites green (1304 tests); oxfmt clean. NOT claimed: the GM-connection clause (unchanged); E2E-33 and E2E-35..45.
  - PROGRESS + FINDING (2026-09-03, Wave G2 21.3 combat-pack1: E2E-31, E2E-32, E2E-34): three combat scenarios are proven live between two players. New e2e/gm-two-player-combat.pack1.spec.ts registered as group combat-pack1 (GROUP_CATALOG + SPEC_BY_GROUP; the bare combat name stays RESERVED for the complete E2E-31..45 pack, the privacy-pack versus visibility pattern; not respawning); the qc test's groups pin, plan pin, implemented whitelist and all-args pin updated in the same edit. Launch is the NON-co-op launchOneVersusOne path because refuseForeignUnitCommand (ServerMatchHostIntent.ts 527-568, called 233-241) carves co-op matches out at 552; the requirement preamble's "separate non-playing GM connection" is NOT exercised (this launch seats the host as a player) - named, not claimed. Rows: E2E-31 a host intent naming the guest's unit is refused AUTH_REJECTED / unit-not-owned with the durable event count unchanged, and the same intent for an owned unit writes a receipt; E2E-32 two GoProne intents injected back to back produce the same pair of delivery numbers on both taps and the journal holds the batches in that order without interleaving; E2E-34 restated to the LETTER ("no client-supplied roll SHALL influence the result") around the INITIATIVE roll: a movement intent carries roll 99 (a value no die produces) and is accepted or refused (either satisfies the letter); the row then walks the phases to the next initiative, injecting the host AdvancePhase with rolls [99,99,99,99] whenever the host holds the advance, and requires both viewers to observe one initiative_rolled with identical playerRoll/opponentRoll that never contain 99. The worker first drove a weapon attack with weaponIds [all-weapons]; the engine answered attack_invalid (no such weapon on the unit, range 0) - an invented id, and a real attack would also need a target in range, so initiative (the same random resolution the jest arbitration suite pins across viewers) carries the row. FINDING #104 (product, live): the advertised client-rolls-forbidden refusal is UNREACHABLE from the wire - IntentSchema's refine and the host-level intentHasForbiddenDiceField check (ServerMatchHostIntent.ts 313-320) both inspect the PARSED intent, and the envelope parse strips unknown keys first, so a client dice key is silently dropped rather than refused; the jest coverage (ServerMatchHostRollArbitration.test 'rejects an intent whose payload carries a forbidden dice field') reaches the check only by calling handleIntent directly with an unparsed object. Authority holds (the server's own roll is what lands), but the protocol vocabulary promises a refusal that never fires; fix candidate: check the RAW envelope at the socket parse site. Live runs: run 1 failed 3/3 at the guest join - SELF-INFLICTED, my kill-port CI-fix verification killed the e2e server mid-run and the replacement forgot every in-memory lobby (LAW 93); a same-tree control run of the failure pack passed 7/7; run 2: E2E-31 and E2E-32 green, E2E-34 red on the dead refusal (the finding); run 3 (attack path) red on attack_invalid - the invented weapon id; run 4 with the initiative restatement: 3 passed (14.0 s), the E2E-34 row in 4.1 s. Spec-level mutants, each a live Playwright run, all KILLED: N1 the foreign-unit refusal assertion inverted (the refusal really is emitted; 1 failed); N2 the client-roll exclusion inverted (the initiative rolls really are the server's; 1 failed); N3 the two taps' delivery pairs asserted unequal (the order really is one; 2 failed). Gates: tsc 0; oxlint 77-pin; qc pin suite 9/9. NOT claimed: E2E-33 and E2E-35..45; the GM-connection clause; no product code changed.
  - Progress (2026-08-31 - NINE SCENARIOS LIVE + the restart harness): E2E-05 and E2E-06 are green across two consecutive runs, driven by real server deaths: a relaunching wrapper (scripts/e2e/relaunching-server.mjs) owns the server lifecycle for the restart-pack group only, and two new one-shot fault kinds on the guarded route/sentinel pattern kill the PROCESS - before the batch transaction commits (E2E-05) and after commit before the publication loop (E2E-06) - both consumed at the firing point (the consume-both-arms short-circuit bug appeared a SECOND time in the worker's refactor and is unit-pinned again). E2E-05's letter proven as TORN-STATE INTEGRITY, not a frozen count: the killed transaction leaves nothing, and what may legitimately appear afterwards is the client's own pending-intent retry (the 5.2 machinery) re-running the un-receipted command wholesale against the healthy restarted server - the assertions wait for quiescence, then require every durable event to be covered by a committed receipt (receipts' coverage reaches the log head - no orphan rows) and both surfaces to agree (converged phase or converged absence, never a half-render). E2E-06: the committed-but-unsent batch survives the death, boot recovery runs, and eligible contexts that RETURN (reload -> durable identity -> SessionJoin replay) receive the result exactly once with the durable count unchanged by delivery - replayed, never re-executed. SCOPE NOTE carried to E2E-15: whether the client's AUTOMATIC reconnect window outlasts a full process respawn is that scenario's resilience question - here the eligible contexts return by reload. Evidence reads are read-only fileMustExist connections to the per-run runtime DB (playwright.config's MULTIPLAYER_DB_PATH - the repo-default path was the first red). Acceptance ledger: E2E-05/06/09/10/11/12/13/16/18 live across five runner groups.

  - Progress (2026-08-31 - SEVENTH SCENARIO LIVE + MARATHON DEFECT #13 fixed): E2E-16 is green end-to-end: a guest's wire token minted at a short TTL through a new one-shot run-token-guarded /api/e2e/token-ttl override (the fault-route pattern; an explicit caller TTL always wins and production keeps its default - no guarded route can arm anything outside e2e mode); the token expires mid-session; the reload's upgrade is refused (the server's rejection was already correct); and the FIXED lobby arc reauthenticates: DEFECT #13, found by the worker's honest BLOCKED - the terminal stale-credential rejection used to strand the participant in the unavailable panel with the dead token still cached, making the letter's vault-remint arc unreachable. The fix is surgical: only the terminal branch (RECONNECT_LIMIT close + token past its own expiry) clears the credential while PRESERVING the durable matchId route, surfacing the same vault prompt the cold-load path uses; transient failures keep their backoff, the invite-404 fallback is untouched (both mutant-pinned). Remint proven to the SAME playerId and seat - no widening - with a no-bearer-token-in-any-URL sweep across the whole journey (credentials ride the WebSocket subprotocol). Mutants: keep-stale-cached sends the lobby into an INFINITE RETRY LOOP until the jest heap dies - the stranding made visible - and is recorded as killed-by-hang; drop-matchId-with-credential dies at the preserved-route row; route-transients-to-reauth dies at the transient row. Acceptance ledger: E2E-09/10/11/12/13/16/18 live across four runner groups. The acceptance drive has now surfaced FOUR product defects (#10 idempotency, #11 heartbeat, #12 delivery arms, #13 stale-credential stranding) - three fixed same-day, one owner-forked.

  - Progress (2026-08-31 - SIX SCENARIOS LIVE + the fault lever): E2E-13 is green end-to-end (persistence failure is truthful): a run-token-guarded /api/e2e/fault route arms the store's explicit head-update crash for exactly ONE append; the actor's real intent dies in the transaction; the typed STORE_FAILURE Error frame reaches the actor's socket (observed through a pass-through routeWebSocket tap); no page renders the phase advance (the shipped contract closes the match - the absence-safe assertion accepts closed-and-gone, never a rendered success); the arm is consumed at the throw so the next append is untouched (unit-proven with rollback: no events, no outbox rows). ENGINEERING LESSON WORTH THE PRICE: the first live run proved the fault never fired because the route armed the NEXT bundle's module flag while the socket host's store lives in the tsx graph - the two-module-graphs split AGAIN (the server.js SQLite singleton lesson); the arm now ALSO writes a sentinel file consumed (unlinked) at the failure point, graph-agnostic, env-gated so production never pays the stat - and the first sentinel draft leaked into a SECOND append because the short-circuit evaluated the module flag first without consuming the file; both arms are now evaluated-and-consumed together (unit row pins it). Acceptance ledger: E2E-09/10/11/12/13/18 live across three runner groups (authority-pack1, exactly-once-pack, fault-pack); 'authority' still reserved for the complete pack per 21.4. The fault lever is the reusable foundation for E2E-05/06 (crash before/after commit) and the 22.x failure pack.

- [x] 21.4 Register an exact command `npm.cmd run verify:qc:gm-two-player-campaign -- --group=authority` and make failures retain the complete evidence bundle.
  - DONE (2026-09-02, Wave G 21.4): `--group=authority` is registered now that every E2E-01..18 row carries a titled @E2E-NN tag on main: 01/02 authority-recovery pack (genesis rows are expected failures until the cutover), 03/08/18 authority pack1, 04/07 authority-order, 05/06 restart, 09-12 exactly-once, 13 fault, 14/15 resilience, 16/17 token. SPEC_BY_GROUP.authority is the Set-deduped list in pin order (pack1, exactly-once, fault, token, restart, resilience, authority-order, authority-recovery); authority sits in RESPAWNING_GROUPS (restart, resilience and recovery kill the server) so the plan runs --workers=1 under the relaunching wrapper; the reserved / NOT_IMPLEMENTED handling is gone and the qc pin moved authority into the implemented set (red on main before: NOT_IMPLEMENTED group=authority owner=29); `all` still names each of these specs once. Proof: every member pack was proven green pack-by-pack at its own landing (LAW 63) and authority-order re-proven alone on this main (2/2, 13 s); the union was run once, honestly: 20/20 passed in 4.5 min on this main with a freshly built live tree (run w21-4-authority-union-3), after two 20/20-red runs on the same tree's stale build (LAW 69). Finding #102 (a union of respawning packs can poison later packs) applies to authority exactly as to all; a union red is reported, never re-tried into green. LAW 69 found on the way: the relaunching wrapper serves the PRODUCTION build, so two 20/20-red unions on a stale-build tree were the build, not the packs - the live tree is rebuilt before any respawning proof. Not claimed: per-pack respawn isolation inside one union (the #102 wrapper reset) is still open.


## 22. Strict Acceptance Scenarios E2E-46 Through E2E-80

- [ ] 22.1 Implement and pass E2E-46 through E2E-60 for time cascade, retroactive rebuild, invalidation, scenario continuity, customized-unit fidelity, ten-scenario restarts, checkpoint equivalence, receipts, provenance, and impact preview.
- [ ] 22.2 Implement and pass E2E-61 through E2E-70 for malformed/replay attacks, atomic failure, fail-closed projection, GM loss, partition catch-up, stale branch, quarantine, bounded replay, and socket failure.
  - ERRATUM + PROGRESS (2026-09-04, finding #103 RETRACTED, 22.2 E2E-70 proven live as a real assertion): the 2026-09-03 finding "DurableMatchStore rejoin after a fault-skipped send does not resume at the missed delivery" was WRONG; the product resumes correctly and the defects were in the row. Evidence, four instrumented live runs on the production build (temporary server logs, never committed): the victim was the HOST in every run (first recipient in the fan-out); it lost delivery 5 (authority 5, movement_declared), received 6 live, and its client's own gap recovery (src/lib/multiplayer/client.ts requestResync) re-sent SessionJoin ON THE SAME SOCKET with deliveryCursor 4; the server answered firstMissed=5, resumeFrom=5, fetched [5,6] (movement_declared, movement_locked) and sent ReplayStart fromDeliverySequence 5 with a ReplayChunk stamped deliverySequences [5,6], all within one second; the victim tap read [...,4,6,5,6]. A durable-store copy of the jest row (DurableMatchStore ':memory:') passed on the first run, refuting the store attribution. Row defects: (1) victim identification subtracted one tap's delivery numbers from the other's - delivery numbers are PER VIEWER and fog withholds per viewer, so the guest legitimately ended at 5 while the host held 6, and the row named the GUEST the victim and waited for a frame it was never owed; (2) context.setOffline(true/false) never severs an established WebSocket in Chromium - the server log showed no socket close and no new upgrade during the row, so the "reconnect" was a no-op; (3) any absence-of-missed assertion after a fixed wait raced the one-second recovery. Row rewritten (title and tags unchanged): authority intact (durable event count grows; journal+outbox census unchanged over one second); the victim is the tap that received a mid-match RESUME (resumeStart: a ReplayStart whose fromDeliverySequence is above zero; the healthy tap never receives one) - ORCHESTRATOR FIX: the worker's live-Event hole scan read every viewer's initial ReplayChunk prefix (numbers 0 and 1 arrive as a chunk on both taps) as a hole on both, which would time out the exactly-one poll; the victim's LIVE Events skipped missed and carried missed+1 (the hole its client asked about); the victim holds a ReplayChunk whose deliverySequences include missed; the victim's deduplicated numbers are contiguous through at least missed+1 and the healthy tap's are contiguous from 0; census unchanged after the resume. No test.fail, no offline step, no cross-viewer subtraction. Jest: ServerMatchHostEventsSendFault.test.ts is now a describe.each matrix over InMemoryMatchStore and DurableMatchStore (':memory:'), same row, both green. LIVE (l89, production build, failure group): first run 8/8 red on "Cannot find module for page" for every lobby - an environment failure matching the stale-server signature (a leftover server attached by reuseExistingServer serving a build directory this run had just rewritten), not the row; rerun: 8 passed (32.2 s) with E2E-70 a REAL assertion for the first time. Live mutant NOVEL N1: the resume assertion inverted (the victim's ReplayChunk must NOT carry missed) - E2E-70 red, 7 passed: KILLED; restored. Gates: tsc 0 (two Set spreads replaced by Array.from for the e2e target); oxlint 77-pin; 140 multiplayer-server suites green (1056 tests); oxfmt clean. LAWS: never subtract delivery numbers across viewers; setOffline is not a reconnect; instrument before attributing a live red to a product layer. NOT claimed: no participant dimension on the fault scope (unchanged); a real reconnect resume (socket severed) is proven by the jest matrix's explicit rejoin, not by this live row.
  - PROGRESS + FINDING (2026-09-03, Wave G1 22.2 E2E-70): a single post-commit send failure is now injectable and its first two clauses are proven live; the third is a strict expected failure carrying a finding. Product: E2EFaultKind gains post-commit-send (DurableMatchStore.ts; the audit counted 5 kinds, there were 7, now 8; the CoordinatedOutcomeCorrectionRun catalog pin moved 7 to 8), exported firing helper throwForPostCommitSendFault, fired in ServerMatchHostEvents.sendNumberedLiveFrame immediately before the per-viewer safeSend (the audit named ServerMatchHost.ts:1239 and a function called safeSend; the real per-viewer site is lines about 370 to 389); one-shot, that recipient is skipped and the rest of the fan-out continues; the delivery number was already consumed before the send, by the same design that makes a backpressure refusal resumable, so a rejoin resumes at exactly the first frame the viewer lacks. Jest row (ServerMatchHostEventsSendFault.test.ts) RED against shipped code: exactly one per-viewer send throws, only that viewer's cursor stays unadvanced, and a rejoin quoting its held cursor receives the missed number first - ORCHESTRATOR FIX: the worker's helper counted only live Event frames while the resume arrives as ReplayChunk frames carrying deliverySequences, so the row read the rejoin as empty; widened to the backpressure suite's shape. Jest mutants: M1 fault never fires KILLED; NOVEL N1 throw moved after the send (victim receives yet is skipped) KILLED; NOVEL N2 fault not one-shot KILLED (first run was void - the anchor hit a different consumeE2EFault call; redone on the throwForE2EFault body); M2 the catch swallows every error SURVIVED and is EQUIVALENT - ServerMatchBroadcaster.safeSend catches both serialize and send failures itself, so no real error can reach that branch; the narrow message check stays as documentation. LIVE (l89, production build, failure group): run 1 failed on a fragile premise (the victim held one frame beyond its pre-arm head while still lacking the missed number; that equality is dropped, the strict head-below-missed check stays); runs 2 and 3: the journal+outbox census is byte-identical after the failed send and the healthy peer receives the event live, but after the victim's offline/online reconnect the tap (a page.routeWebSocket handler that re-fires per socket, so it does follow the reconnect) never showed the missed delivery number - the durable-store rejoin did not replay it, while jest proves the identical resume on the in-memory store. FINDING #103: DurableMatchStore rejoin after a fault-skipped send does not resume at the missed delivery; suspect the durable cursor/outbox path versus ViewerDeliveryCursors in memory. The resume clause is a strict test.fail scoped to that clause only (@until-durable-rejoin-resume; the census and healthy-peer clauses stay real reds), the pack runs 8 passed with it. Gates: tsc 0; oxlint 77-pin; 139 multiplayer-server suites green plus the new suite. NOT claimed: no participant dimension on the fault scope (matchId only; the row is victim-agnostic); the broadcaster is untouched; E2E-70's letter is NOT yet satisfied live until #103 is resolved.
  - PROGRESS (2026-09-02, Wave E3 PR-A2, E2E-66 under the #90 ruling): the partition scenario is carried as TWO rows in e2e/gm-two-player-failure.pack.spec.ts because the two reconnect paths take different delivery routes. R66a 'a partitioned player catches up in place, exactly once': the guest is set offline, the host commits a unilateral GoProne (#88: a phase advance needs both players, so it can never be the partitioned commit), the durable log grows while the guest's delivery numbers do not, and on reconnect exactly ONE new delivery number arrives, contiguous with the guest's prior high-water, with ZERO ReplayChunk frames (the vacuity control that this is the live-outbox path) and at least one live Event. R66b 'a reloaded player rebuilds from zero and still applies the missed event once': same partition and commit, then page.reload(); the missed number appears in BOTH a live Event frame and a ReplayChunk array; WITHIN the replay stream no number repeats and coverage is contiguous from 0; at least one ReplayChunk was received. The delivery counter now flattens the singular deliverySequence of live frames AND the plural deliverySequences[] of ReplayChunk frames (its docblock names the failure mode: a counter that read only the singular field was blind to every replay chunk, which is how #90's mutant survived). The apply-side proof is a jest row against the real client (src/lib/multiplayer/__tests__/client.test.ts) driving the measured frame order - live Event deliverySequence=5, then ReplayStart, ReplayChunk [0..5], ReplayEnd - asserting the applied list is unchanged by the replay, with a paired control that a genuinely new event (6) after the replay still applies; the observable is what the client emits to its consumer, because the durable match log is keyed by (matchId, sequence) so a double apply overwrites invisibly, and the networked surface renders no per-event list. Live results and orchestrator mutants in the PR body (N1 the client applies the overlapping live frame and the replayed copy; N2 the counter reads only the singular field; N3 the replay resumes after the last held event). NOT CLAIMED: E2E-64/65/67/68/69/70 still not carried; exactly-once is apply-side, not 'no repeats anywhere on the wire'.

  - PROGRESS (2026-09-02, Wave E3 PR-A, failure pack part 1): e2e/gm-two-player-failure.pack.spec.ts lands E2E-61 (malformed intent: typed refusal AND zero durable movement), E2E-62 (verbatim replay of a recorded intent: same counts AND same sequence set), E2E-63 whole (all three failure points, one row each, on A0's scoped fault kinds) - three scenarios, five rows, green live (--group=failure). The shared match flow moved to e2e/helpers/gmTwoPlayerMatchFlow.ts, the consolidation three pack headers deferred; the fault pack consumes it (-149) rather than a fourth copy. REPAIR: A0 (#1550) broke gm-two-player-fault.pack.spec.ts live on main - it armed {kind, mode:'once'} with no matchId and A0 made an unscoped arm a 400; no jest gate could see it (nothing in jest drives an e2e spec), so --group=fault-pack was RED on main between #1550 and this PR; routed through armScopedFault(request, 'append-head-update', match.matchId). SPLIT at the prefix-green boundary (whole seam measured 1148 non-generated lines): E2E-66 is HELD for PR-A2, NOT carved out of the program - the pack header says so and why - together with the flattened singular+plural delivery counter, hostOwnedUnitId, and the client apply-side row; see #90 for the ruling that reshaped it. Live mutants (restored, md5): M-A ClientMessageSchema.safeParse guard short-circuited -> E2E-61 the ONLY red; M-B throw new Error('test-append-event-insert') removed -> E2E-63 red (a first anchor miss came back a silent all-green - grep -c on the mutated token is now the check before trusting a survive). Orchestrator errata: (a) the jest slice under concurrent load showed faultRoute.test.ts + camp01-cleanup-authority.test.ts failing (105 s) - both pass in isolation; (b) one orchestrator --group=failure run failed every row with 'response.json: Protocol error (Network.getResponseBody): No resource with given identifier found' immediately after a held dev server, green on re-run - the harness's server handoff between back-to-back invocations is NOT root-caused and NOT claimed fixed. NOT IN THIS PR: E2E-66 (PR-A2); E2E-68 - its live half needs a write-capable corruption fixture that deliberately does not exist (the evidence reader refuses anything but SELECT) and #83/#84 predict it proves a gap rather than a pass; carved out in the pack header with E2E-64/65/67/69/70.

  - PROGRESS (2026-09-02, Wave F2 PR-A0, findings #72/#75): the fault lever now meets its own contract before any failure scenario is built on it. Session scope: IE2EFaultScope {matchId} on the arm, the route 400s without it, every firing point compares scope BEFORE consuming (consume-then-compare would silently disarm another match's fault); positive control that the fault still fires for its own match. Run-scoped sentinels data/.e2e-fault/<runId>/<kind> carrying {matchId, armedAt}; clearStaleE2EFaultSentinels() sweeps every other run's directory at server start and logs the count (sourced from the store module - one owner of the path convention). The two missing E2E-63 fault points added: append-event-insert and append-outbox-insert, fired at the MIDDLE of the batch (failing the first event would only prove an empty transaction rolls back; the outbox fault fires after its own event insert - the window the outbox exists to close). Deleted: the unscoped _armFailAtHeadUpdateOnce and the static E2E_FAULT_SENTINELS record. Red-first: 5 route rows red for their own reasons (no scope -> 400; foreign match untouched; sentinel under the run; two new kinds arm); the two sweep rows were written AFTER the sweep and are mutant-verified, stated not implied. Mutants killed: consume ignores scope, flag cleared but not the sentinel (the twice-shipped bug - standing mutant), sweep no-op; a first M3 attempt missed its anchor and reported a silent full pass - caught as implausible (harness failure mode named). Orchestrator: scope-ignored re-run and parser-trusts-nothing killed; 'consume BEFORE compare' SURVIVED first - the exact defect the code comment names had no row (the foreign-match row proved the foreign match untouched but not that the sentinel stayed ARMED) - fixed red-first with an arm-A / append-B / sentinel-still-exists / A-then-fires row. Scenario mapping for this box (E2E-61..70): runnable with no new kind 61/62/66/68; 63 now whole; 65 runnable but ~75 s; 70 taken as a client-side frame drop (a stated reading); GATED 64 (no projection fault kind), 67 (section 14/16 + #38), 69 (rides F1's cold catch-up). PR-A carries the scenarios + group `failure` OFF the relaunching wrapper.

- [ ] 22.3 Implement and pass E2E-71 through E2E-80 for latency, catch-up, backpressure, accessible lifecycle UX, confirmation, typed conflicts, evidence completeness, cleanup, and exact-main post-merge audits.
  - PROGRESS (2026-09-03, Wave G1 22.3 E2E-79): ownership-scoped cleanup is asserted on the finish AND abort paths, and the spec now runs. Before: e2e/gm-two-player-cleanup-ownership.spec.ts held only the two ambient/idempotent rows and appeared in no group and no other file (grep over scripts/ and e2e/ returned nothing else), so it ran nowhere. Fix: group cleanup-ownership registered in GROUP_CATALOG + SPEC_BY_GROUP (owner 22), the qc test's groups pin, implemented whitelist, plan pin and all-args pin updated in the same edit; the spec spawns an owned child that holds a server, a client socket, a temp SQLite DB and per-run artifacts, and asserts after a FINISHED run (finish byte) and after an ABORTED run (kill mid-scenario, no finish byte) that the socket is destroyed, the child pid is gone, the DB file is unlinkable (connections closed), artifacts sit under the run's own runtime and evidence roots, and a sibling path is refused with FOREIGN_PATH; releaseOwnedRun in a finally is the harness. ORCHESTRATOR ROW on top: the two rows drive the child's own shutdown, so a mutant that disabled the harness release SURVIVED them - a third row 'after a run that dies mid-drive the harness still releases everything' throws before telling the child anything and asserts the release still leaves nothing behind while the drive error still surfaces (rejects.toThrow('drive-crash')). Live on the production build (l83): 5 passed (11.4 s). Mutants (spec-level, live Playwright per mutant): M1 harness release does not kill SURVIVED the worker's rows, KILLED by the crash row (4 passed 1 failed); M2 harness release keeps the client socket SURVIVED and is EQUIVALENT - when the child dies the peer closes and the client socket reports destroyed on its own, so the release's destroy is defensive, kept; NOVEL N1 group unregistered again KILLED by the qc pin (jest 1 red). Gates: tsc 0; oxlint 77-pin; qc pin suite 9/9. Could NOT assert (named by the worker): the Playwright webServer itself is ambient and outlives the row (the letter preserves it); product campaign sockets on that server; a full-disk leak scan (scoped writes plus the guard only). Lesson (LAW 87): a Python re.sub replacement expands backslash escapes - use a callable or plain str.replace when the replacement holds a literal backslash-n.
  - PROGRESS (2026-09-03, Wave G2 22.3 E2E-78): an evidence bundle finalizes only when every artifact kind is present for every role, or the run fails naming every missing cell. Before: e2e/fixtures/gmTwoPlayerEvidence.ts collected missing entries ({kind, role, why}) and finalize STILL SUCCEEDED, writing the list into the manifest (was lines 158-175), so a run could claim a complete bundle while carrying a declared hole; roles were not declared in the fixture at all (they lived in gmTwoPlayerCampaign.ts:13). Fix: EVIDENCE_KINDS (9: trace, screenshot, socket-transcript, projection, latency, durable-rows, hashes, environment, cleanup-log) and EVIDENCE_ROLES (3: future-gm, future-player-1, future-player-2) are declared constants; listMissingEvidenceCells computes the absent kind-by-role cells; finalize throws EvidenceBundleError EVIDENCE_INCOMPLETE naming every missing cell and writes no manifest; a complete bundle finalizes exactly as before; writeCompleteEvidenceMatrix is the test helper. Opt-in, never a default: IFinalizeOptions.allowIncompleteEvidence: true, passed by the three packs that record a subset by design - gm-two-player-fixture.smoke (environment only), gm-two-player-membership.smoke (admission transcripts + durable rows), gm-two-player-performance.pack (one latency report) - and by the jest row that declares what it meant to capture; the new completeness rows do not use it. Rows RED against shipped code: jest 'refuses to finalize when a kind-role cell is absent'; live @E2E-78 in gm-two-player-fixture.smoke (the pack that already finalized evidence) - an incomplete bundle fails closed naming the cells and a complete one finalizes. Jest mutants, all KILLED: M1 the gate never throws; M2 partial evidence allowed by default (the opt-in flipped to an opt-out); NOVEL N1 the error names only the first missing cell. LIVE (l83, production build, fixture-smoke): 2 passed. Gates: tsc 0; oxlint 77-pin; evidence fixture suite 12/12. NOT claimed: the existing packs do not emit a full 27-cell bundle (the three named packs opt in to partial evidence); scripts/qc has no consumer of the manifest's missing field and is untouched.
  - ERRATA (2026-09-03, same audit): FINDING #47 as recorded above is STALE and must not be cited as live evidence. Its first half is false on main: projectionSignal IS passed by the lobby page and the spectate page, and useMultiplayerSession both holds and returns it. Only the second half survives, and only for one value - tacticalLifecycleState maps PROJECTION_REBUILDING and nothing else, so 'rebuilding' is now fed while 'rewound' is still unreachable. A finding in this file is a dated claim, not a standing fact.
- [x] 22.4 Register `--group=campaign`, `--group=failure`, `--group=performance`, and `--group=all` selectors without weakening strict backing requirements.
  - DONE (2026-09-02, Wave G 22.4-c, ERRATA on #102): the `all` union is GREEN: 39/39 rows passed in 9.3 minutes under the relaunching wrapper on a freshly built live tree (run w102-all-rebuilt-1; fixture, membership, pack1, exactly-once, fault, token, restart, resilience, authority-order, authority-recovery, privacy, proposals, performance E2E-71/72/73 inside budget, three-context, two-device, failure). Finding #102 (the union of respawning packs poisons the packs that follow: page.waitForURL timeouts after the restart pack's deaths, an empty lifecycle read on E2E-15, the performance row one frame short) is WITHDRAWN as a wrapper defect and reclassified: the relaunching wrapper runs server.js, which serves the PRODUCTION build, and a live tree whose build predates its checkout answers PageNotFoundError for the lobby page after every respawn - exactly the rows that failed. The same day the authority union (21.4) went 20/20 red twice on a stale build and 20/20 green after `npm run build`, and this run went 39/39 with the same discipline; the earlier 30/34 and 29/34 were that build, not the wrapper (LAW 69). The per-pack wrapper reset named as still-open is therefore not needed and is not built. Honest bound: the tree's source was at 688a10ecb while its build was made at fe8304479 plus the 21.4 registration; the delta (16.4-c, the server-side artifact consult on the campaign command route) was not in the served build, so this run proves the wrapper and the union, and the 16.4-c route is proven by its own suite, not by this union. All four selectors (campaign, failure, performance, all) are registered with their plans pinned and their members proven pack by pack; `all` is now also proven as a union. Still true: `all` is not a CI gate and no CI job runs it (a 9-minute live union stays a local/nightly proof).

  - PROGRESS (2026-09-02, Wave G 22.4-b): `--group=all` is registered as an expansion in the qc core (every SPEC_BY_GROUP member except the reserved names that still throw - authority until 21.4, campaign, visibility, combat, the evidence/fault smokes; specs de-duplicated in pin order; run behind the relaunching wrapper because restart-pack and resilience-pack are members) and pinned (plan args, 15 specs, --workers=1; moved from the NOT_IMPLEMENTED loop). `--group=failure` was already registered and pinned. `--group=campaign` stays NOT_IMPLEMENTED on purpose: no pack carries an E2E-46..60 title (22.1 has no receipts). FINDING #102 - the union is NOT green and this box does not claim it: two full runs on main gave 30/34 then 29/34. The first run exposed two real regressions that also failed ALONE (#100 fault-lever scope, #101 veto confirm; fixed in #1594). The second run, with those fixed, fails only rows that pass alone: after the restart pack's process death late in the union, the packs that follow (token E2E-16/17, restart E2E-06 itself on its second death) time out on page.waitForURL - the relaunching wrapper does not hand the next pack a healthy server once several deaths have happened in one process lifetime; resilience E2E-15 read an empty lifecycle state after 3.8 minutes; and the performance row E2E-71/72/73 missed its painted-delivery budget by ONE frame (999/1000) under the long run. So `all` is a registered selector whose union is an order/lifetime interaction still to fix (the respawning wrapper's per-pack reset, the perf row's margin under a 15-minute session); it is not a CI gate and no CI job runs it. Live evidence: scratchpad live-22-4-all.log / live-22-4-all-b.log (orchestrator). STILL OPEN on 22.4: campaign (22.1 rows), authority (21.4), and #102 before `all` can be called passing.

  - PROGRESS (2026-09-02, Wave F1 PR B): `--group=performance` registered in scripts/qc/gm-two-player-campaign-core.cjs (performance:33, no renumbering) with the plan pin and archive-path pin asserted in gm-two-player-campaign-qc.test.ts; strict backing unchanged. campaign / failure / all selectors still open (Wave G).


## 23. Performance and Long-Run Gates

- [x] 23.1 Commit the controlled fixture configuration: 20 warm-up commands, at least 200 measured representative commands, monotonic correlated clocks, nearest-rank percentile calculation, named runner class, 100-event/512-KiB replay chunks, 256-envelope/1-MiB queue bound, 128-MiB server post-warm memory-growth ceiling, and 64-MiB post-warm ceiling per controlled browser client.
  - CLOSED (2026-09-02, Wave F1 PR A, src/lib/multiplayer/performance/controlledLoopbackFixture.ts): CONTROLLED_LOOPBACK_FIXTURE frozen and compared whole by the unit suite - 20 warm-up, 200 measured representative commands, monotonic correlated clocks (correlateClocks), nearestRankPercentile pinned to a hand-computed table, named runner class ControlledLoopbackPerformanceRunner, 100-event/512-KiB replay chunks, 256-envelope/1-MiB queue bound, 128-MiB server and 64-MiB per-client post-warm ceilings; the product constants the ceilings bound (REPLAY_CHUNK_SIZE=64 <= 100, MAX_BUFFERED_BYTES=1_048_576 <= 1 MiB) are asserted against them. Warm-up samples recorded and EXCLUDED from the percentiles (row: 20 warm-ups at 5000 ms + 200 measured at 10 ms -> p95 10). Red-first: module absent. Mutants killed: nearest rank off by one (N=20 p95 -> 19 not 20), warm-up counted as measured, p95 gated on the p99 budget, gate reads a stale archive (StalePerformanceArchiveError on run id AND measured-sample count); orchestrator: p95-on-p99 re-run, cold catch-up gate removed, p99 computed as p95.

- [x] 23.2 Gate p95 at 250 milliseconds, p99 at 750 milliseconds, and 1,000-event cold catch-up at 2 seconds on the recorded controlled loopback runner; keep the 2,000-millisecond Playwright wait as a functional timeout only.
  - CLOSED (2026-09-02, Wave F1 PR A + measured in PR B): evaluate() gates p95 <= 250 ms, p99 <= 750 ms, 1,000-event cold catch-up <= 2,000 ms on the recorded ControlledLoopbackPerformanceRunner:controlled-loopback-local; the 2,000 ms Playwright wait is functionalWaitMs used only by waitForSequence, never read by a gate. Measured twice on the final code (PR B archives): p95 28.0/31.6 ms, p99 38.7/46.5 ms, cold catch-up 930/755 ms over 1,020 events. Vacuity closures: a p95 quietly compared against the p99 budget and a gate reading an earlier run's archive both have a mutant and a named killer row.

- [ ] 23.3 Run ten sequential scenarios with periodic player reconnects, GM reconnect, server restart, correction, pre-receipt rewind, post-receipt coordinated correction, and checkpoint/full-replay comparison.
- [x] 23.4 Verify `npm.cmd run verify:qc:gm-two-player-campaign -- --group=performance` and `npm.cmd run verify:qc:campaign-long`; archive latency and memory JSON with the run.
  - DONE (2026-09-03, audit Wave 0): both named commands ran green on a tree built from this checkout. verify:qc:gm-two-player-campaign --group=performance --run-id=task-23-4-20260903 passed its single row (E2E-71/72/73, controlled loopback latency, catch-up and memory) in 3.9 minutes, and verify:qc:campaign-long exited 0. The run was built first: the relaunching wrapper serves the production build, and a stale build reds unrelated rows, which is this change's own LAW 69 errata. THE NUMBERS, quoted rather than archived because test-results/ is gitignored (so an 'archived with the run' claim could never be a tracked file): the report is written to test-results/gm-two-player/task-23-4-20260903/controlled.latency.performance.json beside its manifest, over 400 measured samples after 40 warm-ups, with a representative mix of AdvanceDay, AllocateSalvage and SpendFunds. Latency p95 22.6 ms against a 250 ms budget and p99 30.9 ms against 750 ms. Cold catch-up replayed 1020 events in 714 ms against a 2000 ms budget. Server memory grew 32.9 MB RSS (16.3 MB heap) against a 134 MB ceiling, and no browser context grew. Verdict object records passed with an empty failure list. Environment: node v22.22.0, chromium 143.0.7499.4, win32. Two corrections to earlier readings of this box: one green performance run satisfies the letter (the 'twice consecutively' discipline belongs to a different receipt), and the archive clause is met by quoting the path and the values, not by committing the file. Not folded in: the implementation plan's --warmups/--samples flags exit 2 INVALID_ARGUMENT on this runner; that is a separate defect and is not this box's letter.

  - PROGRESS (2026-09-02, Wave F1 PR B, e2e/gm-two-player-performance.pack.spec.ts = E2E-71/72/73): the performance half is verified - the pack ran 9 times in the l83 e2e worktree, passed twice consecutively on the final code (wave-f1-perf-final-2, -final-3; one environmental flake final-1: SecurityError reading localStorage at context startup before any measurement), latency + memory JSON archived through the run-owned evidence bundle and RE-READ from disk with a freshness check (run id + measured-sample count) before any gate reads a verdict. Numbers: p95 28.0/31.6 ms, p99 38.7/46.5 ms, cold catch-up 930/755 ms over 1,020 events, queue peak 1 envelope / 605 B, chunk max 1 / 605 B, server growth 0 B over the fixture window and 12.6/1.7 MB across catch-up, per-context growth <= 9.8 MB (env: node v22.22.0, Chromium 143.0.7499.4, win32 10.0.26200). Server clock + RSS exposed by the e2e-guarded GET /api/e2e/performance-probe (404 outside e2e mode or without the run token); a read-only E2E-guarded accessor in storeExposure.ts reaches the surface's own transport (a second GM socket would read as a host reconnect and pause the session). Commands issued one at a time so 'correlated by command identity' is literally true. NOT CLAIMED: measuredSamples are 400 (200 commands x 2 guest contexts) and the archive says so; queue depth is application-level, never ws.bufferedAmount (#19); the campaign channel serves a cold join as baseline-plus-tail (RESYNC_SNAPSHOT_GAP=50), so E2E-73 is measured as recovered tail DEPTH with the chunk/queue/memory ceilings proving boundedness; the 800-command scaffold beyond the letter's fixture grew the server ~851 MB RSS / ~466 MB heap - recorded in the archive as serverMemory.tailBuild, UNGATED, the 128-MiB ceiling not widened (finding #41); verify:qc:campaign-long and the ten-scenario run (23.3) untouched.


## 24. Cutover Rollback and Exact-Main Regression

- [x] 24.1 Prove shadow dual-write state and audience digests match before enabling journal authority for new sandbox sessions.
  - Receipt (2026-08-29 - CLOSES this box; both clauses delivered, sealing SECTION 24 at 4/4): the shadow comparison now computes PER-AUDIENCE digests - gm and player:<id> for both seats - by running the decided events and the live events through the REAL fog filter (fresh visibility cache, real match config) and the REAL viewer field-projection policy (ViewerFrameProjector's own removal rules, GM keeping sequence), with seat identities threaded from the SAME getMatchMeta source the live broadcast uses - no parallel seat authority invented. Any audience mismatch lands in the ShadowComparisonRecord as audience-digest-mismatch:<audience> and flips equal=false, which the 4.2 admission tripwire already consumes - an audience divergence now refuses journal authority for new matches automatically. The divergence surface this adds over event equality is exactly fog state: the proof row corrupts SCRATCH-ONLY visibility ownership while keeping events and the authority digest equal, and the audience digest catches it. SANDBOX RUN: a real host in mode shadow with fog ENABLED drives real commands and every recorded comparison is equal across GM, Player 1, and Player 2 - the shadow-equality run for controlled sandbox sessions; the production mode flip remains the deployment act. Mutations, worker-run then reviewer-re-run: digesting events without state dies at the fog-state row; dropping Player 2 dies at the audience-completeness row; the reviewer's NOVEL mutant - stripping sequence from the GM audience through the refactored projector policy - dies at the authority-keep row, proving the 11.x contract survived the refactor. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 5 suites / 40 (reviewer subset 3/25); server tree 98/707; multiplayer tree 109 suites / 836 passed. This machinery is also 12.2's per-viewer digest foundation - the parity-across-surfaces rows remain that box's own work.

- [x] 24.2 Cut over new sessions behind the reviewed feature flag, preserve legacy completed-log read compatibility, and refuse ambiguous active-session migration.
  - Receipt (2026-08-29, combat scope - campaign cutover stays delegated to design-campaign-authority-and-sync per wave-map row 5; verified on main 293790e09): per-match admission at creation behind the off-by-default reviewed flag; legacy reads schema-compatible (DurableMatchStore suites green in both reconciliation runs); ambiguity REFUSED on two independent axes - an imported legacy stream is refused journal authority before any flag is consulted, and recovery selects its reader from durable facts alone (matchRollbackReaderSelection.ts, #1451), the options.recovered bypass deleted. The leaf's five SQL-corruption rollback proofs (#1453) pin the refusal shapes against a real store.

- [x] 24.3 Document rollback that stops new admission, preserves journal/branch/receipt/audit rows, and returns to a schema-compatible reader without destructive history edits.
  - Receipt (2026-08-29, combat scope, verified on main 293790e09): documented normatively in the leaf's delta spec (adopt-combat-event-journal-authority/specs/event-store/spec.md, 'Combat Rollback Preserves Journal Authority' - rollback SHALL stop admission, preserve rows/receipts/head/generation/recovery state, and never substitute an incompatible log), implemented as the pure fact-driven selector wired through recovery, and PROVEN against real SQLite with SQL-written corruptions: blocked states refuse new command/effect admission with typed errors, serve no legacy substitution (digest-asserted), and leave a byte-identical store dump. NOTE: the delta text merges into main specs at the leaf's archive; the normative content exists and is implemented today.

- [x] 24.4 After the pre-harness ABI-checker merge, archive its exact-main native-store regression. Land the isolated three-context fixture next, then after every later major merge update the evidence ledger and run the applicable staged `npm.cmd run verify:qc:gm-two-player-campaign -- --group=<slice>` against exact main before the next dependent PR: `fixture-smoke` before role admission, `membership-smoke` when durable GM/P1/P2 roles land, and the evolving `smoke` subset thereafter.
  - Receipt (2026-08-29, second entry - CLOSES this box; the staged ladder is complete and exercised): the evolving `smoke` group now exists as the composition of both landed groups in ONE runner plan (fixture + membership specs, multi-file forwarding proven by the 3.4-erratum probe), grown one line per landed group inside buildRunPlan so the subset can never silently narrow elsewhere - and NARROWING IT IS CAUGHT: dropping either spec from the list reds the plan row (mutant killed). First smoke run on merged main 2185b5e3c: both specs green in one invocation (2 passed). All three named groups have now run green against exact main today; every unimplemented group still throws typed NOT_IMPLEMENTED (7/7 runner suite). The box's per-merge discipline (update the evidence ledger, run the applicable slice before the next dependent PR) continues as a standing landing ritual, the same way the exact-main regression rituals do - the deliverable machinery and its first exercised pass are what this box names, and both exist.

  - Progress (2026-08-29, the membership-smoke clause is DELIVERED; box stays open for the evolving smoke subset): the membership-smoke group now exists and runs - a three-context spec (e2e/gm-two-player-membership.smoke.spec.ts) driving the production match API and campaign WebSocket with the fixture's three authenticated identities: GM seat, two tactical seats, a seeded FOURTH identity refused campaign-tactical-seats-full with NOTHING streamed, durable campaign_session_participant rows verified through the fixture's read-only evidence reader, role-labeled evidence bundle finalized. Green via BOTH invocation paths (direct playwright and the runner --group=membership-smoke); fixture-smoke regression green through the runner. BEFORE ITS FIRST GREEN RUN this group surfaced THREE product defects, each fixed under its own law: (1) the co-op join streamed the CampaignSnapshot before the durable seat cap refused (#1461 - seat-before-stream, order-restore mutant); (2) the socket module graph's SQLite singleton was never initialized - tsx-required modules hold a DIFFERENT singleton than Next's API bundle, so every durable-membership dispatch threw 'Database not initialized' and closed the socket (server.js now initializes at loadMultiplayerRuntime; idempotent, shared DATABASE_PATH, WAL-safe); (3) the grant-channel served arm NEVER consulted the durable seat system at all - two shipped membership systems had not met, so seats never filled on the live path and the cap could not fire (handleRoomCodeGuestJoin now binds the tactical seat before any grant/snapshot/attach, refusing typed on full). Mutations, both killed live: the stranger control asserted admitted reds at the real refusal; the durability read starved to an empty result reds at the participant-row assertion. Gates: tsc 0; oxlint 0 errors / 77 warnings; runner+socket suites 59/59 (bindCampaignSyncConnection's 41 intact); the spec 1/1 post-oxfmt. Every other runner group still throws NOT_IMPLEMENTED - the evolving smoke subset remains this box's open clause.


## 25. Final Verification and Documentation

- [ ] 25.1 Run focused changed-area Jest and Playwright suites, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run format:check`, `npm.cmd run verify:qc:multiplayer:contracts`, `npm.cmd run verify:qc:multiplayer:browser`, `npm.cmd run verify:qc:coop-campaign-journey`, `npm.cmd run verify:qc:encounter-combat-continuity`, `npm.cmd run verify:qc:replay-recovery`, and the complete GM/two-player command.
- [x] 25.2 Run `openspec validate harden-gm-two-player-campaign-sessions --strict --no-interactive`, `npm.cmd run qc:openspec-ci:validate`, and LSP diagnostics for every modified TypeScript/TSX file.
  - CLOSED (2026-09-02, run by the orchestrator on main at f249e977a, after #1592): `openspec validate harden-gm-two-player-campaign-sessions --strict --no-interactive` -> Change is valid; `npm.cmd run qc:openspec-ci:validate` -> workflowContracts=8/8 aggregatorNeeds=18/18 protectedContexts=4 packageScripts=3/3 activeOpenSpecChanges=8 accountedActiveOpenSpecChanges=8 errors=0; TypeScript diagnostics for every modified file: `npx tsc --noEmit` clean on main, and every seam PR today carried the same gate (tsc + oxlint 'Found 77 warnings and 0 errors' + oxfmt on every diffed file) before merge - that per-seam ladder is the LSP-diagnostic pass the letter names, applied to each modified file at the time it changed rather than once at the end. Later seams re-run the same three commands as part of their ladders; this box records the pass, it does not freeze main.

- [x] 25.3 Update operator and contributor documentation for authority versus delivery sequence, membership/reauthentication, host loss, rewind boundary, recovery/quarantine, evidence bundle, performance methodology, and exact-main post-merge audit.
  - CLOSED (2026-09-02, Wave G 25.3): docs/features/gm-two-player-campaign.md (operator: authority - one batch per command in the durable store; delivery - per-viewer cursors and acks, the socket buffer cap, membership and host loss; the rewind boundary - non-mutating recorded preview, commit through the branches store, the delivered campaign receipt as the hard stop, lineage on the viewer history; recovery and quarantine; evidence - the group catalog, the per-run sqlite files under .sisyphus/e2e-runtime/<run-id>/, @E2E-NN title tags; performance - the loopback probe and budgets; exact-main) and docs/development/gm-two-player-campaign-contributing.md (contributor: the store ports, the rewind modules, live-pack conventions - one live group at a time, respawning groups, the runner's derived port versus the Playwright default; how to add a row and register + pin a group; the ladder and mutant discipline), with pointers in docs/README.md. Every statement cites the file it was read from. Honestly LEFT OUT because it was not on main when written: the per-viewer unacked bound (21.1 PR3-a, in flight - the docs say the bound is pending and describe the buffer cap that is on main), the coordinated correction as a shipped command (17.2), the ten-scenario run (23.3). A follow-up docs pass is expected when those land; the letter's four subjects are covered from sourced facts.

- [ ] 25.4 Reconcile all 80 acceptance scenarios to test identifiers and evidence paths, leave no unchecked implementation task without a linked blocker, and archive the change only after every applicable strict gate passes.
  - NOTE (2026-09-04, owner ruling): the reachable acceptance surface at the split is 34 of 80 scenarios; six rows (E2E-40 to 44, E2E-76) run as strict expected failures tagged @until-journal-cutover in group rewind-pack and are owned by successor `adopt-combat-journal-cutover-and-gm-rewind` (its box 0.1 delivers the 80-scenario reconcile this box names). The archive receipt of this change MUST state that count and that one headline capability was withdrawn to that successor.
