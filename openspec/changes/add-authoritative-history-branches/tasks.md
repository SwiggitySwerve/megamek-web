Every PR in this change MUST stay under 1000 non-generated changed lines and 15 files, own one behavior seam, and split through an OpenSpec update before implementation if the cap cannot be met. (Cap follows wave-map rule 7; line cap raised 500 -> 1000 on 2026-08-28.)

Work-path trace: this final event-history leaf follows `add-cross-stream-effect-receipts` in `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md` for receipt-consuming PR 4+; the branch storage/port seams in PR 1-3 may proceed under the September 2 gate amendment. Live Branch PR 3 rewind acceptance follows combat-journal cutover S1-S4 and its correctness gates; that cutover does not require this whole change to be complete or archived.

## 0. Effect-Receipt Admission — Pre-Implementation Receipt

- [ ] 0.1 Before receipt-consuming PR 4+, verify on fetched exact main that `add-cross-stream-effect-receipts` is synced and archived, its task 10.4 and post-merge terminal evidence pass, its active ledger entry/directory are absent, and its merged branch/worktree are pruned. Record the predecessor merge SHA and fail closed on missing or contradictory evidence. PR 1-3 use the explicit September 2 cross-stream predecessor-receipt-only exemption below; it does not waive this gate for PR 4+. PR 1-3 still retain their own exact-main proof, independent review, and package-specific approval/behavior receipts.
  - Gate amendment (2026-09-02, owner-directed under the finish-all-107 program directive; precedent: the 2026-08-20 per-group gate refinement #1259): NARROWED for PR 1-3 only. The original clause demands add-cross-stream-effect-receipts synced and archived, but PR 1 (branch/effective-head/supersession tables + prior-head resolver), PR 2 (candidate build + atomic activation), and PR 3 (combat rewind) consume NONE of the cross-stream machinery - they are pure branch-storage and combat-side work; the umbrella's sections 13.4/14/15/16/17/18.x/19.2-19.3 (23 boxes) are gated on exactly these three PRs. PR 4+ (campaign replacement, coordinated correction saga) DO consume effect receipts and KEEP the original gate untouched. Recorded predecessor state at amendment time: adopt-combat-event-journal-authority archived 2026-08-29 (merge base 293790e09); cross-stream 0/51 open, gated on the campaign journal cutover. This amendment is spec-only, reversible, and rides PR 1 for review.

- [ ] 0.2 Before PR 1, re-review every PR seam in this file against the then-current codebase for the 1000-line/15-file cap and one-behavior-seam rule, and re-confirm seam ownership against `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`; if any seam no longer fits or ownership has drifted, split or reassign it through a spec-only OpenSpec update before implementation, and fail closed on unresolved ownership conflicts.
  - Progress (2026-09-02): re-reviewed at implementation time. Section 1 as one seam measured 18 files / 2108 non-comment added lines against the 15-file / 1000-line cap (5 of the files are the forced trigger-catalog consumers, 33 lines), so PR 1 was split into three sequential PRs with no file straddling a seam and no back-edges: PR 1a schema + backfill (tasks 1.1/1.2; 11 files), PR 1b contract + store (typed refusals; 3 files), PR 1c resolver + expected-head (tasks 1.3/1.4; 4 files); each prefix proven independently green (tsc 0; 98 / 106 / 1763 tests). Seam ownership per event-history-wave-map.md unchanged. The box stays open until the same re-review is done for PR 2+ seams.


## 1. Genesis Branch and Prior-Head Resolver — PR 1

- [x] 1.1 Add branch, effective-head, and supersession tables with one genesis/effective branch at generation `1` per existing journal stream; preserve the linear stream's stored generation idempotently.
  - Receipt (2026-09-02, PR 1 of 3 for this section - the section was split per the 1000-line cap, see 0.2): SQLite migration 23 `event_history_branches_schema` (sidecar SQLiteService.historyBranches.migration.ts) adds three ADDITIVE tables - event_history_branches (one immutable row per branch: stream, opaque id, parent, base revision/event/digest, creator, reason, typed status), event_history_effective_heads (one row per stream naming the answering branch and its generation), event_history_supersessions (one immutable row per superseded branch binding prior to replacement generation, CHECK replacement = prior + 1) - with no foreign key into event_journal_* and the journal's branch_id='root' pin untouched (design D1). Genesis backfill: one effective branch per stream that already has journal events, root digest = sha256(canonicalizeJsonV1([])) pinned as a literal (the contract test proves it equals the derivation and the value genesisJournalAuthorityBaseline records); the generation is READ - match_authority_baseline.effective_generation when stored, else 1 - never computed from stream_revision; both statements NOT EXISTS-guarded and proven idempotent by cold reopen and by replaying the migration after deleting its ledger record. Migration-head pins in the eventJournal and campaignOutcomeInbox suites are retitled to derive from the MIGRATIONS catalog instead of the literal 22. Red-first: module-not-found on the sidecar. Mutants: (c) generation derived from stream_revision - killed by 'preserves the stored generation across repeated migration' and the backfill row; orchestrator re-ran it (2 red) plus N1 partial-index WHERE dropped (9 red) and N2 ancestry guard made cross-stream (1 red). Gates: tsc 0; oxlint 77-pin; events+persistence trees 47 suites / 863 green; openspec validate clean.

- [x] 1.2 Enforce same-stream acyclic immutable ancestry, root genesis semantics, typed monotonic statuses, and exactly one effective branch.
  - Receipt (2026-09-02, PR 1 of 3): the four rules land as CONSTRAINTS, not convention. Same-stream acyclic ancestry: ancestor_depth is 0 at the root and the event_history_branches_ancestry_guard trigger requires each child's parent to already exist IN THE SAME STREAM at exactly depth-1, so depth strictly increases along parentage; lineage columns are immutable (_immutable_lineage trigger; only status may move) and rows cannot be deleted (_no_delete), so no later write can bend an edge into a cycle. Root genesis semantics: depth 0, null parent, null base event, base revision 0 are one fact stated four ways and the CHECKs require them to agree, so a child cannot masquerade as a root. Exactly one effective branch: a PARTIAL unique index over (stream_type, stream_id) WHERE status='effective' - the load-bearing guard that binds ANY writer (the effective-heads primary key alone only says which branch is installed). Monotonic status: a rank ladder building < waiting-effects < blocked < effective < superseded an UPDATE may only climb (the precise legal transition table is the store's typed refusal in PR 2 - proven NOT redundant there: blocked->effective climbs the ladder and only the table refuses it). Worker mutants killed: (a1) depth clause dropped and (a2) immutable-lineage trigger deleted - both by 'refuses ancestry that leaves the stream, skips a generation, or closes a cycle'; (b) partial unique index deleted - by 'permits exactly one effective branch per stream'. Five exhaustive trigger-catalog suites are mechanical consumers of the six new triggers. Typed refusals over these constraints (invalid-ancestry, duplicate-effective-branch, illegal-status-transition) are PR 2's store.

- [x] 1.3 Resolve verified parent prefix plus contiguous child suffix at an explicit branch/revision and prove entity state/history at prior heads.
  - Receipt (2026-09-02, PR 1c of 3 - PR 1a schema #1495, PR 1b contract+store #1497): EventHistoryBranchResolver resolves a branch path at an explicit branch/revision as the immutable parent PREFIX through the base revision followed by the child's own contiguous SUFFIX beginning at baseRevision+1 (resolveBranchPath -> prefix/suffix segments); materializeBranchPath verifies event identity, revision contiguity, digest linkage, and event schema version on every resolved event before use and quarantines with the typed branch-integrity error otherwise (six quarantine cases tested); entity state/history at a prior head is proven with a REAL SQLiteEventJournal append driving the production journalBranchSegmentReader adapter. The resolver works over a structurally narrower IBranchEventView that every IStoredEvent satisfies, so multi-branch paths are expressible while EventBranchId stays pinned to 'root' (widening it would ripple through the zod literals and every journal consumer; production genuinely stays genesis-only). NOT claimed: projector compatibility - that is the checkpoint contract and lands with candidate verification in PR 2; stated in the module header. Red-first: module-not-found on the resolver. Worker mutant (d) drop the branchId != active -> STALE_BRANCH check - killed by three expected-head rows. Gates at the full-section tree: tsc 0; oxlint 77-pin; events+persistence 47 suites / 863 green; the section's own 161 suites / 1763.

- [x] 1.4 Add branch-aware expected-head validation and typed `STALE_BRANCH` without enabling branch creation in production.
  - Receipt (2026-09-02, PR 1c of 3): validateExpectedBranchHead (EventHistoryExpectedHead.ts) is the branch-aware expected-head validation: a command naming a branch other than the stream's active head is refused STALE_BRANCH carrying the active head and a resync action, and it appends NOTHING - proven by a before/after snapshot of branches, heads, supersessions and the journal event count around the refusal (including a superseded branch). Branch creation stays OFF in production: createBranch requires an explicit IBranchCreationSeam capability and the production seam refuses ('keeps branch creation off in production and open only through the explicit seam'); the authorized GM correction/rewind finalization that will open it is PR 3. NOT claimed: activation compare-and-swap, correction leases, generation increment, GENERATION_EXHAUSTED, the supersession WRITER, PROJECTION_REBUILDING - all PR 2+.

- [ ] 1.5 Run focused branch storage/resolver/integrity tests, typecheck/lint/format, strict OpenSpec validation, and independent lineage/security review.
  - Progress (2026-09-02): focused storage/resolver/integrity suites, typecheck/lint/format, and strict OpenSpec validation ran green on every one of the three PRs (receipts under 1.1-1.4); the independent lineage/security review half stays open until a fresh-context reviewer reads PR 1a-1c together on merged main.

- [ ] 1.6 After merge, rerun exact-main resolver receipts and prune the merged branch/worktree.

## 2. Candidate Build and Atomic Activation — PR 2

- [x] 2.1 Add authorized candidate creation plus a durable correction lease with opaque ID, owner, expiry, monotonically increasing fencing epoch, expected branch/revision/digest/generation, actor, and reason.
  - Receipt (2026-09-02, PR 2 cut 3 of 3 - CLOSES this box; cuts 1-2 are #1504 schema + #1507 store, see the Progress notes above): authorized candidate creation is createCorrectionCandidateBranch (EventHistoryCandidateBuild.ts) - four ordered refusable steps inside ONE db.transaction: prove the lease LIVE via requireLiveLease presenting id + owner + epoch; re-verify the bound head (branch/revision/generation through PR 1's validateExpectedBranchHead, digest through the now-public assertExpectedHeadIsCurrent narrowed to the four bound facts, so acquisition and build ask ONE verifier); resolve the base event from the real journal; create through a store holding the permitting seam. The seam is a MODULE-PRIVATE constant - no export, no factory; `grep allowsBranchCreation: true` finds it and PR 1's test-only helper and nothing else - so production stays creation-off. The candidate is a 'building' child anchored to exactly the lease's branch/revision/event/digest with createdBy = the lease owner; NO activation (effective head still root / generation 1, supersessions empty, asserted). Abandoned candidates stay building, never promoted, never deleted, with a machine-readable reason naming the lease (readCandidateLeaseRef). Real SQLite appending through the shipped SQLiteEventJournal so the base event/digest are production's. Worker mutants killed incl. 'takeover does not fence' - which SURVIVED the first pass because the takeover row only ever varied the owner too; fixed with the row where the live holder presents its real id and name with a stale epoch. A test aimed at an unreachable arm (an empty stream has no head row, gets no genesis branch, cannot be leased - refusal lands at acquisition as no-effective-branch) was corrected and the dead revision<1 guard removed.

  - Progress (2026-09-02, PR 2 cut 2 of 3 - box stays OPEN; the STORE half of the correction lease): EventHistoryCorrectionLeaseContract.ts + SQLiteEventHistoryCorrectionLeaseStore.ts over migration 24. The distinction the store turns on is ACTIVE (a storage state a row keeps until reaped) vs LIVE (inside its expiry on an explicit injected clock - the only question that decides anything): readActiveLease vs readLiveLease, and nothing lets a stale active row stand in for a live lease. Takeover is not a third primitive: reaping the expired lease and minting the replacement at a strictly higher epoch is ONE transaction (the partial unique index never sees two active rows, the ladder never reuses a rung); two terminal states, released and expired. Expected head verified through PR 1's validateExpectedBranchHead (branch/revision/generation) so a lease and an ordinary command cannot disagree about stale; the digest compared separately with its own STALE_DIGEST reason. Renewal preserves the epoch (D2) and refuses rather than clamps a shortening; owner (fencing identity, changes on takeover) is not actor (authorizing principal, survives handover). Real SQLite incl. cold reopen recovering the live lease/owner/epoch from nothing in process memory. Worker mutants killed on this base (4/4: expiry ignored, takeover while live, epoch minted on renewal, digest half dropped); orchestrator mutants recorded in the PR. Lesson recorded: a shared scratchpad script name was clobbered between concurrent agents and a mutant driver that applied NOTHING reported all-green - drivers now hard-fail on anchor mismatch. NOT claimed: authorized candidate creation, PROJECTION_REBUILDING, restart recovery beyond what the store proves (nothing gates commands on the answer yet) - cut 3.

  - Progress (2026-09-02, PR 2 cut 1 of 3 - box stays OPEN; the STORAGE half of the correction lease): SQLite migration 24 `event_history_correction_leases` (sidecar SQLiteService.correctionLeases.migration.ts) - one additive table: opaque 32-hex lease_id, owner (the process fencing compares; changes on takeover) kept separate from actor (the authorizing principal that survives handover), reason, fencing_epoch, expected branch/revision/digest/generation, acquired_at_ms/expires_at_ms, state. Four rules as CONSTRAINTS: the fencing epoch climbs by EXACTLY one per stream counting terminal rows (gapless is stronger than 'monotonically increasing' and makes epoch comparison a total order; the delivery_generation idiom); at most one ACTIVE lease per stream via a partial unique index; identity immutable, expiry extends only, state leaves 'active' exactly once (a no-op active->active write is refused so a reaper cannot believe it reclaimed a live lease), no delete (dead rows ARE the epoch ledger - deleting one reopens a spent epoch); composite FK into event_history_branches, none into event_journal_*. Deliberate non-constraint: expiry is stored, never enforced by SQL (SQLite now is not the domain clock) - liveness is the store's decision on an explicit clock. Forced consumers: the 5 trigger-catalog suites (+5 names); the migration-head pin in the branches suite retitled to membership + <= head + a row-level 'version 23 recorded'. LATENT BUG FIXED: the branches suite's replayMigration deleted only its own ledger record while the runner applies everything above MAX(version), so with 24 recorded the replay silently never ran (genesis backfill never fired, 5 rows red) - now deletes from its own version upward, matching the runner suite. Red-first: module-not-found. Worker mutants killed: ladder disabled, one-active index dropped, expiry ignored, takeover while live, renewal minting a fresh epoch, expected-digest half dropped. Cut plan (seam 2a measured 1139 lines with only the lease half built): cut 1 schema (this), cut 2 lease contract + store (written, held), cut 3 authorized candidate creation + PROJECTION_REBUILDING (2.2). Receipt-consuming halves of 2.4/2.5/2.6 NOT claimed per the gate 0.1 amendment.

- [x] 2.2 Reject commands with `PROJECTION_REBUILDING` while the live lease rebuilds; make restart recovery/expiry explicit and never queue commands invisibly.
  - CLOSED (2026-09-02, Wave C3 seam 3a): readRebuildRefusal extracted from admitStreamCommand (one row pins the extracted arm and the full check return the identical object); adopted at BOTH live admission sites - combat: refuseDuringHistoryRebuild in ServerMatchHostIntent.handleIntent immediately after the rollbackBlockReason refusal and before refuseUnauthorizedCommand (after lobby routing on purpose - a rebuild stops the engine mutating, not a player taking a seat), stream-level typed refusal, no audit row, nothing appended, durable state via the store-capability idiom (IMatchStreamRebuildStore / hasMatchStreamRebuildReader, DurableMatchStore implements, InMemoryMatchStore honestly lacks it = no rebuild) delegating to EventHistoryDurableRebuild.readDurableStreamRebuild because the lease/branch tables live in mekstation.db, not the match db; campaign: exactly one third `blocked` arm in executeCampaignCommand (reason PROJECTION_REBUILDING, route already maps blocked -> 409), reader an optional dep defaulting to the durable one so the production shape is the one proven. Expiry explicit: a lapsed lease admits again (row). Red-first: combat intent admitted with no refusal; campaign command COMMITTED and changed the balance through a live lease. Mutants killed (worker): gate removed, campaign arm removed, gate before lobby routing (seat refused), lease read without stream_id, refusal broadcast but appended anyway, capability inverted; orchestrator: capability inverted re-run, gate asks about another match, refusal carries the wrong code. NOT CLAIMED: the staleness arm on the combat path (no client-claimed expected head on the wire - finding #38, its own protocol decision).

  - Progress (2026-09-02, PR 2 cut 3 - box stays OPEN on ADOPTION): admitStreamCommand (EventHistoryCommandAdmission.ts) is the gate: PROJECTION_REBUILDING reported BEFORE staleness (a mid-rebuild client is usually also stale and STALE_BRANCH would send it to resync against the head being replaced), verdict carries code, retryable, leaseId, owner, fencingEpoch, activeHead and action 'retry-after-rebuild'; NOTHING is queued and that is structural - a synchronous return value from a read-only gate, pinned as not-a-thenable (the async mutant dies there); restart recovery is explicit (a resuming owner re-establishes through requireLiveLease); expiry releases the stream BY THE CLOCK, not by a write (readLiveLease stops reporting a lapsed lease the instant it lapses; admission resumes with no reaper and the storage census byte-identical); takeover fences the old owner (its next build refuses on epoch). Worker mutants killed: refusal removed (5 red), queued behind a promise (7 red), expired lease still blocks (2), takeover not fenced (1), staleness before rebuild (1), head re-check dropped (1). SCOPING FACT: no production command path calls validateExpectedBranchHead OR this gate - PR 1c shipped expected-head validation as a decision module nobody calls and said so; SQLiteEventHistoryBranchStore has no consumer outside src/lib/events/journal. 'Reject commands while the live lease rebuilds' is therefore true at the module level and NOT yet on the live match/campaign command paths; wiring them (the multiplayer server's command gate beside its existing typed refusals) is PR 3's first step and what closes this box. No migration in this cut.

- [x] 2.3 Verify replay, viewer projections, and an immutable server-derived affected-artifact manifest before activation.
  - Receipt (2026-09-02, PR 2 verification cut 3b' - CLOSES this box; cuts 3a #1515 storage, 3b #1518 verification, see the Progress notes above): EventHistoryImpactDerivation.deriveAndSealCandidateImpact runs the locked order - verify the candidate (3b) -> materialise the prior effective head -> probe BOTH heads TWICE per viewer (GM/P1/P2, injected viewer-projection probe; a probe that will not reproduce is refused as typed branch-integrity) -> replay_checkpoints rows with revision > baseRevision (one AT the base describes history the candidate keeps) -> seal through 3a's store. Manifest entries are the artifacts whose viewer projection CHANGES between prior head and candidate (projection kind, at the candidate's base revision) plus the superseded checkpoints; an unchanged projection is NOT listed (padding inflates a blast radius somebody must review). SERVER-DERIVED: every digest comes from events this module materialised itself and checkpoint rows read from storage; no caller inventory is accepted. Control flow, not atomicity, stated plainly: materialisation is async and better-sqlite3 transactions are synchronous, so verification cannot share the seal's transaction; the seal itself is one transaction (3a) and 'no manifest for an unverified candidate' holds because the function returns before sealing - pinned from outside (zero manifest rows, zero entry rows on a failed verification). Resolver change: materializeBranchPath now SKIPS an empty segment instead of calling the reader (a rewind candidate anchored at its parent's head contributes an empty suffix; journalBranchSegmentReader refuses any branch but root by name, so the old path refused a question nobody asked; the base-vs-anchor check still runs first; 176 suites / 1900 green). Worker mutants killed (6): changed projection omitted (3 red), unchanged projection included (2 red), sealing before verification, probe answer trusted without candidate-side re-derivation (its own row holds the prior-head digest identical for every viewer so only the candidate side varies - the 2c takeover lesson applied before the fact), probe called once per head, checkpoint filter off by one at the base. FINDING recorded: the replay and export artifact kinds are NOT derivable server-side today - the replay index is simulation-reports/replay-index.json keyed by matchId with no revision field, and exports are not persisted anywhere - so sourceRevision cannot be recorded for either; both stay in the schema's closed set for a later change that gives them a revision-carrying index (no injected inventory - accepting a list and calling it derived would break the one property the manifest exists for). Second finding for PR 3: 2c's authorized creation anchors a candidate at the lease's expected (current) head; a candidate anchored at a PRIOR revision is what a rewind produces and minting one is PR 3's work - the suite seeds that row directly. NOT claimed: the concrete campaign/combat viewer digesters (probe injected; adoption is PR 3's step). Remaining in section 2: 3c - fence the prior generation, the without-correction activation arm (generation +1, supersession row, invalidations from the manifest, GENERATION_EXHAUSTED before mutation), the 2.6 receipt-free proofs.

  - Progress (2026-09-02, PR 2 verification cut 3b - box stays OPEN on the impact derivation (3b')): EventHistoryCandidateVerification verifies a candidate path BEFORE activation can matter: deterministic replay - the path is materialised TWICE and a digest over exactly what the resolver verifies (identities, revisions, digest chain; payloads deliberately excluded) must agree, a differing reader refused as typed branch-integrity; projector compatibility - PR 1's stated non-claim, closed as far as main allows by COMPOSING the replay-safety harness's recoverState (which already refuses identity-only compatibility and a gapped tail and never materialises state from a rejected cache; a refused checkpoint is a verification that took the reference path, reported as absent/used/refused with reasons) plus one runtime rule: an offer missing either digest expectation is refused outright (identity-only = silent downgrade). The segment reader and materialiser become generic over the event they carry (TYPE change only - the production reader always returned whole stored events; the narrow view discarded eventType/payload a projector needs; default unchanged; a row pins identities/per-event digests/path digest identical widened vs narrow). Worker mutants killed (5): second materialisation replaced by the first, path digest drops the chain (SURVIVED first pass - every row varied ids or count; the relinked-chain row now kills it), identity-only offer passed down, refused checkpoint reported as used, widening narrows before verification. Driver lesson (third in this family): a reverse-replace revert corrupted the file when the replacement was a repeated substring - drivers now snapshot exact bytes and restore verbatim with md5 after EVERY mutant. FINDING recorded: the projector-compatibility contract (ReplayCheckpointCompatibility, recoverState with the digestsVerified law) exists with NO production consumer - live checkpoints are not produced on main (SQLiteReplayCheckpointRepository test-only; ReplayEquivalenceHarness says nothing in production imports it); 3b verifies checkpoints handed to it. NEXT: 3b' per-viewer projection re-derivation over an injected probe -> changed projections become manifest entries sealed through 3a; then 3c fence + activation.

  - Progress (2026-09-02, PR 2 verification cut 3a of 3 - box stays OPEN; the manifest STORAGE): SQLite migration 25 (sidecar SQLiteService.artifactManifest.migration.ts) adds two additive tables - event_history_artifact_manifest_entries (one immutable row per affected artifact: closed kind set replay|export|checkpoint|projection, id, the source revision whose replacement stales it) and event_history_artifact_manifests (the sealing header: digest of the entries, count, derived-at); composite FK into event_history_branches, none into event_journal_* (PRAGMA-asserted). THE SEAL is the load-bearing constraint: entries first, header last, and a trigger refuses any further entry once the header exists, so an activation's blast radius is fixed when recorded and cannot be widened by a writer that reviewed nothing; both tables refuse UPDATE and DELETE; sealing is per candidate. The store (EventHistoryArtifactManifest.ts) seals atomically, digests over the journal's canonicalizeJsonV1 + sha256Sync after sorting entries into canonical order (order-free), keeps sourceRevision in the digest material, seals an EMPTY manifest rather than skipping it, and verifyArtifactManifest recomputes count + digest from the rows (SQL deliberately does not check them - it cannot hash the canonical form). SERVER-DERIVED is preserved by omission: no path takes a caller's word for what is affected. Forced consumers: the 5 trigger-catalog suites (+5), the correction-lease suite's head pin retitled, the branches suite's event_history_% catalog. Worker mutants killed (6): seal removed, manifests editable, discovery-order digest (3 red), sourceRevision dropped, re-seal overwrite, header trusted instead of recomputed. Cut plan for the receipt-free remainder of 2.3-2.6 (the manifest seam alone measured 834 lines / 12 files): 3a this; 3b the derivation - deterministic replay through materializeBranchPath, projector compatibility (PR 1's non-claim), per-viewer projection re-derivation producing the entries 3a seals; 3c fence + activation + the 2.6 receipt-free proofs.

- [ ] 2.4 Fence the prior effective generation; serialize the fence against lease-to-admitted promotion, stop new leases/admissions, supersede unleased pending rows, and keep the candidate waiting plus prior branch effective while an old-generation delivery has an unknown target result.
  - PROGRESS (2026-09-02, Wave C2 seam 3c, EventHistoryActivation.ts): the prior effective generation IS fenced - the supersession row (prior_generation = N) is the fence, recorded once inside the activation transaction, no new table; new correction leases naming the fenced generation are refused by the existing expected-head comparison (stale-expected-head / STALE_BRANCH), pinned by a row rather than a second guard. NOT CLAIMED and gated on the cross-stream effect-receipt change (unstarted on main - there is no delivery lease, no admission state, no lease-to-admitted promotion, and the only outbox is a pending/published publication outbox): serializing the fence against lease-to-admitted promotion, stopping new admissions, superseding unleased pending rows, and the waiting arm (candidate waits, prior branch stays effective while an old-generation delivery has an unknown target result - the one arm where a separate pre-activation fence record would earn its existence).

- [ ] 2.5 Lock and verify the current unexpired correction lease ID, owner, and epoch with the expected-head comparison. If a prior receipt was accepted, atomically create the higher-version replacement outbox and pending saga while activating the candidate, incrementing generation by exactly one, superseding the prior branch, and publishing invalidations; otherwise activate without correction. Never wait for a pre-activation replacement receipt.
  - PROGRESS (2026-09-02, Wave C2 seam 3c): the current unexpired correction lease is locked and its id/owner/epoch verified with the expected-head comparison inside ONE db.transaction (every write is synchronous, unlike 3b' where async materialization made the ordering control flow); demote-before-promote forced by the partial unique index; generation incremented by exactly one; GENERATION_EXHAUSTED refused before any write. NOT CLAIMED: the accepted-prior-receipt arm (atomic higher-version replacement outbox + pending saga) - no receipt exists to have been accepted; closes with the effect-receipt change.

- [ ] 2.6 Prove deterministic replay, correction-lease restart/expiry/takeover, stale-owner activation rejection with an unchanged head, stale-head activation, safe-integer generation exhaustion before mutation, injected activation failure, both fence/admission serial orders, accepted-prior-receipt activation, and target outage/restart at each saga boundary.
  - PROGRESS (2026-09-02, Wave C2 seam 3c, EventHistoryActivation.test.ts against real SQLite): stale-owner activation rejected with a byte-identical census; stale-head activation refused; safe-integer generation exhaustion refused before mutation; injected mid-transaction failure rolls the whole activation back with NO production test seam (a stray supersession row makes activation's own INSERT collide on the primary key after the branches moved); both serial orders (activation-then-lease refused on generation, lease-then-activation refused on the lease) proven against the correction lease, which is the lease this stream actually has; unsealed manifest refused. Worker mutants killed (7): generation jump, promote-before-demote, unchecked bound, manifest not required, lease unverified, supersession row unwritten, head comparison skipped. Orchestrator: promote-before-demote re-run, N1 generation does not advance, N2 candidate reported as the superseded branch. Happy-path invalidations pinned in CANONICAL order (the manifest store's order-free digest, 3a). NOT CLAIMED: fence/admission serial orders against a delivery lease, accepted-prior-receipt proofs, and the waiting arm - all gated on the effect-receipt change.

- [ ] 2.7 After independent review and focused gates pass, merge, rerun activation/failure on exact main, and prune the branch/worktree.

## 3. Combat Rewind — PR 3

- [ ] 3.1 Keep GM combat rewind preview non-mutating and bind finalization to current branch/revision/digest/generation.
- [ ] 3.2 Rebuild combat state, RNG provenance, fog, sealed choices, viewer projections, and affected artifacts on a candidate branch before activation.
- [ ] 3.3 Prove stale-branch, rebuild-time, unauthorized-player, failed-verification, and owner-restart commands append nothing.
- [ ] 3.4 Run GM intervention, combat replay, fog, multiplayer, command-browser, viewport/accessibility, and independent visual/privacy review.
- [ ] 3.5 After merge, rerun exact-main rewind/reconnect proof and prune the merged branch/worktree.

## 4. Campaign Replacement and Artifact Invalidation — PR 4

- [ ] 4.1 Require campaign correction preview to declare every affected state and externalized artifact family.
- [ ] 4.2 Build backward-time and retroactive correction as replacement-branch replay; invalidate stale missions, force selections, outcomes, and readiness artifacts by identity and source revision.
- [ ] 4.3 Prove replacement construction, failed validation, and stale-artifact rejection without cross-stream outcome delivery or UI changes.
- [ ] 4.4 Run focused campaign time-cascade, ledger, branch, and artifact tests plus independent authority review.
- [ ] 4.5 After merge, rerun exact-main campaign replacement proof and prune the merged branch/worktree.

## 5. Coordinated Post-Receipt Correction Saga — PR 5

- [ ] 5.1 Reject combat-only rewind after the active outcome receipt and create the new effective branch, higher-version source correction, replacement outbox, and pending saga in one source activation transaction.
- [ ] 5.2 Persist canonical command bytes/digest/schema/canonicalizer versions; recovery never regenerates them from mutable projection state.
- [ ] 5.3 Apply the target-scoped replacement receipt and deterministic campaign consequence batch in one target-local transaction; do not claim cross-store atomicity.
- [ ] 5.4 Persist pending, retrying, blocked, and applied saga states and keep scenario progression gated until the active target receipt and projections converge.
- [ ] 5.5 Prove accepted prior receipt → source activation → replacement dispatch → target receipt → applied saga, plus retry, lost acknowledgement, source/target restart at every boundary, unsupported stored version, target-scope mismatch, activation-fence versus delivery-admission race, and blocked recovery apply replacement consequences once.
- [ ] 5.6 Run focused cross-stream receipt/correction tests and independent durability/security review.
- [ ] 5.7 After merge, rerun exact-main coordinated correction proof and prune the merged branch/worktree.

## 6. Branch Timeline and Recovery UX — PR 6

- [ ] 6.1 Add authorization-filtered branch/supersession/impact timeline and effect-free, cursor-neutral prior-head inspection.
- [ ] 6.2 Add desktop/narrow accessible status, impact confirmation, rebuild progress, correction-saga state, failure focus, and recovery feedback.
- [ ] 6.3 Prove player views expose no GM-private reasons, hidden facts, authority gaps, or inaccessible branch identifiers.
- [ ] 6.4 Run deep audit, viewport sweep, replay/export parity, campaign-long, post-battle, and independent visual/security review.
- [ ] 6.5 After merge, rerun exact-main correction/next-scenario browser proof, archive authority evidence, and prune the merged branch/worktree.

## 7. Branch/Generation Rollback Compatibility — PR 7

- [ ] 7.1 Add a branch/generation-aware rollback reader compatible with the persisted journal schema, upcasters, canonicalizer, and effective-head contract.
- [ ] 7.2 Preserve the exact effective branch/revision/digest/generation plus fences, outbox/admission/receipt, saga, candidate, and supersession rows across rollback and cold restart.
- [ ] 7.3 If compatibility is unavailable, stop command/effect/correction admission and expose a typed blocked state; never substitute root, prior/superseded history, or generation `1`.
- [ ] 7.4 Prove rollback from generation greater than one, non-effective candidate retention, incompatible-reader blocking, and exact-head/generation restart without new admission.
- [ ] 7.5 Run focused rollback/restart/integrity tests and independent durability/security review.
- [ ] 7.6 After merge, rerun exact-main rollback receipts and prune the merged branch/worktree.

## 8. Optional Simulation Promotion — Deferred

- [ ] 8.1 Keep player-authored simulation creation disabled; enabling it requires a separate approved UX change.
- [ ] 8.2 A future change may promote only by revalidating semantic commands against the current target and emitting new target events with provenance.
- [ ] 8.3 A future change MUST prove simulations cannot dispatch effects, reveal hidden history, mutate authority, or bypass branch/revision validation.
